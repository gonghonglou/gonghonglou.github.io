<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>与佳期的个人博客</title>
  
  <subtitle>登白薠兮骋望，与佳期兮夕张。</subtitle>
  <link href="/atom.xml" rel="self"/>
  
  <link href="http://gonghonglou.com/"/>
  <updated>2021-03-09T05:38:08.967Z</updated>
  <id>http://gonghonglou.com/</id>
  
  <author>
    <name>与佳期</name>
    
  </author>
  
  <generator uri="http://hexo.io/">Hexo</generator>
  
  <entry>
    <title>Xcode12 执行 pod lib lint 报错：building for iOS Simulator, not found for architecture arm64</title>
    <link href="http://gonghonglou.com/2021/03/09/xcode12-lint-error/"/>
    <id>http://gonghonglou.com/2021/03/09/xcode12-lint-error/</id>
    <published>2021-03-09T04:43:52.000Z</published>
    <updated>2021-03-09T05:38:08.967Z</updated>
    
    <content type="html"><![CDATA[<h1 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h1><p>之前打包机升级 Xcode12 之后发现私有库发版时执行 <code>pod lib lint</code> 命令报错，一开始看报错信息提示以为是 Xcode12 或者 CocoaPods 的锅，想着蹲个新版本出来也许就好了，就暂时没管，打包机仍旧使用 Xcode11 发版。后来因为另外的需求需要我们升级 Xcode12，要优先解决组件发版报错问题，所以就仔细排查了下这个问题。</p><h1 id="复现"><a href="#复现" class="headerlink" title="复现"></a>复现</h1><p>随便找一个简单的开源库，以 <a href="https://github.com/HoloFoundation/HoloTableView" target="_blank" rel="noopener">HoloTableView</a> 为例，在其 podsepc 里随便依赖一个静态库，以 <a href="https://github.com/AppsFlyerSDK/AppsFlyerFramework" target="_blank" rel="noopener">AppsFlyerFramework</a> 为例：<br><figure class="highlight ruby"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">s.dependency <span class="string">'AppsFlyerFramework'</span></span><br><span class="line">s.static_framework = <span class="literal">true</span></span><br></pre></td></tr></table></figure></p><p>执行 lint 命令：<code>pod lib lint --allow-warnings --verbose</code> 即可得到报错内容：<br><figure class="highlight ruby"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="symbol">ld:</span> <span class="keyword">in</span> /var/folders/z7/<span class="number">9</span>hq4dfq97cn4xt4v56vqykph0000gn/T/CocoaPods-Lint-<span class="number">20210302</span>-<span class="number">64250</span>-<span class="number">1</span>vekyyp-HoloTableView/Pods/AppsFlyerFramework/iOS/AppsFlyerLib.framework/AppsFlyerLib(AFSDKKeychainFactory.o), building <span class="keyword">for</span> iOS Simulator, but linking <span class="keyword">in</span> object file built <span class="keyword">for</span> iOS, file <span class="string">'/var/folders/z7/9hq4dfq97cn4xt4v56vqykph0000gn/T/CocoaPods-Lint-20210302-64250-1vekyyp-HoloTableView/Pods/AppsFlyerFramework/iOS/AppsFlyerLib.framework/AppsFlyerLib'</span> <span class="keyword">for</span> architecture arm64</span><br><span class="line"><span class="symbol">clang:</span> <span class="symbol">error:</span> linker command failed with exit code <span class="number">1</span> (use -v to see invocation)</span><br></pre></td></tr></table></figure></p><p>报错信息写的很清楚，在为 iOS 模拟器编译过程中，链接 <code>AppsFlyerFramework</code> 这个库时找不到 <code>arm64</code> 架构。我们知道以往模拟器仅支持 <code>x86_64</code> 架构即可，因为我们的 Mac 就是 <code>x86_64</code> 架构，而 Mac 上带的模拟器自然是同一架构。现在竟然在模拟器上需要 <code>arm64</code> 架构了，应该是为了支持 Apple silicon。M1 正是 arm 架构，那 M1 上的模拟器自然就是需要 <code>arm64</code> 架构的了。</p><p>根据报错信息 Google 一下就会发现已经有很多网友遇到了该问题并给出了解释和解决方案，主要有以下参考链接：</p><p>1、<a href="https://github.com/CocoaPods/CocoaPods/issues/10065#issuecomment-694266259" target="_blank" rel="noopener">https://github.com/CocoaPods/CocoaPods/issues/10065#issuecomment-694266259</a><br>(Braintree 他们首次在 4.36.1 版本里遇到了该问题，解决方案正是来自这个 issue：<a href="https://github.com/braintree/braintree_ios/blob/4.36.1/Braintree.podspec#L136-L138" target="_blank" rel="noopener">Braintree.podspec</a>)</p><p>2、<a href="https://github.com/CocoaPods/CocoaPods/issues/10104#issuecomment-700918704" target="_blank" rel="noopener">https://github.com/CocoaPods/CocoaPods/issues/10104#issuecomment-700918704</a></p><p>3、<a href="https://stackoverflow.com/questions/63607158/xcode-12-building-for-ios-simulator-but-linking-in-object-file-built-for-ios" target="_blank" rel="noopener">https://stackoverflow.com/questions/63607158/xcode-12-building-for-ios-simulator-but-linking-in-object-file-built-for-ios</a></p><p>问题似乎得到了解决，但实际应用中我们发现了新的问题（见下一章节 疑问 6）。也有同事发现 <code>pod lib lint</code> 跟上 <code>--skip-import-validation</code> 参数即可通过 lint 验证（见下一章节 疑问5）。</p><p>最终我们向这个问题妥协了，选择了使用 <code>--skip-import-validation</code> 参数，通过跳过“某些”验证来让 lint 暂时通过以完成组件发版工作。但截至目前我们还不知究竟 “skip” 了哪些内容。</p><p>最终关于这个问题我们存在以下这些疑问需要搞清楚。</p><h1 id="疑问"><a href="#疑问" class="headerlink" title="疑问"></a>疑问</h1><p><strong>疑问 1</strong>、为什么有的库报错，有的库不报错？</p><p><strong>疑问 2</strong>、为什么 Xcode12 报错， Xcode11 不报错？</p><p><strong>疑问 3</strong>、为什么 Pod Example 工程编译没问题，lint 却报错？</p><p><strong>疑问 4</strong>、为什么在 podspec 加上以下命令即可通过 lint 验证？这两行具体更改了什么？<br><figure class="highlight ruby"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">s.pod_target_xcconfig = &#123; <span class="string">'EXCLUDED_ARCHS[sdk=iphonesimulator*]'</span> =&gt; <span class="string">'arm64'</span> &#125;</span><br><span class="line">s.user_target_xcconfig = &#123; <span class="string">'EXCLUDED_ARCHS[sdk=iphonesimulator*]'</span> =&gt; <span class="string">'arm64'</span> &#125;</span><br></pre></td></tr></table></figure></p><p><strong>疑问 5</strong>、为什么 <code>pod lib lint</code> 跟上 <code>--skip-import-validation</code> 参数亦可通过 lint 验证？字面意思上的“跳过引用验证”究竟 “skip” 了什么？</p><p><strong>疑问 6</strong>、PodA 依赖 PodB、PodB 依赖 PodC。PodC 删除了模拟器 <code>arm64</code> 架构导致 PodA lint 失败，即使 PodA 也删除了模拟器 <code>arm64</code> 架构，这是为什么？</p><h1 id="从头开始"><a href="#从头开始" class="headerlink" title="从头开始"></a>从头开始</h1><p>带着上述这些问题我们来一一排查，相信解决完以上疑问后收获的绝不仅仅是 解决了一个“Xcode12 上发版失败”的问题。那么接下来就介绍下从头解决以上问题的经历。</p><p>注：如果以上章节的内容你都熟悉了并搞清楚了这个问题的本质，那么以下章节刚开始的过程会让你觉得思路很愚蠢，但我只是想完整记录下“发现-排查-解决”这个问题的完整过程。</p><h2 id="关于疑问-1：为什么有的库报错，有的库不报错？"><a href="#关于疑问-1：为什么有的库报错，有的库不报错？" class="headerlink" title="关于疑问 1：为什么有的库报错，有的库不报错？"></a>关于疑问 1：为什么有的库报错，有的库不报错？</h2><p>最开始我们发布业务库时几乎全在报错，导致我以为单纯的是 Xcode12 的问题，所以在 Google 到解决方案后计划在组件发版的时候，在 podspec 里无脑加上以下命令：<br><figure class="highlight ruby"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">s.pod_target_xcconfig = &#123; <span class="string">'EXCLUDED_ARCHS[sdk=iphonesimulator*]'</span> =&gt; <span class="string">'arm64'</span> &#125;</span><br><span class="line">s.user_target_xcconfig = &#123; <span class="string">'EXCLUDED_ARCHS[sdk=iphonesimulator*]'</span> =&gt; <span class="string">'arm64'</span> &#125;</span><br></pre></td></tr></table></figure></p><p>但当我们发布一个基础组件的时候却出现了「疑问 6」问题（关于这个疑问稍后解析），所以将以上命令暂时移除，决定看看基础库能不能通过验证。果然发布成功了，那么接下来就是找到哪些库能成功哪些库会报错，再看问题出现的根源。</p><p>果然不久我们就发现了问题的根源，只有组件依赖了二进制库的时候，lint 才会报这个错。这么一看就恍然大悟了，当然啦，只有那些已经编译好的二进制库，它们已经包含了固定支持的架构。以 <code>HoloTableView</code> 的 Example 工程为例，<code>pod install</code> 之后在 <code>/Pods/AppsFlyerFramework/iOS/AppsFlyerLib.framework/Versions/A</code> 目录下找到 <code>AppsFlyerLib</code> 文件并用 <code>lipo -info AppsFlyerLib</code> 命令查看得到的结果是：<br><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Architectures in the fat file: AppsFlyerLib are: i386 armv7 x86_64 arm64</span><br></pre></td></tr></table></figure></p><p>在胖二进制（也叫通用二进制）文件 <code>AppsFlyerLib</code> 里包含的架构有 <code>i386、armv7、x86_64、arm64</code>，其中 <code>x86_64</code> 用于模拟器，而这里的 <code>arm64</code> 是支持真机架构的。所以在这样一个已经编译好了的二进制库被依赖了，lint 过程中链接器打算链接模拟器 <code>arm64</code> 架构时发现 <code>AppsFlyerLib</code> 没有这样的架构而报错：<br><figure class="highlight ruby"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">Ld /Users/gonghonglou/Library/Developer/Xcode/DerivedData/App-aahatildizoismejitnvvpgympoq/Build/Intermediates.noindex/App.build/Release-iphonesimulator/App.build/Objects-normal/arm64/Binary/App normal arm64 (<span class="keyword">in</span> target <span class="string">'App'</span> from project <span class="string">'App'</span>)</span><br><span class="line">        cd /var/folders/z7/<span class="number">9</span>hq4dfq97cn4xt4v56vqykph0000gn/T/CocoaPods-Lint-<span class="number">20210303</span>-<span class="number">54294</span>-ti9bsz-HoloTableView</span><br><span class="line">        /Applications/Xcode.app/Contents/Developer/Toolchains/XcodeDefault.xctoolchain/usr/bin/clang -target arm64-apple-ios8.<span class="number">0</span>-simulator -isysroot /Applications/Xcode.app/Contents/Developer/Platforms/iPhoneSimulator.platform/Developer/SDKs/iPhoneSimulator14.<span class="number">4</span>.sdk -L/Users/gonghonglou/Library/Developer/Xcode/DerivedData/App-aahatildizoismejitnvvpgympoq/Build/Products/Release-iphonesimulator -F/Users/gonghonglou/Library/Developer/Xcode/DerivedData/App-aahatildizoismejitnvvpgympoq/Build/Products/Release-iphonesimulator -F/Users/gonghonglou/Library/Developer/Xcode/DerivedData/App-aahatildizoismejitnvvpgympoq/Build/Products/Release-iphonesimulator/HoloTableView -F/var/folders/z7/<span class="number">9</span>hq4dfq97cn4xt4v56vqykph0000gn/T/CocoaPods-Lint-<span class="number">20210303</span>-<span class="number">54294</span>-ti9bsz-HoloTableView/Pods/AppsFlyerFramework/iOS -filelist /Users/gonghonglou/Library/Developer/Xcode/DerivedData/App-aahatildizoismejitnvvpgympoq/Build/Intermediates.noindex/App.build/Release-iphonesimulator/App.build/Objects-normal/arm64/App.LinkFileList -Xlinker -rpath -Xlinker @executable_path/Frameworks -Xlinker -rpath -Xlinker @loader_path/Frameworks -Xlinker -rpath -Xlinker @executable_path/Frameworks -dead_strip -Xlinker -object_path_lto -Xlinker /Users/gonghonglou/Library/Developer/Xcode/DerivedData/App-aahatildizoismejitnvvpgympoq/Build/Intermediates.noindex/App.build/Release-iphonesimulator/App.build/Objects-normal/arm64/App_lto.o -Xlinker -objc_abi_version -Xlinker <span class="number">2</span> -fobjc-arc -fobjc-link-runtime -ObjC -framework AppsFlyerLib -framework CoreTelephony -framework HoloTableView -framework Security -framework SystemConfiguration -Xlinker -sectcreate -Xlinker __TEXT -Xlinker __entitlements -Xlinker /Users/gonghonglou/Library/Developer/Xcode/DerivedData/App-aahatildizoismejitnvvpgympoq/Build/Intermediates.noindex/App.build/Release-iphonesimulator/App.build/App.app-Simulated.xcent -framework Foundation -framework Pods_App -Xlinker -no_adhoc_codesign -Xlinker -dependency_info -Xlinker /Users/gonghonglou/Library/Developer/Xcode/DerivedData/App-aahatildizoismejitnvvpgympoq/Build/Intermediates.noindex/App.build/Release-iphonesimulator/App.build/Objects-normal/arm64/App_dependency_info.dat -o /Users/gonghonglou/Library/Developer/Xcode/DerivedData/App-aahatildizoismejitnvvpgympoq/Build/Intermediates.noindex/App.build/Release-iphonesimulator/App.build/Objects-normal/arm64/Binary/App</span><br><span class="line">    <span class="symbol">ld:</span> <span class="keyword">in</span> /var/folders/z7/<span class="number">9</span>hq4dfq97cn4xt4v56vqykph0000gn/T/CocoaPods-Lint-<span class="number">20210303</span>-<span class="number">54294</span>-ti9bsz-HoloTableView/Pods/AppsFlyerFramework/iOS/AppsFlyerLib.framework/AppsFlyerLib(AFSDKKeychainFactory.o), building <span class="keyword">for</span> iOS Simulator, but linking <span class="keyword">in</span> object file built <span class="keyword">for</span> iOS, file <span class="string">'/var/folders/z7/9hq4dfq97cn4xt4v56vqykph0000gn/T/CocoaPods-Lint-20210303-54294-ti9bsz-HoloTableView/Pods/AppsFlyerFramework/iOS/AppsFlyerLib.framework/AppsFlyerLib'</span> <span class="keyword">for</span> architecture arm64</span><br><span class="line">    <span class="symbol">clang:</span> <span class="symbol">error:</span> linker command failed with exit code <span class="number">1</span> (use -v to see invocation)</span><br></pre></td></tr></table></figure></p><p>以上关键命令：<code>clang -target arm64-apple-ios8.0-simulator</code></p><h2 id="关于疑问-2：为什么-Xcode12-报错，-Xcode11-不报错？"><a href="#关于疑问-2：为什么-Xcode12-报错，-Xcode11-不报错？" class="headerlink" title="关于疑问 2：为什么 Xcode12 报错， Xcode11 不报错？"></a>关于疑问 2：为什么 Xcode12 报错， Xcode11 不报错？</h2><p>从以上报错信息可以看到关键报错日志为：<code>/Applications/Xcode.app/Contents/Developer/Toolchains/XcodeDefault.xctoolchain/usr/bin/clang -target arm64-apple-ios8.0-simulator</code></p><p>至于为什么 Xcode11 上不报错，我们保持相同内容的 podspec 在 Xcode11 执行下 <code>pod lib lint --allow-warnings --verbose</code> 看是什么结果。日志很多，我们用 <code>/Applications/Xcode.app/Contents/Developer/Toolchains/XcodeDefault.xctoolchain/usr/bin/clang -target</code> 全局搜索看看结果：</p><p><img src="http://image.gonghonglou.com/xcode12-lint-error/xcode11-lint-log.png" alt=""></p><p>可以发现 Xcode11 上只链接了 <code>i386</code> 和 <code>x86_64</code> 两种模拟器架构，并未涉及 <code>arm64</code> 模拟器架构，所以也就不会报错。由此可见这是 Xcode12 带来的对 Apple silicon 的支持。</p><p>在 Xcode 的 <code>Build Settings</code> 里查看 <code>Architectures</code> 模块可以看出差别：</p><p><img src="http://image.gonghonglou.com/xcode12-lint-error/xcode11-build-settings-architectures.png" alt=""></p><p><img src="http://image.gonghonglou.com/xcode12-lint-error/xcode12-build-settings-architectures.png" alt=""></p><p>可以看到，Xcode11 指定了有效架构，而在 Xcode12 里将 <code>Valid Architectures</code> 这一项被移除掉了，新增了 <code>Excluded Architectures</code> 选项并默认为空。</p><p>Xcode12 环境下，当我们在某一 podspec 里加上 <code>s.user_target_xcconfig = { &#39;EXCLUDED_ARCHS[sdk=iphonesimulator*]&#39; =&gt; &#39;arm64&#39; }</code> 命令就能在这里看到移除了模拟器环境下的 <code>arm64</code> 架构。当然 CocoaPods 官方十分不建议使用 <code>user_target_xcconfig</code> 来更改宿主工程的环境，但这也正是令 <code>pod lib lint</code> 通过的根本原因。</p><h2 id="关于疑问-3：为什么-Pod-Example-工程编译没问题，lint-却报错？"><a href="#关于疑问-3：为什么-Pod-Example-工程编译没问题，lint-却报错？" class="headerlink" title="关于疑问 3：为什么 Pod Example 工程编译没问题，lint 却报错？"></a>关于疑问 3：为什么 Pod Example 工程编译没问题，lint 却报错？</h2><p>解决了以上问题这个问题就更简单了，我们在 Xcode 里看下 Example 工程的编译过程：</p><p><img src="http://image.gonghonglou.com/xcode12-lint-error/xcode12-pod-example-build.png" alt=""></p><p>为了更方便的查看编译过程，点击 “Export…” 按钮，能够导出一份 <code>Build HoloTableView-Example_2021-03-03T22-50-02.txt</code> 文本，在命令行通过 <code>cat</code> 命令查看下内容。由于内容很多，我们依旧用 <code>/Applications/Xcode.app/Contents/Developer/Toolchains/XcodeDefault.xctoolchain/usr/bin/clang -target</code> 全局搜索看看结果：</p><p><img src="http://image.gonghonglou.com/xcode12-lint-error/xcode12-pod-example-build-log.png" alt=""></p><p>可以发现，仅存在一处对 <code>x86_64-apple-ios12.0-simulator</code> 的链接。这是因为我们在编译的时候选择了模拟器 iPhone12 Pro 机型。如果我们选择了 <code>Any iOS Device (arm64)</code> ，那搜索结果就是对 <code>arm64-apple-ios12.0</code> 的链接，也不会出错。理论上如果我们在 Mac M1 上选择模拟器进行编译应该就会出现对 <code>arm64-apple-ios12.0-simulator</code> 链接的报错了。</p><h2 id="关于疑问-4：为什么在-podspec-加上以下命令即可通过-lint-验证？"><a href="#关于疑问-4：为什么在-podspec-加上以下命令即可通过-lint-验证？" class="headerlink" title="关于疑问 4：为什么在 podspec 加上以下命令即可通过 lint 验证？"></a>关于疑问 4：为什么在 podspec 加上以下命令即可通过 lint 验证？</h2><figure class="highlight ruby"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">s.pod_target_xcconfig = &#123; <span class="string">'EXCLUDED_ARCHS[sdk=iphonesimulator*]'</span> =&gt; <span class="string">'arm64'</span> &#125;</span><br><span class="line">s.user_target_xcconfig = &#123; <span class="string">'EXCLUDED_ARCHS[sdk=iphonesimulator*]'</span> =&gt; <span class="string">'arm64'</span> &#125;</span><br></pre></td></tr></table></figure><p>为了解答这一疑问，我们将会上述 lint 报错的 <code>HoloTableView.podspec</code> 里加上这两行命令，再次执行 <code>pod lib lint --allow-warnings --verbose</code> 命令，然后同样用 <code>/Applications/Xcode.app/Contents/Developer/Toolchains/XcodeDefault.xctoolchain/usr/bin/clang -target</code> 全局搜索看看结果：</p><p><img src="http://image.gonghonglou.com/xcode12-lint-error/xcode12-lint-success-log.png" alt=""></p><p>可以发现，仅存在两处，<code>x86_64-apple-ios8.0-simulator</code> 和 <code>i386-apple-ios8.0-simulator</code> 链接操作。不再对 <code>arm64-apple-ios8.0-simulator</code> 进行链接也就不会再报错了。</p><p><strong>至于这两行具体更改了什么呢？</strong></p><p>先来根据 CocoaPods 的官方文档认识下 <a href="https://guides.cocoapods.org/syntax/podspec.html#pod_target_xcconfig" target="_blank" rel="noopener"><code>pod_target_xcconfig</code></a> 和 <a href="https://guides.cocoapods.org/syntax/podspec.html#user_target_xcconfig" target="_blank" rel="noopener"><code>user_target_xcconfig</code></a>：</p><blockquote><p>Typically clang compiler flags or precompiler macro definitions go in here if they are required when importing the pod in the user target. Note that, this influences not only the compiler view of the public interface of your pod, but also all other integrated pods alongside to yours. You should always prefer pod_target_xcconfig, which can contain the same settings, but only influence the toolchain when compiling your pod target.</p></blockquote><p>可以理解为 <code>pod_target_xcconfig</code> 仅作用于当前 pod，而 <code>user_target_xcconfig</code> 作用于当前 pod 的宿主工程，包括引入的其他 pod。</p><p>根据实际的应用来看下这两行命令的具体作用：</p><p>1、添加 <code>s.pod_target_xcconfig = { &#39;EXCLUDED_ARCHS[sdk=iphonesimulator*]&#39; =&gt; &#39;arm64&#39; }</code> 这么一行之后执行 <code>pod install</code> 命令。在 <code>HoloTableView.debug.xcconfig</code> 和 <code>HoloTableView.release.xcconfig</code> 文件里就出现 <code>EXCLUDED_ARCHS[sdk=iphonesimulator*] = arm64</code> 这么一行，其会修改这一 pod target 的 <code>Build Settings</code>：</p><p><img src="http://image.gonghonglou.com/xcode12-lint-error/pod-build-settings-excluded-archs.png" alt=""></p><p>2、添加 <code>s.user_target_xcconfig = { &#39;EXCLUDED_ARCHS[sdk=iphonesimulator*]&#39; =&gt; &#39;arm64&#39; }</code> 这么一行之后执行 <code>pod install</code> 命令。会修改当前 pod 宿主工程的 <code>Build Settings</code>：</p><p><img src="http://image.gonghonglou.com/xcode12-lint-error/user-build-settings-excluded-archs.png" alt=""></p><p>正是因为在 <code>Build Settings</code> 里删除了模拟器环境下 <code>arm64</code> 架构才得以让 build 成功。</p><h2 id="关于疑问-5：为什么-pod-lib-lint-跟上-skip-import-validation-参数亦可通过-lint-验证？字面意思上的“跳过引用验证”究竟-“skip”-了什么？"><a href="#关于疑问-5：为什么-pod-lib-lint-跟上-skip-import-validation-参数亦可通过-lint-验证？字面意思上的“跳过引用验证”究竟-“skip”-了什么？" class="headerlink" title="关于疑问 5：为什么 pod lib lint 跟上 --skip-import-validation 参数亦可通过 lint 验证？字面意思上的“跳过引用验证”究竟 “skip” 了什么？"></a>关于疑问 5：为什么 <code>pod lib lint</code> 跟上 <code>--skip-import-validation</code> 参数亦可通过 lint 验证？字面意思上的“跳过引用验证”究竟 “skip” 了什么？</h2><p>要弄明白这个问题，无疑查看 CocoaPods 源码是最直接的方式。在 <code>~/.rvm/rubies/ruby-2.5.1/lib/ruby/gems/2.5.0/gems/cocoapods-1.9.3/lib/cocoapods/command/lib/lint.rb</code> 目录打开 <code>lint.rb</code> 文件：<br><figure class="highlight ruby"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">initialize</span><span class="params">(argv)</span></span></span><br><span class="line">  ......</span><br><span class="line">  @skip_import_validation = argv.flag?(<span class="string">'skip-import-validation'</span>, <span class="literal">false</span>)</span><br><span class="line">  <span class="keyword">super</span></span><br><span class="line"><span class="keyword">end</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">run</span></span></span><br><span class="line">  UI.puts</span><br><span class="line">  podspecs_to_lint.each <span class="keyword">do</span> <span class="params">|podspec|</span></span><br><span class="line">    validator                = Validator.new(podspec, @source_urls, @platforms)</span><br><span class="line">    validator.skip_import_validation = @skip_import_validation</span><br><span class="line">    ......</span><br><span class="line">    validator.validate</span><br><span class="line"><span class="keyword">end</span></span><br></pre></td></tr></table></figure></p><p>省略了一些无关代码，可以看到传入的 <code>--skip-import-validation</code> 参数赋值给了 <code>@skip_import_validation</code> 字段，这个值在 <code>run</code>（类似 <code>main</code> 函数）函数里赋值给了新创建的一个 <code>Validator</code> 对象，并调用了这个对象的 <code>validate</code> 方法进行验证，这就是 <code>pod lib lint</code> 的入口函数。</p><p>为了查看 <code>Validator</code> 这个类的 <code>validate</code> 方法又做了哪些事情，在 <code>~/.rvm/rubies/ruby-2.5.1/lib/ruby/gems/2.5.0/gems/cocoapods-1.9.3/lib/cocoapods/validator.rb</code> 目录打开 <code>validator.rb</code> 文件，感兴趣的话可以仔细看下这个类的代码，这里为了快速查找问题我们全局搜索 <code>skip_import_validation</code> 字段，能够定位到以下函数代码：<br><figure class="highlight ruby"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">build_pod</span></span></span><br><span class="line">      scheme = <span class="keyword">if</span> skip_import_validation?</span><br><span class="line">                 <span class="comment"># pod 名称</span></span><br><span class="line">                 validation_pod_target.label <span class="keyword">if</span> validation_pod_target.should_build?</span><br><span class="line">               <span class="keyword">else</span></span><br><span class="line">                 <span class="string">'App'</span></span><br><span class="line">               <span class="keyword">end</span></span><br><span class="line">          output = xcodebuild(<span class="string">'build'</span>, scheme, <span class="string">'Release'</span>)</span><br><span class="line"><span class="keyword">end</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">xcodebuild</span><span class="params">(action, scheme, configuration)</span></span></span><br><span class="line">  command = <span class="string">%W(clean <span class="subst">#&#123;action&#125;</span> -workspace <span class="subst">#&#123;File.join(validation_dir, <span class="string">'App.xcworkspace'</span>)&#125;</span> -scheme <span class="subst">#&#123;scheme&#125;</span> -configuration <span class="subst">#&#123;configuration&#125;</span>)</span></span><br><span class="line">  ......</span><br><span class="line"></span><br><span class="line">  _xcodebuild(command, <span class="literal">true</span>)</span><br><span class="line"><span class="keyword">end</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">_xcodebuild</span><span class="params">(command, raise_on_failure = <span class="literal">false</span>)</span></span></span><br><span class="line">  Executable.execute_command(<span class="string">'xcodebuild'</span>, command, raise_on_failure)</span><br><span class="line"><span class="keyword">end</span></span><br></pre></td></tr></table></figure></p><p>删除了一些无关代码逻辑就变的很简单了：声明了一个 <code>scheme</code> 变量，如果传入了 <code>skip_import_validation</code> 参数，<code>scheme</code> 变量赋值为当前 pod 名称，否则赋值 <code>App</code> 字符串，最终将这个变量值传给 <code>xcodebuild</code> 命令的 <code>scheme</code> 值。</p><blockquote><p>额外说明的是：关于 <code>App</code> 这个字符串，在执行 <code>pod lib lint</code> 命令时，CocoaPods 会根据当前的 podspec 文件创建一个叫 <code>App</code> 的工程，执行 <code>pod install</code> 命令生成 <code>App.xcworkspace</code> 文件，对这个工程文件执行 <code>xcodebuild</code> 命令进行校验。感兴趣详细过程的话可以仔细阅读下 <code>lint.rb</code> 这几个文件。</p></blockquote><p>到这里我们也就搞清楚了传入的 <code>--skip-import-validation</code> 参数究竟作用在了哪里，如果没传该参数的话 <code>xcodebuild</code> 命令的 <code>scheme</code> 值为 <code>App</code>，也就是将包含当前 pod 的所有相关 pod 链接进主工程进行编译；如果传递了该参数的话 <code>xcodebuild</code> 命令的 <code>scheme</code> 值为 “当前 pod 名称”，也就是仅编译当前 pod。通过查看这两种命令执行的 log 日志便一目了然：</p><p><img src="http://image.gonghonglou.com/xcode12-lint-error/pod-lib-lint-no-skip.png" alt=""></p><p><img src="http://image.gonghonglou.com/xcode12-lint-error/pod-lib-lint-has-skip.png" alt=""></p><p>查看 log 日志结尾能够看到命令执行的结果最终的产物：</p><p><img src="http://image.gonghonglou.com/xcode12-lint-error/pod-lib-lint-no-skip-CreateUniversalBinary.png" alt=""></p><p><img src="http://image.gonghonglou.com/xcode12-lint-error/pod-lib-lint-has-skip-CreateUniversalBinary.png" alt=""></p><p><code>- scheme App</code> 的最终产物是<code>App.app</code>，而 <code>- scheme HoloTableView</code> 的最终产物仅是 <code>HoloTableView.freamwork</code>。至此，也就明白了 <code>--skip-import-validation</code> 参数的字面意思：“跳过引用验证”，就是仅仅编译当前 pod 这个 <code>freamwoek</code>，不会将这个 <code>freamwork</code> 引用进主工程进行链接验证。</p><h2 id="关于疑问-6：PodA-依赖-PodB、PodB-依赖-PodC。PodC-删除了模拟器-arm64-架构导致-PodA-lint-失败，即使-PodA-也删除了模拟器-arm64-架构，这是为什么？"><a href="#关于疑问-6：PodA-依赖-PodB、PodB-依赖-PodC。PodC-删除了模拟器-arm64-架构导致-PodA-lint-失败，即使-PodA-也删除了模拟器-arm64-架构，这是为什么？" class="headerlink" title="关于疑问 6：PodA 依赖 PodB、PodB 依赖 PodC。PodC 删除了模拟器 arm64 架构导致 PodA lint 失败，即使 PodA 也删除了模拟器 arm64 架构，这是为什么？"></a>关于疑问 6：PodA 依赖 PodB、PodB 依赖 PodC。PodC 删除了模拟器 arm64 架构导致 PodA lint 失败，即使 PodA 也删除了模拟器 arm64 架构，这是为什么？</h2><p>这个疑问正是前文提到的，我们一开始在一个基础库 podspec 里移除了模拟器 <code>arm64</code> 架构，导致发布其他业务库失败的问题。关于这个问题的解答依然可以通过仔细观察 lint log 日志找到答案。现在我们模拟一个 lint 报错的环境：</p><p>1、我们使用 <a href="https://github.com/HoloFoundation/HoloCollectionView" target="_blank" rel="noopener">HoloCollectionView</a>（PodA），使其依赖 <a href="https://github.com/HoloFoundation/HoloTableViewMGPlugin" target="_blank" rel="noopener">HoloTableViewMGPlugin</a>（PodB），并移除模拟器环境下 <code>arm64</code> 架构，其 podspec 如下：<br><figure class="highlight ruby"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">s.dependency <span class="string">'HoloTableViewMGPlugin'</span></span><br><span class="line">s.pod_target_xcconfig = &#123; <span class="string">'EXCLUDED_ARCHS[sdk=iphonesimulator*]'</span> =&gt; <span class="string">'arm64'</span> &#125;</span><br><span class="line">s.user_target_xcconfig = &#123; <span class="string">'EXCLUDED_ARCHS[sdk=iphonesimulator*]'</span> =&gt; <span class="string">'arm64'</span> &#125;</span><br></pre></td></tr></table></figure></p><p>2、使 <a href="https://github.com/HoloFoundation/HoloTableViewMGPlugin" target="_blank" rel="noopener">HoloTableViewMGPlugin</a>（PodB） 依赖 <a href="https://github.com/HoloFoundation/HoloTableView" target="_blank" rel="noopener">HoloTableView</a>（PodC），其 podspec 如下：<br><figure class="highlight ruby"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">s.dependency <span class="string">'HoloTableView'</span>, <span class="string">'~&gt; 2.0'</span></span><br></pre></td></tr></table></figure></p><p>3、使 <a href="https://github.com/HoloFoundation/HoloTableView" target="_blank" rel="noopener">HoloTableView</a>（PodC）移除模拟器环境下 <code>arm64</code> 架构，其 podspec 如下：<br><figure class="highlight ruby"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">s.pod_target_xcconfig = &#123; <span class="string">'EXCLUDED_ARCHS[sdk=iphonesimulator*]'</span> =&gt; <span class="string">'arm64'</span> &#125;</span><br><span class="line">s.user_target_xcconfig = &#123; <span class="string">'EXCLUDED_ARCHS[sdk=iphonesimulator*]'</span> =&gt; <span class="string">'arm64'</span> &#125;</span><br></pre></td></tr></table></figure></p><p>在这种环境下对 HoloCollectionView（PodA）执行 <code>pod lib lint --allow-warnings --verbose</code> 命令，应该就能出现期望的报错了。</p><p>但因为 <code>pod lib lint</code> 命令是通过在一个临时目录下创建一个临时工程（<code>App.xcodeproj</code>）执行 <code>pod install</code> 生成 <code>App.xcworkspace</code> 进行 <code>xcodebuild</code> 验证的。那在 <code>pod install</code> 时要去拉取正式环境的开源组件，我不想发布这个一个测试版本到正式环境，所以手动修改下 repo 源里的 <code>HoloTableView.podspec.json</code> 的内容来模拟测试环境：</p><p>使用 <code>pod repo list</code> 命令，获取 repo 地址，通过 <code>find * -iname HoloTableView</code> 命令找到 <code>HoloTableView.podspec.json</code> 进行修改。<br>注 1：因为没有指定依赖版本，默认使用最新版本，我只修改了当前的最新版本（2.4.0）的 <code>HoloTableView.podspec.json</code>。<br>注 2：因为存在 master 和 trunk 两个源，我这里 lint 默认用的是 trunk，所以我只修改了 trunk 源里的 <code>HoloTableView.podspec.json</code>。如果你那使用的 master 源进行 lint 就修改 master 源里的 <code>HoloTableView.podspec.json</code>。</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line">gonghonglou@yujiaqideMacBook-Pro ~ % pod repo </span><br><span class="line"></span><br><span class="line">master</span><br><span class="line">- Type: git (master)</span><br><span class="line">- URL:  https://github.com/CocoaPods/Specs.git</span><br><span class="line">- Path: /Users/gonghonglou/.cocoapods/repos/master</span><br><span class="line"></span><br><span class="line">trunk</span><br><span class="line">- Type: CDN</span><br><span class="line">- URL:  https://cdn.cocoapods.org/</span><br><span class="line">- Path: /Users/gonghonglou/.cocoapods/repos/trunk</span><br><span class="line"></span><br><span class="line">2 repos</span><br><span class="line">gonghonglou@yujiaqideMacBook-Pro ~ % find /Users/gonghonglou/.cocoapods/repos/trunk -iname HoloTableView </span><br><span class="line">/Users/gonghonglou/.cocoapods/repos/trunk/Specs/4/3/b/HoloTableView</span><br><span class="line">gonghonglou@yujiaqideMacBook-Pro ~ % open /Users/gonghonglou/.cocoapods/repos/trunk/Specs/4/3/b/HoloTableView</span><br></pre></td></tr></table></figure><p>如下所示，在 <code>HoloTableView.podspec.json</code> 末尾加上相关命令：<br><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br></pre></td><td class="code"><pre><span class="line">&#123;</span><br><span class="line">  <span class="attr">"name"</span>: <span class="string">"HoloTableView"</span>,</span><br><span class="line">  <span class="attr">"version"</span>: <span class="string">"2.4.0"</span>,</span><br><span class="line">  <span class="attr">"summary"</span>: <span class="string">"Harness the power of UITableView with a simplified, chainable and expressive syntax."</span>,</span><br><span class="line">  <span class="attr">"description"</span>: <span class="string">"HoloTableView is a light-weight extension for UITableView. Harness the power of UITableView with a simplified, chainable and expressive syntax."</span>,</span><br><span class="line">  <span class="attr">"homepage"</span>: <span class="string">"https://github.com/HoloFoundation/HoloTableView"</span>,</span><br><span class="line">  <span class="attr">"license"</span>: &#123;</span><br><span class="line">    <span class="attr">"type"</span>: <span class="string">"MIT"</span>,</span><br><span class="line">    <span class="attr">"file"</span>: <span class="string">"LICENSE"</span></span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="attr">"authors"</span>: &#123;</span><br><span class="line">    <span class="attr">"gonghonglou"</span>: <span class="string">"gonghonglou@icloud.com"</span></span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="attr">"source"</span>: &#123;</span><br><span class="line">    <span class="attr">"git"</span>: <span class="string">"https://github.com/HoloFoundation/HoloTableView.git"</span>,</span><br><span class="line">    <span class="attr">"tag"</span>: <span class="string">"2.4.0"</span></span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="attr">"platforms"</span>: &#123;</span><br><span class="line">    <span class="attr">"ios"</span>: <span class="string">"8.0"</span></span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="attr">"source_files"</span>: <span class="string">"HoloTableView/Classes/**/*"</span>,</span><br><span class="line">  <span class="attr">"pod_target_xcconfig"</span>: &#123;</span><br><span class="line">    <span class="attr">"EXCLUDED_ARCHS[sdk=iphonesimulator*]"</span>: <span class="string">"arm64"</span></span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="attr">"user_target_xcconfig"</span>: &#123;</span><br><span class="line">    <span class="attr">"EXCLUDED_ARCHS[sdk=iphonesimulator*]"</span>: <span class="string">"arm64"</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>另外还要避免之前 CocoaPods 缓存的影响，可以通过 <code>pod cache clean --all</code> 删除全部缓存。我这里为了避免清空其他不必要的缓存，做法是：<code>pod cache list</code> 获取缓存目录，打开相应目录找到 <code>HoloTableView</code> 相关的源码缓存和 <code>HoloTableView.podspec.json</code> 缓存直接删除掉。</p><p>提醒：如果有读者要做实验的话，记得修改过后再改回来啊！</p><p>完成以上步骤对 <code>HoloCollectionView</code> 执行 <code>pod lib lint --allow-warnings --verbose</code> 命令就能得到报错：<br><figure class="highlight ruby"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">Ld /Users/gonghonglou/Library/Developer/Xcode/DerivedData/App-dzvtulystxmgqlauixgfblgkmaay/Build/Intermediates.noindex/Pods.build/Release-iphonesimulator/HoloTableViewMGPlugin.build/Objects-normal/arm64/Binary/HoloTableViewMGPlugin normal arm64 (<span class="keyword">in</span> target <span class="string">'HoloTableViewMGPlugin'</span> from project <span class="string">'Pods'</span>)</span><br><span class="line">    cd /var/folders/z7/<span class="number">9</span>hq4dfq97cn4xt4v56vqykph0000gn/T/CocoaPods-Lint-<span class="number">20210308</span>-<span class="number">58021</span>-<span class="number">1</span>him894-HoloCollectionView/Pods</span><br><span class="line">    /Applications/Xcode.app/Contents/Developer/Toolchains/XcodeDefault.xctoolchain/usr/bin/clang -target arm64-apple-ios8.<span class="number">0</span>-simulator -dynamiclib -isysroot /Applications/Xcode.app/Contents/Developer/Platforms/iPhoneSimulator.platform/Developer/SDKs/iPhoneSimulator14.<span class="number">4</span>.sdk -L/Users/gonghonglou/Library/Developer/Xcode/DerivedData/App-dzvtulystxmgqlauixgfblgkmaay/Build/Products/Release-iphonesimulator/HoloTableViewMGPlugin -F/Users/gonghonglou/Library/Developer/Xcode/DerivedData/App-dzvtulystxmgqlauixgfblgkmaay/Build/Products/Release-iphonesimulator/HoloTableViewMGPlugin -F/Users/gonghonglou/Library/Developer/Xcode/DerivedData/App-dzvtulystxmgqlauixgfblgkmaay/Build/Products/Release-iphonesimulator/HoloTableView -F/Users/gonghonglou/Library/Developer/Xcode/DerivedData/App-dzvtulystxmgqlauixgfblgkmaay/Build/Products/Release-iphonesimulator/MGSwipeTableCell -filelist /Users/gonghonglou/Library/Developer/Xcode/DerivedData/App-dzvtulystxmgqlauixgfblgkmaay/Build/Intermediates.noindex/Pods.build/Release-iphonesimulator/HoloTableViewMGPlugin.build/Objects-normal/arm64/HoloTableViewMGPlugin.LinkFileList -install_name @rpath/HoloTableViewMGPlugin.framework/HoloTableViewMGPlugin -Xlinker -rpath -Xlinker @executable_path/Frameworks -Xlinker -rpath -Xlinker @loader_path/Frameworks -dead_strip -Xlinker -object_path_lto -Xlinker /Users/gonghonglou/Library/Developer/Xcode/DerivedData/App-dzvtulystxmgqlauixgfblgkmaay/Build/Intermediates.noindex/Pods.build/Release-iphonesimulator/HoloTableViewMGPlugin.build/Objects-normal/arm64/HoloTableViewMGPlugin_lto.o -Xlinker -objc_abi_version -Xlinker <span class="number">2</span> -fobjc-arc -fobjc-link-runtime -framework Foundation -framework HoloTableView -framework MGSwipeTableCell -Xlinker -no_adhoc_codesign -compatibility_version <span class="number">1</span> -current_version <span class="number">1</span> -Xlinker -dependency_info -Xlinker /Users/gonghonglou/Library/Developer/Xcode/DerivedData/App-dzvtulystxmgqlauixgfblgkmaay/Build/Intermediates.noindex/Pods.build/Release-iphonesimulator/HoloTableViewMGPlugin.build/Objects-normal/arm64/HoloTableViewMGPlugin_dependency_info.dat -o /Users/gonghonglou/Library/Developer/Xcode/DerivedData/App-dzvtulystxmgqlauixgfblgkmaay/Build/Intermediates.noindex/Pods.build/Release-iphonesimulator/HoloTableViewMGPlugin.build/Objects-normal/arm64/Binary/HoloTableViewMGPlugin</span><br><span class="line"><span class="symbol">ld:</span> <span class="symbol">warning:</span> ignoring file /Users/gonghonglou/Library/Developer/Xcode/DerivedData/App-dzvtulystxmgqlauixgfblgkmaay/Build/Products/Release-iphonesimulator/HoloTableView/HoloTableView.framework/HoloTableView, missing required architecture arm64 <span class="keyword">in</span> file /Users/gonghonglou/Library/Developer/Xcode/DerivedData/App-dzvtulystxmgqlauixgfblgkmaay/Build/Products/Release-iphonesimulator/HoloTableView/HoloTableView.framework/HoloTableView (<span class="number">2</span> slices)</span><br><span class="line">Undefined symbols <span class="keyword">for</span> architecture <span class="symbol">arm64:</span></span><br><span class="line">  <span class="string">"_OBJC_CLASS_$_HoloTableRowMaker"</span>, referenced <span class="symbol">from:</span></span><br><span class="line">      __OBJC<span class="number">_</span>$_CATEGORY_HoloTableRowMaker_$_MGPlugin <span class="keyword">in</span> HoloTableRowMaker+MGPlugin.o</span><br><span class="line"><span class="symbol">ld:</span> symbol(s) <span class="keyword">not</span> found <span class="keyword">for</span> architecture arm64</span><br><span class="line"><span class="symbol">clang:</span> <span class="symbol">error:</span> linker command failed with exit code <span class="number">1</span> (use -v to see invocation)</span><br></pre></td></tr></table></figure></p><p>从报错信息清楚的查到报错原因： <code>HoloTableViewMGPlugin</code>（PodB）因为依赖 <code>HoloTableView</code>（PodC），在 <code>arm64-apple-ios8.0-simulator</code> 环境下链接 <code>HoloTableView.framework</code> 找不到 <code>arm64</code> 架构。这当然了，因为我们把 <code>HoloTableView.framework</code> 的模拟器 <code>arm64</code> 架构删掉了嘛。</p><p>仔细查看全文 log （<strong><a href="http://image.gonghonglou.com/xcode12-lint-error/pod-lib-lint-HoloCollectionView-podspec.txt" target="_blank" rel="noopener">pod-lib-lint-HoloCollectionView-podspec.txt</a></strong>）可以看出，<code>pod lib lint --allow-warnings --verbose</code> 命令就是为了校验 <code>App.xcworkspace</code> 工程能够编译成功，而在编译 <code>App.xcworkspace</code> 这个主工程之前会先编译引入的各个 Pod，将各个 Pod 编译成功之后再引入到 <code>App.xcworkspace</code> 里进行打包。</p><p>在整个编译过程中，先编译了 <code>HoloTableView</code> 得到了产物 <code>HoloTableView.framework</code>（不包含模拟器 <code>arm64</code> 架构），然后在编译 <code>HoloTableViewMGPlugin</code>（依赖 <code>HoloTableView</code>）时，对 <code>arm64-apple-ios8.0-simulator</code> 环境下链接 <code>HoloTableView.framework</code> 出错了。</p><h1 id="后记"><a href="#后记" class="headerlink" title="后记"></a>后记</h1><p>所以，如果你在使用 Xcode12 进行组件发版，对于这个问题的解决方案可以参考 Braintree 的方案（<a href="https://github.com/braintree/braintree_ios/blob/4.36.1/Braintree.podspec#L136-L138" target="_blank" rel="noopener">Braintree.podspec</a>)，在 podsepc 里加上移除模拟器环境 <code>arm64</code> 架构支持的命令。但因为 “疑问 6” 场景的存在，这意味着你可能需要将所有的组件重新打个版本。<br>或者也可以像我们一样暂时使用 <code>pod lib lint --skip-import-validation</code> 参数跳过引用验证，但这可能导致某些问题的隐藏，并非长久之计。<br>看来也只有等待三方库们各自支持了模拟器环境下的 <code>arm64</code> 架构之后才能彻底忽略这一问题了。</p><p>终于，我们通过查看大量的 log 日志和部分 CocoaPods 源码搞清楚了所有的疑问。也弄明白了 lint 过程，或者说 <code>xcodebuild</code> 过程的一些内容。过后才觉得对我们来说这本该是 App 编译产出的基础知识，却因为学了一点 iOS 技能就早早开始了业务开发，平时也不太用到所以就被忽略了，导致一开始看到问题的时候并未理解到本质。希望之后能多了解些基础知识，多遇到些这样的问题和这样的 Debug 过程吧。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h1 id=&quot;前言&quot;&gt;&lt;a href=&quot;#前言&quot; class=&quot;headerlink&quot; title=&quot;前言&quot;&gt;&lt;/a&gt;前言&lt;/h1&gt;&lt;p&gt;之前打包机升级 Xcode12 之后发现私有库发版时执行 &lt;code&gt;pod lib lint&lt;/code&gt; 命令报错，一开始看报错信息提示
      
    
    </summary>
    
      <category term="iOS" scheme="http://gonghonglou.com/categories/iOS/"/>
    
    
  </entry>
  
  <entry>
    <title>Say Bye 2020</title>
    <link href="http://gonghonglou.com/2020/12/31/saybye2020/"/>
    <id>http://gonghonglou.com/2020/12/31/saybye2020/</id>
    <published>2020-12-30T16:26:13.000Z</published>
    <updated>2021-01-04T16:31:23.529Z</updated>
    
    <content type="html"><![CDATA[<p>眨眼又到年终</p><p>2020，真是感觉过得又快又慢。快的是还没觉察呢这一年就要结束了，感觉今年啥也没干就过去了，仿佛年初在家办公躲避疫情的日子就在昨天；慢的是恍然想起今年发生的什么事好像是很久很久之前发生的一样，不像是在今年，真是神奇。</p><p>不过说到底，今年因为疫情都没出去玩，一直呆在家里，感觉好久没出门了。即使回过老家也是因为家里有事情。没有出去玩就像是一直被关在家里。去年因为换工作，所以十一没去成台湾环岛骑行，今年因为疫情又没能去，明天大概率也是没法去了，唉，记得18年去环海南岛的时候鼓励自己的理由是：“如果今年不去，可能往后就去不成，不管是没时间还是没毅力，总会有各种各样的的理由“，果然，台湾之行就是这种情形了。</p><h1 id="工作"><a href="#工作" class="headerlink" title="工作"></a>工作</h1><p>去年换了工作，又回了上海。本来去年的年终总结该提到这个的，可是去年的时候因为生活不可爱，年终总结都没写。刚到新公司的时候看到公司里的项目工程，和之前工作过的项目工程差别还挺大的，各种代码写的很基础，待改进的地方很多，有技术含量的内容又是少之又少。所以还是造成了很大的焦虑的，当然从头开始组件化，实践组件化过程中各种问题的解决方案，搭建项目基础架构，写出优秀的工具组件，可以做各种优化，这些也是吸引我的地方了。</p><p>从创建私有源，拆分第一个私有组件开始，规范组件发版过程；组件开发过程中的分支管理，修改代码后打包即时生效；编写 CocoaPods 插件提高发版效率；组件化分发生命周期的探索；组件化页面路由方案的探索；组件化资源文件管理方案的探索；重写网络请求框架，提高复用性，易用性；重写基础 VC，导航栏控件；重写各种工具类，重构各种业务模块 等等等等</p><p>下半年项目开始转 Swift，一直觉得应该要学习 Swift 了，但是总没有一个契机入手。从今年下半年开始项目开始转 Swift，本来以为会花费蛮长时间上手的，结果写了一两天感觉就可以无障碍上手写业务了，当然还是有很多 Swift 语法特性是提前看了很久的文档，还有之后慢慢学习上手的。但终归是开始了 Swift 的工作，上手还是很轻松的。需要的就是后期慢慢深入了。</p><p>之后还要努力往底层和性能优化方向多学习：Swift 也需要往更底层学习；卡顿监控，耗时监控，崩溃监控等 APM 工具；优秀的热修方案等</p><p>值得欣慰的是今年写了几篇技术博客，创建了 <a href="https://github.com/HoloFoundation" target="_blank" rel="noopener">HoloFoundation</a> 写了些开源组件，也让博客和 GitHub 像了点样子。</p><h1 id="生活"><a href="#生活" class="headerlink" title="生活"></a>生活</h1><p>又回了上海工作，住在家里，和父母一起。不用考虑房租，回家就有饭吃的感觉，在经历了一年杭州的独自居住和现实生活的暴击之后愈发显得珍贵了。</p><p>在杭州工作了一年，缴纳了一年社保，通过人才引进落户了杭州。年中的时候开始正式买房的打算，来来回回周末去了好多次杭州，摇了好几次号也不中。经历了西溪公馆的万人摇之后迟迟没有新楼盘开张，好不容易等到有心仪的楼盘“万科-天空之城”开盘，杭州又出新政，判定为热门楼盘的话只能是高层次人才和无房家庭才能参与摇号，无房家庭指的是已婚没房的和三十岁以上单身的。不符合条件的我也就丧失了参与摇号的资格。对摇号买房失去信心之后又跑去杭州看了一圈二手房，发现二手房较于新房又旧又贵又小，不得已还是打算再摇号试试吧，所幸的是接连在拱墅又有好几个位置价格户型都合适楼盘将要开盘，可惜摇了两次都没中。但竟然在十二月的末尾的又一次摇号中摇中了，203号，还挺靠后的，本来感觉希望不大了，最后却是幸运了捡漏抢到了一套。全家都很开心，接着第二天就去付了首付。算下来面积适当，首付没有很多，月供压力也不算大，总之就是挺合适的，终于了了一件大事。</p><h1 id="读书-动漫-桌球"><a href="#读书-动漫-桌球" class="headerlink" title="读书/动漫/桌球"></a>读书/动漫/桌球</h1><p>好长时间没有静下心了好好读几本书了，今年看了几本技术类书籍也都没有完全看完，零散看了一点小说也没有看完，总之读书这一项最近两年：完败！</p><p>倒是动漫看了不少：《鬼灭之刃》、《咒术回战》、刚开始更新的《进击的巨人最终季》，国漫里的《一人之下3》、《全职高手2》、《雾山五行》等等</p><p>最开心的就是今年在公司遇到了一位球友了，几乎每个周五下班后都会去打几个小时，有段时间进步还挺明显的，跟着学了挺多走位的意识，虽然打得依旧很菜，多多练习吧</p><h1 id="计划"><a href="#计划" class="headerlink" title="计划"></a>计划</h1><p>工作方面多往底层基础架构层面深入，找到一些方向深入钻研！继续更新博客，写一些比较有技术深度的文章。继续更新 GitHub，写一些比较有意义的代码。</p><p>生活方面不知道环台湾岛骑行什么时候能被安排上，我想，我总会去的，嗯！</p><p>少些时间玩手机，多些时间看看书吧。</p><p>尽快在 GitHub 上开一个算法仓库，开始刷刷算法，补补一直以来的明显弱点。</p><p>嗯，就这样，不要被生活打倒！</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;眨眼又到年终&lt;/p&gt;
&lt;p&gt;2020，真是感觉过得又快又慢。快的是还没觉察呢这一年就要结束了，感觉今年啥也没干就过去了，仿佛年初在家办公躲避疫情的日子就在昨天；慢的是恍然想起今年发生的什么事好像是很久很久之前发生的一样，不像是在今年，真是神奇。&lt;/p&gt;
&lt;p&gt;不过说到底，今
      
    
    </summary>
    
      <category term="Life" scheme="http://gonghonglou.com/categories/Life/"/>
    
    
  </entry>
  
  <entry>
    <title>细数 iOS 里的 Hook 方案</title>
    <link href="http://gonghonglou.com/2020/11/08/hook/"/>
    <id>http://gonghonglou.com/2020/11/08/hook/</id>
    <published>2020-11-07T16:08:14.000Z</published>
    <updated>2020-11-08T08:14:30.140Z</updated>
    
    <content type="html"><![CDATA[<p>之前在实现 <a href="http://gonghonglou.com/2020/03/08/pod-lifecycle-aop/">组件化分发生命周期 - AOP 方案</a> 过程中用到了 <a href="https://github.com/steipete/Aspects" target="_blank" rel="noopener">Aspects</a> 这个库，所以当时又仔细看了看源码，借鉴了些消息分发的写法。后来又为了性能考虑用 <a href="https://github.com/libffi/libffi" target="_blank" rel="noopener">libffi</a> 这个库替换了 Aspects 的实现 ，参考了 <a href="https://github.com/eleme/Stinger" target="_blank" rel="noopener">Stinger</a> 这个库的实现。本质上都是在做 Hook 操作，加上最近看了些 <a href="https://github.com/facebook/fishhook" target="_blank" rel="noopener">fishhook</a> 的原理，涉及了很多 Mach-O 等底层原理，所以打算把 iOS 里的一些 Hook 方案列一下，做个笔记总结，就是这篇文章的初衷了。</p><p>看了一段时间的底层原理就有些倦怠了，所以计划了一场组内分享驱动着看完了 fishhook 相关的知识。Demo 和 keynote 放在了 GitHub上，地址在这：<a href="https://github.com/gonghonglou/HookDemo" target="_blank" rel="noopener">https://github.com/gonghonglou/HookDemo</a></p><h1 id="Preview"><a href="#Preview" class="headerlink" title="Preview"></a>Preview</h1><blockquote><p>1、Method Swizzling<br>2、Message Forwarding<br>3、libffi<br>4、fishhook<br>5、静态库插桩</p><p>6、基于桥的全量方法 Hook 方案 TrampoLineHook<br>7、Dobby / Frida</p></blockquote><h1 id="小试牛刀：Method-Swizzling"><a href="#小试牛刀：Method-Swizzling" class="headerlink" title="小试牛刀：Method Swizzling"></a>小试牛刀：Method Swizzling</h1><p>这是 iOS 里最基础最原生的 Hook 方法了，当然也是性能最好的选择。本质上就是交换两个方法的 IMP（函数指针），即：</p><p><img src="http://image.gonghonglou.com/hook/method_swizzling.png" alt="Method Swizzling"></p><p>常见的写法也很简单：</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">@implementation</span> <span class="title">HookDemoObj</span> (<span class="title">HK</span>)</span></span><br><span class="line"></span><br><span class="line">+ (<span class="keyword">void</span>)load &#123;</span><br><span class="line">    <span class="keyword">static</span> <span class="built_in">dispatch_once_t</span> onceToken;</span><br><span class="line">    <span class="built_in">dispatch_once</span>(&amp;onceToken, ^&#123;</span><br><span class="line">        Class <span class="keyword">class</span> = [<span class="keyword">self</span> <span class="keyword">class</span>];</span><br><span class="line"></span><br><span class="line">        SEL originalSelector = <span class="keyword">@selector</span>(loopLogWithCount:);</span><br><span class="line">        SEL swizzledSelector = <span class="keyword">@selector</span>(hook_loopLogWithCount:);</span><br><span class="line"></span><br><span class="line">        Method originalMethod = class_getInstanceMethod(<span class="keyword">class</span>, originalSelector);</span><br><span class="line">        Method swizzledMethod = class_getInstanceMethod(<span class="keyword">class</span>, swizzledSelector);</span><br><span class="line"></span><br><span class="line">        <span class="built_in">BOOL</span> success = class_addMethod(<span class="keyword">class</span>, originalSelector, method_getImplementation(swizzledMethod), method_getTypeEncoding(swizzledMethod));</span><br><span class="line">        <span class="keyword">if</span> (success) &#123;</span><br><span class="line">            class_replaceMethod(<span class="keyword">class</span>, swizzledSelector, method_getImplementation(originalMethod), method_getTypeEncoding(originalMethod));</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            method_exchangeImplementations(originalMethod, swizzledMethod);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">- (<span class="keyword">void</span>)hook_loopLogWithCount:(<span class="built_in">NSInteger</span>)count &#123;</span><br><span class="line">    [<span class="keyword">self</span> hook_loopLogWithCount:count];</span><br><span class="line">    <span class="built_in">NSLog</span>(<span class="string">@"hook after count: %d"</span>, count);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>需要注意的也就是以下几点：</p><p>1、为什么 load 时机 可以成功执行？</p><p>1.1、启动 dyld（the dynamic link editor）将应用程序加载到二进制中<br>1.2、Runtime 向 dyld 中注册回调函数<br>1.3、通过 ImageLoader 将所有的 image 加载到内存中<br>1.4、dyld 在 image 发生改变时，主动调用回调函数<br>1.5、Runtime 接收到 dyld 的函数回调，开始 map_images、load_images等操作，并回调 +load 方法<br>1.6、调用 mian 函数</p><p>其中 3、4、5会执行多次，在 ImageLoader 加载新的 image 进内存后就会执行一次<br>ImageLoader 是 image 的加载器，image 可以理解为编译后的二进制</p><p>2、dispatch_once 保证，避免方法被多次交换。</p><p>3、调用原方法，因为方法已经被交换过了，所以这里调用 <code>[self hook_loopLogWithCount:count];</code> 即执行的是原函数（originalIMP）。</p><h1 id="穿针引线：Message-Forwarding"><a href="#穿针引线：Message-Forwarding" class="headerlink" title="穿针引线：Message Forwarding"></a>穿针引线：Message Forwarding</h1><p>主要是 <a href="https://github.com/steipete/Aspects" target="_blank" rel="noopener">Aspects</a> 的原理，让目标方法在被执行时直接进入快速消息转发流程，在最后的 <code>forwardInvocation:</code> 方法里拿到 <code>NSInvocation</code> 对象，即包含了被调用方法的所有信息，主要是参数个数，参数值。然后在调用原方法之前或之后调用传入的 Block。</p><p>看一下 Aspects 的使用：</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">HookDemoObj *forwardingObj = [HookDemoObj new];</span><br><span class="line"><span class="comment">// 无参数的 block</span></span><br><span class="line">[forwardingObj aspect_hookSelector:<span class="keyword">@selector</span>(logString:) withOptions:AspectPositionAfter usingBlock: ^&#123;</span><br><span class="line">    <span class="built_in">NSLog</span>(<span class="string">@"Aspects after"</span>); <span class="comment">// Aspects after</span></span><br><span class="line">&#125; error:<span class="literal">nil</span>];</span><br><span class="line"><span class="comment">// 有参数的 block</span></span><br><span class="line">[forwardingObj aspect_hookSelector:<span class="keyword">@selector</span>(logString:) withOptions:AspectPositionAfter usingBlock: ^(<span class="keyword">id</span>&lt;AspectInfo&gt; info, <span class="built_in">NSString</span> *str)&#123;</span><br><span class="line">    <span class="built_in">NSLog</span>(<span class="string">@"Aspects after: %@"</span>, str); <span class="comment">// Aspects after: abc</span></span><br><span class="line">&#125; error:<span class="literal">nil</span>];</span><br><span class="line">[forwardingObj logString:<span class="string">@"abc"</span>];</span><br></pre></td></tr></table></figure><p>需要注意的有以下几点：</p><p>1、isa-swizzling 的应用<br>2、消息转发流程。如何不重写 “methodSignatureForSelector:” 方法也能进入 “forwardInvocation:” 阶段？<br>3、如何获取 block NSMethodSignature？并且在不定参数情况下执行 block？</p><p>关于isa-swizzling 的应用：isa-swizzling 的原理如下图。即 KVO 的基本原理：原来的对象 isa 指向一个 class，通过 runtime 动态创建一个 class 的子类，并将原来对象的 isa 指向这个新建的子类，重写这个子类 getter、setter 即可。</p><p><img src="http://image.gonghonglou.com/hook/isa-swizzling.png" alt="isa-swizzling"></p><p>Aspects 的应用：<br><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// Default case. Create dynamic subclass.</span></span><br><span class="line"><span class="keyword">const</span> <span class="keyword">char</span> *subclassName = [className stringByAppendingString:AspectsSubclassSuffix].UTF8String;</span><br><span class="line">Class subclass = objc_getClass(subclassName);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 判断目标类是否创建过</span></span><br><span class="line"><span class="keyword">if</span> (subclass == <span class="literal">nil</span>) &#123;</span><br><span class="line">    <span class="comment">// 动态创建子类</span></span><br><span class="line">    subclass = objc_allocateClassPair(baseClass, subclassName, <span class="number">0</span>);</span><br><span class="line">    <span class="keyword">if</span> (subclass == <span class="literal">nil</span>) &#123;</span><br><span class="line">        <span class="built_in">NSString</span> *errrorDesc = [<span class="built_in">NSString</span> stringWithFormat:<span class="string">@"objc_allocateClassPair failed to allocate class %s."</span>, subclassName];</span><br><span class="line">        AspectError(AspectErrorFailedToAllocateClassPair, errrorDesc);</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">nil</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 交换 forwardInvocation: 方法</span></span><br><span class="line">    aspect_swizzleForwardInvocation(subclass);</span><br><span class="line">    <span class="comment">// 重写 class 方法</span></span><br><span class="line">    aspect_hookedGetClass(subclass, statedClass);</span><br><span class="line">    aspect_hookedGetClass(object_getClass(subclass), statedClass);</span><br><span class="line">    <span class="comment">// 注册动态创建的类</span></span><br><span class="line">    objc_registerClassPair(subclass);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 修改 isa 指针指向新创建的子类</span></span><br><span class="line">object_setClass(<span class="keyword">self</span>, subclass);</span><br></pre></td></tr></table></figure></p><p>关于消息转发流程：正常的消息转发流程如下：<br><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line">+ (<span class="built_in">BOOL</span>)resolveInstanceMethod:(SEL)sel &#123;</span><br><span class="line">    <span class="built_in">NSLog</span>(<span class="string">@"1:%@"</span>, <span class="built_in">NSStringFromSelector</span>(_cmd));</span><br><span class="line">    <span class="keyword">return</span> [<span class="keyword">super</span> resolveInstanceMethod:sel];</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">- (<span class="keyword">id</span>)forwardingTargetForSelector:(SEL)aSelector &#123;</span><br><span class="line">    <span class="built_in">NSLog</span>(<span class="string">@"2:%@"</span>, <span class="built_in">NSStringFromSelector</span>(_cmd));</span><br><span class="line">    <span class="keyword">return</span> [<span class="keyword">super</span> forwardingTargetForSelector:aSelector];</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">- (<span class="built_in">NSMethodSignature</span> *)methodSignatureForSelector:(SEL)aSelector &#123;</span><br><span class="line">    <span class="built_in">NSLog</span>(<span class="string">@"3:%@"</span>, <span class="built_in">NSStringFromSelector</span>(_cmd));</span><br><span class="line">    <span class="keyword">return</span> [<span class="keyword">super</span> methodSignatureForSelector:aSelector];</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">- (<span class="keyword">void</span>)forwardInvocation:(<span class="built_in">NSInvocation</span> *)anInvocation &#123;</span><br><span class="line">    <span class="built_in">NSLog</span>(<span class="string">@"4:%@"</span>, <span class="built_in">NSStringFromSelector</span>(_cmd));</span><br><span class="line">    [<span class="keyword">super</span> forwardInvocation:anInvocation];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>注意，必须实现 <code>methodSignatureForSelector:</code> 方法返回 <code>NSMethodSignature</code> 才会进入 <code>forwardInvocation:</code> 方法。但 Aspects 并没有实现 <code>methodSignatureForSelector:</code> 方法。Aspects 的做法是：把要 hook 的方法通过 <code>class_replaceMethod()</code> 接口指向 <code>_objc_msgForward</code>，这是一个全局 IMP，OC 调用方法不存在时都会转发到这个 IMP 上，直接把方法替换成这个 IMP，这样调用 hook 方法时就会走到 <code>forwardInvocation:</code>：<br><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">Method targetMethod = class_getInstanceMethod(klass, selector);</span><br><span class="line"><span class="keyword">const</span> <span class="keyword">char</span> *typeEncoding = method_getTypeEncoding(targetMethod);</span><br><span class="line">class_replaceMethod(klass, selector, _objc_msgForward, typeEncoding);</span><br></pre></td></tr></table></figure></p><p>关于 Block NSMethodSignature 和调用：</p><p>这里 <a href="https://opensource.apple.com/source/libclosure/libclosure-73/Block_private.h.auto.html" target="_blank" rel="noopener">Block_layout</a> 是 Block 的源码，Block 的结构体内容是：<br><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#define BLOCK_DESCRIPTOR_1 1</span></span><br><span class="line"><span class="keyword">struct</span> Block_descriptor_1 &#123;</span><br><span class="line">    uintptr_t reserved;</span><br><span class="line">    uintptr_t size;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="meta">#define BLOCK_DESCRIPTOR_2 1</span></span><br><span class="line"><span class="keyword">struct</span> Block_descriptor_2 &#123;</span><br><span class="line">    <span class="comment">// requires BLOCK_HAS_COPY_DISPOSE</span></span><br><span class="line">    BlockCopyFunction <span class="keyword">copy</span>;</span><br><span class="line">    BlockDisposeFunction dispose;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="meta">#define BLOCK_DESCRIPTOR_3 1</span></span><br><span class="line"><span class="keyword">struct</span> Block_descriptor_3 &#123;</span><br><span class="line">    <span class="comment">// requires BLOCK_HAS_SIGNATURE</span></span><br><span class="line">    <span class="keyword">const</span> <span class="keyword">char</span> *signature;</span><br><span class="line">    <span class="keyword">const</span> <span class="keyword">char</span> *layout;     <span class="comment">// contents depend on BLOCK_HAS_EXTENDED_LAYOUT</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">struct</span> Block_layout &#123;</span><br><span class="line">    <span class="keyword">void</span> *isa;</span><br><span class="line">    <span class="keyword">volatile</span> int32_t flags; <span class="comment">// contains ref count</span></span><br><span class="line">    int32_t reserved;</span><br><span class="line">    BlockInvokeFunction invoke;</span><br><span class="line">    <span class="keyword">struct</span> Block_descriptor_1 *descriptor;</span><br><span class="line">    <span class="comment">// imported variables</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></p><p>isa 指向 Block 对象的类；<br>flags 决定 Block 包含的 Block_descriptor_1、Block_descriptor_2、Block_descriptor_3 信息；<br>reserved 预留字段</p><p>Aspects 获取 Block signature 的方式：<br><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 这里自己定义了和系统 Block_layout 布局一致的 block 结构体</span></span><br><span class="line"><span class="comment">// Block internals.</span></span><br><span class="line"><span class="keyword">typedef</span> <span class="built_in">NS_OPTIONS</span>(<span class="keyword">int</span>, AspectBlockFlags) &#123;</span><br><span class="line">AspectBlockFlagsHasCopyDisposeHelpers = (<span class="number">1</span> &lt;&lt; <span class="number">25</span>),</span><br><span class="line">AspectBlockFlagsHasSignature          = (<span class="number">1</span> &lt;&lt; <span class="number">30</span>)</span><br><span class="line">&#125;;</span><br><span class="line"><span class="keyword">typedef</span> <span class="keyword">struct</span> _AspectBlock &#123;</span><br><span class="line">__unused Class isa;</span><br><span class="line">AspectBlockFlags flags;</span><br><span class="line">__unused <span class="keyword">int</span> reserved;</span><br><span class="line"><span class="keyword">void</span> (__unused *invoke)(<span class="keyword">struct</span> _AspectBlock *block, ...);</span><br><span class="line"><span class="keyword">struct</span> &#123;</span><br><span class="line"><span class="keyword">unsigned</span> <span class="keyword">long</span> <span class="keyword">int</span> reserved;</span><br><span class="line"><span class="keyword">unsigned</span> <span class="keyword">long</span> <span class="keyword">int</span> size;</span><br><span class="line"><span class="comment">// requires AspectBlockFlagsHasCopyDisposeHelpers</span></span><br><span class="line"><span class="keyword">void</span> (*<span class="keyword">copy</span>)(<span class="keyword">void</span> *dst, <span class="keyword">const</span> <span class="keyword">void</span> *src);</span><br><span class="line"><span class="keyword">void</span> (*dispose)(<span class="keyword">const</span> <span class="keyword">void</span> *);</span><br><span class="line"><span class="comment">// requires AspectBlockFlagsHasSignature</span></span><br><span class="line"><span class="keyword">const</span> <span class="keyword">char</span> *signature;</span><br><span class="line"><span class="keyword">const</span> <span class="keyword">char</span> *layout;</span><br><span class="line">&#125; *descriptor;</span><br><span class="line"><span class="comment">// imported variables</span></span><br><span class="line">&#125; *AspectBlockRef;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 用该方法获取 block 的 signature</span></span><br><span class="line"><span class="keyword">static</span> <span class="built_in">NSMethodSignature</span> *aspect_blockMethodSignature(<span class="keyword">id</span> block, <span class="built_in">NSError</span> **error) &#123;</span><br><span class="line">    <span class="comment">// 将 block 强转为自己定义的结构体：AspectBlockRef，和系统 Block_layout 定义的一致</span></span><br><span class="line">    AspectBlockRef layout = (__bridge <span class="keyword">void</span> *)block;</span><br><span class="line"><span class="keyword">if</span> (!(layout-&gt;flags &amp; AspectBlockFlagsHasSignature)) &#123;</span><br><span class="line">        <span class="built_in">NSString</span> *description = [<span class="built_in">NSString</span> stringWithFormat:<span class="string">@"The block %@ doesn't contain a type signature."</span>, block];</span><br><span class="line">        AspectError(AspectErrorMissingBlockSignature, description);</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">nil</span>;</span><br><span class="line">    &#125;</span><br><span class="line"><span class="keyword">void</span> *desc = layout-&gt;descriptor;</span><br><span class="line">    <span class="comment">// 1、偏移 reserved、size 两个 Int 类型的大小</span></span><br><span class="line">desc += <span class="number">2</span> * <span class="keyword">sizeof</span>(<span class="keyword">unsigned</span> <span class="keyword">long</span> <span class="keyword">int</span>);</span><br><span class="line"><span class="keyword">if</span> (layout-&gt;flags &amp; AspectBlockFlagsHasCopyDisposeHelpers) &#123;</span><br><span class="line">        <span class="comment">// 2、偏移 copy、dispose 两个 指针 类型的大小</span></span><br><span class="line">desc += <span class="number">2</span> * <span class="keyword">sizeof</span>(<span class="keyword">void</span> *);</span><br><span class="line">    &#125;</span><br><span class="line"><span class="keyword">if</span> (!desc) &#123;</span><br><span class="line">        <span class="built_in">NSString</span> *description = [<span class="built_in">NSString</span> stringWithFormat:<span class="string">@"The block %@ doesn't has a type signature."</span>, block];</span><br><span class="line">        AspectError(AspectErrorMissingBlockSignature, description);</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">nil</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 3、即拿到了 signature</span></span><br><span class="line"><span class="keyword">const</span> <span class="keyword">char</span> *signature = (*(<span class="keyword">const</span> <span class="keyword">char</span> **)desc);</span><br><span class="line"><span class="keyword">return</span> [<span class="built_in">NSMethodSignature</span> signatureWithObjCTypes:signature];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>将这些信息保存下来，在方法进入 <code>forwardInvocation:</code> 阶段，拿到方法的参数个数和入参值等信息分发到传入的 Block 上：</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br></pre></td><td class="code"><pre><span class="line">- (<span class="built_in">BOOL</span>)invokeWithInfo:(<span class="keyword">id</span>&lt;AspectInfo&gt;)info &#123;</span><br><span class="line">    <span class="comment">// block Invocation</span></span><br><span class="line">    <span class="built_in">NSInvocation</span> *blockInvocation = [<span class="built_in">NSInvocation</span> invocationWithMethodSignature:<span class="keyword">self</span>.blockSignature];</span><br><span class="line">    <span class="comment">// 要 hook 方法的 Invocation</span></span><br><span class="line">    <span class="built_in">NSInvocation</span> *originalInvocation = info.originalInvocation;</span><br><span class="line">    <span class="comment">// 获取参数个数</span></span><br><span class="line">    <span class="built_in">NSUInteger</span> numberOfArguments = <span class="keyword">self</span>.blockSignature.numberOfArguments;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// Be extra paranoid. We already check that on hook registration.</span></span><br><span class="line">    <span class="keyword">if</span> (numberOfArguments &gt; originalInvocation.methodSignature.numberOfArguments) &#123;</span><br><span class="line">        AspectLogError(<span class="string">@"Block has too many arguments. Not calling %@"</span>, info);</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">NO</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// The `self` of the block will be the AspectInfo. Optional.</span></span><br><span class="line">    <span class="keyword">if</span> (numberOfArguments &gt; <span class="number">1</span>) &#123;</span><br><span class="line">        <span class="comment">// block 与普通 OC 方法不同的是第 0 位是 block 自己，往后排列的是参数。</span></span><br><span class="line">        <span class="comment">// 这里第 1 位存放 info 信息，从第 2 位开始恰好和普通参数位置对其</span></span><br><span class="line">        [blockInvocation setArgument:&amp;info atIndex:<span class="number">1</span>];</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line"><span class="keyword">void</span> *argBuf = <span class="literal">NULL</span>;</span><br><span class="line">    <span class="comment">// 普通 OC 方法的第 0 位是 id 类型，即对象自己；第 1 位 是 SEL 类型，即方法本身；往后排列的才是参数</span></span><br><span class="line">    <span class="comment">// 所以这里从 index=2 开始遍历</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="built_in">NSUInteger</span> idx = <span class="number">2</span>; idx &lt; numberOfArguments; idx++) &#123;</span><br><span class="line">        <span class="keyword">const</span> <span class="keyword">char</span> *type = [originalInvocation.methodSignature getArgumentTypeAtIndex:idx];</span><br><span class="line"><span class="built_in">NSUInteger</span> argSize;</span><br><span class="line"><span class="built_in">NSGetSizeAndAlignment</span>(type, &amp;argSize, <span class="literal">NULL</span>);</span><br><span class="line">        </span><br><span class="line"><span class="keyword">if</span> (!(argBuf = reallocf(argBuf, argSize))) &#123;</span><br><span class="line">            AspectLogError(<span class="string">@"Failed to allocate memory for block invocation."</span>);</span><br><span class="line"><span class="keyword">return</span> <span class="literal">NO</span>;</span><br><span class="line">&#125;</span><br><span class="line">        </span><br><span class="line">        <span class="comment">// 对方法对应位置上取出入参值赋予 block Invocation 对应位置上</span></span><br><span class="line">[originalInvocation getArgument:argBuf atIndex:idx];</span><br><span class="line">[blockInvocation setArgument:argBuf atIndex:idx];</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    [blockInvocation invokeWithTarget:<span class="keyword">self</span>.block];</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">if</span> (argBuf != <span class="literal">NULL</span>) &#123;</span><br><span class="line">        free(argBuf);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">YES</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h1 id="移花接木：libffi"><a href="#移花接木：libffi" class="headerlink" title="移花接木：libffi"></a>移花接木：libffi</h1><p><a href="https://github.com/libffi/libffi" target="_blank" rel="noopener">libffi</a> 相当于 C 语言上的 runtime，拥有动态调用 C 方法及 OC 方法的能力。简单介绍下用法，例如，调用 C 函数：</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">int</span> c_func(<span class="keyword">int</span> a , <span class="keyword">int</span> b) &#123;</span><br><span class="line">    <span class="keyword">int</span> sum = a + b;</span><br><span class="line">    <span class="keyword">return</span> sum;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">- (<span class="keyword">void</span>)libffi_call_c_func &#123;</span><br><span class="line">    ffi_cif cif;</span><br><span class="line">    ffi_type *argTypes[] = &#123;&amp;ffi_type_sint, &amp;ffi_type_sint&#125;;</span><br><span class="line">    ffi_prep_cif(&amp;cif, FFI_DEFAULT_ABI, <span class="number">2</span>, &amp;ffi_type_sint, argTypes);</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">int</span> a = <span class="number">1</span>;</span><br><span class="line">    <span class="keyword">int</span> b = <span class="number">2</span>;</span><br><span class="line">    <span class="keyword">void</span> *args[] = &#123;&amp;a, &amp;b&#125;;</span><br><span class="line">    <span class="keyword">int</span> retValue;</span><br><span class="line">    ffi_call(&amp;cif, (<span class="keyword">void</span> *)c_func, &amp;retValue, args); <span class="comment">// retValue = 3</span></span><br><span class="line">    </span><br><span class="line">    <span class="built_in">NSLog</span>(<span class="string">@"libffi_call_c_func, retValue:%d"</span>, retValue);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>调用 OC 函数：</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line">- (<span class="keyword">int</span>)oc_func:(<span class="keyword">int</span>)a b:(<span class="keyword">int</span>)b &#123;</span><br><span class="line">    <span class="keyword">int</span> sum = a + b;</span><br><span class="line">    <span class="keyword">return</span> sum;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">- (<span class="keyword">void</span>)libffi_call_oc_func &#123;</span><br><span class="line">    SEL selector = <span class="keyword">@selector</span>(oc_func:b:);</span><br><span class="line">    <span class="built_in">NSMethodSignature</span> *signature = [<span class="keyword">self</span> methodSignatureForSelector:selector];</span><br><span class="line">    </span><br><span class="line">    ffi_cif cif;</span><br><span class="line">    ffi_type *argTypes[] = &#123;&amp;ffi_type_pointer, &amp;ffi_type_pointer, &amp;ffi_type_sint, &amp;ffi_type_sint&#125;;</span><br><span class="line">    ffi_prep_cif(&amp;cif, FFI_DEFAULT_ABI, (uint32_t)signature.numberOfArguments, &amp;ffi_type_sint, argTypes);</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">int</span> arg1 = <span class="number">1</span>;</span><br><span class="line">    <span class="keyword">int</span> arg2 = <span class="number">2</span>;</span><br><span class="line">    <span class="keyword">void</span> *args[] = &#123;(__bridge <span class="keyword">void</span> *)(<span class="keyword">self</span>), selector, &amp;arg1, &amp;arg2&#125;;</span><br><span class="line">    <span class="keyword">int</span> retValue;</span><br><span class="line">    IMP func = [<span class="keyword">self</span> methodForSelector:selector];</span><br><span class="line">    ffi_call(&amp;cif, (<span class="keyword">void</span> *)func, &amp;retValue, args); <span class="comment">// retValue = 3</span></span><br><span class="line">    </span><br><span class="line">    <span class="built_in">NSLog</span>(<span class="string">@"libffi_call_oc_func, retValue:%d"</span>, retValue);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>函数调用 <code>ffi_call</code> 方法需要传入：1、函数模版（<code>ffi_cif</code>），2、函数指针，3、返回值，4、参数数组 这四个参数字段。<br>重点在于 函数模版（<code>ffi_cif</code>）。用 <code>ffi_cif</code> 生成一套函数模版，这个模版定义了调用一个函数时，这个函数的：1、参数个数（<code>int</code>），2、返回值类型（<code>ffi_type</code>），3、包含各个入参类型（<code>ffi_type</code>）的数组</p><p>这样，<code>libffi</code> 的 <code>ffi_call</code> 方法就可以根据这个传入的 函数模版（<code>ffi_cif</code>）去调用函数了。</p><p><code>ffi_type</code> 类型对应着系统的 <a href="https://developer.apple.com/library/archive/documentation/Cocoa/Conceptual/ObjCRuntimeGuide/Articles/ocrtTypeEncodings.html" target="_blank" rel="noopener">Type Encodings</a> 类型</p><p>除了动态调用 C &amp; OC 方法外，<code>libffi</code> 提供的另外一个关键的能力是：<br>通过 <code>ffi_closure_alloc</code> 方法创建一个关联着函数指针（<code>IMP</code>）的闭包（<code>closure</code>）<br>通过 <code>ffi_prep_closure_loc</code> 函数给 <code>cif</code> 关联上这个闭包，并传入一个函数实体（<code>fun</code>）和刚才闭包里的 <code>IMP</code></p><p>这样，当 <code>IMP</code> 被执行的时候，就会执行函数实体（<code>fun</code>），并且能在这个函数里拿到：1、<code>cif</code>，2、返回值，3、参数地址，4、自定义的关联数据（<code>userdata</code>），例如：</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">void</span> *newIMP = <span class="literal">NULL</span>;</span><br><span class="line">ffi_closure *closure = ffi_closure_alloc(<span class="keyword">sizeof</span>(ffi_closure), (<span class="keyword">void</span> **)&amp;newIMP);</span><br><span class="line">ffi_status prepClosureStatus = ffi_prep_closure_loc(closure, cif, holo_lifecycle_ffi_closure_func, (__bridge <span class="keyword">void</span> *)info, newIMP);</span><br><span class="line"><span class="keyword">if</span> (prepClosureStatus != FFI_OK) &#123;</span><br><span class="line">    <span class="keyword">return</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">void</span> holo_lifecycle_ffi_closure_func(ffi_cif *cif, <span class="keyword">void</span> *ret, <span class="keyword">void</span> **args, <span class="keyword">void</span> *userdata) &#123;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>有了这样的能力，我们就可以将上述的 <code>newIMP</code> 与我们要 hook 的方法 <code>IMP</code> 交换，这样调用 hook 方法的时候，就会执行到 <code>holo_lifecycle_ffi_closure_func</code> 方法，在这个方法里可以拿到原方法的参数个数、参数类型能信息，我们只要在这个方法里再调用一下原方法的 <code>IMP</code>，并在之前和之后分别调用要转发的目标对象的同名方法即可。</p><p>实现了一份简单版的基于 libffi 的 hook 方法代码：</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br><span class="line">90</span><br><span class="line">91</span><br><span class="line">92</span><br><span class="line">93</span><br><span class="line">94</span><br><span class="line">95</span><br><span class="line">96</span><br><span class="line">97</span><br><span class="line">98</span><br><span class="line">99</span><br><span class="line">100</span><br><span class="line">101</span><br><span class="line">102</span><br><span class="line">103</span><br><span class="line">104</span><br><span class="line">105</span><br><span class="line">106</span><br><span class="line">107</span><br><span class="line">108</span><br><span class="line">109</span><br><span class="line">110</span><br><span class="line">111</span><br><span class="line">112</span><br><span class="line">113</span><br><span class="line">114</span><br><span class="line">115</span><br><span class="line">116</span><br><span class="line">117</span><br><span class="line">118</span><br><span class="line">119</span><br><span class="line">120</span><br><span class="line">121</span><br><span class="line">122</span><br><span class="line">123</span><br><span class="line">124</span><br><span class="line">125</span><br><span class="line">126</span><br><span class="line">127</span><br><span class="line">128</span><br><span class="line">129</span><br><span class="line">130</span><br><span class="line">131</span><br><span class="line">132</span><br><span class="line">133</span><br><span class="line">134</span><br><span class="line">135</span><br><span class="line">136</span><br><span class="line">137</span><br><span class="line">138</span><br><span class="line">139</span><br><span class="line">140</span><br><span class="line">141</span><br><span class="line">142</span><br><span class="line">143</span><br><span class="line">144</span><br><span class="line">145</span><br><span class="line">146</span><br><span class="line">147</span><br><span class="line">148</span><br><span class="line">149</span><br><span class="line">150</span><br><span class="line">151</span><br><span class="line">152</span><br><span class="line">153</span><br><span class="line">154</span><br><span class="line">155</span><br><span class="line">156</span><br><span class="line">157</span><br><span class="line">158</span><br><span class="line">159</span><br><span class="line">160</span><br><span class="line">161</span><br><span class="line">162</span><br><span class="line">163</span><br><span class="line">164</span><br><span class="line">165</span><br><span class="line">166</span><br><span class="line">167</span><br><span class="line">168</span><br><span class="line">169</span><br><span class="line">170</span><br><span class="line">171</span><br><span class="line">172</span><br><span class="line">173</span><br><span class="line">174</span><br><span class="line">175</span><br><span class="line">176</span><br><span class="line">177</span><br><span class="line">178</span><br><span class="line">179</span><br><span class="line">180</span><br><span class="line">181</span><br><span class="line">182</span><br><span class="line">183</span><br><span class="line">184</span><br><span class="line">185</span><br><span class="line">186</span><br><span class="line">187</span><br><span class="line">188</span><br><span class="line">189</span><br><span class="line">190</span><br><span class="line">191</span><br><span class="line">192</span><br><span class="line">193</span><br><span class="line">194</span><br><span class="line">195</span><br><span class="line">196</span><br><span class="line">197</span><br><span class="line">198</span><br><span class="line">199</span><br><span class="line">200</span><br><span class="line">201</span><br><span class="line">202</span><br><span class="line">203</span><br><span class="line">204</span><br><span class="line">205</span><br><span class="line">206</span><br><span class="line">207</span><br><span class="line">208</span><br><span class="line">209</span><br><span class="line">210</span><br><span class="line">211</span><br><span class="line">212</span><br><span class="line">213</span><br><span class="line">214</span><br><span class="line">215</span><br><span class="line">216</span><br><span class="line">217</span><br><span class="line">218</span><br><span class="line">219</span><br><span class="line">220</span><br><span class="line">221</span><br><span class="line">222</span><br><span class="line">223</span><br><span class="line">224</span><br><span class="line">225</span><br><span class="line">226</span><br><span class="line">227</span><br><span class="line">228</span><br><span class="line">229</span><br><span class="line">230</span><br><span class="line">231</span><br><span class="line">232</span><br><span class="line">233</span><br><span class="line">234</span><br><span class="line">235</span><br><span class="line">236</span><br><span class="line">237</span><br><span class="line">238</span><br><span class="line">239</span><br><span class="line">240</span><br><span class="line">241</span><br><span class="line">242</span><br><span class="line">243</span><br><span class="line">244</span><br><span class="line">245</span><br><span class="line">246</span><br><span class="line">247</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">@implementation</span> <span class="title">NSObject</span> (<span class="title">LibffiHook</span>)</span></span><br><span class="line"></span><br><span class="line">- (<span class="keyword">void</span>)hook_method:(SEL)sel withBlock:(<span class="keyword">id</span>)block &#123;</span><br><span class="line">    libffi_hook_func(<span class="keyword">self</span>, sel, block);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">@end</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment">// =================== LibffiHookInfo</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">@interface</span> <span class="title">LibffiHookInfo</span> : <span class="title">NSObject</span> </span>&#123;</span><br><span class="line">    <span class="keyword">@public</span></span><br><span class="line">    </span><br><span class="line">    Class cls;</span><br><span class="line">    SEL sel;</span><br><span class="line">    <span class="keyword">void</span> *_originalIMP;</span><br><span class="line">    <span class="built_in">NSMethodSignature</span> *_signature;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">id</span> _block;</span><br><span class="line">    ffi_cif *_block_cif;</span><br><span class="line">    <span class="keyword">void</span> *_block_IMP;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">@end</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">@implementation</span> <span class="title">LibffiHookInfo</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">@end</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// =================== LibffiHook</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">typedef</span> <span class="keyword">void</span> *LibffiHookBlockIMP;</span><br><span class="line"><span class="keyword">typedef</span> <span class="keyword">struct</span> LibffiHookBlock_layout LibffiHookBlock;</span><br><span class="line"></span><br><span class="line"><span class="keyword">struct</span> LibffiHookBlock_layout &#123;</span><br><span class="line">    <span class="keyword">void</span> *isa;  <span class="comment">// initialized to &amp;_NSConcreteStackBlock or &amp;_NSConcreteGlobalBlock</span></span><br><span class="line">    <span class="keyword">volatile</span> <span class="keyword">int</span> flags; <span class="comment">// contains ref count</span></span><br><span class="line">    <span class="keyword">int</span> reserved;</span><br><span class="line">    <span class="keyword">void</span> (*invoke)(<span class="keyword">void</span> *, ...);</span><br><span class="line">    <span class="keyword">struct</span> Block_descriptor_1 *descriptor;</span><br><span class="line">    <span class="comment">// imported variables</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">@implementation</span> <span class="title">LibffiHook</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">void</span> libffi_hook_func(<span class="keyword">id</span> obj, SEL sel, <span class="keyword">id</span> block) &#123;</span><br><span class="line">    <span class="built_in">NSString</span> *selStr = [<span class="string">@"libffi_hook_"</span> stringByAppendingString:<span class="built_in">NSStringFromSelector</span>(sel)];</span><br><span class="line">    <span class="keyword">const</span> SEL key = <span class="built_in">NSSelectorFromString</span>(selStr);</span><br><span class="line">    <span class="keyword">if</span> (objc_getAssociatedObject(obj, key)) &#123;</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    LibffiHookInfo *info = [LibffiHookInfo new];</span><br><span class="line">    info-&gt;cls = [obj <span class="keyword">class</span>];</span><br><span class="line">    info-&gt;sel = sel;</span><br><span class="line">    info-&gt;_block = block;</span><br><span class="line">    </span><br><span class="line">    objc_setAssociatedObject(obj, key, info, OBJC_ASSOCIATION_RETAIN_NONATOMIC);</span><br><span class="line">    </span><br><span class="line">    Method method = class_getInstanceMethod([obj <span class="keyword">class</span>], sel);</span><br><span class="line">    <span class="keyword">const</span> <span class="keyword">char</span> *typeEncoding = method_getTypeEncoding(method);</span><br><span class="line">    <span class="built_in">NSMethodSignature</span> *signature = [<span class="built_in">NSMethodSignature</span> signatureWithObjCTypes:typeEncoding];</span><br><span class="line">    </span><br><span class="line">    info-&gt;_signature = signature;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">const</span> <span class="keyword">unsigned</span> <span class="keyword">int</span> argsCount = method_getNumberOfArguments(method);</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 1、构造参数类型列表</span></span><br><span class="line">    ffi_type **argTypes = calloc(argsCount, <span class="keyword">sizeof</span>(ffi_type *));</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; argsCount; ++i) &#123;</span><br><span class="line">        <span class="keyword">const</span> <span class="keyword">char</span> *argType = [signature getArgumentTypeAtIndex:i];</span><br><span class="line">        ffi_type *arg_ffi_type = libffi_hook_ffi_type(argType);</span><br><span class="line">        <span class="built_in">NSCAssert</span>(arg_ffi_type, <span class="string">@"LibffiHook: can't find a ffi_type: %s"</span>, argType);</span><br><span class="line">        argTypes[i] = arg_ffi_type;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 2、返回值类型</span></span><br><span class="line">    ffi_type *retType = libffi_hook_ffi_type(signature.methodReturnType);</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 3、准备 cif</span></span><br><span class="line">    <span class="comment">// 需要在堆上开辟内存，否则会出现内存问题 (LibffiHookInfo 释放时会 free 掉)</span></span><br><span class="line">    ffi_cif *cif = calloc(<span class="number">1</span>, <span class="keyword">sizeof</span>(ffi_cif));</span><br><span class="line">    <span class="comment">// 生成 ffi_cfi 模版对象，保存函数参数个数、类型等信息，相当于一个函数原型</span></span><br><span class="line">    ffi_status prepCifStatus = ffi_prep_cif(cif, FFI_DEFAULT_ABI, argsCount, retType, argTypes);</span><br><span class="line">    <span class="keyword">if</span> (prepCifStatus != FFI_OK) &#123;</span><br><span class="line">        <span class="built_in">NSCAssert</span>(<span class="literal">NO</span>, <span class="string">@"LibffiHook: ffi_prep_cif failed: %d"</span>, prepCifStatus);</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 4、生成新的 IMP</span></span><br><span class="line">    <span class="keyword">void</span> *newIMP = <span class="literal">NULL</span>;</span><br><span class="line">    ffi_closure *closure = ffi_closure_alloc(<span class="keyword">sizeof</span>(ffi_closure), (<span class="keyword">void</span> **)&amp;newIMP);</span><br><span class="line">    ffi_status prepClosureStatus = ffi_prep_closure_loc(closure, cif, libffi_hook_ffi_closure_func, (__bridge <span class="keyword">void</span> *)info, newIMP);</span><br><span class="line">    <span class="keyword">if</span> (prepClosureStatus != FFI_OK) &#123;</span><br><span class="line">        <span class="built_in">NSCAssert</span>(<span class="literal">NO</span>, <span class="string">@"LibffiHook: ffi_prep_closure_loc failed: %d"</span>, prepClosureStatus);</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 5、替换 IMP 实现</span></span><br><span class="line">    Class hookClass = [obj <span class="keyword">class</span>];</span><br><span class="line">    SEL aSelector = method_getName(method);</span><br><span class="line">    <span class="keyword">if</span> (!class_addMethod(hookClass, aSelector, newIMP, typeEncoding)) &#123;</span><br><span class="line">        IMP originIMP = method_setImplementation(method, newIMP);</span><br><span class="line">        <span class="keyword">if</span> (info-&gt;_originalIMP != originIMP) &#123;</span><br><span class="line">            info-&gt;_originalIMP = originIMP;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 收集 block 信息（hook 的时候准备好，执行的时候会快点）</span></span><br><span class="line">    <span class="comment">// block 没有 SEL，所以比普通方法少一个参数</span></span><br><span class="line">    uint blockArgsCount = argsCount - <span class="number">1</span>;</span><br><span class="line">    ffi_type **blockArgTypes = calloc(blockArgsCount, <span class="keyword">sizeof</span>(ffi_type *));</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 1、构造参数类型列表</span></span><br><span class="line">    <span class="comment">// 第一个参数是 block 自己，肯定为指针类型</span></span><br><span class="line">    blockArgTypes[<span class="number">0</span>] = &amp;ffi_type_pointer;</span><br><span class="line">    <span class="keyword">for</span> (<span class="built_in">NSInteger</span> i = <span class="number">2</span>; i &lt; argsCount; ++i) &#123;</span><br><span class="line">        blockArgTypes[i - <span class="number">1</span>] = libffi_hook_ffi_type([info-&gt;_signature getArgumentTypeAtIndex:i]);</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 2、准备 cif</span></span><br><span class="line">    ffi_cif *callbackCif = calloc(<span class="number">1</span>, <span class="keyword">sizeof</span>(ffi_cif));</span><br><span class="line">    <span class="keyword">if</span> (ffi_prep_cif(callbackCif, FFI_DEFAULT_ABI, blockArgsCount, &amp;ffi_type_void, blockArgTypes) == FFI_OK) &#123;</span><br><span class="line">        info-&gt;_block_cif = callbackCif;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="built_in">NSCAssert</span>(<span class="literal">NO</span>, <span class="string">@"ffi_prep_cif failed"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 3、获取 block IMP</span></span><br><span class="line">    LibffiHookBlock *blockRef = (__bridge LibffiHookBlock *)block;</span><br><span class="line">    info-&gt;_block_IMP = blockRef-&gt;invoke;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">void</span> libffi_hook_ffi_closure_func(ffi_cif *cif, <span class="keyword">void</span> *ret, <span class="keyword">void</span> **args, <span class="keyword">void</span> *userdata) &#123;</span><br><span class="line">    LibffiHookInfo *info = (__bridge LibffiHookInfo *)userdata;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 1、before</span></span><br><span class="line">    </span><br><span class="line"><span class="comment">//    NSLog(@"LibffiHook before, class: %@, sel: %@", NSStringFromClass(info-&gt;cls), NSStringFromSelector(info-&gt;sel));</span></span><br><span class="line">    </span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 2、call original IMP</span></span><br><span class="line">    </span><br><span class="line">    ffi_call(cif, info-&gt;_originalIMP, ret, args);</span><br><span class="line">    </span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 3、after 回调 block</span></span><br><span class="line">    </span><br><span class="line">    <span class="comment">// block 没有 SEL，所以比普通方法少一个参数</span></span><br><span class="line">    <span class="keyword">void</span> **callbackArgs = calloc(info-&gt;_signature.numberOfArguments - <span class="number">1</span>, <span class="keyword">sizeof</span>(<span class="keyword">void</span> *));</span><br><span class="line">    <span class="comment">// 第一个参数是 block 自己</span></span><br><span class="line">    callbackArgs[<span class="number">0</span>] = (__bridge <span class="keyword">void</span> *)(info-&gt;_block);</span><br><span class="line">    <span class="comment">// 从 index = 2 位置开始把 args 中的数据拷贝到 callbackArgs中 (从 index = 1 开始，第 0 个位置留给 block 自己)</span></span><br><span class="line">    memcpy(callbackArgs + <span class="number">1</span>, args + <span class="number">2</span>, <span class="keyword">sizeof</span>(*args)*(info-&gt;_signature.numberOfArguments - <span class="number">2</span>));</span><br><span class="line"><span class="comment">//    for (NSInteger i = 2; i &lt; info-&gt;_signature.numberOfArguments; ++i) &#123;</span></span><br><span class="line"><span class="comment">//        callbackArgs[i - 1] = args[i];</span></span><br><span class="line"><span class="comment">//    &#125;</span></span><br><span class="line">    ffi_call(info-&gt;_block_cif, info-&gt;_block_IMP, <span class="literal">NULL</span>, callbackArgs);</span><br><span class="line">    free(callbackArgs);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="built_in">NS_INLINE</span> ffi_type *libffi_hook_ffi_type(<span class="keyword">const</span> <span class="keyword">char</span> *c) &#123;</span><br><span class="line">    <span class="keyword">switch</span> (c[<span class="number">0</span>]) &#123;</span><br><span class="line">        <span class="keyword">case</span> <span class="string">'v'</span>:</span><br><span class="line">            <span class="keyword">return</span> &amp;ffi_type_void;</span><br><span class="line">        <span class="keyword">case</span> <span class="string">'c'</span>:</span><br><span class="line">            <span class="keyword">return</span> &amp;ffi_type_schar;</span><br><span class="line">        <span class="keyword">case</span> <span class="string">'C'</span>:</span><br><span class="line">            <span class="keyword">return</span> &amp;ffi_type_uchar;</span><br><span class="line">        <span class="keyword">case</span> <span class="string">'s'</span>:</span><br><span class="line">            <span class="keyword">return</span> &amp;ffi_type_sshort;</span><br><span class="line">        <span class="keyword">case</span> <span class="string">'S'</span>:</span><br><span class="line">            <span class="keyword">return</span> &amp;ffi_type_ushort;</span><br><span class="line">        <span class="keyword">case</span> <span class="string">'i'</span>:</span><br><span class="line">            <span class="keyword">return</span> &amp;ffi_type_sint;</span><br><span class="line">        <span class="keyword">case</span> <span class="string">'I'</span>:</span><br><span class="line">            <span class="keyword">return</span> &amp;ffi_type_uint;</span><br><span class="line">        <span class="keyword">case</span> <span class="string">'l'</span>:</span><br><span class="line">            <span class="keyword">return</span> &amp;ffi_type_slong;</span><br><span class="line">        <span class="keyword">case</span> <span class="string">'L'</span>:</span><br><span class="line">            <span class="keyword">return</span> &amp;ffi_type_ulong;</span><br><span class="line">        <span class="keyword">case</span> <span class="string">'q'</span>:</span><br><span class="line">            <span class="keyword">return</span> &amp;ffi_type_sint64;</span><br><span class="line">        <span class="keyword">case</span> <span class="string">'Q'</span>:</span><br><span class="line">            <span class="keyword">return</span> &amp;ffi_type_uint64;</span><br><span class="line">        <span class="keyword">case</span> <span class="string">'f'</span>:</span><br><span class="line">            <span class="keyword">return</span> &amp;ffi_type_float;</span><br><span class="line">        <span class="keyword">case</span> <span class="string">'d'</span>:</span><br><span class="line">            <span class="keyword">return</span> &amp;ffi_type_double;</span><br><span class="line">        <span class="keyword">case</span> <span class="string">'F'</span>:</span><br><span class="line"><span class="meta">#if CGFLOAT_IS_DOUBLE</span></span><br><span class="line">            <span class="keyword">return</span> &amp;ffi_type_double;</span><br><span class="line"><span class="meta">#else</span></span><br><span class="line">            <span class="keyword">return</span> &amp;ffi_type_float;</span><br><span class="line"><span class="meta">#endif</span></span><br><span class="line">        <span class="keyword">case</span> <span class="string">'B'</span>:</span><br><span class="line">            <span class="keyword">return</span> &amp;ffi_type_uint8;</span><br><span class="line">        <span class="keyword">case</span> <span class="string">'^'</span>:</span><br><span class="line">            <span class="keyword">return</span> &amp;ffi_type_pointer;</span><br><span class="line">        <span class="keyword">case</span> <span class="string">'@'</span>:</span><br><span class="line">            <span class="keyword">return</span> &amp;ffi_type_pointer;</span><br><span class="line">        <span class="keyword">case</span> <span class="string">'#'</span>:</span><br><span class="line">            <span class="keyword">return</span> &amp;ffi_type_pointer;</span><br><span class="line">        <span class="keyword">case</span> <span class="string">':'</span>:</span><br><span class="line">            <span class="keyword">return</span> &amp;ffi_type_pointer;</span><br><span class="line">        <span class="keyword">case</span> <span class="string">'&#123;'</span>: &#123;</span><br><span class="line">            <span class="comment">// http://www.chiark.greenend.org.uk/doc/libffi-dev/html/Type-Example.html</span></span><br><span class="line">            ffi_type *type = malloc(<span class="keyword">sizeof</span>(ffi_type));</span><br><span class="line">            type-&gt;type = FFI_TYPE_STRUCT;</span><br><span class="line">            <span class="built_in">NSUInteger</span> size = <span class="number">0</span>;</span><br><span class="line">            <span class="built_in">NSUInteger</span> alignment = <span class="number">0</span>;</span><br><span class="line">            <span class="built_in">NSGetSizeAndAlignment</span>(c, &amp;size, &amp;alignment);</span><br><span class="line">            type-&gt;alignment = alignment;</span><br><span class="line">            type-&gt;size = size;</span><br><span class="line">            <span class="keyword">while</span> (c[<span class="number">0</span>] != <span class="string">'='</span>) ++c; ++c;</span><br><span class="line">            </span><br><span class="line">            <span class="built_in">NSPointerArray</span> *pointArray = [<span class="built_in">NSPointerArray</span> pointerArrayWithOptions:<span class="built_in">NSPointerFunctionsOpaqueMemory</span>];</span><br><span class="line">            <span class="keyword">while</span> (c[<span class="number">0</span>] != <span class="string">'&#125;'</span>) &#123;</span><br><span class="line">                ffi_type *elementType = <span class="literal">NULL</span>;</span><br><span class="line">                elementType = libffi_hook_ffi_type(c);</span><br><span class="line">                <span class="keyword">if</span> (elementType) &#123;</span><br><span class="line">                    [pointArray addPointer:elementType];</span><br><span class="line">                    c = <span class="built_in">NSGetSizeAndAlignment</span>(c, <span class="literal">NULL</span>, <span class="literal">NULL</span>);</span><br><span class="line">                &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                    <span class="keyword">return</span> <span class="literal">NULL</span>;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="built_in">NSInteger</span> count = pointArray.count;</span><br><span class="line">            ffi_type **types = malloc(<span class="keyword">sizeof</span>(ffi_type *) * (count + <span class="number">1</span>));</span><br><span class="line">            <span class="keyword">for</span> (<span class="built_in">NSInteger</span> i = <span class="number">0</span>; i &lt; count; i++) &#123;</span><br><span class="line">                types[i] = [pointArray pointerAtIndex:i];</span><br><span class="line">            &#125;</span><br><span class="line">            types[count] = <span class="literal">NULL</span>; <span class="comment">// terminated element is NULL</span></span><br><span class="line">            </span><br><span class="line">            type-&gt;elements = types;</span><br><span class="line">            <span class="keyword">return</span> type;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">NULL</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">@end</span></span><br></pre></td></tr></table></figure><p><code>libffi_hook_ffi_type</code> 方法直接拷贝自：<a href="https://github.com/eleme/Stinger" target="_blank" rel="noopener">Stinger</a> 里的 <a href="https://github.com/eleme/Stinger/blob/master/Stinger/Classes/STMethodSignature.m#L131-L209" target="_blank" rel="noopener">ffi_type <em>_st_ffiTypeWithType(const char </em>c)</a> 方法</p><h1 id="偷梁换柱：fishhook"><a href="#偷梁换柱：fishhook" class="headerlink" title="偷梁换柱：fishhook"></a>偷梁换柱：fishhook</h1><p>以 hook NSLog 为例，使用方式如下：</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br></pre></td><td class="code"><pre><span class="line">- (<span class="keyword">void</span>)fishhook_nslog &#123;</span><br><span class="line">    <span class="built_in">NSLog</span>(<span class="string">@"fishhook before"</span>); <span class="comment">// fishhook before</span></span><br><span class="line">    </span><br><span class="line">    <span class="keyword">struct</span> rebinding rebindingLog;</span><br><span class="line">    <span class="comment">// 需要 hook 的方法名</span></span><br><span class="line">    rebindingLog.name = <span class="string">"NSLog"</span>;</span><br><span class="line">    <span class="comment">// 用哪个方法来替换</span></span><br><span class="line">    rebindingLog.replacement = myLog;</span><br><span class="line">    <span class="comment">// 保存原本函数指针</span></span><br><span class="line">    rebindingLog.replaced = (<span class="keyword">void</span> **)&amp;sys_nslog;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">struct</span> rebinding rebindings[] = &#123;rebindingLog&#125;;</span><br><span class="line">    </span><br><span class="line">    rebind_symbols(rebindings, <span class="number">1</span>);</span><br><span class="line">    </span><br><span class="line">    <span class="built_in">NSLog</span>(<span class="string">@"fishhook after"</span>); <span class="comment">// fishhook after----&gt;🍺🍺🍺</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment">// 函数指针，用来保存原来的函数</span></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">void</span> (*sys_nslog)(<span class="built_in">NSString</span> *format, ...);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 新函数（注意：不定参数未处理）</span></span><br><span class="line"><span class="keyword">void</span> myLog(<span class="built_in">NSString</span> * _Nonnull format, ...) &#123;</span><br><span class="line">    <span class="built_in">NSString</span> *message = [format stringByAppendingString:<span class="string">@"----&gt;🍺🍺🍺"</span>];</span><br><span class="line">    (*sys_nslog)(message);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><a href="https://github.com/facebook/fishhook" target="_blank" rel="noopener">fishhook</a> 是 FaceBook 出品的能够 hook 动态库方法的框架（注意：仅能 hook 系统动态库方法），其源代码仅仅不足两百行，但是要搞明白这两百行代码的工作原理所需要的基础知识却是非常多的。</p><p>以下是 App 启动的大致过程，大致分为：开辟进程、加载可执行文件、加载 Dyld、Dyld 加载各个动态库、Rebase（因为 ALSR 技术进行基址重定位）、Bind（动态库的符号绑定）、加载 OC 类，分类、init 方法、调用 Main 函数、调用 UIApplicationMain 函数、起一个主线程的 runloop。</p><p><img src="http://image.gonghonglou.com/hook/launch.png" alt="launch"></p><p><code>fishhook</code> 的工作原理就是 Bind 阶段：符号重绑定。</p><p>普及一些涉及的基础概念：</p><h4 id="为什么要动态链接？"><a href="#为什么要动态链接？" class="headerlink" title="为什么要动态链接？"></a>为什么要动态链接？</h4><p>远古时代，所有源代码都在一个文件上（想象下开发一个App，所有源代码都在main.m上，这个 main.m 有几百万行代码。多人协同开发、如何维护、复用、每次编译几分钟….）。<br>为了解决上面问题，于是有了静态链接。像我们平时开发，每个人开发自己的模块功能，最后编译链接在一起。解决了协同开发、可维护、可复用、编译速度也很快了（未改动的模块用编译好的缓存）。</p><p>静态链接好像已经很完美了。我们平时开发 App，都会用到 UIKit、Foundation 等许多系统库。假如都是通过静态链接的，手机里每个App都包含了一份这些系统库，每个App包体积变大了，占用磁盘空间；每个 App 运行时都要在内存里分别加载这些库，占用内存。</p><p>假设 UIKit 里某个函数有bug，需要更新，所有 App 都要重新静态链接最新的 UIKit 库，然后发版。</p><p>为了这些问题，于是产生了动态链接。</p><h4 id="position-independent-code-（PIC-地址无关代码）"><a href="#position-independent-code-（PIC-地址无关代码）" class="headerlink" title="position-independent code （PIC 地址无关代码）"></a>position-independent code （PIC 地址无关代码）</h4><p><strong>产生地址无关代码原因：</strong>dylib 在编译时候，是不知道自己在进程中的虚拟内存地址的。因为 dylib 可以被多个进程共享，比如进程 1 可以在空闲地址 0x1000-0x2000 放共享对象 a，但是进程 2 的 0x1000-0x2000 已经被主模块占用了，只有空闲地址 0x3000-0x4000 可以放这个共享对象 a。<br>所以共享对象 a 里面有一个函数，在进程 1 中的虚拟内存地址是 0x10f4，在进程 2 中的虚拟内存地址就成了 0x30f4。那机器指令就不能包含绝地地址了（动态库代码段所有进程共享；可修改的数据段，每个进程有一个副本，私有的）。</p><p><strong>PIC原理：</strong>为了解决 dylib 的代码段能被共享，PIC（地址无关代码）技术就产生了。PIC 原理就是把指令中那些需要被修改的部分分离出来，跟数据部分放在一起，这样指令部分就可以保持不变，而数据部分是每个进程都有一个副本。</p><h4 id="ALSR："><a href="#ALSR：" class="headerlink" title="ALSR："></a>ALSR：</h4><p>在计算机科学中，地址空间配置随机加载（英语：Address space layout randomization，缩写ASLR，又称地址空间配置随机化、地址空间布局随机化）是一种防范内存损坏漏洞被利用的计算机安全技术。ASLR通过随机放置进程关键数据区域的地址空间来防止攻击者能可靠地跳转到内存的特定位置来利用函数。现代操作系统一般都加设这一机制，以防范恶意程序对已知地址进行Return-to-libc攻击。</p><h4 id="Mach-O-文件"><a href="#Mach-O-文件" class="headerlink" title="Mach-O 文件"></a>Mach-O 文件</h4><p>在了解 fishhook 的具体原理之前还要熟悉下 Mach-O 文件：MacOS 上的可执行文件格式，类似于 Linux 和大部分 UNIX 的原生格式 ELF（Extensible Firmware Interface）。</p><p><img src="http://image.gonghonglou.com/hook/macho.png" alt="Mach-O"></p><p>1、Header：magic 魔数，内核识别 MachO<br>2、Load Commands：存储 Mach-O 的布局信息<br>3、Data：包含实际的代码和数据，Data 被分割为多个 Segment。每个 Segment 被分割为多个 Section，分别存放不同的数据</p><p>标准的三个 Segment：TEXT、DATA、LINKEDIT</p><p>3.1、TEXT：代码段，只读可执行，存储函数的二进制代码(__text)，常量字符串(__cstring)，Objective C 的类/方法名等信息<br>3.2、DATA：数据段，<strong>读写</strong>，存储 Objective C 的字符串(__cfstring)，以及运行时的元数据：class/protocol/method…<br>3.3、LINKEDIT：启动 App 需要的信息，如 bind &amp; rebase 的地址，代码签名，符号表…</p><p>Mach-O 中 __DATA 段有两个 Section 与动态符号绑定有关系</p><p>__nl_symbol_ptr ：存储了 non-lazily 绑定的符号，这些符号在 Mach-O 加载的时候绑定完成<br>__la_symbol_ptr ：存储了 lazy 绑定的方法，这些方法在第一次调用时，由 dyld_stub_binder 进行绑定</p><p>为了实现系统动态库的共用，有了上文提到的动态链接。PIC 原理里提到了把那些需要共用的符号放在了 DATA 段，DATA 段的权限是可读写的，fishhook 就是在运行期修改 DATA 段里的数据，把系统符号绑定的地址重新绑定位我们自己定义的 hook 函数地址。</p><p>比如 NSLog 就是懒加载的，在第一次访问 NSLog 符号的时候先去 stub，stub 告诉从 __la_symbol_ptr 查找，__la_symbol_ptr 表示还没有 NSLog 符号真实函数地址，需要动态绑定，于是去 __nl_symbol_ptr 查找 dyld_stub_binder 函数的地址，进行查找真实的 NSLog 地址。<br>找到后调用 NSLog 函数，并把这个地址保存进 __la_symbol_ptr。</p><p>下次调用 NSLog　函数的时候在 __la_symbol_ptr 就能得到真实地址进行跳转。</p><h4 id="fashhook-工作流程："><a href="#fashhook-工作流程：" class="headerlink" title="fashhook 工作流程："></a>fashhook 工作流程：</h4><p>下图是 fashhook 在 GitHub 上 README.md 里的图，非常清晰的介绍了 fashhook 的工作流程：</p><p><img src="http://image.gonghonglou.com/hook/fishhook.png" alt="fashhook"></p><p>以 hook NSLog 方法为例：</p><p>1、在 Lazy Symbol Pointer Table 找到 NSLog 顺序<br>2、按上面的顺序在 Indirect Symbol Table 找到 NSLog<br>3、把 Indirect Symbol Table 中 NSLog 的 data 值转为 10 进制，作为角标在 Symbols Table -&gt; Symbols 中查找<br>4、把 Symbols 表中 NSLog 的 data 值加上 String Table 中的第一条数据（base value）的值，确认找到了目标符号</p><p>把 Lazy Symbol Pointer Table 里的角标位置上的值修改为我们自己函数的地址，即完成了符号重绑定过程。</p><p>核心代码如下：</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// dyld 在 image 发生改变时，主动调用回调函数</span></span><br><span class="line">_dyld_register_func_for_add_image(_rebind_symbols_for_image);</span><br><span class="line"></span><br><span class="line"><span class="comment">// slide 即 ALSR 产生的随机偏移量</span></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">void</span> _rebind_symbols_for_image(<span class="keyword">const</span> <span class="keyword">struct</span> mach_header *header,</span><br><span class="line">                                      intptr_t slide) &#123;</span><br><span class="line">    rebind_symbols_for_image(_rebindings_head, header, slide);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">void</span> perform_rebinding_with_section(<span class="keyword">struct</span> rebindings_entry *rebindings,</span><br><span class="line">                                           section_t *section,</span><br><span class="line">                                           intptr_t slide,</span><br><span class="line">                                           nlist_t *symtab,</span><br><span class="line">                                           <span class="keyword">char</span> *strtab,</span><br><span class="line">                                           uint32_t *indirect_symtab) &#123;</span><br><span class="line">  uint32_t *indirect_symbol_indices = indirect_symtab + section-&gt;reserved1;</span><br><span class="line">  <span class="keyword">void</span> **indirect_symbol_bindings = (<span class="keyword">void</span> **)((uintptr_t)slide + section-&gt;addr);</span><br><span class="line">  <span class="keyword">for</span> (uint i = <span class="number">0</span>; i &lt; section-&gt;size / <span class="keyword">sizeof</span>(<span class="keyword">void</span> *); i++) &#123;</span><br><span class="line">    uint32_t symtab_index = indirect_symbol_indices[i];</span><br><span class="line">    <span class="keyword">if</span> (symtab_index == INDIRECT_SYMBOL_ABS || symtab_index == INDIRECT_SYMBOL_LOCAL ||</span><br><span class="line">        symtab_index == (INDIRECT_SYMBOL_LOCAL   | INDIRECT_SYMBOL_ABS)) &#123;</span><br><span class="line">      <span class="keyword">continue</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    uint32_t strtab_offset = symtab[symtab_index].n_un.n_strx;</span><br><span class="line">    <span class="keyword">char</span> *symbol_name = strtab + strtab_offset;</span><br><span class="line">    <span class="keyword">struct</span> rebindings_entry *cur = rebindings;</span><br><span class="line">    <span class="keyword">while</span> (cur) &#123;</span><br><span class="line">      <span class="keyword">for</span> (uint j = <span class="number">0</span>; j &lt; cur-&gt;rebindings_nel; j++) &#123;</span><br><span class="line">        <span class="keyword">if</span> (strlen(symbol_name) &gt; <span class="number">1</span> &amp;&amp;</span><br><span class="line">            strcmp(&amp;symbol_name[<span class="number">1</span>], cur-&gt;rebindings[j].name) == <span class="number">0</span>) &#123;</span><br><span class="line">          <span class="keyword">if</span> (cur-&gt;rebindings[j].replaced != <span class="literal">NULL</span> &amp;&amp;</span><br><span class="line">              indirect_symbol_bindings[i] != cur-&gt;rebindings[j].replacement) &#123;</span><br><span class="line">            *(cur-&gt;rebindings[j].replaced) = indirect_symbol_bindings[i];</span><br><span class="line">          &#125;</span><br><span class="line">          <span class="comment">// 找到目标位置，将该角标下的函数地址修改为我们自己的 hook 函数地址</span></span><br><span class="line">          indirect_symbol_bindings[i] = cur-&gt;rebindings[j].replacement;</span><br><span class="line">          <span class="keyword">goto</span> symbol_loop;</span><br><span class="line">        &#125;</span><br><span class="line">      &#125;</span><br><span class="line">      cur = cur-&gt;next;</span><br><span class="line">    &#125;</span><br><span class="line">  symbol_loop:;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h1 id="李代桃僵：静态库插桩"><a href="#李代桃僵：静态库插桩" class="headerlink" title="李代桃僵：静态库插桩"></a>李代桃僵：静态库插桩</h1><p>主要是 <a href="https://juejin.im/post/6844904098580398087" target="_blank" rel="noopener">静态插桩的方式来实现Hook Method</a> 这个文化里提到的技术，</p><p>文章里主要是操作的 <code>_objc_msgSend</code> 方法。基本原理大概是：</p><p>把自己的组件打成静态库，编译阶段因为不知道引用的外部符号的具体地址，只在符号表里做了标记，需要在链接阶段再查找外部符号的引用进行绑定。</p><p>通过脚本手动替换掉 .a 文件里的符号（<code>_objc_msgSend</code>）为我们自定义的符号（<code>_hook_msgSend</code>），注意两个符号必须等长。</p><p>再自己在 text 段定义一个 <code>_hook_msgSend</code> 函数，这样，链接阶段查找外部符号就绑定成了自己定义的函数。</p><p>脚本代码如下，需要对 Mach-O 格式非常熟悉：</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br><span class="line">90</span><br><span class="line">91</span><br><span class="line">92</span><br><span class="line">93</span><br><span class="line">94</span><br><span class="line">95</span><br><span class="line">96</span><br><span class="line">97</span><br><span class="line">98</span><br><span class="line">99</span><br><span class="line">100</span><br><span class="line">101</span><br><span class="line">102</span><br><span class="line">103</span><br><span class="line">104</span><br><span class="line">105</span><br><span class="line">106</span><br><span class="line">107</span><br><span class="line">108</span><br><span class="line">109</span><br><span class="line">110</span><br><span class="line">111</span><br><span class="line">112</span><br><span class="line">113</span><br><span class="line">114</span><br><span class="line">115</span><br><span class="line">116</span><br><span class="line">117</span><br><span class="line">118</span><br><span class="line">119</span><br><span class="line">120</span><br><span class="line">121</span><br><span class="line">122</span><br><span class="line">123</span><br><span class="line">124</span><br><span class="line">125</span><br><span class="line">126</span><br><span class="line">127</span><br><span class="line">128</span><br><span class="line">129</span><br><span class="line">130</span><br><span class="line">131</span><br><span class="line">132</span><br><span class="line">133</span><br><span class="line">134</span><br><span class="line">135</span><br><span class="line">136</span><br><span class="line">137</span><br><span class="line">138</span><br><span class="line">139</span><br><span class="line">140</span><br><span class="line">141</span><br><span class="line">142</span><br><span class="line">143</span><br><span class="line">144</span><br><span class="line">145</span><br><span class="line">146</span><br><span class="line">147</span><br><span class="line">148</span><br><span class="line">149</span><br><span class="line">150</span><br><span class="line">151</span><br><span class="line">152</span><br><span class="line">153</span><br><span class="line">154</span><br><span class="line">155</span><br><span class="line">156</span><br><span class="line">157</span><br><span class="line">158</span><br><span class="line">159</span><br><span class="line">160</span><br><span class="line">161</span><br><span class="line">162</span><br><span class="line">163</span><br><span class="line">164</span><br><span class="line">165</span><br><span class="line">166</span><br><span class="line">167</span><br><span class="line">168</span><br><span class="line">169</span><br><span class="line">170</span><br><span class="line">171</span><br><span class="line">172</span><br><span class="line">173</span><br><span class="line">174</span><br><span class="line">175</span><br><span class="line">176</span><br><span class="line">177</span><br><span class="line">178</span><br><span class="line">179</span><br><span class="line">180</span><br><span class="line">181</span><br><span class="line">182</span><br><span class="line">183</span><br><span class="line">184</span><br><span class="line">185</span><br><span class="line">186</span><br><span class="line">187</span><br><span class="line">188</span><br><span class="line">189</span><br><span class="line">190</span><br><span class="line">191</span><br><span class="line">192</span><br><span class="line">193</span><br><span class="line">194</span><br><span class="line">195</span><br><span class="line">196</span><br><span class="line">197</span><br><span class="line">198</span><br><span class="line">199</span><br><span class="line">200</span><br><span class="line">201</span><br><span class="line">202</span><br><span class="line">203</span><br><span class="line">204</span><br><span class="line">205</span><br><span class="line">206</span><br><span class="line">207</span><br><span class="line">208</span><br><span class="line">209</span><br><span class="line">210</span><br><span class="line">211</span><br><span class="line">212</span><br><span class="line">213</span><br><span class="line">214</span><br><span class="line">215</span><br><span class="line">216</span><br><span class="line">217</span><br><span class="line">218</span><br><span class="line">219</span><br><span class="line">220</span><br><span class="line">221</span><br><span class="line">222</span><br><span class="line">223</span><br><span class="line">224</span><br><span class="line">225</span><br><span class="line">226</span><br><span class="line">227</span><br><span class="line">228</span><br><span class="line">229</span><br><span class="line">230</span><br><span class="line">231</span><br><span class="line">232</span><br><span class="line">233</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta"># -*- coding: utf-8 -*</span></span><br><span class="line">import os</span><br><span class="line">import re</span><br><span class="line">import <span class="keyword">struct</span></span><br><span class="line">from pathlib import Path</span><br><span class="line"></span><br><span class="line"><span class="string">'''</span></span><br><span class="line">静态库结构</span><br><span class="line"></span><br><span class="line"><span class="number">1</span>、魔数 <span class="number">8</span>个字节</span><br><span class="line">magic(8) = '!&lt;arch&gt;\n'</span><br><span class="line"></span><br><span class="line"><span class="number">2</span>、符号表头结构 <span class="number">80</span>个字节</span><br><span class="line"><span class="keyword">struct</span> symtab_header &#123;</span><br><span class="line">    <span class="keyword">char</span>        name[<span class="number">16</span>];       <span class="comment">/* 名称 */</span></span><br><span class="line">    <span class="keyword">char</span>        timestamp[<span class="number">12</span>];  <span class="comment">/* 库创建的时间戳 */</span></span><br><span class="line">    <span class="keyword">char</span>        userid[<span class="number">6</span>];        <span class="comment">/* 用户id */</span></span><br><span class="line">    <span class="keyword">char</span>        groupid[<span class="number">6</span>];  <span class="comment">/* 组id */</span></span><br><span class="line">    uint64_t    mode;            <span class="comment">/* 文件访问模式 */</span></span><br><span class="line">    uint64_t    size;            <span class="comment">/* 符号表占总字节大小 */</span></span><br><span class="line">    uint32_t    endheader;        <span class="comment">/* 头结束标志 */</span></span><br><span class="line">    <span class="keyword">char</span>        longname[<span class="number">20</span>];   <span class="comment">/* 符号表长名 */</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="number">3</span>、符号表 <span class="number">4</span>+size个字节</span><br><span class="line"><span class="keyword">struct</span> symbol_table &#123;</span><br><span class="line">    uint32_t       size;           <span class="comment">/* 符号表占用的总字节数 */</span></span><br><span class="line">    symbol_info syminfo[<span class="number">0</span>];      <span class="comment">/* 符号信息，它的个数是 size / sizeof(symbol_info) */</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="number">3</span>、字符串表 <span class="number">4</span>+size个字节</span><br><span class="line"><span class="keyword">struct</span> stringtab</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">int</span> size;     <span class="comment">//字符串表的尺寸</span></span><br><span class="line">    <span class="keyword">char</span> strings[<span class="number">0</span>];   <span class="comment">//字符串表的内容，每个字符串以\0分隔。</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="number">4</span>、目标文件头结构（跟符号表头结构一样） <span class="number">80</span>个字节</span><br><span class="line"></span><br><span class="line"><span class="keyword">struct</span> object_header &#123;</span><br><span class="line">    <span class="keyword">char</span>        name[<span class="number">16</span>];       <span class="comment">/* 名称 */</span></span><br><span class="line">    <span class="keyword">char</span>        timestamp[<span class="number">12</span>];  <span class="comment">/* 目标文件创建的时间戳 */</span></span><br><span class="line">    <span class="keyword">char</span>        userid[<span class="number">6</span>];        <span class="comment">/* 用户id */</span></span><br><span class="line">    <span class="keyword">char</span>        groupid[<span class="number">6</span>];  <span class="comment">/* 组id */</span></span><br><span class="line">    uint64_t    mode;            <span class="comment">/* 文件访问模式 */</span></span><br><span class="line">    uint64_t    size;            <span class="comment">/* 符号表占总字节大小 */</span></span><br><span class="line">    uint32_t    endheader;        <span class="comment">/* 头结束标志 */</span></span><br><span class="line">    <span class="keyword">char</span>        longname[<span class="number">20</span>];   <span class="comment">/* 符号表长名 */</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="number">5</span>、目标文件</span><br><span class="line">这个可以参考我的博客：https:<span class="comment">//juejin.im/post/5d5275b251882505417927b5</span></span><br><span class="line"></span><br><span class="line">....<span class="number">.4</span>、<span class="number">5</span>循环（如果有多个目标文件）</span><br><span class="line"></span><br><span class="line"><span class="string">'''</span></span><br><span class="line"></span><br><span class="line">def deal_fat_file():</span><br><span class="line">    global staticLibPath, fatFilePath</span><br><span class="line">    fatFilePath = staticLibPath</span><br><span class="line">    (fatFileDir, fatFileName) = os.path.split(fatFilePath)</span><br><span class="line">    fatFileName = 'tmp-arm64-'+fatFileName</span><br><span class="line">    staticLibPath = os.path.join(fatFileDir, fatFileName)</span><br><span class="line">    os.system('lipo ' + fatFilePath + ' -thin ' + 'arm64 -output '+ staticLibPath)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">def replace_fat_file():</span><br><span class="line">    os.system('lipo '+fatFilePath+' -replace arm64 '+staticLibPath+' -output '+fatFilePath)</span><br><span class="line">    os.remove(staticLibPath)</span><br><span class="line">    </span><br><span class="line"></span><br><span class="line">def get_valid_staticLib_path():</span><br><span class="line">    <span class="keyword">if</span> not Path(staticLibPath).is_file():</span><br><span class="line">        return False, 'invalid path, please input valid staticLib path!!!'</span><br><span class="line">    output = os.popen('lipo -info '+staticLibPath).read().strip()</span><br><span class="line">    if not output.endswith('architecture: arm64'):  # re.match(r'.*architecture: arm64$', output):</span><br><span class="line">        if output.startswith('Architectures in the fat file:') and output.find('arm64'):</span><br><span class="line">            deal_fat_file()</span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            return False, 'invalid staticLib or fat file not contain arm64 lib'</span><br><span class="line">    with open(staticLibPath, 'rb') as fileobj:</span><br><span class="line">        magic = fileobj.read(<span class="number">8</span>)</span><br><span class="line">        (magic,) = struct.unpack('8s', magic)</span><br><span class="line">        magic = magic.decode('utf-8')</span><br><span class="line">        if not magic == '!&lt;arch&gt;\n':</span><br><span class="line">            return False, 'error magic, invalid staticLib.'</span><br><span class="line">    return True, 'valid path!'</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="meta"># 返回(name, location, size)</span></span><br><span class="line">def resolver_object_header(offset):</span><br><span class="line">    with open(staticLibPath, 'rb') as fileobj:</span><br><span class="line">        fileobj.seek(offset)</span><br><span class="line">        name = fileobj.read(<span class="number">16</span>)</span><br><span class="line">        (name,) = struct.unpack('16s', name)</span><br><span class="line">        name = name.decode()</span><br><span class="line">        <span class="meta"># offset(48+offset) = offset + name(16) + timestamp(12) + userid(6) + groupid(6) + mode(8)</span></span><br><span class="line">        fileobj.seek(<span class="number">48</span>+offset)</span><br><span class="line">        size = fileobj.read(<span class="number">8</span>)</span><br><span class="line">        (size,) = struct.unpack('8s', size)</span><br><span class="line">        size = <span class="keyword">int</span>(size.decode())</span><br><span class="line">        location = <span class="number">60</span> + offset</span><br><span class="line">        if name.startswith('#1/'):</span><br><span class="line">            nameLen = <span class="keyword">int</span>(name[<span class="number">3</span>:])</span><br><span class="line">            size = size - nameLen</span><br><span class="line">            location = location + nameLen</span><br><span class="line">            fileobj.seek(<span class="number">60</span>+offset)</span><br><span class="line">            name = fileobj.read(nameLen)</span><br><span class="line">            (name,) = <span class="keyword">struct</span>.unpack(str(nameLen)+<span class="string">'s'</span>, name)</span><br><span class="line">            name = name.decode().strip()</span><br><span class="line">    <span class="keyword">return</span> (name, location, size)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">def find_symtab(location, size):</span><br><span class="line">    with open(staticLibPath, 'rb') as fileobj:</span><br><span class="line">        fileobj.seek(location)</span><br><span class="line">        magic = fileobj.read(<span class="number">4</span>)</span><br><span class="line">        (magic,) = <span class="keyword">struct</span>.unpack(<span class="string">'I'</span>, magic)</span><br><span class="line">        <span class="meta"># arm64 mach-o magic</span></span><br><span class="line">        <span class="keyword">if</span> not magic == <span class="number">0xFEEDFACF</span>:</span><br><span class="line">            exit('静态库里的machO文件不是arm64平台的！')</span><br><span class="line">        fileobj.seek(location+<span class="number">16</span>)</span><br><span class="line">        num_command = fileobj.read(<span class="number">4</span>)</span><br><span class="line">        (num_command,) = <span class="keyword">struct</span>.unpack(<span class="string">'I'</span>, num_command)</span><br><span class="line">        offset = location+<span class="number">32</span></span><br><span class="line">        <span class="keyword">while</span> num_command &gt; <span class="number">0</span>:</span><br><span class="line">            fileobj.seek(offset)</span><br><span class="line">            cmd = fileobj.read(<span class="number">4</span>)</span><br><span class="line">            (cmd,) = <span class="keyword">struct</span>.unpack(<span class="string">'I'</span>, cmd)</span><br><span class="line">            <span class="keyword">if</span> cmd == <span class="number">0x2</span>: <span class="meta"># LC_SYMTAB = 0x2</span></span><br><span class="line">                offset = offset + <span class="number">16</span></span><br><span class="line">                fileobj.seek(offset)</span><br><span class="line">                stroff = fileobj.read(<span class="number">4</span>)</span><br><span class="line">                (stroff,) = <span class="keyword">struct</span>.unpack(<span class="string">'I'</span>, stroff)</span><br><span class="line">                strsize = fileobj.read(<span class="number">4</span>)</span><br><span class="line">                (strsize,) = <span class="keyword">struct</span>.unpack(<span class="string">'I'</span>, strsize)</span><br><span class="line">                symtabList_loc_size.append((stroff+location, strsize))</span><br><span class="line">                <span class="keyword">break</span></span><br><span class="line">            cmd_size = fileobj.read(<span class="number">4</span>)</span><br><span class="line">            (cmd_size,) = <span class="keyword">struct</span>.unpack(<span class="string">'I'</span>, cmd_size)</span><br><span class="line">            offset = offset + cmd_size</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">def replace_Objc_MsgSend(fileLen):</span><br><span class="line">    print('开始替换objc_msgSend...(静态库很大的话，可能需要等十几秒)!!!')</span><br><span class="line">    pos = <span class="number">0</span></span><br><span class="line">    bytes = b<span class="string">''</span></span><br><span class="line">    (loc, size) = symtabList_loc_size[0]</span><br><span class="line">    listIndex = <span class="number">1</span></span><br><span class="line">    with open(staticLibPath, 'rb') as fileobj:</span><br><span class="line">        <span class="keyword">while</span> pos &lt; fileLen:</span><br><span class="line">            <span class="keyword">if</span> pos == loc:</span><br><span class="line">                content = fileobj.read(size)</span><br><span class="line">                content = content.replace(b'\x00_objc_msgSend\x00', b'\x00_hook_msgSend\x00')</span><br><span class="line">                pos = pos + size</span><br><span class="line">                <span class="keyword">if</span> listIndex &lt; len(symtabList_loc_size):</span><br><span class="line">                    (loc, size) = symtabList_loc_size[listIndex]</span><br><span class="line">                    listIndex = <span class="number">1</span> + listIndex</span><br><span class="line">            <span class="keyword">else</span>:</span><br><span class="line">                step = <span class="number">4</span></span><br><span class="line">                <span class="keyword">if</span> loc &gt; pos:</span><br><span class="line">                    step = loc - pos</span><br><span class="line">                <span class="keyword">else</span>:</span><br><span class="line">                    step = fileLen - pos</span><br><span class="line">                content = fileobj.read(step)</span><br><span class="line">                pos = pos + step</span><br><span class="line">            bytes = bytes + content</span><br><span class="line">    with open(staticLibPath, 'wb+') as fileobj:</span><br><span class="line">        print('开始写入文件...')</span><br><span class="line">        fileobj.write(bytes)</span><br><span class="line">        </span><br><span class="line">    <span class="keyword">if</span> len(fatFilePath) &gt; <span class="number">0</span>:</span><br><span class="line">        replace_fat_file()</span><br><span class="line"></span><br><span class="line">    print('处理完了！！！')</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">need_process_objFile = set() <span class="meta"># set('xx1', 'xx2') 表示静态库中，仅xx1跟xx2需要处理</span></span><br><span class="line">needless_process_objFile = set() <span class="meta"># set('xx1', 'xx2') 表示静态库中，xx1跟xx2不需要处理，剩下的都需要处理</span></span><br><span class="line"></span><br><span class="line">def process_object_file(name, location, size):</span><br><span class="line">    <span class="meta"># 根据需要，下面三行中，只需打开一行，另外两行需要注释掉</span></span><br><span class="line">    process_mode = 'default' # 默认处理该静态库中的所有目标文件(类)</span><br><span class="line">    <span class="meta">#process_mode = 'need_process_objFile' # 只处理need_process_objFile集合(上面的集合，需要赋值)中的类</span></span><br><span class="line">    <span class="meta">#process_mode = 'needless_process_objFile' # 除了needless_process_objFile集合(上面的集合，需要赋值)中的类不处理，剩下的都需要处理</span></span><br><span class="line"></span><br><span class="line">    <span class="meta"># 这里可以过滤不需要处理的目标文件，或者只选择需要处理的目标文件</span></span><br><span class="line">    <span class="meta"># 默认处理该静态库中的所有目标文件</span></span><br><span class="line">    if process_mode == 'need_process_objFile':</span><br><span class="line">        <span class="keyword">if</span> name <span class="keyword">in</span> need_process_objFile:</span><br><span class="line">            find_symtab(location, size)</span><br><span class="line">    elif process_mode == 'needless_process_objFile':</span><br><span class="line">        <span class="keyword">if</span> not name <span class="keyword">in</span> need_process_objFile:</span><br><span class="line">            find_symtab(location, size)</span><br><span class="line">    <span class="keyword">else</span>:</span><br><span class="line">        find_symtab(location, size)</span><br><span class="line">    </span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="meta"># 静态库的路径</span></span><br><span class="line">staticLibPath = '完整的静态库路径'</span><br><span class="line">fatFilePath = str()</span><br><span class="line"><span class="meta"># objc_msgSend被替换的名字（两者长度需一致）</span></span><br><span class="line"><span class="meta"># hookObjcMsgSend-arm64.s里定义了函数名为hook_msgSend，如果修改脚本里的函数名，hookObjcMsgSend-arm64.s里的函数名，也需跟脚本保持一致</span></span><br><span class="line"><span class="meta"># 建议不修改hook_msgSend</span></span><br><span class="line">hook_msgSend_method_name = 'hook_msgSend'</span><br><span class="line">symtabList_loc_size = list()</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">if __name__ == '__main__':</span><br><span class="line">    <span class="meta"># staticLibPath = '/Users/xx/xx/xx'.strip()</span></span><br><span class="line">    staticLibPath = input('请输入静态库的路径：').strip()</span><br><span class="line"></span><br><span class="line">    if not len(hook_msgSend_method_name) == len('objc_msgSend'):</span><br><span class="line">        exit('need len(\'hook_msgSend\') == len(\'objc_msgSend\')!')</span><br><span class="line">    isValid, desc = get_valid_staticLib_path()</span><br><span class="line">    <span class="keyword">if</span> not isValid:</span><br><span class="line">        exit(desc)</span><br><span class="line">    <span class="meta"># 找到每个目标文件里的字符串表location 跟 size</span></span><br><span class="line">    fileLen = Path(staticLibPath).stat().st_size</span><br><span class="line">    offset = <span class="number">8</span></span><br><span class="line">    <span class="keyword">while</span> offset &lt; fileLen:</span><br><span class="line">        (name, location, size) = resolver_object_header(offset)</span><br><span class="line">        offset = location+size</span><br><span class="line">        endIndex = name.find('.o')</span><br><span class="line">        <span class="keyword">if</span> endIndex == <span class="number">-1</span>:</span><br><span class="line">            <span class="meta">#静态库的符号表，不需要处理</span></span><br><span class="line">            <span class="keyword">continue</span></span><br><span class="line">        process_object_file(name[:endIndex], location, size)</span><br><span class="line">    <span class="keyword">if</span> len(symtabList_loc_size) &gt; <span class="number">0</span>:</span><br><span class="line">        replace_Objc_MsgSend(fileLen)</span><br></pre></td></tr></table></figure><p>在 text 段定义一个 <code>_hook_msgSend</code> 函数：</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#ifdef __arm64__</span></span><br><span class="line"><span class="meta">#include <span class="meta-string">&lt;arm/arch.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">.macro ENTRY <span class="comment">/* name */</span></span><br><span class="line">    .text</span><br><span class="line">    .align <span class="number">5</span></span><br><span class="line">    .private_extern    $<span class="number">0</span></span><br><span class="line">$<span class="number">0</span>:</span><br><span class="line">.endmacro</span><br><span class="line"></span><br><span class="line">.macro END_ENTRY <span class="comment">/* name */</span></span><br><span class="line">LExit$<span class="number">0</span>:</span><br><span class="line">.endmacro</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">.macro BACKUP_REGISTERS</span><br><span class="line">    stp q6, q7, [sp, <span class="meta">#-0x20]!</span></span><br><span class="line">    stp q4, q5, [sp, <span class="meta">#-0x20]!</span></span><br><span class="line">    stp q2, q3, [sp, <span class="meta">#-0x20]!</span></span><br><span class="line">    stp q0, q1, [sp, <span class="meta">#-0x20]!</span></span><br><span class="line">    stp x6, x7, [sp, <span class="meta">#-0x10]!</span></span><br><span class="line">    stp x4, x5, [sp, <span class="meta">#-0x10]!</span></span><br><span class="line">    stp x2, x3, [sp, <span class="meta">#-0x10]!</span></span><br><span class="line">    stp x0, x1, [sp, <span class="meta">#-0x10]!</span></span><br><span class="line">    str x8,  [sp, <span class="meta">#-0x10]!</span></span><br><span class="line">.endmacro</span><br><span class="line"></span><br><span class="line">.macro RESTORE_REGISTERS</span><br><span class="line">    ldr x8,  [sp], <span class="meta">#0x10</span></span><br><span class="line">    ldp x0, x1, [sp], <span class="meta">#0x10</span></span><br><span class="line">    ldp x2, x3, [sp], <span class="meta">#0x10</span></span><br><span class="line">    ldp x4, x5, [sp], <span class="meta">#0x10</span></span><br><span class="line">    ldp x6, x7, [sp], <span class="meta">#0x10</span></span><br><span class="line">    ldp q0, q1, [sp], <span class="meta">#0x20</span></span><br><span class="line">    ldp q2, q3, [sp], <span class="meta">#0x20</span></span><br><span class="line">    ldp q4, q5, [sp], <span class="meta">#0x20</span></span><br><span class="line">    ldp q6, q7, [sp], <span class="meta">#0x20</span></span><br><span class="line">.endmacro</span><br><span class="line"></span><br><span class="line">.macro <span class="built_in">CALL_HOOK_BEFORE</span></span><br><span class="line">    BACKUP_REGISTERS</span><br><span class="line">    mov x2, lr</span><br><span class="line">    bl _hook_objc_msgSend_before</span><br><span class="line">    RESTORE_REGISTERS</span><br><span class="line">.endmacro</span><br><span class="line"></span><br><span class="line">.macro <span class="built_in">CALL_HOOK_AFTER</span></span><br><span class="line">    BACKUP_REGISTERS</span><br><span class="line">    bl _hook_objc_msgSend_after</span><br><span class="line">    mov lr, x0</span><br><span class="line">    RESTORE_REGISTERS</span><br><span class="line">.endmacro</span><br><span class="line"></span><br><span class="line"><span class="meta"># hookObjcMsgSend.py里定义了函数名为hook_msgSend，如果修改脚本里的函数名，这里的函数名，也需跟脚本保持一致</span></span><br><span class="line">ENTRY _hook_msgSend</span><br><span class="line"></span><br><span class="line"><span class="built_in">CALL_HOOK_BEFORE</span></span><br><span class="line">bl _objc_msgSend</span><br><span class="line"><span class="built_in">CALL_HOOK_AFTER</span></span><br><span class="line">ret</span><br><span class="line"></span><br><span class="line">END_ENTRY _hook_msgSend</span><br><span class="line"></span><br><span class="line"><span class="comment">// void hook_msgSend(...);</span></span><br><span class="line"><span class="comment">//ENTRY _hook_msgSend_stret</span></span><br><span class="line"><span class="comment">//b _hook_msgSend</span></span><br><span class="line"><span class="comment">//END_ENTRY _hook_msgSend_stret</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#endif</span></span><br></pre></td></tr></table></figure><p>简单解释下以上 <code>_hook_msgSend</code> 的汇编代码：</p><p>ARM64 有 31 个通用寄存器，每个寄存器可以存取一个 <strong>64</strong> 位的数据。我们可以通过 <code>X0 - X30</code> 来对这些寄存器进行寻址。对应 <code>X0 - X30</code>，<code>W0 - W30</code> 对应的就是相同单元数的低 <code>32</code> 位。<code>W0 - W30</code> 当进行写入操作时，会将高 <code>32</code> 位清零。</p><p>每一个寄存器具体的作用：</p><p><code>X0 - X7</code>：这 8 个寄存器主要 用来存储传递参数 。如果参数超过 8 个，则会通过栈来传递 ；<code>X0</code> 也用来存放上文方法的返回值</p><p><code>X29</code> ：即我们通常所说的帧指针 <code>FP</code>（Frame Pointer），指向当前方法栈的底部</p><p><code>X30</code> ：即链接寄存器 <code>LR</code>（Link Register）。为什么叫做链接，是因为这个寄存器会记录着当前方法的调用方地址 ，即当前方法调用完成时应该返回的位置。例如我们遇到 Crash 要获取方法堆栈，其本质就是不断的向上递归每一个 <code>X30</code> 寄存器的记录状态（也就是栈上 <code>X30</code> 寄存器的内容）来找到上层调用方。</p><p>除了这些通用寄存器，还有一个最重要的 <code>SP</code> 寄存器：</p><p><code>SP</code> 寄存器：即我们通常说的栈帧 <code>SP</code>（Stack Pointer）。指向当前方法栈的顶部。</p><p>这里 <code>_hook_msgSend</code> 方法里因为要用到 <code>X0 - X7</code> 等参数寄存器，所以每次保存下这些寄存器，调用原 <code>_objc_msgSend</code> 方法前再回复这些寄存器的内容，以保证上下文环境不被污染。</p><h1 id="镜花水月：基于桥的全量方法-Hook-方案-TrampolineHook"><a href="#镜花水月：基于桥的全量方法-Hook-方案-TrampolineHook" class="headerlink" title="镜花水月：基于桥的全量方法 Hook 方案 TrampolineHook"></a>镜花水月：基于桥的全量方法 Hook 方案 TrampolineHook</h1><p>这是 <a href="http://satanwoo.github.io/" target="_blank" rel="noopener">五子棋</a> 开源的中心重定向框架：<a href="https://github.com/SatanWoo/TrampolineHook/" target="_blank" rel="noopener">TrampolineHook</a>，用法示例：</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">void</span> myInterceptor() &#123;</span><br><span class="line">    printf(<span class="string">"调用了 myInterceptor\n"</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">- (<span class="keyword">void</span>)trampolineHook &#123;</span><br><span class="line">    THInterceptor *interceptor = [[THInterceptor alloc] initWithRedirectionFunction:(IMP)myInterceptor];</span><br><span class="line">    Method m = class_getInstanceMethod([HookDemoObj <span class="keyword">class</span>], <span class="keyword">@selector</span>(logString:));</span><br><span class="line">    IMP imp = method_getImplementation(m);</span><br><span class="line">    THInterceptorResult *interceptorResult = [interceptor interceptFunction:imp];</span><br><span class="line">    <span class="keyword">if</span> (interceptorResult.state == THInterceptStateSuccess) &#123;</span><br><span class="line">        method_setImplementation(m, interceptorResult.replacedAddress); <span class="comment">// 设置替换的地址</span></span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 执行到这一行时，会调用 myInterceptor 方法</span></span><br><span class="line">    HookDemoObj *obj = [HookDemoObj new];</span><br><span class="line">    [obj logString:<span class="string">@"abc"</span>];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>因为对 TrampolineHook 里的汇编代码还没完全看懂，之前 靛青 曾经写过一篇：<a href="https://blog.indigo.codes/2020/05/11/trampolinehook-study-notes/?from=groupmessage&amp;isappinstalled=0" target="_blank" rel="noopener">TrampolineHook 学习笔记</a> 这里就不错过多原理解析了。</p><p>除了以上提到的 Hook 方案，剩下还有 <a href="https://github.com/jmpews/Dobby" target="_blank" rel="noopener">Dobby</a> / <a href="https://frida.re/docs/ios/" target="_blank" rel="noopener">Frida</a> 逆向领域的 Hook 手段等，因为才疏学浅就不再继续解析了。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;之前在实现 &lt;a href=&quot;http://gonghonglou.com/2020/03/08/pod-lifecycle-aop/&quot;&gt;组件化分发生命周期 - AOP 方案&lt;/a&gt; 过程中用到了 &lt;a href=&quot;https://github.com/steipete/A
      
    
    </summary>
    
      <category term="iOS" scheme="http://gonghonglou.com/categories/iOS/"/>
    
    
  </entry>
  
  <entry>
    <title>组件化分发生命周期 - AOP 方案（libffi 实现）</title>
    <link href="http://gonghonglou.com/2020/09/06/pod-lifecycle-aop-libffi/"/>
    <id>http://gonghonglou.com/2020/09/06/pod-lifecycle-aop-libffi/</id>
    <published>2020-09-06T08:09:39.000Z</published>
    <updated>2020-09-07T16:26:55.928Z</updated>
    
    <content type="html"><![CDATA[<p>组件化分发生命周期 方案实现探索系列文章：<br>1、<a href="http://gonghonglou.com/2019/08/29/pod-lifecycle/">组件化分发生命周期</a><br>2、<a href="http://gonghonglou.com/2020/03/08/pod-lifecycle-aop/">组件化分发生命周期 - AOP 方案</a><br><strong>3、<a href="http://gonghonglou.com/2020/09/06/pod-lifecycle-aop-libffi/">组件化分发生命周期 - AOP 方案（libffi 实现）</a></strong></p><h1 id="background"><a href="#background" class="headerlink" title="background"></a>background</h1><p>在上一篇 <a href="http://gonghonglou.com/2020/03/08/pod-lifecycle-aop/">组件化分发生命周期 - AOP 方案</a> 文章里，介绍了通过 <a href="https://github.com/steipete/Aspects" target="_blank" rel="noopener">Aspects</a> 这个库，面向切面实现分发 <code>UIApplicationDelegate</code> 方法。</p><p>再简单重复介绍下分发思路：</p><p>1) hook <code>UIApplication</code> 的 <code>setDelegate:</code> 方法，拿到 <code>AppDelegate</code> 实例</p><p>2）遍历 <code>AppDelegate</code> 的方法列表，判断是否是 <code>UIApplicationDelegate</code> 的协议方法</p><p>3）对以上遍历得到的方法一一进行 hook</p><p>4）在被 hook 方法执行的之前和之后，调用我们想要分发的那些对象对应的方法</p><p>在以上第 3) 步里，是通过 <code>Aspects</code> 实现的 hook。<code>Aspects</code> hook 方法可以返回 <code>NSInvocation</code> 对象，这个对象里包含着 hook 对象，hook 方法，方法参数个数、参数类型等信息，恰好可以拿这些信息去调用目标分发对象的同名方法。</p><p><code>Aspects</code> 的实现原理是使方法调用（消息发送）立马进入消息转发阶段，在消息转发的最后阶段：<code>forwardInvocation:</code> 方法里拿到当前所 hook 方法的 <code>NSInvocation</code> 对象，并进行原方法调用和 block 的调用。</p><p>这种实现原理会导致原对象方法的调用要经历消息转发阶段，方法调用的性能差了一些。而且工程中如果全局 hook 了 <code>NSObject</code> 的 <code>forwardingTargetForSelector:</code>、<code>resolveInstanceMethod:</code> 等方法，监听 <code>unrecognized selector sent to instance</code> 崩溃类型的话，则会捕获到 <code>Aspects</code> 的 hook 过程。</p><p>所以在这篇文章里，将采用 <a href="https://github.com/libffi/libffi" target="_blank" rel="noopener">libffi</a> 实现上述的第 3) 步的 hook 操作，替换 <code>Aspects</code> 的实现。 <code>libffi</code> 较 <code>Aspects</code> 的好处是，hook 操作是直接交换两个 IMP，和原生的 Method Swizzling 优势一样。而且同样能拿到 hook 方法的参数个数、参数类型等信息。</p><h1 id="libffi-preview"><a href="#libffi-preview" class="headerlink" title="libffi preview"></a>libffi preview</h1><p><a href="https://github.com/libffi/libffi" target="_blank" rel="noopener">libffi</a> 相当于 C 语言上的 runtime，拥有动态调用 C 方法及 OC 方法的能力。简单介绍下用法吧，因为本篇文章的重点不在解析 <code>libffi</code> 及用法，所以这里只做简单的使用说明。</p><p>例如，调用 C 函数：</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">int</span> c_func(<span class="keyword">int</span> a , <span class="keyword">int</span> b) &#123;</span><br><span class="line">    <span class="keyword">int</span> sum = a + b;</span><br><span class="line">    <span class="keyword">return</span> sum;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">- (<span class="keyword">void</span>)libffi_call_c_func &#123;</span><br><span class="line">    ffi_cif cif;</span><br><span class="line">    ffi_type *argTypes[] = &#123;&amp;ffi_type_sint, &amp;ffi_type_sint&#125;;</span><br><span class="line">    ffi_prep_cif(&amp;cif, FFI_DEFAULT_ABI, <span class="number">2</span>, &amp;ffi_type_sint, argTypes);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">int</span> a = <span class="number">1</span>;</span><br><span class="line">    <span class="keyword">int</span> b = <span class="number">2</span>;</span><br><span class="line">    <span class="keyword">void</span> *args[] = &#123;&amp;a, &amp;b&#125;;</span><br><span class="line">    <span class="keyword">int</span> retValue;</span><br><span class="line">    ffi_call(&amp;cif, (<span class="keyword">void</span> *)c_func, &amp;retValue, args); <span class="comment">// retValue = 3</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>调用 OC 函数：</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line">- (<span class="keyword">int</span>)oc_func:(<span class="keyword">int</span>)a b:(<span class="keyword">int</span>)b &#123;</span><br><span class="line">    <span class="keyword">int</span> sum = a + b;</span><br><span class="line">    <span class="keyword">return</span> sum;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">- (<span class="keyword">void</span>)libffi_call_oc_func &#123;</span><br><span class="line">    SEL selector = <span class="keyword">@selector</span>(oc_func:b:);</span><br><span class="line">    <span class="built_in">NSMethodSignature</span> *signature = [<span class="keyword">self</span> methodSignatureForSelector:selector];</span><br><span class="line"></span><br><span class="line">    ffi_cif cif;</span><br><span class="line">    ffi_type *argTypes[] = &#123;&amp;ffi_type_pointer, &amp;ffi_type_pointer, &amp;ffi_type_sint, &amp;ffi_type_sint&#125;;</span><br><span class="line">    ffi_prep_cif(&amp;cif, FFI_DEFAULT_ABI, (uint32_t)signature.numberOfArguments, &amp;ffi_type_sint, argTypes);</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">int</span> arg1 = <span class="number">1</span>;</span><br><span class="line">    <span class="keyword">int</span> arg2 = <span class="number">2</span>;</span><br><span class="line">    <span class="keyword">void</span> *args[] = &#123;&amp;<span class="keyword">self</span>, &amp;selector, &amp;arg1, &amp;arg2&#125;;</span><br><span class="line">    <span class="keyword">int</span> retValue;</span><br><span class="line">    IMP func = [<span class="keyword">self</span> methodForSelector:selector];</span><br><span class="line">    ffi_call(&amp;cif, func, &amp;retValue, args); <span class="comment">// retValue = 3</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>函数调用 <code>ffi_call</code> 方法需要传入：1、函数模版（<code>ffi_cif</code>），2、函数指针，3、返回值，4、参数数组 这四个参数字段。<br>重点在于 函数模版（<code>ffi_cif</code>）。用 <code>ffi_cif</code> 生成一套函数模版，这个模版定义了调用一个函数时，这个函数的：1、参数个数（<code>int</code>），2、返回值类型（<code>ffi_type</code>），3、包含各个入参类型（<code>ffi_type</code>）的数组</p><p>这样，<code>libffi</code> 的 <code>ffi_call</code> 方法就可以根据这个传入的 函数模版（<code>ffi_cif</code>）去调用函数了。</p><p>其中，调用 OC 方法的话比较特殊，像 <code>objc_msgSend</code> 方法一样，第一位参数是函数调用者本身，第二位参数是调用的方法 SEL。</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">void</span> objc_msgSend(<span class="keyword">void</span> <span class="comment">/* id self, SEL op, ... */</span> )</span><br></pre></td></tr></table></figure><p>是不是看起来 <code>libffi</code> 的使用和 <code>NSInvocation</code> 很像。</p><h1 id="libffi-hook"><a href="#libffi-hook" class="headerlink" title="libffi hook"></a>libffi hook</h1><p>除了动态调用 C &amp; OC 方法外，<code>libffi</code> 提供的另外一个关键的能力是：<br>通过 <code>ffi_closure_alloc</code> 方法创建一个关联着函数指针（<code>IMP</code>）的闭包（<code>closure</code>）<br>通过 <code>ffi_prep_closure_loc</code> 函数给 <code>cif</code> 关联上这个闭包，并传入一个函数实体（<code>fun</code>）和刚才闭包里的 <code>IMP</code></p><p>这样，当 <code>IMP</code> 被执行的时候，就会执行函数实体（<code>fun</code>），并且能在这个函数里拿到：1、<code>cif</code>，2、返回值，3、参数地址，4、自定义的关联数据（<code>userdata</code>）<br>例如：</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">void</span> *newIMP = <span class="literal">NULL</span>;</span><br><span class="line">ffi_closure *closure = ffi_closure_alloc(<span class="keyword">sizeof</span>(ffi_closure), (<span class="keyword">void</span> **)&amp;newIMP);</span><br><span class="line">ffi_status prepClosureStatus = ffi_prep_closure_loc(closure, cif, holo_lifecycle_ffi_closure_func, (__bridge <span class="keyword">void</span> *)info, newIMP);</span><br><span class="line"><span class="keyword">if</span> (prepClosureStatus != FFI_OK) &#123;</span><br><span class="line">    <span class="keyword">return</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">void</span> holo_lifecycle_ffi_closure_func(ffi_cif *cif, <span class="keyword">void</span> *ret, <span class="keyword">void</span> **args, <span class="keyword">void</span> *userdata) &#123;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>有了这样的能力，我们就可以将上述的 <code>newIMP</code> 与我们要 hook 的方法 <code>IMP</code> 交换，这样调用 hook 方法的时候，就会执行到 <code>holo_lifecycle_ffi_closure_func</code> 方法，在这个方法里可以拿到原方法的参数个数、参数类型能信息，我们只要在这个方法里再调用一下原方法的 <code>IMP</code>，并在之前和之后分别调用要转发的目标对象的同名方法即可。</p><p>其中，还需要额外指出的一点是，在动态 hook 方法的时候，如何根据 C&amp;OC 方法里的参数类型转换成 <code>ffi_type</code> 呢？<br>苹果的官方文档 <a href="https://developer.apple.com/library/archive/documentation/Cocoa/Conceptual/ObjCRuntimeGuide/Articles/ocrtTypeEncodings.html" target="_blank" rel="noopener">Objective-C Type Encodings</a> 给出了参数 type encoding 字符串和参数类型的对应关系，也可以参考 NSHipster 的这篇文章：<a href="https://nshipster.cn/type-encodings/" target="_blank" rel="noopener">Type Encodings</a>。<br>其实每种类型，或者说相应字符，在 <code>libffi</code> 里也都存在着对应的 <code>ffi_type</code> 类型。这里直接拷贝了 <a href="https://github.com/eleme/Stinger" target="_blank" rel="noopener">Stinger</a> 里的 <a href="https://github.com/eleme/Stinger/blob/master/Stinger/Classes/STMethodSignature.m#L131-L209" target="_blank" rel="noopener">ffi_type <em>_st_ffiTypeWithType(const char </em>c)</a> 方法。</p><p>代码实现参见：</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br><span class="line">90</span><br><span class="line">91</span><br><span class="line">92</span><br><span class="line">93</span><br><span class="line">94</span><br><span class="line">95</span><br><span class="line">96</span><br><span class="line">97</span><br><span class="line">98</span><br><span class="line">99</span><br><span class="line">100</span><br><span class="line">101</span><br><span class="line">102</span><br><span class="line">103</span><br><span class="line">104</span><br><span class="line">105</span><br><span class="line">106</span><br><span class="line">107</span><br><span class="line">108</span><br><span class="line">109</span><br><span class="line">110</span><br><span class="line">111</span><br><span class="line">112</span><br><span class="line">113</span><br><span class="line">114</span><br><span class="line">115</span><br><span class="line">116</span><br><span class="line">117</span><br><span class="line">118</span><br><span class="line">119</span><br><span class="line">120</span><br><span class="line">121</span><br><span class="line">122</span><br><span class="line">123</span><br><span class="line">124</span><br><span class="line">125</span><br><span class="line">126</span><br><span class="line">127</span><br><span class="line">128</span><br><span class="line">129</span><br><span class="line">130</span><br><span class="line">131</span><br><span class="line">132</span><br><span class="line">133</span><br><span class="line">134</span><br><span class="line">135</span><br><span class="line">136</span><br><span class="line">137</span><br><span class="line">138</span><br><span class="line">139</span><br><span class="line">140</span><br><span class="line">141</span><br><span class="line">142</span><br><span class="line">143</span><br><span class="line">144</span><br><span class="line">145</span><br><span class="line">146</span><br><span class="line">147</span><br><span class="line">148</span><br><span class="line">149</span><br><span class="line">150</span><br><span class="line">151</span><br><span class="line">152</span><br><span class="line">153</span><br><span class="line">154</span><br><span class="line">155</span><br><span class="line">156</span><br><span class="line">157</span><br><span class="line">158</span><br><span class="line">159</span><br><span class="line">160</span><br><span class="line">161</span><br><span class="line">162</span><br><span class="line">163</span><br><span class="line">164</span><br><span class="line">165</span><br><span class="line">166</span><br><span class="line">167</span><br><span class="line">168</span><br><span class="line">169</span><br><span class="line">170</span><br><span class="line">171</span><br><span class="line">172</span><br><span class="line">173</span><br><span class="line">174</span><br><span class="line">175</span><br><span class="line">176</span><br><span class="line">177</span><br><span class="line">178</span><br><span class="line">179</span><br><span class="line">180</span><br><span class="line">181</span><br><span class="line">182</span><br><span class="line">183</span><br><span class="line">184</span><br><span class="line">185</span><br><span class="line">186</span><br><span class="line">187</span><br><span class="line">188</span><br><span class="line">189</span><br><span class="line">190</span><br><span class="line">191</span><br><span class="line">192</span><br><span class="line">193</span><br><span class="line">194</span><br><span class="line">195</span><br><span class="line">196</span><br><span class="line">197</span><br><span class="line">198</span><br><span class="line">199</span><br><span class="line">200</span><br><span class="line">201</span><br><span class="line">202</span><br><span class="line">203</span><br><span class="line">204</span><br><span class="line">205</span><br><span class="line">206</span><br><span class="line">207</span><br><span class="line">208</span><br><span class="line">209</span><br><span class="line">210</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">@implementation</span> <span class="title">UIApplication</span> (<span class="title">HoloLifecycle</span>)</span></span><br><span class="line"></span><br><span class="line">+ (<span class="keyword">void</span>)load &#123;</span><br><span class="line">    <span class="keyword">static</span> <span class="built_in">dispatch_once_t</span> onceToken;</span><br><span class="line">    <span class="built_in">dispatch_once</span>(&amp;onceToken, ^&#123;</span><br><span class="line">        Class <span class="keyword">class</span> = [<span class="keyword">self</span> <span class="keyword">class</span>];</span><br><span class="line">        </span><br><span class="line">        SEL originalSelector = <span class="keyword">@selector</span>(setDelegate:);</span><br><span class="line">        SEL swizzledSelector = <span class="keyword">@selector</span>(_holo_setDelegate:);</span><br><span class="line">        </span><br><span class="line">        Method originalMethod = class_getInstanceMethod(<span class="keyword">class</span>, originalSelector);</span><br><span class="line">        Method swizzledMethod = class_getInstanceMethod(<span class="keyword">class</span>, swizzledSelector);</span><br><span class="line">        </span><br><span class="line">        <span class="built_in">BOOL</span> success = class_addMethod(<span class="keyword">class</span>, originalSelector, method_getImplementation(swizzledMethod), method_getTypeEncoding(swizzledMethod));</span><br><span class="line">        <span class="keyword">if</span> (success) &#123;</span><br><span class="line">            class_replaceMethod(<span class="keyword">class</span>, swizzledSelector, method_getImplementation(originalMethod), method_getTypeEncoding(originalMethod));</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            method_exchangeImplementations(originalMethod, swizzledMethod);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">- (<span class="keyword">void</span>)_holo_setDelegate:(<span class="keyword">id</span> &lt;<span class="built_in">UIApplicationDelegate</span>&gt;)delegate &#123;</span><br><span class="line">    <span class="keyword">unsigned</span> count = <span class="number">0</span>;</span><br><span class="line">    Method *methods = class_copyMethodList([delegate <span class="keyword">class</span>], &amp;count);</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; count; i++) &#123;</span><br><span class="line">        Method method = methods[i];</span><br><span class="line">        SEL sel = method_getName(method);</span><br><span class="line">        <span class="keyword">struct</span> objc_method_description methodDesc = protocol_getMethodDescription(<span class="class"><span class="keyword">@protocol</span>(<span class="title">UIApplicationDelegate</span>), <span class="title">sel</span>, <span class="title">NO</span>, <span class="title">YES</span>);</span></span><br><span class="line">        <span class="keyword">if</span> (methodDesc.name != <span class="literal">NULL</span> &amp;&amp; methodDesc.types != <span class="literal">NULL</span>) &#123;</span><br><span class="line">            holo_lifecycle_hook_func(delegate, sel);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    [<span class="keyword">self</span> _holo_setDelegate:delegate];</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">void</span> holo_lifecycle_hook_func(<span class="keyword">id</span> obj, SEL sel) &#123;</span><br><span class="line">    <span class="built_in">NSString</span> *selStr = [<span class="string">@"holo_lifecycle_"</span> stringByAppendingString:<span class="built_in">NSStringFromSelector</span>(sel)];</span><br><span class="line">    <span class="keyword">const</span> SEL key = <span class="built_in">NSSelectorFromString</span>(selStr);</span><br><span class="line">    <span class="keyword">if</span> (objc_getAssociatedObject(obj, key)) &#123;</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    HoloLifecycleHookInfo *info = [HoloLifecycleHookInfo new];</span><br><span class="line">    info.cls = [obj <span class="keyword">class</span>];</span><br><span class="line">    info.sel = sel;</span><br><span class="line">    </span><br><span class="line">    objc_setAssociatedObject(obj, key, info, OBJC_ASSOCIATION_RETAIN_NONATOMIC);</span><br><span class="line">    </span><br><span class="line">    Method method = class_getInstanceMethod([obj <span class="keyword">class</span>], sel);</span><br><span class="line">    <span class="keyword">const</span> <span class="keyword">char</span> *typeEncoding = method_getTypeEncoding(method);</span><br><span class="line">    <span class="built_in">NSMethodSignature</span> *signature = [<span class="built_in">NSMethodSignature</span> signatureWithObjCTypes:typeEncoding];</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">const</span> <span class="keyword">unsigned</span> <span class="keyword">int</span> argsCount = method_getNumberOfArguments(method);</span><br><span class="line">    <span class="comment">// 构造参数类型列表</span></span><br><span class="line">    ffi_type **argTypes = calloc(argsCount, <span class="keyword">sizeof</span>(ffi_type *));</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; argsCount; ++i) &#123;</span><br><span class="line">        <span class="keyword">const</span> <span class="keyword">char</span> *argType = [signature getArgumentTypeAtIndex:i];</span><br><span class="line">        ffi_type *arg_ffi_type = holo_lifecycle_ffi_type(argType);</span><br><span class="line">        <span class="built_in">NSCAssert</span>(arg_ffi_type, <span class="string">@"HoloLifecycle: can't find a ffi_type: %s"</span>, argType);</span><br><span class="line">        argTypes[i] = arg_ffi_type;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 返回值类型</span></span><br><span class="line">    ffi_type *retType = holo_lifecycle_ffi_type(signature.methodReturnType);</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 需要在堆上开辟内存，否则会出现内存问题 (HoloLifecycleHookInfo 释放时会 free 掉)</span></span><br><span class="line">    ffi_cif *cif = calloc(<span class="number">1</span>, <span class="keyword">sizeof</span>(ffi_cif));</span><br><span class="line">    <span class="comment">// 生成 ffi_cfi 模版对象，保存函数参数个数、类型等信息，相当于一个函数原型</span></span><br><span class="line">    ffi_status prepCifStatus = ffi_prep_cif(cif, FFI_DEFAULT_ABI, argsCount, retType, argTypes);</span><br><span class="line">    <span class="keyword">if</span> (prepCifStatus != FFI_OK) &#123;</span><br><span class="line">        <span class="built_in">NSCAssert</span>(<span class="literal">NO</span>, <span class="string">@"HoloLifecycle: ffi_prep_cif failed: %d"</span>, prepCifStatus);</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 生成新的 IMP</span></span><br><span class="line">    <span class="keyword">void</span> *newIMP = <span class="literal">NULL</span>;</span><br><span class="line">    ffi_closure *closure = ffi_closure_alloc(<span class="keyword">sizeof</span>(ffi_closure), (<span class="keyword">void</span> **)&amp;newIMP);</span><br><span class="line">    ffi_status prepClosureStatus = ffi_prep_closure_loc(closure, cif, holo_lifecycle_ffi_closure_func, (__bridge <span class="keyword">void</span> *)info, newIMP);</span><br><span class="line">    <span class="keyword">if</span> (prepClosureStatus != FFI_OK) &#123;</span><br><span class="line">        <span class="built_in">NSCAssert</span>(<span class="literal">NO</span>, <span class="string">@"HoloLifecycle: ffi_prep_closure_loc failed: %d"</span>, prepClosureStatus);</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 替换 IMP 实现</span></span><br><span class="line">    Class hookClass = [obj <span class="keyword">class</span>];</span><br><span class="line">    SEL aSelector = method_getName(method);</span><br><span class="line">    <span class="keyword">if</span> (!class_addMethod(hookClass, aSelector, newIMP, typeEncoding)) &#123;</span><br><span class="line">        IMP originIMP = method_setImplementation(method, newIMP);</span><br><span class="line">        <span class="keyword">if</span> (info-&gt;_originalIMP != originIMP) &#123;</span><br><span class="line">            info-&gt;_originalIMP = originIMP;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">void</span> holo_lifecycle_ffi_closure_func(ffi_cif *cif, <span class="keyword">void</span> *ret, <span class="keyword">void</span> **args, <span class="keyword">void</span> *userdata) &#123;</span><br><span class="line">    HoloLifecycleHookInfo *info = (__bridge HoloLifecycleHookInfo *)userdata;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// before</span></span><br><span class="line">    <span class="keyword">for</span> (HoloBaseLifecycle *lifecycle <span class="keyword">in</span> [HoloLifecycleManager sharedInstance].beforeInstances) &#123;</span><br><span class="line">        holo_lifecycle_call_sel(lifecycle, info.sel, cif, args);</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// call original IMP</span></span><br><span class="line">    ffi_call(cif, info-&gt;_originalIMP, ret, args);</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// after</span></span><br><span class="line">    <span class="keyword">for</span> (HoloBaseLifecycle *lifecycle <span class="keyword">in</span> [HoloLifecycleManager sharedInstance].afterInstances) &#123;</span><br><span class="line">        holo_lifecycle_call_sel(lifecycle, info.sel, cif, args);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">void</span> holo_lifecycle_call_sel(HoloBaseLifecycle *lifecycle, SEL sel, ffi_cif *originalCif, <span class="keyword">void</span> **originalArgs) &#123;</span><br><span class="line">    <span class="keyword">if</span> (![lifecycle respondsToSelector:sel]) &#123;</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 复用 cif，构造参数，重置 args[0]、args[1]</span></span><br><span class="line">    <span class="built_in">NSMethodSignature</span> *signature = [lifecycle methodSignatureForSelector:sel];</span><br><span class="line">    <span class="built_in">NSUInteger</span> argsCount = signature.numberOfArguments;</span><br><span class="line">    <span class="keyword">void</span> **args = calloc(argsCount, <span class="keyword">sizeof</span>(<span class="keyword">void</span> *));</span><br><span class="line">    args[<span class="number">0</span>] = &amp;lifecycle;</span><br><span class="line">    args[<span class="number">1</span>] = &amp;sel;</span><br><span class="line">    memcpy(args + <span class="number">2</span>, originalArgs + <span class="number">2</span>, <span class="keyword">sizeof</span>(*originalArgs)*(argsCount - <span class="number">2</span>));</span><br><span class="line">    </span><br><span class="line">    IMP func = [lifecycle methodForSelector:sel];</span><br><span class="line">    ffi_call(originalCif, func, <span class="literal">NULL</span>, args);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="built_in">NS_INLINE</span> ffi_type *holo_lifecycle_ffi_type(<span class="keyword">const</span> <span class="keyword">char</span> *c) &#123;</span><br><span class="line">    <span class="keyword">switch</span> (c[<span class="number">0</span>]) &#123;</span><br><span class="line">        <span class="keyword">case</span> <span class="string">'v'</span>:</span><br><span class="line">            <span class="keyword">return</span> &amp;ffi_type_void;</span><br><span class="line">        <span class="keyword">case</span> <span class="string">'c'</span>:</span><br><span class="line">            <span class="keyword">return</span> &amp;ffi_type_schar;</span><br><span class="line">        <span class="keyword">case</span> <span class="string">'C'</span>:</span><br><span class="line">            <span class="keyword">return</span> &amp;ffi_type_uchar;</span><br><span class="line">        <span class="keyword">case</span> <span class="string">'s'</span>:</span><br><span class="line">            <span class="keyword">return</span> &amp;ffi_type_sshort;</span><br><span class="line">        <span class="keyword">case</span> <span class="string">'S'</span>:</span><br><span class="line">            <span class="keyword">return</span> &amp;ffi_type_ushort;</span><br><span class="line">        <span class="keyword">case</span> <span class="string">'i'</span>:</span><br><span class="line">            <span class="keyword">return</span> &amp;ffi_type_sint;</span><br><span class="line">        <span class="keyword">case</span> <span class="string">'I'</span>:</span><br><span class="line">            <span class="keyword">return</span> &amp;ffi_type_uint;</span><br><span class="line">        <span class="keyword">case</span> <span class="string">'l'</span>:</span><br><span class="line">            <span class="keyword">return</span> &amp;ffi_type_slong;</span><br><span class="line">        <span class="keyword">case</span> <span class="string">'L'</span>:</span><br><span class="line">            <span class="keyword">return</span> &amp;ffi_type_ulong;</span><br><span class="line">        <span class="keyword">case</span> <span class="string">'q'</span>:</span><br><span class="line">            <span class="keyword">return</span> &amp;ffi_type_sint64;</span><br><span class="line">        <span class="keyword">case</span> <span class="string">'Q'</span>:</span><br><span class="line">            <span class="keyword">return</span> &amp;ffi_type_uint64;</span><br><span class="line">        <span class="keyword">case</span> <span class="string">'f'</span>:</span><br><span class="line">            <span class="keyword">return</span> &amp;ffi_type_float;</span><br><span class="line">        <span class="keyword">case</span> <span class="string">'d'</span>:</span><br><span class="line">            <span class="keyword">return</span> &amp;ffi_type_double;</span><br><span class="line">        <span class="keyword">case</span> <span class="string">'F'</span>:</span><br><span class="line"><span class="meta">#if CGFLOAT_IS_DOUBLE</span></span><br><span class="line">            <span class="keyword">return</span> &amp;ffi_type_double;</span><br><span class="line"><span class="meta">#else</span></span><br><span class="line">            <span class="keyword">return</span> &amp;ffi_type_float;</span><br><span class="line"><span class="meta">#endif</span></span><br><span class="line">        <span class="keyword">case</span> <span class="string">'B'</span>:</span><br><span class="line">            <span class="keyword">return</span> &amp;ffi_type_uint8;</span><br><span class="line">        <span class="keyword">case</span> <span class="string">'^'</span>:</span><br><span class="line">            <span class="keyword">return</span> &amp;ffi_type_pointer;</span><br><span class="line">        <span class="keyword">case</span> <span class="string">'@'</span>:</span><br><span class="line">            <span class="keyword">return</span> &amp;ffi_type_pointer;</span><br><span class="line">        <span class="keyword">case</span> <span class="string">'#'</span>:</span><br><span class="line">            <span class="keyword">return</span> &amp;ffi_type_pointer;</span><br><span class="line">        <span class="keyword">case</span> <span class="string">':'</span>:</span><br><span class="line">            <span class="keyword">return</span> &amp;ffi_type_pointer;</span><br><span class="line">        <span class="keyword">case</span> <span class="string">'&#123;'</span>: &#123;</span><br><span class="line">            <span class="comment">// http://www.chiark.greenend.org.uk/doc/libffi-dev/html/Type-Example.html</span></span><br><span class="line">            ffi_type *type = malloc(<span class="keyword">sizeof</span>(ffi_type));</span><br><span class="line">            type-&gt;type = FFI_TYPE_STRUCT;</span><br><span class="line">            <span class="built_in">NSUInteger</span> size = <span class="number">0</span>;</span><br><span class="line">            <span class="built_in">NSUInteger</span> alignment = <span class="number">0</span>;</span><br><span class="line">            <span class="built_in">NSGetSizeAndAlignment</span>(c, &amp;size, &amp;alignment);</span><br><span class="line">            type-&gt;alignment = alignment;</span><br><span class="line">            type-&gt;size = size;</span><br><span class="line">            <span class="keyword">while</span> (c[<span class="number">0</span>] != <span class="string">'='</span>) ++c; ++c;</span><br><span class="line">            </span><br><span class="line">            <span class="built_in">NSPointerArray</span> *pointArray = [<span class="built_in">NSPointerArray</span> pointerArrayWithOptions:<span class="built_in">NSPointerFunctionsOpaqueMemory</span>];</span><br><span class="line">            <span class="keyword">while</span> (c[<span class="number">0</span>] != <span class="string">'&#125;'</span>) &#123;</span><br><span class="line">                ffi_type *elementType = <span class="literal">NULL</span>;</span><br><span class="line">                elementType = holo_lifecycle_ffi_type(c);</span><br><span class="line">                <span class="keyword">if</span> (elementType) &#123;</span><br><span class="line">                    [pointArray addPointer:elementType];</span><br><span class="line">                    c = <span class="built_in">NSGetSizeAndAlignment</span>(c, <span class="literal">NULL</span>, <span class="literal">NULL</span>);</span><br><span class="line">                &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                    <span class="keyword">return</span> <span class="literal">NULL</span>;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="built_in">NSInteger</span> count = pointArray.count;</span><br><span class="line">            ffi_type **types = malloc(<span class="keyword">sizeof</span>(ffi_type *) * (count + <span class="number">1</span>));</span><br><span class="line">            <span class="keyword">for</span> (<span class="built_in">NSInteger</span> i = <span class="number">0</span>; i &lt; count; i++) &#123;</span><br><span class="line">                types[i] = [pointArray pointerAtIndex:i];</span><br><span class="line">            &#125;</span><br><span class="line">            types[count] = <span class="literal">NULL</span>; <span class="comment">// terminated element is NULL</span></span><br><span class="line">            </span><br><span class="line">            type-&gt;elements = types;</span><br><span class="line">            <span class="keyword">return</span> type;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">NULL</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">@end</span></span><br></pre></td></tr></table></figure><p>完整实现代码参见：<a href="https://github.com/gonghonglou/HoloLifecycle" target="_blank" rel="noopener">HoloLifecycle</a></p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;组件化分发生命周期 方案实现探索系列文章：&lt;br&gt;1、&lt;a href=&quot;http://gonghonglou.com/2019/08/29/pod-lifecycle/&quot;&gt;组件化分发生命周期&lt;/a&gt;&lt;br&gt;2、&lt;a href=&quot;http://gonghonglou.com/
      
    
    </summary>
    
      <category term="iOS" scheme="http://gonghonglou.com/categories/iOS/"/>
    
    
  </entry>
  
  <entry>
    <title>HoloTableView &amp; HoloCollectionView 让你的列表更好维护</title>
    <link href="http://gonghonglou.com/2020/08/10/HoloTableView-HoloCollectionView/"/>
    <id>http://gonghonglou.com/2020/08/10/HoloTableView-HoloCollectionView/</id>
    <published>2020-08-10T15:31:01.000Z</published>
    <updated>2020-08-11T13:17:53.311Z</updated>
    
    <content type="html"><![CDATA[<h1 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h1><p>受 <code>Masonry</code> 链式语法和之前公司做法的影响，我封装了 <code>HoloTableView</code> &amp; <code>HoloCollectionView</code> 两个库，提供链式调用组装数据源数据及代理事件。</p><p>1、省去了设置代理（<code>tableView.dataSource = self;</code> <code>tableView.delegate = self;</code>）、遵守代理（<code>&lt;UITableViewDataSource, UITableViewDelegate&gt;</code>）以及实现各种代理方法的工作。<br>2、消除了 <code>NSIndexPath</code> 的概念，用代码执行次数和顺序决定 <code>cell</code> 数量及位置，并且将所有的代理事件分发到每个 <code>cell</code> 身上。</p><h2 id="Preview-1-GitHub-地址"><a href="#Preview-1-GitHub-地址" class="headerlink" title="Preview 1: GitHub 地址"></a>Preview 1: GitHub 地址</h2><p><a href="https://github.com/HoloFoundation/HoloTableView" target="_blank" rel="noopener">https://github.com/HoloFoundation/HoloTableView</a></p><p><a href="https://github.com/HoloFoundation/HoloCollectionView" target="_blank" rel="noopener">https://github.com/HoloFoundation/HoloCollectionView</a></p><h2 id="Preview-2-使用预览"><a href="#Preview-2-使用预览" class="headerlink" title="Preview 2: 使用预览"></a>Preview 2: 使用预览</h2><p>给 <code>UITableView</code>、<code>UICollectionView</code> 扩展了 <code>holo_makeRows:</code> 方法，在这个方法里每次 <code>make.row()</code> 就代表着创建一个 <code>cell</code>，后边的 <code>.model()</code>、<code>.height()</code> 就是给这个 <code>cell</code> 设置 model、高度等信息</p><p><code>HoloTableView</code> 简要用法：<br><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">UITableView</span> *tableView = [[<span class="built_in">UITableView</span> alloc] initWithFrame:<span class="keyword">self</span>.view.bounds style:<span class="built_in">UITableViewStylePlain</span>];</span><br><span class="line">[<span class="keyword">self</span>.view addSubview:tableView];</span><br><span class="line"></span><br><span class="line">[tableView holo_makeRows:^(HoloTableViewRowMaker * _Nonnull make) &#123;</span><br><span class="line">   <span class="comment">// make a cell</span></span><br><span class="line">   make.row(ExampleTableViewCell.class).model(<span class="built_in">NSDictionary</span>.new).height(<span class="number">44</span>);</span><br><span class="line">   </span><br><span class="line">   <span class="comment">// make a list</span></span><br><span class="line">   <span class="keyword">for</span> (<span class="built_in">NSObject</span> *obj <span class="keyword">in</span> <span class="built_in">NSArray</span>.new) &#123;</span><br><span class="line">       make.row(ExampleTableViewCell.class).model(obj).didSelectHandler(^(<span class="keyword">id</span>  _Nullable model) &#123;</span><br><span class="line">           <span class="built_in">NSLog</span>(<span class="string">@"did select row : %@"</span>, model);</span><br><span class="line">       &#125;);</span><br><span class="line">   &#125;</span><br><span class="line">&#125;];</span><br><span class="line"></span><br><span class="line">[tableView reloadData];</span><br></pre></td></tr></table></figure></p><p><code>HoloCollectionView</code> 简要用法：<br><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">UICollectionViewFlowLayout</span> *flowLayout = [<span class="built_in">UICollectionViewFlowLayout</span> new];</span><br><span class="line"><span class="built_in">UICollectionView</span> *collectionView = [[<span class="built_in">UICollectionView</span> alloc] initWithFrame:<span class="keyword">self</span>.view.bounds collectionViewLayout:flowLayout];</span><br><span class="line">[<span class="keyword">self</span>.view addSubview:collectionView];</span><br><span class="line"></span><br><span class="line">[collectionView holo_makeRows:^(HoloCollectionViewRowMaker * _Nonnull make) &#123;</span><br><span class="line">    <span class="comment">// make a cell</span></span><br><span class="line">    make.row(ExampleCollectionViewCell.class).model(<span class="built_in">NSDictionary</span>.new).size(<span class="built_in">CGSizeMake</span>(<span class="number">100</span>, <span class="number">200</span>));</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// make a list</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="built_in">NSObject</span> *obj <span class="keyword">in</span> <span class="built_in">NSArray</span>.new) &#123;</span><br><span class="line">        make.row(ExampleCollectionViewCell.class).model(obj).didSelectHandler(^(<span class="keyword">id</span>  _Nullable model) &#123;</span><br><span class="line">            <span class="built_in">NSLog</span>(<span class="string">@"did select row : %@"</span>, model);</span><br><span class="line">        &#125;);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;];</span><br><span class="line"></span><br><span class="line">[collectionView reloadData];</span><br></pre></td></tr></table></figure></p><p><strong> 注：从以上简要用法里也可以看出，<code>HoloTableView</code> 和 <code>HoloCollectionView</code> 的调用 API 几乎一致，所以为节省篇幅以下介绍及代码示例主要以 <code>HoloTableView</code> 为例。</strong></p><h1 id="封装历程"><a href="#封装历程" class="headerlink" title="封装历程"></a>封装历程</h1><p>以上篇幅主要是为了让读者对这两个库有个大概的认知，以下来讲一下对 <code>UITableView</code> 到 <code>HoloTableView</code> 的封装历程。以 <code>UITableView</code> 的实际使用举例。<br>以下代码极大的简化了逻辑，主要是为了展示 <code>UITableView</code> 代理方法的实现方式。</p><h2 id="1、入门版写法"><a href="#1、入门版写法" class="headerlink" title="1、入门版写法"></a>1、入门版写法</h2><p>假设现在要写一个列表，如下是入门版代理方法的实现过程：1、总共多少行，2、每行什么 cell，3、每个 cell 高度多少，4、点击每行 cell 的事件是什么</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br></pre></td><td class="code"><pre><span class="line">- (<span class="built_in">NSInteger</span>)tableView:(<span class="built_in">UITableView</span> *)tableView numberOfRowsInSection:(<span class="built_in">NSInteger</span>)section &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">2</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">- (<span class="built_in">UITableViewCell</span> *)tableView:(<span class="built_in">UITableView</span> *)tableView cellForRowAtIndexPath:(<span class="built_in">NSIndexPath</span> *)indexPath &#123;</span><br><span class="line">    <span class="keyword">if</span> (indexPath.row == <span class="number">0</span>) &#123;</span><br><span class="line">        ProfileTableViewCell *cell = [tableView dequeueReusableCellWithIdentifier:<span class="string">@"ProfileTableViewCell"</span>];</span><br><span class="line">        <span class="keyword">return</span> cell;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        OrderTableViewCell *cell = [tableView dequeueReusableCellWithIdentifier:<span class="string">@"OrderTableViewCell"</span>];</span><br><span class="line">        <span class="keyword">return</span> cell;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">- (<span class="built_in">CGFloat</span>)tableView:(<span class="built_in">UITableView</span> *)tableView heightForRowAtIndexPath:(<span class="built_in">NSIndexPath</span> *)indexPath &#123;</span><br><span class="line">    <span class="keyword">if</span> (indexPath.row == <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">100</span>;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">60</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">- (<span class="keyword">void</span>)tableView:(<span class="built_in">UITableView</span> *)tableView didSelectRowAtIndexPath:(<span class="built_in">NSIndexPath</span> *)indexPath &#123;</span><br><span class="line">    <span class="keyword">if</span> (indexPath.row == <span class="number">0</span>) &#123;</span><br><span class="line">        [<span class="keyword">self</span> _pushVC1];</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        [<span class="keyword">self</span> _pushVC2];</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="2、优化版写法"><a href="#2、优化版写法" class="headerlink" title="2、优化版写法"></a>2、优化版写法</h2><p>以上写法的问题是，假设某天要在中间插入一行别样的 cell，那么在每个代理方法都要去改对应的 if-else 逻辑，势必造成代码的可维护性很差，为了解决这一问题可将代理优化成如下样子：组装数据源，根据数据源实现代理方法</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 数据源</span></span><br><span class="line">- (<span class="built_in">NSArray</span>&lt;<span class="built_in">NSDictionary</span> *&gt; *)listData &#123;</span><br><span class="line">    <span class="keyword">return</span> @[</span><br><span class="line">            @&#123;<span class="string">@"cell"</span>   : ProfileTableViewCell.class,</span><br><span class="line">              <span class="string">@"height"</span> : @<span class="number">100</span>,</span><br><span class="line">              <span class="string">@"didSelectSEL"</span>  : <span class="built_in">NSStringFromSelector</span>(<span class="keyword">@selector</span>(_pushVC1))</span><br><span class="line">            &#125;,</span><br><span class="line">            @&#123;<span class="string">@"cell"</span>   : OrderTableViewCell.class,</span><br><span class="line">              <span class="string">@"height"</span> : @<span class="number">60</span>,</span><br><span class="line">              <span class="string">@"didSelectSEL"</span>  : <span class="built_in">NSStringFromSelector</span>(<span class="keyword">@selector</span>(_pushVC2))</span><br><span class="line">            &#125;</span><br><span class="line">    ];</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 代理方法实现</span></span><br><span class="line">- (<span class="built_in">NSInteger</span>)tableView:(<span class="built_in">UITableView</span> *)tableView numberOfRowsInSection:(<span class="built_in">NSInteger</span>)section &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">self</span>.listData.count;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">- (<span class="built_in">UITableViewCell</span> *)tableView:(<span class="built_in">UITableView</span> *)tableView cellForRowAtIndexPath:(<span class="built_in">NSIndexPath</span> *)indexPath &#123;</span><br><span class="line">    <span class="built_in">NSDictionary</span> *dict = <span class="keyword">self</span>.listData[indexPath.row];</span><br><span class="line">    <span class="built_in">NSString</span> *identifier = <span class="built_in">NSStringFromClass</span>(dict[<span class="string">@"cell"</span>]);</span><br><span class="line">    <span class="built_in">UITableViewCell</span> *cell = [tableView dequeueReusableCellWithIdentifier:identifier];</span><br><span class="line">    <span class="keyword">return</span> cell;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">- (<span class="built_in">CGFloat</span>)tableView:(<span class="built_in">UITableView</span> *)tableView heightForRowAtIndexPath:(<span class="built_in">NSIndexPath</span> *)indexPath &#123;</span><br><span class="line">    <span class="built_in">NSDictionary</span> *dict = <span class="keyword">self</span>.listData[indexPath.row];</span><br><span class="line">    <span class="keyword">return</span> [dict[<span class="string">@"height"</span>] floatValue];</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">- (<span class="keyword">void</span>)tableView:(<span class="built_in">UITableView</span> *)tableView didSelectRowAtIndexPath:(<span class="built_in">NSIndexPath</span> *)indexPath &#123;</span><br><span class="line">    <span class="built_in">NSDictionary</span> *dict = <span class="keyword">self</span>.listData[indexPath.row];</span><br><span class="line">    SEL sel = <span class="built_in">NSSelectorFromString</span>(dict[<span class="string">@"didSelectedSEL"</span>]);</span><br><span class="line">    <span class="keyword">if</span> (sel) &#123;</span><br><span class="line">        [<span class="keyword">self</span> performSelector:sel];</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="3、复用版写法"><a href="#3、复用版写法" class="headerlink" title="3、复用版写法"></a>3、复用版写法</h2><p>以上的优化版解决简单场景还好，但如果场景复杂，需要组装的数据源就会变得复杂起来，而且每个页面都要写这么一套还是会显得很繁琐。由此，我们可以想到进一步封装，将这一套逻辑封装成统一的框架，随处可用。<br>需要提供的数据源工具无非就两种，一种 <code>row</code>：带有 <code>cell</code> 类型，高度，数据，点击事件等；一种 <code>section</code>：带有 <code>header</code>、<code>footer</code> 等和一份 <code>row</code> 的数组。<br>使用时，组装好一份 <code>section</code> 数组，根据这个数据来实现代理方法就好了，如下：</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 数据类型</span></span><br><span class="line"><span class="class"><span class="keyword">@interface</span> <span class="title">Section</span> : <span class="title">NSObject</span></span></span><br><span class="line"><span class="keyword">@property</span> (<span class="keyword">nonatomic</span>, <span class="keyword">copy</span>) <span class="built_in">NSArray</span>&lt;Row *&gt; *rows;</span><br><span class="line"><span class="keyword">@property</span> (<span class="keyword">nonatomic</span>, <span class="keyword">assign</span>) Class header;</span><br><span class="line"><span class="keyword">@property</span> (<span class="keyword">nonatomic</span>, <span class="keyword">assign</span>) Class footer;</span><br><span class="line"><span class="keyword">@property</span> (<span class="keyword">nonatomic</span>, <span class="keyword">assign</span>) <span class="built_in">CGFloat</span> headerHeight;</span><br><span class="line"><span class="keyword">@property</span> (<span class="keyword">nonatomic</span>, <span class="keyword">assign</span>) <span class="built_in">CGFloat</span> footerHeight;</span><br><span class="line"><span class="keyword">@property</span> (<span class="keyword">nonatomic</span>, <span class="keyword">strong</span>) <span class="keyword">id</span> headerModel;</span><br><span class="line"><span class="keyword">@property</span> (<span class="keyword">nonatomic</span>, <span class="keyword">strong</span>) <span class="keyword">id</span> footerModel;</span><br><span class="line"><span class="keyword">@end</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">@interface</span> <span class="title">Row</span> : <span class="title">NSObject</span></span></span><br><span class="line"><span class="keyword">@property</span> (<span class="keyword">nonatomic</span>, <span class="keyword">assign</span>) Class cell;</span><br><span class="line"><span class="keyword">@property</span> (<span class="keyword">nonatomic</span>, <span class="keyword">assign</span>) <span class="built_in">CGFloat</span> height;</span><br><span class="line"><span class="keyword">@property</span> (<span class="keyword">nonatomic</span>, <span class="keyword">strong</span>) <span class="keyword">id</span> model;</span><br><span class="line"><span class="keyword">@property</span> (<span class="keyword">nonatomic</span>, <span class="keyword">copy</span>) <span class="keyword">void</span> (^didSelectHandler)(<span class="keyword">id</span> _Nullable model);</span><br><span class="line"><span class="keyword">@end</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 代理对象</span></span><br><span class="line"><span class="class"><span class="keyword">@interface</span> <span class="title">TableViewProxy</span> () &lt;<span class="title">UITableViewDataSource</span>, <span class="title">UITableViewDelegate</span>&gt;</span></span><br><span class="line"><span class="keyword">@property</span> (<span class="keyword">nonatomic</span>, <span class="keyword">copy</span>) <span class="built_in">NSArray</span> &lt;Section *&gt; *sectionArray;</span><br><span class="line"><span class="keyword">@end</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">@implementation</span> <span class="title">TableViewProxy</span></span></span><br><span class="line"><span class="meta">#pragma mark - reload 方法</span></span><br><span class="line">- (<span class="keyword">void</span>)reloadDataWithTableView:(<span class="built_in">UITableView</span> *)tableView listData:(<span class="built_in">NSArray</span>&lt;Section *&gt; *)listData &#123;</span><br><span class="line">    tableView.delegate = <span class="keyword">self</span>;</span><br><span class="line">    tableView.dataSource = <span class="keyword">self</span>;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">self</span>.sectionArray = listData;</span><br><span class="line">    </span><br><span class="line">    [tableView reloadData];</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">#pragma mark - UITableViewDataSource, UITableViewDelegate</span></span><br><span class="line">- (<span class="built_in">NSInteger</span>)numberOfSectionsInTableView:(<span class="built_in">UITableView</span> *)tableView &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">self</span>.sectionArray.count;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">- (<span class="built_in">NSInteger</span>)tableView:(<span class="built_in">UITableView</span> *)tableView numberOfRowsInSection:(<span class="built_in">NSInteger</span>)section &#123;</span><br><span class="line">    Section *section = <span class="keyword">self</span>.sectionArray[section];</span><br><span class="line">    <span class="keyword">return</span> section.rows.count;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">- (<span class="built_in">UITableViewCell</span> *)tableView:(<span class="built_in">UITableView</span> *)tableView cellForRowAtIndexPath:(<span class="built_in">NSIndexPath</span> *)indexPath &#123;</span><br><span class="line">    Section *section = <span class="keyword">self</span>.sectionArray[indexPath.section];</span><br><span class="line">    Row *row = section.rows[indexPath.row];</span><br><span class="line">    <span class="built_in">UITableViewCell</span> *cell = [tableView dequeueReusableCellWithIdentifier:<span class="built_in">NSStringFromClass</span>(row.cell)];</span><br><span class="line">    <span class="keyword">if</span> (!cell) &#123;</span><br><span class="line">        cell = [[row.cell alloc] initWithStyle:<span class="built_in">UITableViewCellStyleDefault</span> reuseIdentifier:<span class="built_in">NSStringFromClass</span>(row.cell)];</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> cell;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">- (<span class="built_in">CGFloat</span>)tableView:(<span class="built_in">UITableView</span> *)tableView heightForRowAtIndexPath:(<span class="built_in">NSIndexPath</span> *)indexPath &#123;</span><br><span class="line">    Section *section = <span class="keyword">self</span>.sectionArray[indexPath.section];</span><br><span class="line">    Row *row = section.rows[indexPath.row];</span><br><span class="line">    <span class="keyword">return</span> row.height;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">- (<span class="keyword">void</span>)tableView:(<span class="built_in">UITableView</span> *)tableView didSelectRowAtIndexPath:(<span class="built_in">NSIndexPath</span> *)indexPath &#123;</span><br><span class="line">    Section *section = <span class="keyword">self</span>.sectionArray[indexPath.section];</span><br><span class="line">    Row *row = section.rows[indexPath.row];</span><br><span class="line">    <span class="keyword">if</span> (row.didSelectHandler) &#123;</span><br><span class="line">        row.didSelectHandler(row.model);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这样一来，使用的时候只需组装出一份 <code>section</code> 数组进行 <code>reloadData</code> 就可以刷出列表了，如：</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 组装数据</span></span><br><span class="line">Section *section = [Section new];</span><br><span class="line">Row *row1 = [Row new];</span><br><span class="line">row1.cellClass = ProfileTableViewCell.class;</span><br><span class="line">row1.height = <span class="number">100</span>;</span><br><span class="line"></span><br><span class="line">Row *row2 = [Row new];</span><br><span class="line">row2.cellClass = OrderTableViewCell.class;</span><br><span class="line">row2.height = <span class="number">60</span>;</span><br><span class="line">row2.didSelectHandler = ^(<span class="keyword">id</span>  _Nullable model) &#123;</span><br><span class="line">    [<span class="keyword">self</span> _pushVC1];</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">section.rows = @[row1, row2];</span><br><span class="line"><span class="built_in">NSArray</span> *sections = @[section];</span><br><span class="line"></span><br><span class="line"><span class="comment">// 刷新列表</span></span><br><span class="line"><span class="keyword">self</span>.proxy = [TableViewProxy new];</span><br><span class="line">[<span class="keyword">self</span>.proxy reloadDataWithTableView:<span class="keyword">self</span>.tableView listData:sections];</span><br></pre></td></tr></table></figure><p>至此，就是 <code>HoloTableView</code> 封装及分发代理方法的核心思想了，进一步要做的就是完善 <code>row</code> 和 <code>section</code> 的属性支持，实现更全面的代理方法。</p><blockquote><p>完善的属性支持见：<a href="https://github.com/HoloFoundation/HoloTableView/blob/master/HoloTableView/Classes/HoloMaker/Row/HoloTableRow.h" target="_blank" rel="noopener">HoloTableRow.h</a>、<a href="https://github.com/HoloFoundation/HoloTableView/blob/master/HoloTableView/Classes/HoloMaker/Section/HoloTableSection.h" target="_blank" rel="noopener">HoloTableSection.h</a><br>全面的代理实现见：<a href="https://github.com/HoloFoundation/HoloTableView/blob/master/HoloTableView/Classes/HoloProxy/HoloTableViewProxy.m" target="_blank" rel="noopener">HoloTableViewProxy.m</a></p></blockquote><p>而除了完善更多的字段支持和代理实现之外，如何更方便快捷的组装数据就是 <code>HoloTableView</code> 的另一重点了。以下来介绍链式语法的实现来便捷的组装数据。</p><h1 id="链式语法"><a href="#链式语法" class="headerlink" title="链式语法"></a>链式语法</h1><p><code>HoloTableView</code> 完全参照 <code>Masonry</code> 的设计思路，甚至 API 的定义也是高度致敬 <code>Masonry</code>。先说下 <code>Masonry</code> 里的链式语法的大体实现，主要是：<code>- (NSArray *)mas_makeConstraints:(void(NS_NOESCAPE ^)(MASConstraintMaker *make))block;</code> 这个 API<br>1、声明一个 block，传入一个 <code>maker</code> 对象<br>2、给 <code>maker</code> 声明了一堆的属性（<code>left</code>，<code>right</code> 等），每个属性的返回值类型都相同，就可以实现链式语法<br>3、调用 <code>maker</code> 的每个属性都是往同一个对象上（当前 View）设置不同的值（left 约束，right 约束等）<br>4、最后执行 <code>install</code> 方法返回这个包含这堆约束的数组</p><p><code>HoloTableView</code> 也是这样的做法，提供 <code>- (void)holo_makeRows:(void(NS_NOESCAPE ^)(HoloTableViewRowMaker *make))block;</code> API 用来组装数据<br>1、声明一个 block，传入一个 <code>maker</code> 对象<br>2、给 <code>maker</code> 声明了一个属性：<code>row</code>，这个属性再返回 <code>HoloTableRowMaker</code> 类型，通过给这个类提供一堆的属性（<code>cell</code>，<code>model</code>，<code>height</code> 等），每个属性的返回值类型相同，实现链式调用<br>3、调用这些属性都是为了组装 <code>HoloTableRow</code>，设置每个 <code>row</code> 的各种数据<br>4、最后内部执行 <code>install</code> 方法返回一个包含 <code>HoloTableRow</code> 的数组</p><p>特别要说明一点的是，<code>Masonry</code> 提供的 <code>mas_makeConstraints:</code> 方法是为了操作同一个 View，所以他的 Maker 提供的每个属性都是同级的，每个属性的返回值类型都相同<br>但是 <code>HoloTableView</code> 提供的 <code>holo_makeRows:</code> 方法是为了组装一堆的 cell，每次 <code>make.row()</code> 都代表创建一个新的 cell，每个 cell 都有独立的数据。所以 <code>maker</code> 提供了一个一级属性 <code>row</code>，通过这个属性创建 <code>HoloTableRow</code> 对象并返回另一个 <code>RowMaker</code> 类来提供二级属性操作 <code>HoloTableRow</code> 对象</p><p>另外一点不同的是，<code>Masonry</code> 提供的属性大部分都是不带参数的，比如 <code>make.top.left.right</code>，这些是通过属性（property）提供的：<br><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">@property</span> (<span class="keyword">nonatomic</span>, <span class="keyword">strong</span>, <span class="keyword">readonly</span>) MASConstraint *left;</span><br><span class="line"><span class="keyword">@property</span> (<span class="keyword">nonatomic</span>, <span class="keyword">strong</span>, <span class="keyword">readonly</span>) MASConstraint *top;</span><br><span class="line"><span class="keyword">@property</span> (<span class="keyword">nonatomic</span>, <span class="keyword">strong</span>, <span class="keyword">readonly</span>) MASConstraint *right;</span><br></pre></td></tr></table></figure></p><p>也有一些带参数的，比如 equalTo 方法：<code>make.top.equalTo(self)</code><br><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">- (MASConstraint * (^)(<span class="keyword">id</span>))equalTo &#123;</span><br><span class="line">    <span class="keyword">return</span> ^<span class="keyword">id</span>(<span class="keyword">id</span> attribute) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">self</span>.equalToWithRelation(attribute, <span class="built_in">NSLayoutRelationEqual</span>);</span><br><span class="line">    &#125;;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p><code>HoloTableRow</code> 提供的字段都是需要传入参数的，采用的是：声明 block 属性和入参，标记为 <code>readonly</code>，并重写这个属性的 <code>getter</code> 方法。<code>getter</code> 方法里创建一个 block 对象并返回，执行这个 block 的话，将传入参数设置给 已创建的 <code>HoloTableRow</code> 对象，并返回 <code>self</code>，保证可以继续链式调用。以 model 这个字段举例：<br><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// .h 声明</span></span><br><span class="line"><span class="keyword">@property</span> (<span class="keyword">nonatomic</span>, <span class="keyword">copy</span>, <span class="keyword">readonly</span>) HoloTableRowMaker *(^model)(<span class="keyword">id</span> model);</span><br><span class="line"></span><br><span class="line"><span class="comment">// .m 重写 getter</span></span><br><span class="line">- (HoloTableRowMaker *(^)(<span class="keyword">id</span>))model &#123;</span><br><span class="line">    <span class="keyword">return</span> ^<span class="keyword">id</span>(<span class="keyword">id</span> obj) &#123;</span><br><span class="line">        <span class="keyword">self</span>.tableRow.model = obj;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">self</span>;</span><br><span class="line">    &#125;;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>调用的时候就是 <code>make.row(UITableViewCell.class).model(NSDictionary.new)</code>，这里的 <code>model()</code> 就是执行了上边的 block，将入参（一个字典对象）设置给了 <code>tableRow</code> 对象的 <code>model</code> 属性，再返回 <code>self</code>。</p><p>之所以选择定义成一个属性而不是像 <code>equalTo</code> 一样定义成方法是因为入参看起来更明确，此外还有个好处是，兼容 Swift 里的方法调用。如果定义成方法，原本在 OC 里调用方式为 <code>.model(NSObject.new)</code> 的链式，在 Swift 需要写成这样 <code>.model()(NSObject.new)</code>，Swift 需要先执行 <code>model()</code> 方法获取闭包再调用，这种写法显然是不能接受的。</p><h1 id="用法介绍"><a href="#用法介绍" class="headerlink" title="用法介绍"></a>用法介绍</h1><p>了解了以上介绍的「组装数据源思想」和「通过链式语法便捷组装数据」这两块主要卖点后，接下来就看下具体怎么使用的吧</p><h2 id="row"><a href="#row" class="headerlink" title="row"></a>row</h2><p><code>UITableView</code> 最常见的用法就是创建一个简单的列表，在文章开头的预览里也看到了：<br><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">[<span class="keyword">self</span>.tableView holo_makeRows:^(HoloTableViewRowMaker * _Nonnull make) &#123;</span><br><span class="line">   make.row(ExampleTableViewCell.class);</span><br><span class="line">   make.row(ExampleTableViewCell.class);</span><br><span class="line">&#125;];</span><br></pre></td></tr></table></figure></p><p>直接调用     <code>holo_makeRows:</code> 方法的话，会默认创建一个 tag 为 nil 的 section（section 的用法下一章节介绍） 用来承载将要创建的 row，就像系统代理方法 <code>numberOfSectionsInTableView:</code> 一样，不实现的话默认为 1。</p><p>每次 <code>make.row()</code> 都是创建一个 row（即一个 cell），上边示例代理里执行了两次那就是创建两行 cell，传入 cell 的 class。</p><p><strong>A：怎么给 cell 设置 model 呢？</strong><br>Q：<code>make.row().mode()</code> &amp; <code>- (void)holo_configureCellWithModel:(id)model;</code><br>通过 <code>.mode()</code> 传入 model 数据，但是传入的 model 怎么设置给 cell 呢？你在 cell 的 .m 里怎么拿到这个 model 呢？<code>HoloTableView</code> 是根据你传入的 cell class，new 一个 cell 对象，用这个 cell 调用默认的方法：<code>holo_configureCellWithModel:</code>，所以你需要在你的 cell 里实现这个方法，就能拿到传入的 model 了。如果你不喜欢这个方法的名字，可以通过 <code>make.row().mode().configSEL()</code> 传入你自己的方法名。<br><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 给 cell 赋值，这里的 model 就是 make.model() 传入的数据</span></span><br><span class="line">- (<span class="keyword">void</span>)holo_configureCellWithModel:(<span class="keyword">id</span>)model &#123;</span><br><span class="line">    <span class="keyword">self</span>.model = model;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p><strong>A：怎么给 cell 设置高度呢？</strong><br>Q：三种方式：<br>1、<code>make.row().height(44)</code><br>2、<code>make.row().heightHandler(^CGFloat(id  _Nullable model) { return 44 })</code><br>3、<code>+ (CGFloat)holo_heightForCellWithModel:(id)model;</code><br>如果 cell 的高度固定又比较简单可以通过 <code>.height()</code> 直接设置一个高度。<br>如果 cell 的高度是变化的，可以通过 <code>.heightHandler</code> 这个 block 返回高度。<br>如果这些计算高度的代码不想写在 VC 里，想要 cell 自己处理，那么可以在你的 cell 的 .m 里实现 <code>holo_heightForCellWithModel:</code> 方法返回高度。建议尽量用这个方法来控制高度，可以把 cell 高度的代码逻辑内敛到 cell 内部。当然，如果你不喜欢这个方法名还可以通过 <code>make.row().heightSEL()</code> 指定自己的方法名。<br><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 返回 cell 高度，这里的 model 就是 make.model() 传入的数据</span></span><br><span class="line">+ (<span class="built_in">CGFloat</span>)holo_heightForCellWithModel:(<span class="keyword">id</span>)model &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">44</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>除了 height 这个逻辑（<code>height</code>/<code>heightHandler</code>/<code>heightSEL</code>），你在 <a href="https://github.com/HoloFoundation/HoloTableView/blob/master/HoloTableView/Classes/HoloMaker/Row/HoloTableRowMaker.h" target="_blank" rel="noopener">HoloTableRowMaker.h</a> 这个文件里会发现提供给的很多字段都同时拥有三个类似的属性：<code>xxx</code>、<code>xxxHandler</code>、<code>xxxSEL</code>，这类的字段都拥有<strong>调用优先级</strong>：<br>1、如果你的 cell 里实现了 <code>xxxSEL</code> 方法，会优先使用这个方法，否则的话<br>2、如果你的 maker 设置了 <code>xxxHandler</code> block，会优先使用这个 block，否则的话<br>3、才会使用 <code>xxx</code> 这个字段</p><p>有些功能可能只有以上其中两个字断，优先级同样如上。</p><p><strong>A：<a href="https://github.com/HoloFoundation/HoloTableView/blob/master/HoloTableView/Classes/HoloMaker/Row/HoloTableRowMaker.h" target="_blank" rel="noopener">HoloTableRowMaker.h</a> 里 tag 属性的作用是什么？</strong><br>Q：<code>HoloTableView</code> 消除了 <code>NSIndexPath</code> 的概念，所以需要用 tag 值来给每个 row，每个 section 打上标记，通过这个 tag 来找到目标，执行更新、删除等操作。（tag 的用法后续章节介绍 row、section 更新操作时会有示例）</p><p><strong>A：cell 不再是自己创建了，那如果 cell 有事件产生怎么回调到 VC 里呢？</strong><br>Q：rowMaker 的 <code>willDisplayHandler</code> 这个 block 会在 <code>willDisplayCell:</code> 代理方法执行的时候被回调，并且传回当前 cell，就可以拿到 cell 对象了，至于 cell 头文件暴露了什么也一并可以操作了。<br><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">[<span class="keyword">self</span>.tableView holo_makeRows:^(HoloTableViewRowMaker * _Nonnull make) &#123;</span><br><span class="line">    make.row(ExampleTableViewCell.class).willDisplayHandler(^(<span class="built_in">UITableViewCell</span> * _Nonnull cell, <span class="keyword">id</span>  _Nullable model) &#123;</span><br><span class="line">        <span class="built_in">NSLog</span>(<span class="string">@"cell:%@"</span>, cell);</span><br><span class="line">    &#125;);</span><br><span class="line">&#125;];</span><br></pre></td></tr></table></figure></p><p>还有一种方式是，传入的 model 是你自己自装的对象：model 里声明 block，VC 里实现这个 blcok，cell 内部调用这个 block。</p><p>或者：给 <code>UIResponder</code> 扩展了一个方法 <a href="https://github.com/HoloFoundation/HoloTableView/blob/master/HoloTableView/Classes/Responder%20Event/UIResponder%2BHoloEvent.m" target="_blank" rel="noopener">UIResponder+HoloEvent.m</a>，沿着响应者链向上传递消息：<br><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// UIResponder 扩展方法</span></span><br><span class="line">- (<span class="keyword">void</span>)holo_event:(<span class="built_in">NSString</span> *)event params:(<span class="built_in">NSDictionary</span> *)params &#123;</span><br><span class="line">    [<span class="keyword">self</span>.nextResponder holo_event:event params:params];</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// cell 里发送消息</span></span><br><span class="line">[<span class="keyword">self</span>.nextResponder holo_event:<span class="string">@"event"</span> params:@&#123;&#125;];</span><br><span class="line"><span class="comment">// vc 里拦截消息</span></span><br><span class="line">- (<span class="keyword">void</span>)holo_event:(<span class="built_in">NSString</span> *)event params:(<span class="built_in">NSDictionary</span> *)params &#123;</span><br><span class="line">    <span class="keyword">if</span> ([event isEqualToString:<span class="string">@"event"</span>]) &#123;</span><br><span class="line">        <span class="built_in">NSLog</span>(<span class="string">@"params: %@"</span>, params);</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><h2 id="section"><a href="#section" class="headerlink" title="section"></a>section</h2><p>同样可以通过 <code>holo_makeSections:</code> 方法创建一个或多个 <code>section</code>，每个 <code>section</code> 可以包含着 <code>header</code>、<code>fooer</code> 和 一堆的 <code>row</code>：<br><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">[<span class="keyword">self</span>.tableView holo_makeSections:^(HoloTableViewSectionMaker * _Nonnull make) &#123;</span><br><span class="line">    make.section(<span class="string">@"tag1"</span>);</span><br><span class="line">    make.section(<span class="string">@"tag2"</span>)</span><br><span class="line">&#125;];</span><br></pre></td></tr></table></figure></p><p>和    <code>holo_makeRows:</code> 方法一样，每次 <code>make.section()</code> 都是创建一个 section，可以给这个 section 设置 header 及相关的数据，比如 model、高度等；可以设置 footer 及相关的数据，比如 model、高度等：<br><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">[<span class="keyword">self</span>.tableView holo_makeSections:^(HoloTableViewSectionMaker * _Nonnull make) &#123;</span><br><span class="line">    make.section(<span class="string">@"tag1"</span>)</span><br><span class="line">    .header(ExampleHeaderView.class).headerModel(@&#123;&#125;).headerHeight(<span class="number">100</span>)</span><br><span class="line">    .footer(ExampleFooterView.class).footerModel(@&#123;&#125;).footerHeight(<span class="number">100</span>);</span><br><span class="line"></span><br><span class="line">    make.section(<span class="string">@"tag2"</span>)</span><br><span class="line">    .header(ExampleHeaderView.class).headerModel(@&#123;&#125;).headerHeight(<span class="number">80</span>)</span><br><span class="line">    .footer(ExampleFooterView.class).footerModel(@&#123;&#125;).footerHeight(<span class="number">80</span>);</span><br><span class="line">&#125;];</span><br></pre></td></tr></table></figure></p><p><code>header</code> 或者 <code>footer</code> 的设计可以类比 <code>row</code>，所拥有的字段也都很像，查看：<a href="https://github.com/HoloFoundation/HoloTableView/blob/master/HoloTableView/Classes/HoloMaker/Section/HoloTableSectionMaker.h" target="_blank" rel="noopener">HoloTableSectionMaker.h</a> </p><p><code>section</code> 同样可以通过 <code>makeRows</code> 方法便捷的创建它所拥有的 <code>row</code>：<br><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line">[<span class="keyword">self</span>.tableView holo_makeSections:^(HoloTableViewSectionMaker * _Nonnull make) &#123;</span><br><span class="line">    make.section(<span class="string">@"tag1"</span>)</span><br><span class="line">    .header(ExampleHeaderView.class).headerModel(@&#123;&#125;).headerHeight(<span class="number">100</span>)</span><br><span class="line">    .footer(ExampleFooterView.class).footerModel(@&#123;&#125;).footerHeight(<span class="number">100</span>)</span><br><span class="line">    .makeRows(^(HoloTableViewRowMaker * _Nonnull make) &#123;</span><br><span class="line">        make.row(ExampleTableViewCell.class).height(<span class="number">44</span>);</span><br><span class="line">        make.row(ExampleTableViewCell.class).height(<span class="number">44</span>);</span><br><span class="line">    &#125;);</span><br><span class="line"></span><br><span class="line">    make.section(<span class="string">@"tag2"</span>)</span><br><span class="line">    .header(ExampleHeaderView.class).headerModel(@&#123;&#125;).headerHeight(<span class="number">100</span>)</span><br><span class="line">    .footer(ExampleFooterView.class).footerModel(@&#123;&#125;).footerHeight(<span class="number">100</span>)</span><br><span class="line">    .makeRows(^(HoloTableViewRowMaker * _Nonnull make) &#123;</span><br><span class="line">        make.row(ExampleTableViewCell.class).height(<span class="number">66</span>);</span><br><span class="line">        make.row(ExampleTableViewCell.class).height(<span class="number">66</span>);</span><br><span class="line">    &#125;);</span><br><span class="line">&#125;];</span><br></pre></td></tr></table></figure></p><h2 id="protocol"><a href="#protocol" class="headerlink" title="protocol"></a>protocol</h2><p>前文介绍 row 的时候提到了，如何将 model 设置 cell？cell 需要实现 <code>- (void)holo_configureCellWithModel:(id)model;</code> 方法；如何设定 cell 的高度？cell 可以实现 <code>+ (CGFloat)holo_heightForCellWithModel:(id)model;</code> 方法。除了这两个最常用的方法， <code>HoloTableView</code> 给 cell、header、fooer 提供了很多的其他功能的默认方法，参见：<br><a href="https://github.com/HoloFoundation/HoloTableView/blob/master/HoloTableView/Classes/Holo/HoloProtocol/HoloTableViewCellProtocol.h" target="_blank" rel="noopener">HoloTableViewCellProtocol.h</a><br><a href="https://github.com/HoloFoundation/HoloTableView/blob/master/HoloTableView/Classes/Holo/HoloProtocol/HoloTableViewHeaderProtocol.h" target="_blank" rel="noopener">HoloTableViewHeaderProtocol.h</a><br><a href="https://github.com/HoloFoundation/HoloTableView/blob/master/HoloTableView/Classes/Holo/HoloProtocol/HoloTableViewFooterProtocol.h" target="_blank" rel="noopener">HoloTableViewFooterProtocol.h</a></p><p>在 <code>HoloTableView</code> 的 1.x 时代，是把 <code>UITableView</code> 的代理方法分发到每个 row 身上，比如 <code>didSelectCell</code>、<code>willDisplayCell</code> 等事件，所以给 row 提供了对应的 <code>didSelectHandler</code>、<code>willDisplayHandler</code> 字段。这个字段的使用也大多都在 VC 里，但很多业务逻辑其实只是 cell 自己内部的事情。某些时候，如果把这些逻辑交给 cell 自己处理可能会让代码更清晰，更好维护，所以有了 2.0 版本的迭代。<br>在 <code>HoloTableView</code> 的 2.0 时代，可以把更多的事件分发到 cell 内部处理，比如 <code>didSelectCell</code>、<code>willDisplayCell</code> 等事件，cell 遵守 <a href="https://github.com/HoloFoundation/HoloTableView/blob/master/HoloTableView/Classes/Holo/HoloProtocol/HoloTableViewCellProtocol.h" target="_blank" rel="noopener">HoloTableViewCellProtocol.h</a> 协议并实现 <code>- (void)holo_didSelectCellWithModel:(id)model;</code>、<code>- (void)holo_willDisplayCellWithModel:(id)model;</code> 等方法即可。<br>需要注意的是，一旦 cell 实现了对应的方法，对应的 <code>didSelectCell</code>、<code>willDisplayCell</code> 等 block 便失效了，也就是前文提到的<strong>调用优先级</strong>。</p><p><code>header</code>、<code>footer</code> 同样适用于以上逻辑。以 cell 为例，遵守 <code>HoloTableViewCellProtocol</code> 协议并实现方法：<br><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#import <span class="meta-string">"ExampleTableViewCell.h"</span></span></span><br><span class="line"><span class="meta">#import <span class="meta-string">&lt;HoloTableView/HoloTableView.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">@interface</span> <span class="title">ExampleTableViewCell</span> () &lt;<span class="title">HoloTableViewCellProtocol</span>&gt;</span></span><br><span class="line"><span class="keyword">@property</span> (<span class="keyword">nonatomic</span>, <span class="keyword">strong</span>) <span class="keyword">id</span> model;</span><br><span class="line"><span class="keyword">@end</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">@implementation</span> <span class="title">HoloExampleTableViewCell</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#pragma mark - HoloTableViewCellProtocol</span></span><br><span class="line">- (<span class="keyword">void</span>)holo_configureCellWithModel:(<span class="built_in">NSDictionary</span> *)model &#123;</span><br><span class="line">    <span class="keyword">self</span>.model = model;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">+ (<span class="built_in">CGFloat</span>)holo_heightForCellWithModel:(<span class="built_in">NSDictionary</span> *)model &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">44</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">- (<span class="keyword">void</span>)holo_didSelectCellWithModel:(<span class="keyword">id</span>)model &#123;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">- (<span class="keyword">void</span>)holo_willDisplayCellWithModel:(<span class="keyword">id</span>)model &#123;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// etc</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">@end</span></span><br></pre></td></tr></table></figure></p><h2 id="section-amp-row：make、update、remake、insert、remove"><a href="#section-amp-row：make、update、remake、insert、remove" class="headerlink" title="section &amp; row：make、update、remake、insert、remove"></a>section &amp; row：make、update、remake、insert、remove</h2><p>除了前文提到的 <code>holo_makeSections:</code> 方法用于给 tableView 新增 <code>section</code>；<code>holo_makeRows:</code> 方法用于给 tableView 新增 <code>row</code> 之外，还提供了一系列的方法用于对 <code>section</code>、<code>row</code> 的增删改查操作。<br>参见：<a href="https://github.com/HoloFoundation/HoloTableView/blob/master/HoloTableView/Classes/Holo/UITableView%2BHoloTableView.h" target="_blank" rel="noopener">UITableView+HoloTableView.h</a></p><p>特别说明的是：<br>1、每个方法都同时拥有一个 <code>withReloadAnimation:</code> 方法，比如：<br><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">- (<span class="keyword">void</span>)holo_makeRows:(<span class="keyword">void</span>(<span class="built_in">NS_NOESCAPE</span> ^)(HoloTableViewRowMaker *make))block;</span><br><span class="line">- (<span class="keyword">void</span>)holo_makeRows:(<span class="keyword">void</span>(<span class="built_in">NS_NOESCAPE</span> ^)(HoloTableViewRowMaker *make))block</span><br><span class="line">  withReloadAnimation:(<span class="built_in">UITableViewRowAnimation</span>)animation;</span><br></pre></td></tr></table></figure></p><p>区别在于上边的方法执行过后需要执行 <code>[tableView reloadData]</code> 刷新列表，下边的方法则会自动刷新，但并非调用的 <code>reloadData</code> 方法，而是对应的 <code>insertRowsAtIndexPaths:</code>、<code>reloadRowsAtIndexPaths:</code> 等方法。<br>和原生方法表现一致：如果是新增或者更新一个或者几个 cell 的话，可以使用带 <code>withReloadAnimation:</code> 的方法，仅刷新某一行性能更好。但如果刷新的 cell 很多的话反而不如 <code>reloadData</code> 方法性能表现更好，所以需要合理选择。</p><p>2、类比 <code>Masonry</code> 的 API 逻辑：<br><code>make</code> 系列的方法就是新增 <code>section</code> 或 <code>row</code><br><code>update</code> 系列的方法就是通过 <code>make.tag()</code> 匹配到对应的 <code>section</code> 或 <code>row</code>，进行更新某部分数据，比如 model、高度等<br><code>remake</code> 系列的方法就是通过 <code>make.tag()</code> 匹配到对应的 <code>section</code> 或 <code>row</code>，将原本的数据清空，重新赋值数据<br><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// make</span></span><br><span class="line">[<span class="keyword">self</span>.tableView holo_makeRows:^(HoloTableViewRowMaker * _Nonnull make) &#123;</span><br><span class="line">    make.row(ExampleTableViewCell.class).model(@&#123;&#125;).height(<span class="number">44</span>).tag(<span class="string">@"a"</span>);</span><br><span class="line">&#125;];</span><br><span class="line"><span class="comment">// update</span></span><br><span class="line">[<span class="keyword">self</span>.tableView holo_updateRows:^(HoloTableViewUpdateRowMaker * _Nonnull make) &#123;</span><br><span class="line">    make.tag(<span class="string">@"a"</span>).height(<span class="number">88</span>);</span><br><span class="line">&#125;];</span><br><span class="line"><span class="comment">// remake</span></span><br><span class="line">[<span class="keyword">self</span>.tableView holo_remakeRows:^(HoloTableViewUpdateRowMaker * _Nonnull make) &#123;</span><br><span class="line">    make.tag(<span class="string">@"a"</span>).height(<span class="number">88</span>);</span><br><span class="line">&#125;];</span><br></pre></td></tr></table></figure></p><p>比如以上代码：<br>1）<code>holo_makeRows:</code> 方法创建了一个 <code>row</code>，给了 <code>calss</code>，<code>model</code>，<code>height</code>，设置了 tag 标记<br>2）如果执行 <code>holo_updateRows:</code> 方法的话，那这个 cell 就是从高度 44 刷新到了 88<br>3）如果执行 <code>holo_remakeRows:</code> 方法的话，因为将原来的数据清空了，所以 model 就不在了，class 也不在了，需要重新赋值</p><h2 id="key-Class-map"><a href="#key-Class-map" class="headerlink" title="key-Class map"></a>key-Class map</h2><p>在查看 <a href="https://github.com/HoloFoundation/HoloTableView/blob/master/HoloTableView/Classes/HoloMaker/Row/HoloTableViewRowMaker.h" target="_blank" rel="noopener">HoloTableViewRowMaker.h</a> 提供的 <code>row</code> 字段之外，会发现还有个 <code>rowS</code> 字段，顾名思义，<code>row</code> 接收的是 cell 的 class 类型，而 <code>rowS</code> 接收的是 cell class 的 string 形式，你可以这么用：<code>row(ExampleTableViewCell.class)</code>，也可以这么用：<code>rowS(@&quot;ExampleTableViewCell&quot;)</code></p><p>有一种场景，比如说首页是一个包含很多模块的列表，而到底展示哪些模块是由后台接口配置的，接口返回了一些模块的名称，每个模块名称对应着一种 cell。</p><p>而接收字符串类型的 <code>rowS</code> 字段还拥有的另外一个功能，就是 key-Class 映射。提前做好一份 string 对 cell class 的映射关系，比如 <code>@&quot;cell&quot; : ExampleTableViewCell.class</code>，就可以用 <code>rowS(@&quot;cell&quot;)</code> 来设置 cell 了。</p><p>不仅是 cell，包括 section 的 header、footer 也拥有相同的功能，配置 key-Class 映射关系的方式也很方便，在 <code>holo_makeTableView:</code> 方法里，由 <a href="https://github.com/HoloFoundation/HoloTableView/blob/master/HoloTableView/Classes/HoloMaker/TableView/HoloTableViewMaker.h" target="_blank" rel="noopener">HoloTableViewMaker.h</a> 这个 maker 提供：<br><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 注册 key-Class map</span></span><br><span class="line">[<span class="keyword">self</span>.tableView holo_makeTableView:^(HoloTableViewMaker * _Nonnull make) &#123;</span><br><span class="line">    make</span><br><span class="line">    .makeRowsMap(^(HoloTableViewRowMapMaker * _Nonnull make) &#123;</span><br><span class="line">        make.row(<span class="string">@"cell1"</span>).map(ExampleTableViewCell1.class);</span><br><span class="line">        make.row(<span class="string">@"cell2"</span>).map(ExampleTableViewCell2.class);</span><br><span class="line">    &#125;)</span><br><span class="line">    .makeHeadersMap(^(HoloTableViewHeaderMapMaker * _Nonnull make) &#123;</span><br><span class="line">        make.header(<span class="string">@"header1"</span>).map(ExampleHeaderView1.class);</span><br><span class="line">        make.header(<span class="string">@"header2"</span>).map(ExampleHeaderView2.class);</span><br><span class="line">    &#125;)</span><br><span class="line">    .makeFootersMap(^(HoloTableViewFooterMapMaker * _Nonnull make) &#123;</span><br><span class="line">        make.footer(<span class="string">@"footer1"</span>).map(ExampleFooterView1.class);</span><br><span class="line">        make.footer(<span class="string">@"footer2"</span>).map(ExampleFooterView2.class);</span><br><span class="line">    &#125;);</span><br><span class="line">&#125;];</span><br><span class="line"></span><br><span class="line"><span class="comment">// 使用 key 值</span></span><br><span class="line">[<span class="keyword">self</span>.tableView holo_makeSections:^(HoloTableViewSectionMaker * _Nonnull make) &#123;</span><br><span class="line">    make.section(TAG).headerS(<span class="string">@"header1"</span>).footerS(<span class="string">@"footer1"</span>).makeRows(^(HoloTableViewRowMaker * _Nonnull make) &#123;</span><br><span class="line">        make.rowS(<span class="string">@"cell1"</span>);</span><br><span class="line">        make.rowS(<span class="string">@"cell1"</span>);</span><br><span class="line">    &#125;);</span><br><span class="line">    </span><br><span class="line">    make.section(TAG).headerS(<span class="string">@"header2"</span>).footerS(<span class="string">@"footer2"</span>).makeRows(^(HoloTableViewRowMaker * _Nonnull make) &#123;</span><br><span class="line">        make.rowS(<span class="string">@"cell2"</span>);</span><br><span class="line">        make.rowS(<span class="string">@"cell2"</span>);</span><br><span class="line">    &#125;);</span><br><span class="line">&#125;];</span><br></pre></td></tr></table></figure></p><h2 id="scrollDelegate、delegate、datasource"><a href="#scrollDelegate、delegate、datasource" class="headerlink" title="scrollDelegate、delegate、datasource"></a>scrollDelegate、delegate、datasource</h2><p>在 <code>HoloTableView</code> 代理了 <code>UITableViewDelegate</code>、<code>UITableViewDataSource</code> 之后，某些场景下，你可能还想再拿到这些代理方法，自己实现一些逻辑，尤其是 <code>UIScrollViewDelegate</code> 的代理方法最为常用。<br>因为当前 tableView 的代理已经设置给 <a href="https://github.com/HoloFoundation/HoloTableView/blob/master/HoloTableView/Classes/HoloProxy/HoloTableViewProxy.h" target="_blank" rel="noopener">HoloTableViewProxy.h</a> 对象，再次夺回代理的话有两种方式：<br><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 方式一</span></span><br><span class="line"><span class="keyword">self</span>.tableView.holo_proxy.dataSource = <span class="keyword">self</span>;</span><br><span class="line"><span class="keyword">self</span>.tableView.holo_proxy.delegate = <span class="keyword">self</span>;</span><br><span class="line"><span class="keyword">self</span>.tableView.holo_proxy.scrollDelegate = <span class="keyword">self</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 方式二</span></span><br><span class="line">[<span class="keyword">self</span>.tableView holo_makeTableView:^(HoloTableViewMaker * _Nonnull make) &#123;</span><br><span class="line">    make.dataSource(<span class="keyword">self</span>).delegate(<span class="keyword">self</span>).scrollDelegate(<span class="keyword">self</span>);</span><br><span class="line">&#125;];</span><br></pre></td></tr></table></figure></p><p>设置过代理之后，一旦自己实现了某个代理方法，那这个代理方法 <code>HoloTableView</code> 就不再处理了。比如：<br><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">- (<span class="built_in">NSInteger</span>)numberOfSectionsInTableView:(<span class="built_in">UITableView</span> *)tableView &#123;</span><br><span class="line">    <span class="keyword">if</span> ([<span class="keyword">self</span>.dataSource respondsToSelector:<span class="keyword">@selector</span>(numberOfSectionsInTableView:)]) &#123;</span><br><span class="line">        <span class="keyword">return</span> [<span class="keyword">self</span>.dataSource numberOfSectionsInTableView:tableView];</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">self</span>.holoSections.count;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><h2 id="swipe-action"><a href="#swipe-action" class="headerlink" title="swipe action"></a>swipe action</h2><p><code>UITableViewCell</code> 的左滑、右滑功能在 iOS 各个系统版本间的差异比较大，直接写在业务代码里判断会非常多，<code>HoloTableView</code> 将这部分功能做了个总结，提供了 <a href="https://github.com/HoloFoundation/HoloTableView/blob/master/HoloTableView/Classes/Holo/HoloTableViewRowSwipeAction.h" target="_blank" rel="noopener">HoloTableViewRowSwipeAction.h</a> 这个类，提供的属性都标记了 iOS 版本，调用起来相对会比较方便。</p><p>可以组装好数组传递给 <a href="https://github.com/HoloFoundation/HoloTableView/blob/master/HoloTableView/Classes/HoloMaker/Row/HoloTableRowMaker.h" target="_blank" rel="noopener">HoloTableRowMaker.h</a>   类的 <code>leadingSwipeActions</code> or <code>trailingSwipeActions</code> 属性：<br><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">HoloTableViewRowSwipeAction *leftSwipeAction1 = [HoloTableViewRowSwipeAction rowSwipeActionWithStyle:HoloTableViewRowSwipeActionStyleNormal title:<span class="string">@"left1"</span>];</span><br><span class="line">HoloTableViewRowSwipeAction *leftSwipeAction2 = [HoloTableViewRowSwipeAction rowSwipeActionWithStyle:HoloTableViewRowSwipeActionStyleNormal title:<span class="string">@"left2"</span>];</span><br><span class="line"></span><br><span class="line">HoloTableViewRowSwipeAction *rightSwipeAction1 = [HoloTableViewRowSwipeAction rowSwipeActionWithStyle:HoloTableViewRowSwipeActionStyleNormal title:<span class="string">@"Right1"</span>];</span><br><span class="line">HoloTableViewRowSwipeAction *rightSwipeAction2 = [HoloTableViewRowSwipeAction rowSwipeActionWithStyle:HoloTableViewRowSwipeActionStyleNormal title:<span class="string">@"Right2"</span>];</span><br><span class="line"></span><br><span class="line">[<span class="keyword">self</span>.tableView holo_makeRows:^(HoloTableViewRowMaker * _Nonnull make) &#123;</span><br><span class="line">    make.row(HoloExampleTableViewCell.class)</span><br><span class="line">    .leadingSwipeActions(@[leftSwipeAction1, leftSwipeAction2])</span><br><span class="line">    .trailingSwipeActions(@[rightSwipeAction1, rightSwipeAction2]);</span><br><span class="line">&#125;];</span><br></pre></td></tr></table></figure></p><h1 id="Plugin"><a href="#Plugin" class="headerlink" title="Plugin"></a>Plugin</h1><p>之前遇到过一个关于 <a href="http://localhost:4000/2020/03/01/sdwebimage-gif/" target="_blank" rel="noopener">SDWebImage 4.x &amp; 5.x 对 GIF 类型的处理问题</a> 的bug，在排查问题过程中，查看了 SD 5.x 源码，发现 SD 的一个策略：把对很多第三方库的支持封装成插件（plugin），引入某个插件便能够拥有对某个库的支持，比如这些：<a href="https://github.com/SDWebImage/SDWebImage#integration-with-3rd-party-libraries" target="_blank" rel="noopener">Integration with 3rd party libraries</a>。</p><p>对于 <code>UITableViewCell</code> 的左滑、右滑功能，业界也有个比较知名的三方库：<a href="https://github.com/MortimerGoro/MGSwipeTableCell" target="_blank" rel="noopener">MGSwipeTableCell</a>。<code>HoloTableView</code> 借鉴 <code>SDWebImage</code> 的做法，封装了一个 plugin 库：<a href="https://github.com/HoloFoundation/HoloTableViewMGPlugin" target="_blank" rel="noopener">HoloTableViewMGPlugin</a>，API 的定义完全参照的 <code>MGSwipeTableCell</code>，字段尽量保持一致，以降低上手成本。</p><p>需要注意的是，<code>HoloTableViewMGPlugin</code> 和 <code>HoloTableView</code> 一样，只是帮你处理代理事件。至于其他的必要条件还是要满足的，比如你的 cell 需要继承 <code>MGSwipeTableCell</code> 基类。<br>使用姿势：<br><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#import <span class="meta-string">&lt;HoloTableViewMGPlugin/HoloTableViewMGPlugin.h&gt;</span></span></span><br><span class="line"></span><br><span class="line">[<span class="keyword">self</span>.tableView holo_makeRows:^(HoloTableViewRowMaker * _Nonnull make) &#123;</span><br><span class="line">    <span class="comment">// 方式一：直接赋值</span></span><br><span class="line">    make.row(ExampleMGSwipeTableCell.class)</span><br><span class="line">    .canSwipe(<span class="literal">YES</span>)</span><br><span class="line">    .makeSwipButtons(^(HoloTableRowMGMaker * _Nonnull make) &#123;</span><br><span class="line">        make.direction(MGSwipeDirectionLeftToRight).title(<span class="string">@"Left"</span>).backgroundColor(<span class="built_in">UIColor</span>.redColor);</span><br><span class="line">        make.direction(MGSwipeDirectionRightToLeft).title(<span class="string">@"Right"</span>).backgroundColor(<span class="built_in">UIColor</span>.redColor)</span><br><span class="line">        .callback(^<span class="built_in">BOOL</span>(MGSwipeTableCell * _Nonnull cell) &#123;</span><br><span class="line">            <span class="built_in">NSLog</span>(<span class="string">@"tag Right2 swip button"</span>);</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">YES</span>;</span><br><span class="line">        &#125;);</span><br><span class="line">    &#125;);</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 方式二：blcok 回调</span></span><br><span class="line">    make.row(ExampleMGSwipeTableCell.class)</span><br><span class="line">    .canSwipeHandler(^<span class="built_in">BOOL</span>(MGSwipeTableCell * _Nonnull cell, MGSwipeDirection direction, <span class="built_in">CGPoint</span> fromPoint) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">YES</span>;</span><br><span class="line">    &#125;)</span><br><span class="line">    .swipeButtonsHandler(^<span class="built_in">NSArray</span>&lt;<span class="built_in">UIView</span> *&gt; * _Nonnull(MGSwipeTableCell * _Nonnull cell, MGSwipeDirection direction, MGSwipeSettings * _Nonnull swipeSettings, MGSwipeExpansionSettings * _Nonnull expansionSettings) &#123;</span><br><span class="line">        <span class="keyword">if</span> (direction == MGSwipeDirectionLeftToRight) &#123;</span><br><span class="line">            <span class="keyword">return</span> @[[MGSwipeButton buttonWithTitle:<span class="string">@"Left"</span> backgroundColor:<span class="built_in">UIColor</span>.redColor]];</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="keyword">return</span> @[[MGSwipeButton buttonWithTitle:<span class="string">@"Right"</span> backgroundColor:<span class="built_in">UIColor</span>.redColor]];</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;)</span><br><span class="line">    .willBeginSwipingHandler(^(MGSwipeTableCell * _Nonnull cell) &#123;</span><br><span class="line">        <span class="built_in">NSLog</span>(<span class="string">@"begin swiping: %@"</span>, cell);</span><br><span class="line">    &#125;)</span><br><span class="line">    .willEndSwipingHandler(^(MGSwipeTableCell * _Nonnull cell) &#123;</span><br><span class="line">        <span class="built_in">NSLog</span>(<span class="string">@"end swiping: %@"</span>, cell);</span><br><span class="line">    &#125;);</span><br><span class="line">&#125;];</span><br></pre></td></tr></table></figure></p><h1 id="TODO"><a href="#TODO" class="headerlink" title="TODO"></a>TODO</h1><ul><li>Diff reload data.</li><li>Adapt new APIs from iOS 13 and iOS 14. ( HoloCollectionView )</li><li>Modern Objective-C and better Swift support.</li></ul><h1 id="后记"><a href="#后记" class="headerlink" title="后记"></a>后记</h1><p>感谢不知出处的前辈大佬们提供的「数据源代理」和「链式语法组装数据」思路。<br>感谢 <a href="https://github.com/SnapKit/Masonry" target="_blank" rel="noopener">Masonry</a>、<a href="https://github.com/SDWebImage/SDWebImage" target="_blank" rel="noopener">SDWebImage</a>、<a href="https://github.com/MortimerGoro/MGSwipeTableCell" target="_blank" rel="noopener">MGSwipeTableCell</a> 提供的 API 定义、封装思路 及 源码支持。</p><p>希望 <a href="https://github.com/HoloFoundation/HoloTableView" target="_blank" rel="noopener">HoloTableView</a>、<a href="https://github.com/HoloFoundation/HoloCollectionView" target="_blank" rel="noopener">HoloCollectionView</a> 这两个库可以切实的帮助到你减轻列表维护压力，有任何问题欢迎 Issues 或 PR。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h1 id=&quot;前言&quot;&gt;&lt;a href=&quot;#前言&quot; class=&quot;headerlink&quot; title=&quot;前言&quot;&gt;&lt;/a&gt;前言&lt;/h1&gt;&lt;p&gt;受 &lt;code&gt;Masonry&lt;/code&gt; 链式语法和之前公司做法的影响，我封装了 &lt;code&gt;HoloTableView&lt;/code&gt; 
      
    
    </summary>
    
      <category term="iOS" scheme="http://gonghonglou.com/categories/iOS/"/>
    
    
  </entry>
  
  <entry>
    <title>组件化页面跳转及路由方案</title>
    <link href="http://gonghonglou.com/2020/07/07/pod-target/"/>
    <id>http://gonghonglou.com/2020/07/07/pod-target/</id>
    <published>2020-07-07T13:11:13.000Z</published>
    <updated>2020-07-18T09:50:33.690Z</updated>
    
    <content type="html"><![CDATA[<h1 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h1><p>组件化页面路由算是个老生常谈的话题了，从蘑菇街的注册 url 到 Casa 的 target-action 方案，市面上也有了很多的优化方案。之所以出这篇文章主要是因为已有方案中的硬编码操作一直不被团队接受，所以一直在寻找更合理的方案，直到不久前从前前同事那得到的思路：注册 protocol 与 viewcontroller 的对应关系，根据 protocol 获取 VC 并调用协议方法。再加上在前公司做过的注册 url 方案，所以打算出篇文章，结合这两种思路总结一套适合当前工程的可行性路由方案。</p><h1 id="需求"><a href="#需求" class="headerlink" title="需求"></a>需求</h1><p>之所以文章标题叫做：页面跳转 及 路由 方案，主要是对应组件化工程中常见的两大需求：</p><p>1、组件间页面跳转解耦：PodA 中的 VC1 想要跳转到 PodB 中的 VC2，但两个组件又不能相互依赖。<br>2、后台可配的页面路由：页面存在跳转入口，根据后台配置不同的 url 跳往不同的落地页。</p><p>应对第一种需求，只要实现简单的解耦即可，正如前言里提到的，通过注册 protocol 与 viewcontroller 的键值对，根据 protocol 获取 VC 并调用协议方法然后 push 即可实现，剩下的就是怎么更合理的注册与取值的问题了。</p><p>应对第二种需求，其实可以通过第一种需求的解决方案来实现，只不过那需要大量的 if-else 来判断，并且需要一个中心化的转发类，不管从写法上还是可维护性上都不是最佳方案。<br>参照上一种解决方案，同样可以提前注册 url 与 viewcontroller 的键值对，根据 url 获取 VC 然后 push，至于传参，将 url 中的参数取出作为 NSDicitionary 传递给 VC 即可，具体实现下面会讲。相比于 if-else 的判断，这种注册 url 的方式好处是：某一 url 的路由支持交给各个组件自己支持，参数的接收也有目标 VC 来控制。</p><p>以上，总结出本方案的中心规律：<strong><em>谁提供、谁维护</em></strong>。意思是说，VC 所属于哪个 Pod，那这个 Pod 就有义务为这个 VC 支持 protocol 跳转和 url 跳转，也就是负责注册操作。如果这个 Pod 没有为 VC 进行注册操作，也就没打算将此 VC 对外提供。</p><h1 id="预览"><a href="#预览" class="headerlink" title="预览"></a>预览</h1><p>期望实现的用法如下：</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 1、protocol 方式支持页面跳转</span></span><br><span class="line"><span class="comment">// 提供方提前注册 protocol</span></span><br><span class="line">[[HoloTarget sharedInstance] registTarget:ViewController.class withProtocol:<span class="class"><span class="keyword">@protocol</span>(<span class="title">ViewControllerProtocol</span>)];</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 调用方根据 protocol 获取 vc 跳转</span></span><br><span class="line"><span class="built_in">UIViewController</span> *vc = [HoloNavigator matchViewControllerWithProtocol:<span class="class"><span class="keyword">@protocol</span>(<span class="title">ViewControllerProtocol</span>)];</span></span><br><span class="line">[(<span class="built_in">UIViewController</span>&lt;ViewControllerProtocol&gt; *)vc viewController:<span class="string">@"title"</span>];</span><br><span class="line">[<span class="keyword">self</span>.navigationController pushViewController:vc animated:<span class="literal">YES</span>];</span><br><span class="line">    </span><br><span class="line"></span><br><span class="line"><span class="comment">// 2、url 方式支持页面路由</span></span><br><span class="line"><span class="comment">// 提供方提前注册 url</span></span><br><span class="line">[[HoloTarget sharedInstance] registTarget:ViewController.class withUrl:<span class="string">@"holo://demo/vc?a=1&amp;b=2"</span>];</span><br><span class="line"></span><br><span class="line"><span class="comment">// 调用方根据 url 获取 vc 跳转</span></span><br><span class="line"><span class="built_in">UIViewController</span> *vc = [HoloNavigator matchViewControllerWithUrl:<span class="string">@"holo://demo/vc?a=1&amp;b=2"</span>];</span><br><span class="line">[<span class="keyword">self</span>.navigationController pushViewController:vc animated:<span class="literal">YES</span>];</span><br><span class="line"><span class="comment">// vc 内部获取入参</span></span><br><span class="line"><span class="built_in">NSDictionary</span> *params = [HoloNavigator matchUrlParamsWithViewController:vc];</span><br><span class="line"><span class="built_in">NSLog</span>(<span class="string">@"vc params:%@"</span>, params);</span><br></pre></td></tr></table></figure><h1 id="实现"><a href="#实现" class="headerlink" title="实现"></a>实现</h1><h2 id="HoloTarget"><a href="#HoloTarget" class="headerlink" title="HoloTarget"></a>HoloTarget</h2><p>主要在 <code>HoloTarget</code> 这个核心类来实现，分别为 protocol 和 url 提供了注册和匹配接口：</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/// 根据 protocol 注册 target 类</span></span><br><span class="line">- (<span class="built_in">BOOL</span>)registTarget:(Class)target withProtocol:(Protocol *)protocol;</span><br><span class="line"></span><br><span class="line"><span class="comment">/// 根据 url 注册 target 类</span></span><br><span class="line">- (<span class="built_in">BOOL</span>)registTarget:(Class)target withUrl:(<span class="built_in">NSString</span> *)url;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment">/// 根据 protocol 匹配 target 类</span></span><br><span class="line">- (<span class="keyword">nullable</span> Class)matchTargetWithProtocol:(Protocol *)protocol;</span><br><span class="line"></span><br><span class="line"><span class="comment">/// 根据 url 匹配 target 类</span></span><br><span class="line">- (<span class="keyword">nullable</span> Class)matchTargetWithUrl:(<span class="built_in">NSString</span> *)url;</span><br><span class="line"></span><br><span class="line"><span class="comment">/// 根据 protocol 匹配 target 实例</span></span><br><span class="line">- (<span class="keyword">nullable</span> <span class="keyword">id</span>)matchTargetInstanceWithProtocol:(Protocol *)protocol;</span><br><span class="line"></span><br><span class="line"><span class="comment">/// 根据 url 匹配 target 实例</span></span><br><span class="line">- (<span class="keyword">nullable</span> <span class="keyword">id</span>)matchTargetInstanceWithUrl:(<span class="built_in">NSString</span> *)url;</span><br></pre></td></tr></table></figure><p><code>.m</code> 的实现很简单， <code>HoloTarget</code> 单例维护了一个 <code>&lt;NSString *, Class&gt;</code> 类型的字典而已，核心代码很简单，大部分代码都是做安全判断的，就不贴代码了，细节在 Demo（<a href="https://github.com/HoloFoundation/HoloTarget/blob/master/HoloTarget/Classes/HoloTarget.m" target="_blank" rel="noopener">HoloTarget.m</a>） 里看吧。</p><p>特殊注意的一点是，注册和匹配 url 的时候，是通过 <a href="https://github.com/HoloFoundation/HoloTarget/blob/master/HoloTarget/Classes/NSString%2BHoloTargetUrlParser.m" target="_blank" rel="noopener">NSString+HoloTargetUrlParser</a> 这个分类获取 <code>path</code> 进行注册的。</p><h2 id="HoloNavigator"><a href="#HoloNavigator" class="headerlink" title="HoloNavigator"></a>HoloNavigator</h2><p><code>HoloNavigator</code> 这个类是对  <code>HoloTarget</code> 的封装，提供了 macth viewcontroller 的功能:</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/// 根据 protocol 匹配 viewController 实例</span></span><br><span class="line">+ (<span class="keyword">nullable</span> <span class="built_in">UIViewController</span> *)matchViewControllerWithProtocol:(Protocol *)protocol;</span><br><span class="line"></span><br><span class="line"><span class="comment">/// 根据 url 匹配 viewController 实例</span></span><br><span class="line">+ (<span class="keyword">nullable</span> <span class="built_in">UIViewController</span> *)matchViewControllerWithUrl:(<span class="built_in">NSString</span> *)url;</span><br><span class="line"></span><br><span class="line"><span class="comment">/// 根据 viewController 匹配 url 入参</span></span><br><span class="line">+ (<span class="keyword">nullable</span> <span class="built_in">NSDictionary</span> *)matchUrlParamsWithViewController:(<span class="built_in">UIViewController</span> *)viewController;</span><br></pre></td></tr></table></figure><p>其中在 <code>matchViewControllerWithUrl:</code> 方法里做了 <code>scheme</code> 判断，如果是 <code>http</code> 或者 <code>https</code> 的话，判断代理是否返回 <code>web viewcontroller</code>。</p><p>并且 <code>HoloTarget</code> 设置过 <code>businessScheme</code> 的话，对 <code>url scheme</code> 加了强校验，只有符合 <code>business scheme</code> 时才继续匹配：</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line">+ (<span class="keyword">nullable</span> <span class="built_in">UIViewController</span> *)matchViewControllerWithUrl:(<span class="built_in">NSString</span> *)url &#123;</span><br><span class="line">    <span class="built_in">NSString</span> *scheme = [url holo_targetUrlScheme];</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// web viewcontroller</span></span><br><span class="line">    <span class="keyword">if</span> ([scheme.lowercaseString isEqualToString:<span class="string">@"http"</span>] || [scheme.lowercaseString isEqualToString:<span class="string">@"https"</span>]) &#123;</span><br><span class="line">        <span class="keyword">if</span> ([HoloTarget sharedInstance].delegate &amp;&amp; [[HoloTarget sharedInstance] respondsToSelector:<span class="keyword">@selector</span>(holo_matchWebViewControllerWithUrl:)]) &#123;</span><br><span class="line">            <span class="built_in">UIViewController</span> *webVC = [[HoloTarget sharedInstance].delegate holo_matchWebViewControllerWithUrl:url];</span><br><span class="line">            <span class="keyword">return</span> webVC;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 约定必须遵守 business scheme 才可进行路由</span></span><br><span class="line">    <span class="keyword">if</span> ([HoloTarget sharedInstance].businessScheme.length &gt; <span class="number">0</span> &amp;&amp; ![scheme isEqualToString:[HoloTarget sharedInstance].businessScheme]) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">nil</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    ......</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h1 id="使用姿势"><a href="#使用姿势" class="headerlink" title="使用姿势"></a>使用姿势</h1><h2 id="1、-protocol-pool"><a href="#1、-protocol-pool" class="headerlink" title="1、 protocol pool"></a>1、 protocol pool</h2><p>创建 protocol pool 组件，被所有提供方和调用方依赖，或者将 HoloTarget 集成进当前组件化工程，创建 protocol pool 文件夹管理所有的 protocol。</p><h2 id="2、url-入参取值"><a href="#2、url-入参取值" class="headerlink" title="2、url 入参取值"></a>2、url 入参取值</h2><p>通过 url 匹配到 vc 的原理是获取 url 里的 path 进行匹配，获取到 vc 后，会将 url 里的 params 与 vc 绑定存储起来：</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line">+ (<span class="keyword">nullable</span> <span class="built_in">UIViewController</span> *)matchViewControllerWithUrl:(<span class="built_in">NSString</span> *)url &#123;</span><br><span class="line">    <span class="keyword">id</span> target = [[HoloTarget sharedInstance] matchTargetInstanceWithUrl:url];</span><br><span class="line">    <span class="keyword">if</span> ([target isKindOfClass:<span class="built_in">UIViewController</span>.class]) &#123;</span><br><span class="line">        <span class="built_in">NSDictionary</span> *params = [url holo_targetUrlParams];</span><br><span class="line">        <span class="keyword">if</span> (params) &#123;</span><br><span class="line">            objc_setAssociatedObject(target, &amp;KHoloNavigatorParamsKey, params, OBJC_ASSOCIATION_COPY_NONATOMIC);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> target;</span><br><span class="line">    &#125;</span><br><span class="line">  </span><br><span class="line">  ......</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">+ (<span class="built_in">NSDictionary</span> *)matchUrlParamsWithViewController:(<span class="built_in">UIViewController</span> *)viewController &#123;</span><br><span class="line">    <span class="keyword">return</span> objc_getAssociatedObject(viewController, &amp;KHoloNavigatorParamsKey);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>所以可以在 vc 的 viewDidLoad 方法内获取 params 做入参处理：</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">- (<span class="keyword">void</span>)viewDidLoad &#123;</span><br><span class="line">    [<span class="keyword">super</span> viewDidLoad];</span><br><span class="line">    <span class="comment">// Do any additional setup after loading the view.</span></span><br><span class="line">    </span><br><span class="line">    <span class="built_in">NSDictionary</span> *params = [HoloNavigator matchUrlParamsWithViewController:<span class="keyword">self</span>];</span><br><span class="line">    <span class="built_in">NSLog</span>(<span class="string">@"params:%@"</span>, params);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="3、Target-for-Pod"><a href="#3、Target-for-Pod" class="headerlink" title="3、Target for Pod"></a>3、Target for Pod</h2><p>如果一个 protocol 对应一个 viewcontroller，而当前 Pod 对外提供的 viewcontroller 又比较多的话，可以为 Pod 创建一个 MainTarget 类，进行 MainTarget - MainTargetProtocol 一次注册，在 MainTarget 类里转发对组件内各个 vc 的调用，如：</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 1、提供 MainTarget 类</span></span><br><span class="line"><span class="class"><span class="keyword">@interface</span> <span class="title">MainTarget</span> : <span class="title">NSObject</span> &lt;<span class="title">MainTargetProtocol</span>&gt;</span></span><br><span class="line"></span><br><span class="line">- (<span class="built_in">UIViewController</span> *)viewController1WithTitle:(<span class="built_in">NSString</span> *)title;</span><br><span class="line">- (<span class="built_in">UIViewController</span> *)viewController2WithTitle:(<span class="built_in">NSString</span> *)title;</span><br><span class="line">- (<span class="built_in">UIViewController</span> *)viewController3WithTitle:(<span class="built_in">NSString</span> *)title;</span><br><span class="line"></span><br><span class="line"><span class="keyword">@end</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment">// 2、使用举例</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 提供方提前注册（protocol）</span></span><br><span class="line">[[HoloTarget sharedInstance] registTarget:MainTarget.class withProtocol:<span class="class"><span class="keyword">@protocol</span>(<span class="title">MainTargetProtocol</span>)];</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 调用方获取 vc 跳转（protocol）</span></span><br><span class="line"><span class="keyword">id</span> mainTarget = [[HoloTarget sharedInstance] matchTargetInstanceWithProtocol:<span class="class"><span class="keyword">@protocol</span>(<span class="title">MainTargetProtocol</span>)];</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">UIViewController</span> *vc1 = [mainTarget&lt;MainTargetProtocol&gt; viewController1WithTitle:<span class="string">@"title1"</span>];</span><br><span class="line"><span class="built_in">UIViewController</span> *vc2 = [mainTarget&lt;MainTargetProtocol&gt; viewController1WithTitle:<span class="string">@"title2"</span>];</span><br><span class="line"><span class="built_in">UIViewController</span> *vc3 = [mainTarget&lt;MainTargetProtocol&gt; viewController2WithTitle:<span class="string">@"title3"</span>];</span><br></pre></td></tr></table></figure><h2 id="4、HoloLifecycle-提供注册时机"><a href="#4、HoloLifecycle-提供注册时机" class="headerlink" title="4、HoloLifecycle 提供注册时机"></a>4、HoloLifecycle 提供注册时机</h2><p>对 protocol 和 url 注册操作应该在哪里？这么做？根据 <strong><em>谁提供、谁维护</em></strong> 的原则，应当由各个 Pod 自己负责自己 vc 的注册操作。之前发过的一篇文章 <a href="http://gonghonglou.com/2020/03/08/pod-lifecycle-aop/">组件化分发生命周期</a> 介绍了 <a href="https://github.com/HoloFoundation/HoloLifecycle" target="_blank" rel="noopener">HoloLifecycle</a> 这个工具，正好满足了当下的需求。</p><p>为每个 Pod 创建 Lifecycle 类，将主工程 AppDelegate 的生命周期分发给各个 Pod，在 Lifecycle 类里完成 target 的注册操作（protocol &amp; url）。</p><h2 id="5、YAML-提供注册时机"><a href="#5、YAML-提供注册时机" class="headerlink" title="5、YAML 提供注册时机"></a>5、YAML 提供注册时机</h2><p>除了通过 HoloLifecycle 手动注册，HoloTarget 还提供了通过 YAML 配置文件自动注册的操作，在 Build Phases 添加  <code>holo_target_generator.rb</code> 脚本的执行（注意脚本路径改成脚本最终所在的位置）：</p><p><img src="http://image.gonghonglou.com/pod-target/build_phases.png" alt=""></p><p>该脚本的目的是收集各个 Pod 内的 holo_target.yaml 配置文件。脚本内容：</p><figure class="highlight ruby"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">#!/usr/bin/env ruby</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">require</span> <span class="string">'yaml'</span></span><br><span class="line"></span><br><span class="line">product_path = <span class="string">"<span class="subst">#&#123;ENV[<span class="string">"BUILT_PRODUCTS_DIR"</span>]&#125;</span>/<span class="subst">#&#123;ENV[<span class="string">"PRODUCT_NAME"</span>]&#125;</span>.app"</span></span><br><span class="line"></span><br><span class="line">target_files = Dir.glob(<span class="string">"<span class="subst">#&#123;product_path&#125;</span>/**/holo_target.&#123;yaml,yml&#125;"</span>)</span><br><span class="line"></span><br><span class="line">all_targets = target_files.map <span class="keyword">do</span> <span class="params">|path|</span></span><br><span class="line">    YAML.load(File.open(path))</span><br><span class="line"><span class="keyword">end</span>.reduce(<span class="symbol">:merge</span>)</span><br><span class="line"></span><br><span class="line">File.open <span class="string">"<span class="subst">#&#123;product_path&#125;</span>/.HOLO_ALL_TARGETS.yaml"</span>, <span class="string">"w"</span> <span class="keyword">do</span> <span class="params">|file|</span></span><br><span class="line">    file.write(all_targets.to_yaml)</span><br><span class="line"><span class="keyword">end</span></span><br></pre></td></tr></table></figure><p>调用 <code>HoloTarget.h</code> 类的 <code>registAllTargetsFromYAML</code> 方法进行收集注册，具体实现参考 <a href="https://github.com/HoloFoundation/HoloTarget/blob/a930a47a1294e6955c4199a0b57d2913439d2fbf/HoloTarget/Classes/HoloTarget.m#L28-L80" target="_blank" rel="noopener">HoloTarget.m#L28-L80</a></p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">- (<span class="keyword">void</span>)registAllTargetsFromYAML &#123;</span><br><span class="line">    <span class="built_in">NSString</span> *path = [[<span class="built_in">NSBundle</span> mainBundle] pathForResource:<span class="string">@".HOLO_ALL_TARGETS"</span> ofType:<span class="string">@"yaml"</span>];</span><br><span class="line">    <span class="built_in">NSInputStream</span> *stream = [[<span class="built_in">NSInputStream</span> alloc] initWithFileAtPath:path];</span><br><span class="line">    <span class="built_in">NSDictionary</span> *yaml = [YAMLSerialization objectWithYAMLStream:stream</span><br><span class="line">                                                         options:kYAMLReadOptionStringScalars</span><br><span class="line">                                                           error:<span class="literal">nil</span>];</span><br><span class="line">  ......</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>然后在各个 Pod 内部创建 <code>holo_target.yaml</code> 配置文件。</p><p>注意1：配置文件名称必须是 <code>holo_target.yaml</code>。</p><p>注意2：组件内的 <code>holo_target.yaml</code> 配置文件不要放在 Main Bundle 内，会被互相覆盖。</p><p><code>holo_target.yaml</code> 配置文件示例：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># Requirement: name your file holo_target.yaml</span></span><br><span class="line"><span class="comment"># example</span></span><br><span class="line"></span><br><span class="line"><span class="attr">HoloViewController:</span></span><br><span class="line"><span class="attr">    urls:</span></span><br><span class="line"><span class="bullet">        -</span> <span class="string">holo/vc</span></span><br><span class="line"><span class="attr">    protocols:</span></span><br><span class="line"><span class="bullet">        -</span> <span class="string">HoloViewControllerProtocol</span></span><br><span class="line"></span><br><span class="line"><span class="attr">HoloViewController2:</span></span><br><span class="line"><span class="attr">    urls:</span></span><br><span class="line"><span class="bullet">        -</span> <span class="string">holo/vc1</span></span><br><span class="line"><span class="bullet">        -</span> <span class="string">holo/vc2</span></span><br><span class="line"><span class="attr">    protocols:</span></span><br><span class="line"><span class="bullet">        -</span> <span class="string">HoloViewControllerProtocol1</span></span><br><span class="line"><span class="bullet">        -</span> <span class="string">HoloViewControllerProtocol2</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># etc ...</span></span><br></pre></td></tr></table></figure><p>选择 YAML 格式的主要原因是因为该格式支持添加注释，而且写法也比 JSON 简洁很多，是很多配置文件的首选格式，注意 YAML 使用缩进表示层级关系，参考：<a href="https://www.ruanyifeng.com/blog/2016/07/yaml.html" target="_blank" rel="noopener">YAML 语言教程</a></p><h1 id="安全防护"><a href="#安全防护" class="headerlink" title="安全防护"></a>安全防护</h1><p>通过 protocol 获取 vc 并调用方法时，如：</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">UIViewController</span> *vc = [HoloNavigator matchViewControllerWithProtocol:<span class="class"><span class="keyword">@protocol</span>(<span class="title">ViewControllerProtocol</span>)];</span></span><br><span class="line">[(<span class="built_in">UIViewController</span>&lt;ViewControllerProtocol&gt; *)vc viewController:<span class="string">@"title"</span>];</span><br><span class="line">[<span class="keyword">self</span>.navigationController pushViewController:vc animated:<span class="literal">YES</span>];</span><br></pre></td></tr></table></figure><p>如果该 vc 未实现 <code>viewController:</code> 方法的话程序就会崩溃，虽然 HoloTarget 在注册时做了 taeget 是否遵守 protocol 的判断，如：</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">- (<span class="built_in">BOOL</span>)registTarget:(Class)target withProtocol:(Protocol *)protocol &#123;</span><br><span class="line">......</span><br><span class="line">    &#125; <span class="keyword">else</span> <span class="keyword">if</span> (![target conformsToProtocol:protocol]) &#123;</span><br><span class="line">        HoloLog(<span class="string">@"[HoloTarget] Regist failed because the target (%@) is not conform to the protocol (%@)."</span>, target, protocolString);</span><br><span class="line">        isSuccess = <span class="literal">NO</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    ......</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>但正常经常下，vc 如果遵守了协议但未实现方法的话也只有个警告而已。当然，你可以通过加 <code>respondsToSelector:</code> 判断这种最简单的方式解决这个 crash 隐患：</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> ([vc respondsToSelector:<span class="keyword">@selector</span>(viewController:)]) &#123;</span><br><span class="line">    [(<span class="built_in">UIViewController</span>&lt;ViewControllerProtocol&gt; *)vc viewController:<span class="string">@"title"</span>];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>HoloTarget 为了省掉这个 if 判断，最开始创建了 <code>NSObject+HoloTargetUnrecognizedSelector</code> 分类，想要防护所有类的 <code>unrecognized selector</code> 崩溃：</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">@implementation</span> <span class="title">HoloTargetStubProxy</span></span></span><br><span class="line"></span><br><span class="line">+ (<span class="built_in">BOOL</span>)resolveInstanceMethod:(SEL)sel &#123;</span><br><span class="line">    class_addMethod([<span class="keyword">self</span> <span class="keyword">class</span>], sel, imp_implementationWithBlock(^&#123;</span><br><span class="line">        HoloLog(<span class="string">@"[HoloTarget] Unrecognized selector (%@)"</span>, <span class="built_in">NSStringFromSelector</span>(sel));</span><br><span class="line">    &#125;), <span class="string">"v@:"</span>);</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">YES</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">@end</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">@implementation</span> <span class="title">NSObject</span> (<span class="title">HoloTargetUnrecognizedSelector</span>)</span></span><br><span class="line"></span><br><span class="line">+ (<span class="keyword">void</span>)load &#123;</span><br><span class="line">    [<span class="keyword">self</span> jr_swizzleClassMethod:<span class="keyword">@selector</span>(forwardingTargetForSelector:) withClassMethod:<span class="keyword">@selector</span>(_holo_forwardingTargetForSelector:) error:<span class="literal">nil</span>];</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">- (<span class="keyword">id</span>)_holo_forwardingTargetForSelector:(SEL)aSelector &#123;</span><br><span class="line">    <span class="comment">// Aspects hook 会有问题：动态交换了 forwardInvocation: 方法 (aspect_swizzleForwardInvocation)</span></span><br><span class="line">    <span class="comment">// 问题：怎么判断某个方法被 hook 过了 ?</span></span><br><span class="line">    <span class="comment">// 判断 'forwardingTargetForSelector:'、'resolveInstanceMethod:'、'forwardInvocation:' 被处理过了的话，直接 return nil.</span></span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 自己要处理的话, 直接返回</span></span><br><span class="line">    <span class="keyword">if</span> ([<span class="keyword">self</span> _holo_methodHasOverwrited:<span class="keyword">@selector</span>(resolveInstanceMethod:) cls:<span class="keyword">self</span>.class]) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">nil</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> ([<span class="keyword">self</span> _holo_methodHasOverwrited:<span class="keyword">@selector</span>(forwardInvocation:) cls:<span class="keyword">self</span>.class]) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">nil</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> [HoloTargetStubProxy new];</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 判断 cls 是否重写了 sel 方法, 递归调用判断父类但不包括 NSObject</span></span><br><span class="line">- (<span class="built_in">BOOL</span>)_holo_methodHasOverwrited:(SEL)sel cls:(Class)cls &#123;</span><br><span class="line">   ......</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">@end</span></span><br></pre></td></tr></table></figure><p>正如注释所写的，暂时没办法获取到 <code>forwardInvocation:</code> 方法是否被交换过，如果直接在 <code>forwardingTargetForSelector:</code> 方法里转发的话肯定会有问题。比如被 Aspects hook 过的方法，其实是将被 hook 的对象 isa 指向了动态创建的类，被 hook 的方法执行时进入消息转发流程，并且 Aspects 交换了  <code>forwardInvocation:</code> 方法在新的  <code>forwardInvocation:</code> 方法里执行了被 hook 方法。</p><p>所以如果在以上 <code>_holo_forwardingTargetForSelector:</code> 方法里无脑转发给 <code>[HoloTargetStubProxy new]</code> 对象的话那被 Aspects hook 的方法就是失效了。如果有方法能够判断某个方法是否被交换过就好了，这样就可以做判断：如果当前类及父类重写或交换了 <code>resolveInstanceMethod:</code> 方法、<code>forwardInvocation:</code> 方法，直接  <code>return nil</code>。</p><p>如果你有方法能够判断某个方法是否被交换过，请告知我，谢谢！</p><p>以上方案走不通的话，那可以在工程中添加 OCLint 规则，判断当前类遵守了协议但未实现协议方法的话直接报错，编译失败。</p><p>当前 HoloTarget 也提供了备选方案，创建了 <code>HoloBaseTarget</code>、<code>HoloBaseTargetViewController</code> 分别继承了 <code>NSObject</code>、<code>UIViewController</code> 的两个类，在这两个类的里重写了 <code>resolveInstanceMethod:</code> 方法，做了安全防护：</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">+ (<span class="built_in">BOOL</span>)resolveInstanceMethod:(SEL)sel &#123;</span><br><span class="line">    class_addMethod([<span class="keyword">self</span> <span class="keyword">class</span>], sel, imp_implementationWithBlock(^&#123;</span><br><span class="line">        HoloLog(<span class="string">@"[HoloTarget] -[%@ %@]: unrecognized selector."</span>, <span class="keyword">self</span>.class, <span class="built_in">NSStringFromSelector</span>(sel));        </span><br><span class="line">    &#125;), <span class="string">"v@:"</span>);</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">YES</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>让将要注册的 target 继承这两个类就可以了。</p><p>以上，就是 HoloTarget 的所有内容了</p><p>项目地址：<a href="https://github.com/HoloFoundation/HoloTarget" target="_blank" rel="noopener">https://github.com/HoloFoundation/HoloTarget</a></p><h1 id="isa-swizzling-（更新于-2020-7-18）"><a href="#isa-swizzling-（更新于-2020-7-18）" class="headerlink" title="isa-swizzling （更新于 2020.7.18）"></a>isa-swizzling （更新于 2020.7.18）</h1><p>之前也考虑过这种方案，参考 <code>KVO</code> 的实现原理，通过 <code>isa-swizzling</code> 技术，动态创建一个新的类，将目标对象的 <code>isa</code> 指针指向这个新的类，对新的类做安全保护。但因为之前的思路一直局限于通过 <code>isa-swizzling</code> 保护对象，因为 <code>HoloTarget</code> 还提供了获取 <code>Class</code> 接口，所以一开始并没有选用这种方案。（<code>HoloTarget</code> 提供了 <code>match Target Class</code> 的接口，通过获取 <code>Class</code> 可以调用 <code>init</code> 的方法）</p><p>现在看来的确是之前的思路狭隘了，包括前文提到的：判断 <code>resolveInstanceMethod:</code> 方法、<code>forwardInvocation:</code> 方法等是否被处理过，其实并不需要，我们只需要关心 <code>forwardInvocation:</code> 方法即可，因为如果消息转发的前几步就被特殊处理过了，就不会走到 <code>forwardInvocation:</code> 方法了，而被特殊处理过的对象及类，<code>HoloTarget</code> 就不再关心了。</p><p>所以，最终的解决方案是：</p><p>1、保护对象：</p><p>1.1、根据目标对象的类动态创建一个新的子类<br>1.2、将目标对象的 <code>isa</code> 指针指向这个新的类<br>1.3、交换这个新的类的 <code>forwardInvocation:</code> 方法为自己的 <code>holo_forwardInvocation</code> 方法，只要这个自己的方法不再继续向下走消息转发流程，就不会崩溃了。<br>（注意要保证方法只被交互一次）</p><p>2、保护类：</p><p>将类的 <code>forwardInvocation:</code> 方法交换为自己的 <code>holo_forwardInvocation</code> 方法<br>（注意要保证方法只被交互一次）</p><p>方便的是，以上步骤就是 Aspects 里的一部分逻辑，直接将这部分逻辑单独拷贝出来应用即可：</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br><span class="line">90</span><br><span class="line">91</span><br><span class="line">92</span><br><span class="line">93</span><br><span class="line">94</span><br><span class="line">95</span><br><span class="line">96</span><br><span class="line">97</span><br><span class="line">98</span><br><span class="line">99</span><br><span class="line">100</span><br><span class="line">101</span><br><span class="line">102</span><br><span class="line">103</span><br><span class="line">104</span><br><span class="line">105</span><br><span class="line">106</span><br><span class="line">107</span><br><span class="line">108</span><br><span class="line">109</span><br><span class="line">110</span><br><span class="line">111</span><br><span class="line">112</span><br><span class="line">113</span><br><span class="line">114</span><br><span class="line">115</span><br><span class="line">116</span><br><span class="line">117</span><br><span class="line">118</span><br><span class="line">119</span><br><span class="line">120</span><br><span class="line">121</span><br><span class="line">122</span><br><span class="line">123</span><br><span class="line">124</span><br><span class="line">125</span><br><span class="line">126</span><br><span class="line">127</span><br><span class="line">128</span><br><span class="line">129</span><br><span class="line">130</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">static</span> <span class="built_in">NSString</span> *<span class="keyword">const</span> HoloTargetSubclassSuffix = <span class="string">@"_holoTarget_"</span>;</span><br><span class="line"><span class="keyword">static</span> <span class="built_in">NSString</span> *<span class="keyword">const</span> HoloTargetForwardInvocationSelectorName = <span class="string">@"__holoTarget_forwardInvocation:"</span>;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">@implementation</span> <span class="title">NSObject</span> (<span class="title">HoloTargetUnrecognizedSelector</span>)</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 判断 cls 是否重写了 sel 方法, 递归调用判断父类但不包括 NSObject</span></span><br><span class="line"><span class="keyword">static</span> <span class="built_in">BOOL</span> holo_methodHasOverwrited(Class cls, SEL sel) &#123;</span><br><span class="line">    <span class="keyword">unsigned</span> <span class="keyword">int</span> methodCount = <span class="number">0</span>;</span><br><span class="line">    Method *methods = class_copyMethodList(cls, &amp;methodCount);</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; methodCount; i++) &#123;</span><br><span class="line">        Method method = methods[i];</span><br><span class="line">        <span class="keyword">if</span> (method_getName(method) == sel) &#123;</span><br><span class="line">            free(methods);</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">YES</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    free(methods);</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">if</span> ([cls superclass] != [<span class="built_in">NSObject</span> <span class="keyword">class</span>]) &#123;</span><br><span class="line">        <span class="keyword">return</span> holo_methodHasOverwrited([cls superclass], sel);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">NO</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">static</span> Class holoTarget_hookClass(<span class="built_in">NSObject</span> *<span class="keyword">self</span>) &#123;</span><br><span class="line">    <span class="built_in">NSCParameterAssert</span>(<span class="keyword">self</span>);</span><br><span class="line">    Class statedClass = <span class="keyword">self</span>.class;</span><br><span class="line">    Class baseClass = object_getClass(<span class="keyword">self</span>);</span><br><span class="line">    <span class="built_in">NSString</span> *className = <span class="built_in">NSStringFromClass</span>(baseClass);</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// Already subclassed</span></span><br><span class="line">    <span class="keyword">if</span> ([className hasSuffix:HoloTargetSubclassSuffix]) &#123;</span><br><span class="line">        <span class="keyword">return</span> baseClass;</span><br><span class="line">        </span><br><span class="line">        <span class="comment">// We swizzle a class object, not a single object.</span></span><br><span class="line">    &#125; <span class="keyword">else</span> <span class="keyword">if</span> (class_isMetaClass(baseClass)) &#123;</span><br><span class="line">        <span class="keyword">return</span> holoTarget_swizzleClassInPlace((Class)<span class="keyword">self</span>);</span><br><span class="line">        <span class="comment">// Probably a KVO'ed class. Swizzle in place. Also swizzle meta classes in place.</span></span><br><span class="line">    &#125; <span class="keyword">else</span> <span class="keyword">if</span> (statedClass != baseClass) &#123;</span><br><span class="line">        <span class="keyword">return</span> holoTarget_swizzleClassInPlace(baseClass);</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// Default case. Create dynamic subclass.</span></span><br><span class="line">    <span class="keyword">const</span> <span class="keyword">char</span> *subclassName = [className stringByAppendingString:HoloTargetSubclassSuffix].UTF8String;</span><br><span class="line">    Class subclass = objc_getClass(subclassName);</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">if</span> (subclass == <span class="literal">nil</span>) &#123;</span><br><span class="line">        subclass = objc_allocateClassPair(baseClass, subclassName, <span class="number">0</span>);</span><br><span class="line">        <span class="keyword">if</span> (subclass == <span class="literal">nil</span>) &#123;</span><br><span class="line">            HoloLog(<span class="string">@"[HoloTarget] objc_allocateClassPair failed to allocate class %s."</span>, subclassName);</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">nil</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        </span><br><span class="line">        holoTarget_swizzleForwardInvocation(subclass);</span><br><span class="line">        holoTarget_hookedGetClass(subclass, statedClass);</span><br><span class="line">        holoTarget_hookedGetClass(object_getClass(subclass), statedClass);</span><br><span class="line">        objc_registerClassPair(subclass);</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    object_setClass(<span class="keyword">self</span>, subclass);</span><br><span class="line">    <span class="keyword">return</span> subclass;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">static</span> Class holoTarget_swizzleClassInPlace(Class klass) &#123;</span><br><span class="line">    <span class="built_in">NSCParameterAssert</span>(klass);</span><br><span class="line">    <span class="built_in">NSString</span> *className = <span class="built_in">NSStringFromClass</span>(klass);</span><br><span class="line">    </span><br><span class="line">    holoTarget_modifySwizzledClasses(^(<span class="built_in">NSMutableSet</span> *swizzledClasses) &#123;</span><br><span class="line">        <span class="keyword">if</span> (![swizzledClasses containsObject:className]) &#123;</span><br><span class="line">            holoTarget_swizzleForwardInvocation(klass);</span><br><span class="line">            [swizzledClasses addObject:className];</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;);</span><br><span class="line">    <span class="keyword">return</span> klass;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">void</span> holoTarget_modifySwizzledClasses(<span class="keyword">void</span> (^block)(<span class="built_in">NSMutableSet</span> *swizzledClasses)) &#123;</span><br><span class="line">    <span class="keyword">static</span> <span class="built_in">NSMutableSet</span> *swizzledClasses;</span><br><span class="line">    <span class="keyword">static</span> <span class="built_in">dispatch_once_t</span> pred;</span><br><span class="line">    <span class="built_in">dispatch_once</span>(&amp;pred, ^&#123;</span><br><span class="line">        swizzledClasses = [<span class="built_in">NSMutableSet</span> new];</span><br><span class="line">    &#125;);</span><br><span class="line">    <span class="keyword">@synchronized</span>(swizzledClasses) &#123;</span><br><span class="line">        block(swizzledClasses);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">void</span> holoTarget_swizzleForwardInvocation(Class klass) &#123;</span><br><span class="line">    <span class="built_in">NSCParameterAssert</span>(klass);</span><br><span class="line">    <span class="comment">// If there is no method, replace will act like class_addMethod.</span></span><br><span class="line">    IMP originalImplementation = class_replaceMethod(klass, <span class="keyword">@selector</span>(forwardInvocation:), (IMP)__holoTarget_forwardInvocation__, <span class="string">"v@:@"</span>);</span><br><span class="line">    <span class="keyword">if</span> (originalImplementation) &#123;</span><br><span class="line">        class_addMethod(klass, <span class="built_in">NSSelectorFromString</span>(HoloTargetForwardInvocationSelectorName), originalImplementation, <span class="string">"v@:@"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">void</span> holoTarget_hookedGetClass(Class <span class="keyword">class</span>, Class statedClass) &#123;</span><br><span class="line">    <span class="built_in">NSCParameterAssert</span>(<span class="keyword">class</span>);</span><br><span class="line">    <span class="built_in">NSCParameterAssert</span>(statedClass);</span><br><span class="line">    Method method = class_getInstanceMethod(<span class="keyword">class</span>, <span class="keyword">@selector</span>(<span class="keyword">class</span>));</span><br><span class="line">    IMP newIMP = imp_implementationWithBlock(^(<span class="keyword">id</span> <span class="keyword">self</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> statedClass;</span><br><span class="line">    &#125;);</span><br><span class="line">    class_replaceMethod(<span class="keyword">class</span>, <span class="keyword">@selector</span>(<span class="keyword">class</span>), newIMP, method_getTypeEncoding(method));</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">void</span> __holoTarget_forwardInvocation__(__<span class="keyword">unsafe_unretained</span> <span class="built_in">NSObject</span> *<span class="keyword">self</span>, SEL selector, <span class="built_in">NSInvocation</span> *invocation) &#123;</span><br><span class="line">    HoloLog(<span class="string">@"[HoloTarget] *** Caught exception 'NSInvalidArgumentException', reason: '-[%@ %@]: unrecognized selector sent to instance %p'"</span>, [<span class="keyword">self</span> <span class="keyword">class</span>], <span class="built_in">NSStringFromSelector</span>(selector), <span class="keyword">self</span>);</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">if</span> ([HoloTarget sharedInstance].delegate &amp;&amp;</span><br><span class="line">        [[HoloTarget sharedInstance].delegate respondsToSelector:<span class="keyword">@selector</span>(holo_unrecognizedSelectorSentToTarget:selector:)]) &#123;</span><br><span class="line">        [[HoloTarget sharedInstance].delegate holo_unrecognizedSelectorSentToTarget:<span class="keyword">self</span> selector:selector];</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">+ (<span class="keyword">void</span>)holo_protectUnrecognizedSelector &#123;</span><br><span class="line">    <span class="keyword">if</span> (holo_methodHasOverwrited(<span class="keyword">self</span>, <span class="keyword">@selector</span>(forwardInvocation:))) &#123;</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    holoTarget_hookClass((<span class="keyword">id</span>)<span class="keyword">self</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">- (<span class="keyword">void</span>)holo_protectUnrecognizedSelector &#123;</span><br><span class="line">    <span class="keyword">if</span> (holo_methodHasOverwrited(<span class="keyword">self</span>.class, <span class="keyword">@selector</span>(forwardInvocation:))) &#123;</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    holoTarget_hookClass(<span class="keyword">self</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">@end</span></span><br></pre></td></tr></table></figure><p>其实 <code>HoloTarget</code> 里只保护了类，因为 <code>matchTargetInstance</code> 方法也是直接拿到的类执行了 <code>new</code> 方法。<br>因为加了 <code>holo_methodHasOverwrited</code> 方法判断，类的 <code>forwardInvocation:</code> 方法是否被交换过也提前判断了。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h1 id=&quot;前言&quot;&gt;&lt;a href=&quot;#前言&quot; class=&quot;headerlink&quot; title=&quot;前言&quot;&gt;&lt;/a&gt;前言&lt;/h1&gt;&lt;p&gt;组件化页面路由算是个老生常谈的话题了，从蘑菇街的注册 url 到 Casa 的 target-action 方案，市面上也有了很多的优化方案
      
    
    </summary>
    
      <category term="iOS" scheme="http://gonghonglou.com/categories/iOS/"/>
    
    
  </entry>
  
  <entry>
    <title>组件化资源文件管理方案</title>
    <link href="http://gonghonglou.com/2020/05/14/pod-resource/"/>
    <id>http://gonghonglou.com/2020/05/14/pod-resource/</id>
    <published>2020-05-14T13:25:52.000Z</published>
    <updated>2020-06-14T07:25:09.250Z</updated>
    
    <content type="html"><![CDATA[<h1 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h1><p>最初，所有代码都在主工程的时候，资源文件也在主工程，尤其是大量的图片资源，不可避免的就是命名冲突问题。一般而言，我们需要一份严谨的命名规范，比如根据业务模块或者文件夹划分添加前缀，来避免命名冲入导致的翻车事故。</p><p>进行组件化之后，应用 CocoaPods，终于对命名冲突问题有了解决方案。在我们将主工程拆空，代码分拆至相应 Pod 之后，资源文件也应当拆分至对应 Pod，交由组件自己管理。这样将资源文件从主工程拆分到各个 Pod 不仅令职责划分更合理，额外获得的收益还有：<br>1、拆空主工程：方便版本号等设置项工程化管理；避免不同分支进行 Merge 操作覆盖更改<br>2、当前组件自己管理资源文件：减少外部依赖，方便独立运行</p><p>但因为 CocoaPods 提供的 <code>use_frameworks!</code>、<code>static_framework</code>、<code>resource</code>、<code>resource_bundles</code> 这些功能的选择，以及将其中一些功能组合使用之后资源文件的最终存储路径的不同，导致使用者对图片文件、字体文件、本地化文件、其他各种文件的种种处理方式不明所以。本文旨在讲清楚这些，并期望总结出对资源文件管理的最佳实践方案。</p><p>其实所谓的资源文件处理以及前文提到的 CocoaPods 提供的各种字段对资源文件的影响主要就是路径的差别，我们只需要搞清 将这些字段组合使用后的 资源文件的存储路径差别，处理起来就很简单了。</p><h1 id="概念篇"><a href="#概念篇" class="headerlink" title="概念篇"></a>概念篇</h1><h2 id="use-frameworks"><a href="#use-frameworks" class="headerlink" title="use_frameworks!"></a>use_frameworks!</h2><blockquote><p>use_frameworks tells CocoaPods that you want to use Frameworks instead of Static Libraries. Since Swift does not support Static Libraries you have to use frameworks.</p></blockquote><p>来自 stackoverflow 的问答： <a href="https://stackoverflow.com/questions/41210249/why-do-we-use-use-frameworks-in-cocoapods" target="_blank" rel="noopener">Why do we use use_frameworks in CocoaPods?</a></p><p>该字段在 Podfile 文件中使用。主要是早期 Swift 不支持静态库，所以大家在 OC 工程里引入 Swift 的时候都需要引入该字段。</p><h2 id="static-framework"><a href="#static-framework" class="headerlink" title="static_framework"></a>static_framework</h2><blockquote><p>Up until Xcode 9, support for building Swift into static libraries was non-existent and use of dynamic frameworks was required. This was a deal-breaker for some developers, particularly those worried about the launch performance implications of linked many dynamic binaries.<br>With CocoaPods 1.5.0, developers are no longer restricted into specifying use_frameworks! in their Podfile in order to install pods that use Swift. Interop with Objective-C should just work. However, if your Swift pod depends on an Objective-C, pod you will need to enable “modular headers” (see below) for that Objective-C pod.</p></blockquote><p>来自 CocoaPods 的官网说明：<a href="http://blog.cocoapods.org/CocoaPods-1.5.0/" target="_blank" rel="noopener">CocoaPods 1.5.0 — Swift Static Libraries</a></p><p>该字段在 podspec 中声明。Xcode 9 开始，Swift 支持静态库了，CocoaPods 也添加了支持。</p><h2 id="resource-amp-resource-bundles"><a href="#resource-amp-resource-bundles" class="headerlink" title="resource &amp; resource_bundles"></a>resource &amp; resource_bundles</h2><p>resource 字段在 podspec 中使用</p><blockquote><p>For building the Pod as a static library, we strongly <strong>recommend</strong> library developers to adopt <a href="https://guides.cocoapods.org/syntax/podspec.html#resource_bundles" target="_blank" rel="noopener">resource bundles</a> as there can be name collisions using the resources attribute. Moreover, resources specified with this attribute are copied directly to the client target and therefore they are not optimised by Xcode.</p></blockquote><p>来自 CocoaPods 的官网说明：<a href="https://guides.cocoapods.org/syntax/podspec.html#resources" target="_blank" rel="noopener">resources</a></p><p>resource_bundles 字段在 podspec 中使用</p><blockquote><p>For building the Pod as a static library, we strongly <strong>recommend</strong> library developers to adopt resource bundles as there can be name collisions using the resources attribute.</p></blockquote><p>来自 CocoaPods 的官网说明：<a href="https://guides.cocoapods.org/syntax/podspec.html#resource_bundles" target="_blank" rel="noopener">resource_bundles</a></p><p>这俩字段最主要的区别就在于 <code>resource_bundles</code> 会创建独立的 bundle，能够有效解决资源文件重名问题，也是 CocoaPods 强烈建议使用的字段。 <code>resource</code> 字段则不会创建属于 Pod 自己的 bundle。资源文件很可能会存储到 main bundle 里。对资源文件存储路径的影响在下一章节会有详细的总结。</p><h1 id="组合场景下的资源文件存储路径"><a href="#组合场景下的资源文件存储路径" class="headerlink" title="组合场景下的资源文件存储路径"></a>组合场景下的资源文件存储路径</h1><p><code>use_frameworks!</code>、<code>static_framework</code>、<code>resource_bundles</code>、<code>resources</code> 的不同组合使用，会导致资源文件的最终存放路径不同，以下是各种场景下的路径总结：<br><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="string">use_frameworks!</span></span><br><span class="line"><span class="string">static_framework</span> <span class="string">=</span> <span class="literal">true</span><span class="string">：</span></span><br><span class="line"><span class="attr">resource_bundles:</span> <span class="string">.app/Frameworks/pod_name.framework/pod_name.bundle/.</span></span><br><span class="line"><span class="attr">resources:</span> <span class="string">.app/Frameworks/pod_name.framework/.</span></span><br><span class="line"><span class="string">static_framework</span> <span class="string">=</span> <span class="literal">false</span><span class="string">：</span></span><br><span class="line"><span class="attr">resource_bundles:</span> <span class="string">.app/pod_name.bundle/.</span></span><br><span class="line"><span class="attr">resources:</span> <span class="string">.app/.</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># use_frameworks!</span></span><br><span class="line"><span class="string">static_framework</span> <span class="string">=</span> <span class="literal">true</span><span class="string">：</span></span><br><span class="line"><span class="attr">resource_bundles:</span> <span class="string">.app/pod_name.bundle/.</span></span><br><span class="line"><span class="attr">resources:</span> <span class="string">.app/.</span></span><br><span class="line"><span class="string">static_framework</span> <span class="string">=</span> <span class="literal">false</span><span class="string">：</span></span><br><span class="line"><span class="attr">resource_bundles:</span> <span class="string">.app/pod_name.bundle/.</span></span><br><span class="line"><span class="attr">resources:</span> <span class="string">.app/.</span></span><br></pre></td></tr></table></figure></p><p>（.app/. 代表的是 main bundle）</p><p>不同路径下的图片取值方式及差别可以参考这篇文章：<a href="https://dreampiggy.com/2018/11/26/CocoaPods%E7%9A%84%E8%B5%84%E6%BA%90%E7%AE%A1%E7%90%86%E5%92%8CAsset%20Catalog%E4%BC%98%E5%8C%96/" target="_blank" rel="noopener">CocoaPods的资源管理和Asset Catalog优化</a></p><p>需要注意的是，当 Pod 中的 <code>.xcassets</code> 类型资源文件最终存储在 main bundle 的话会和主工程的 <code>.xcassets</code> 文件冲突，编译会报错：</p><blockquote><p>error: Multiple commands produce ‘/Users/gonghonglou/Library/Developer/Xcode/DerivedData/HoloResource-giqfnwiluvssbzbyvuhpkrjoewvd/Build/Products/Debug-iphonesimulator/HoloResource_Example.app/Assets.car’:<br>1) Target ‘HoloResource_Example’ (project ‘HoloResource’) has compile command with input ‘/Users/gonghonglou/holo/HoloResource/Example/HoloResource/Images.xcassets’<br>2) That command depends on command in Target ‘HoloResource_Example’ (project ‘HoloResource’): script phase “[CP] Copy Pods Resources”</p></blockquote><p>解决方法，在 Podfile 里添加：<br><figure class="highlight ruby"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">install! <span class="string">'cocoapods'</span>, <span class="symbol">:disable_input_output_paths</span> =&gt; <span class="literal">true</span></span><br></pre></td></tr></table></figure></p><p>题外话：Xcode 10 之后引入了新的编译系统，导致 Pod 里有代码改动的话无法及时编译进工程，解决方案也是上述方法。</p><p>参考：<a href="https://developer.apple.com/documentation/xcode_release_notes/xcode_10_release_notes/build_system_release_notes_for_xcode_10?language=objc" target="_blank" rel="noopener">Build System Release Notes for Xcode 10</a></p><h1 id="各类资源文件的处理：预编译宏"><a href="#各类资源文件的处理：预编译宏" class="headerlink" title="各类资源文件的处理：预编译宏"></a>各类资源文件的处理：预编译宏</h1><p>将资源文件放进各个组件里，最大的麻烦就是访问方式的更改，尤其是图片资源，如果组件内的资源文件不在 main bundle 路径下的话，原本使用 <code>[UIImage imageNamed:@&quot;&quot;];</code> 获取图片的方式就需要更改了。比如：</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">NSString</span> *bundlePath = [<span class="built_in">NSBundle</span> bundleForClass:HoloResourceDemoA.class].resourcePath;</span><br><span class="line">bundlePath = [bundlePath stringByAppendingPathComponent:<span class="string">@"HoloResourceDemoA.bundle"</span>];</span><br><span class="line"><span class="built_in">NSBundle</span> *bundle = [<span class="built_in">NSBundle</span> bundleWithPath:bundlePath];</span><br><span class="line">[<span class="built_in">UIImage</span> imageNamed:<span class="string">@""</span> inBundle:bundle compatibleWithTraitCollection:<span class="literal">nil</span>];</span><br></pre></td></tr></table></figure><p>而且需要根据不同的组件名称获取当前组件的 bundle，这就导致不同组件内需要提供不同的取值方法。</p><p>最简单的解决方案是将这些取值方法统一成单一方法，封装进一个基础类，每个组件调用时传入当前组件名称。根据上一节可以发现，最终不同的路径也就只有 4 种而已，如果我们能获取到当前组件的名称，也就可以分别到这 4 个路径下查找文件了。</p><p>但是每个组件调用这个基础类方法时仍然需要传入自己的组件名称，而且是字符串形式，还是会很麻烦。如果在代码执行当前行能够获取到当前 Pod 名称就好，那么我们就可以把这个 “获取代码执行当前行所在 Pod 名称” 的逻辑封装进统一方法里，少传一个参数。</p><p>好在 CocoaPods 给我们提供了给各个组件配置预编译宏的能力。虽然不是提供方法，但是可以提供 宏，在代码执行当前行拿到当前 Pod 名称。</p><h2 id="方案一"><a href="#方案一" class="headerlink" title="方案一"></a>方案一</h2><p>在 podspec 里加上：</p><figure class="highlight ruby"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 配置 Xcode Build Setting</span></span><br><span class="line">s.xcconfig = &#123;</span><br><span class="line"><span class="comment">#'GCC_PREPROCESSOR_DEFINITIONS' =&gt; 'HOLO_POD_NAME=@(\"HoloResource\")'</span></span><br><span class="line">  <span class="string">'GCC_PREPROCESSOR_DEFINITIONS'</span> =&gt; <span class="string">"'HOLO_POD_NAME=@(\"<span class="subst">#&#123;s.name&#125;</span>\")'"</span> <span class="comment"># 配置预编译宏</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>即可在 HoloResource.debug.xcconfig / HoloResource.release.xcconfig 文件里的 GCC预编译头参数所在行：</p><figure class="highlight makefile"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">GCC_PREPROCESSOR_DEFINITIONS = <span class="variable">$(inherited)</span> COCOAPODS=1</span><br></pre></td></tr></table></figure><p>添加预编译宏为：</p><figure class="highlight makefile"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">GCC_PREPROCESSOR_DEFINITIONS = <span class="variable">$(inherited)</span> COCOAPODS=1 'HOLO_POD_NAME=@(<span class="string">"HoloResource"</span>)'</span><br></pre></td></tr></table></figure><p>相应的，在各 Pod 的 TARGET 里能看到的 Preprocessor Macros 为：</p><p><img src="http://image.gonghonglou.com/pod-resource/preprocessor_macros_podspec.png" alt="Preprocessor Macros from pospec"></p><p>这样一来，在组件内使用 <code>HOLO_POD_NAME</code> 这个宏，就能得到 <code>@&quot;HoloResource&quot;</code> 这个字符串了。那我们就可以用这个宏来拼接不同的路径以加载资源文件。比如：</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">[<span class="built_in">UIImage</span> holo_imageNamed:name inBundle:HOLO_POD_NAME];</span><br></pre></td></tr></table></figure><p>每个组件统一调用以上方法就可以访问自己的图片资源了，再把这个方法调用定义一个 宏，就满足我们的需求了，后续章节会给出具体实现。</p><h2 id="方案二"><a href="#方案二" class="headerlink" title="方案二"></a>方案二</h2><p>但是这种在 podspec 里配置预编译宏的方式也会配置在主工程的 Pods-HoloResource_Example.debug.xcconfig / Pods-HoloResource_Example.release.xcconfig 文件里，这就导致了，如果多个组件配置了同名的预编译宏会有多个同名的配置出现在主工程，编译也会报警告。</p><p>替代这种方案的另一种实现是，在 Podfile 里 hook install，在 install 之后给每个工程配置一份预编译宏，通过 <code>post_install</code> 的能力：</p><figure class="highlight ruby"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">post_install <span class="keyword">do</span> <span class="params">|installer|</span></span><br><span class="line">  installer.pods_project.targets.each <span class="keyword">do</span> <span class="params">|target|</span></span><br><span class="line">    target.build_configurations.each <span class="keyword">do</span> <span class="params">|config|</span></span><br><span class="line">      config.build_settings[<span class="string">'GCC_PREPROCESSOR_DEFINITIONS'</span>] = <span class="string">"'HOLO_POD_NAME=@(\"<span class="subst">#&#123;target&#125;</span>\")'"</span></span><br><span class="line">    <span class="keyword">end</span></span><br><span class="line">  <span class="keyword">end</span></span><br><span class="line"><span class="keyword">end</span></span><br></pre></td></tr></table></figure><p>相应的，在各 Pod 的 TARGET 里能看到的 Preprocessor Macros 为：</p><p><img src="http://image.gonghonglou.com/pod-resource/preprocessor_macros_podfile.png" alt="Preprocessor Macros from pospec"></p><h2 id="方案三（推荐）"><a href="#方案三（推荐）" class="headerlink" title="方案三（推荐）"></a>方案三（推荐）</h2><p>虽然主工程 config 文件不会出现 HOLO_POD_NAME，也就不会报警告了。但是从以上图片可以看出，各个 Pod 工程的 Preprocessor Macros 直接被替换了。当然可以在 post_install 的 hook 里把缺失的哪些配置加上，先获取原来有哪些预编译宏，再加上 HOLO_POD_NAME。</p><p>或者可以通过脚本，在 install 之后把各个组件对应的 在 <code>.debug.xcconfig / .release.xcconfig</code> 文件里 GCC_PREPROCESSOR_DEFINITIONS 所在行拼上<code>HOLO_POD_NAME=@(\&quot;HoloResource\&quot;)</code>即可。而且组件化之后大家应该都有封装的自己的 CocoaPods 命令工具，hook pod install 做些额外的工作，那只要再把这个配置预编译宏的逻辑加上就好了。</p><p>参考脚本 <code>holo_pod_name_config.sh</code>：<br><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#!/bin/sh</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># perform after install</span></span><br><span class="line">support_path=<span class="string">'Pods/Target Support Files'</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="title">config_with_name_file</span></span>() &#123;</span><br><span class="line">    pod_name=<span class="variable">$1</span></span><br><span class="line">    xcconfig=<span class="variable">$2</span></span><br><span class="line">    <span class="keyword">if</span> [ -f <span class="string">"<span class="variable">$xcconfig</span>"</span> ]; <span class="keyword">then</span></span><br><span class="line">        target_line=$(sed -n <span class="string">'/GCC_PREPROCESSOR_DEFINITIONS/p'</span> <span class="string">"<span class="variable">$xcconfig</span>"</span>)</span><br><span class="line">        <span class="keyword">if</span> [[ <span class="variable">$target_line</span> != *<span class="variable">$pod_name</span>* ]]; <span class="keyword">then</span></span><br><span class="line">            holo_pod_name=<span class="string">" HOLO_POD_NAME=@(\\\\\"<span class="variable">$pod_name</span>\\\\\")"</span></span><br><span class="line">            sed -i <span class="string">""</span> <span class="string">"s/<span class="variable">$target_line</span>/<span class="variable">$target_line</span><span class="variable">$holo_pod_name</span>/"</span> <span class="string">"<span class="variable">$xcconfig</span>"</span></span><br><span class="line">        <span class="keyword">fi</span></span><br><span class="line">    <span class="keyword">fi</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="title">config_with_name</span></span>() &#123;</span><br><span class="line">    pod_name=<span class="variable">$1</span></span><br><span class="line">    debug_xcconfig=<span class="string">"<span class="variable">$support_path</span>/<span class="variable">$pod_name</span>/<span class="variable">$pod_name</span>.debug.xcconfig"</span></span><br><span class="line">    release_xcconfig=<span class="string">"<span class="variable">$support_path</span>/<span class="variable">$pod_name</span>/<span class="variable">$pod_name</span>.release.xcconfig"</span></span><br><span class="line">    config_with_name_file <span class="variable">$pod_name</span> <span class="string">"<span class="variable">$debug_xcconfig</span>"</span></span><br><span class="line">    config_with_name_file <span class="variable">$pod_name</span> <span class="string">"<span class="variable">$release_xcconfig</span>"</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> [ <span class="variable">$#</span> -ge 1 ];<span class="keyword">then</span></span><br><span class="line">    <span class="keyword">for</span> arg <span class="keyword">in</span> $*; <span class="keyword">do</span></span><br><span class="line">        pod_name=<span class="variable">$arg</span></span><br><span class="line">       config_with_name <span class="variable">$pod_name</span></span><br><span class="line">    <span class="keyword">done</span></span><br><span class="line">    config_with_name HoloResource</span><br><span class="line"><span class="keyword">else</span></span><br><span class="line">    xcconfigs=$(<span class="built_in">cd</span> <span class="string">"<span class="variable">$support_path</span>"</span>; ls */*.*.xcconfig)</span><br><span class="line">    <span class="keyword">for</span> file <span class="keyword">in</span> <span class="variable">$xcconfigs</span>; <span class="keyword">do</span></span><br><span class="line">        <span class="keyword">if</span> [[ <span class="variable">$file</span> != Pods-* ]]; <span class="keyword">then</span></span><br><span class="line">            pod_name=<span class="variable">$&#123;file%/*&#125;</span></span><br><span class="line">            config_with_name <span class="variable">$pod_name</span></span><br><span class="line"><span class="comment">#            xcconfig=$support_path/$file</span></span><br><span class="line"><span class="comment">#            config_with_name_file $pod_name "$xcconfig"</span></span><br><span class="line">        <span class="keyword">fi</span></span><br><span class="line">    <span class="keyword">done</span></span><br><span class="line"><span class="keyword">fi</span></span><br></pre></td></tr></table></figure></p><p>执行以上脚本（<code>holo_pod_name_config.sh</code>）默认给所有 Pod 添加配置，如果想指定某几个 Pod 添加配置的话给脚本传入参数，例如：<br><figure class="highlight mipsasm"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">sh </span>holo_pod_name_config.<span class="keyword">sh </span>PodA PodB PodC</span><br></pre></td></tr></table></figure></p><h2 id="方案四（更新于-2020-6-14，推荐plus）"><a href="#方案四（更新于-2020-6-14，推荐plus）" class="headerlink" title="方案四（更新于 2020.6.14，推荐plus）"></a>方案四（更新于 2020.6.14，推荐plus）</h2><p>为了更方便的给组件配置预编译宏，我写了一个 CocoaPods 插件：<a href="https://github.com/HoloFoundation/cocoapods-resource" target="_blank" rel="noopener">cocoapods-resource</a>，支持给任意 Pod 添加任意 预编译 宏。</p><p>用法如下：<br><figure class="highlight routeros"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 安装</span></span><br><span class="line">sudo gem install cocoapods-resource</span><br><span class="line"></span><br><span class="line"><span class="comment"># 调用</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 1、默认给所有 Pod 添加预编译宏：HOLO_POD_NAME</span></span><br><span class="line">pod<span class="built_in"> resource </span>HOLO_POD_NAME</span><br><span class="line"></span><br><span class="line"><span class="comment"># 2、给指定的某些 Pod 添加预编译宏：HOLO_POD_NAME</span></span><br><span class="line">pod<span class="built_in"> resource </span>HOLO_POD_NAME <span class="attribute">--pods</span>=<span class="string">'PodA, PodB, PodC'</span></span><br></pre></td></tr></table></figure></p><h1 id="各类资源处理实践"><a href="#各类资源处理实践" class="headerlink" title="各类资源处理实践"></a>各类资源处理实践</h1><h2 id="图片-UIImage"><a href="#图片-UIImage" class="headerlink" title="图片 UIImage"></a>图片 UIImage</h2><p>有了上一节的准备，剩下的工作就简单了许多，封装通用的图片获取方法，供所有组件使用：</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#if defined(HOLO_POD_NAME)</span></span><br><span class="line"><span class="meta">#define HoloImageNamed(name) [UIImage holo_imageNamed:name inBundle:HOLO_POD_NAME]</span></span><br><span class="line"><span class="meta">#endif</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">@interface</span> <span class="title">UIImage</span> (<span class="title">HoloResource</span>)</span></span><br><span class="line"></span><br><span class="line">+ (<span class="built_in">UIImage</span> *)holo_imageNamed:(<span class="built_in">NSString</span> *)name inBundle:(<span class="built_in">NSString</span> *)bundleName;</span><br><span class="line"></span><br><span class="line"><span class="keyword">@end</span></span><br></pre></td></tr></table></figure><p>实现为：<br><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">@implementation</span> <span class="title">UIImage</span> (<span class="title">HoloResource</span>)</span></span><br><span class="line"></span><br><span class="line">+ (<span class="built_in">UIImage</span> *)holo_imageNamed:(<span class="built_in">NSString</span> *)name inBundle:(<span class="built_in">NSString</span> *)bundleName &#123;</span><br><span class="line">    <span class="built_in">UIImage</span> *image = [<span class="keyword">self</span> _holo_imageNamed1:name inBundle:bundleName];</span><br><span class="line">    <span class="keyword">if</span> (!image) image = [<span class="keyword">self</span> _holo_imageNamed2:name inBundle:bundleName];</span><br><span class="line">    <span class="keyword">if</span> (!image) image = [<span class="keyword">self</span> _holo_imageNamed3:name inBundle:bundleName];</span><br><span class="line">    <span class="keyword">if</span> (!image) image = [<span class="keyword">self</span> _holo_imageNamed4:name inBundle:bundleName];</span><br><span class="line">    <span class="keyword">return</span> image;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">+ (<span class="built_in">UIImage</span> *)_holo_imageNamed1:(<span class="built_in">NSString</span> *)name inBundle:(<span class="built_in">NSString</span> *)bundleName &#123;</span><br><span class="line">    <span class="built_in">NSString</span> *pathComponent = [<span class="built_in">NSString</span> stringWithFormat:<span class="string">@"%@.bundle"</span>, bundleName];</span><br><span class="line">    <span class="keyword">return</span> [<span class="keyword">self</span> _holo_imageNamed:name pathComponent:pathComponent];</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">+ (<span class="built_in">UIImage</span> *)_holo_imageNamed2:(<span class="built_in">NSString</span> *)name inBundle:(<span class="built_in">NSString</span> *)bundleName &#123;</span><br><span class="line">    <span class="built_in">NSString</span> *pathComponent = [<span class="built_in">NSString</span> stringWithFormat:<span class="string">@"Frameworks/%@.framework/%@.bundle"</span>, bundleName, bundleName];</span><br><span class="line">    <span class="keyword">return</span> [<span class="keyword">self</span> _holo_imageNamed:name pathComponent:pathComponent];</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">+ (<span class="built_in">UIImage</span> *)_holo_imageNamed3:(<span class="built_in">NSString</span> *)name inBundle:(<span class="built_in">NSString</span> *)bundleName &#123;</span><br><span class="line">    <span class="keyword">return</span> [<span class="built_in">UIImage</span> imageNamed:name];</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">+ (<span class="built_in">UIImage</span> *)_holo_imageNamed4:(<span class="built_in">NSString</span> *)name inBundle:(<span class="built_in">NSString</span> *)bundleName &#123;</span><br><span class="line">    <span class="built_in">NSString</span> *pathComponent = [<span class="built_in">NSString</span> stringWithFormat:<span class="string">@"Frameworks/%@.framework"</span>, bundleName];</span><br><span class="line">    <span class="keyword">return</span> [<span class="keyword">self</span> _holo_imageNamed:name pathComponent:pathComponent];</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">+ (<span class="built_in">UIImage</span> *)_holo_imageNamed:(<span class="built_in">NSString</span> *)name pathComponent:(<span class="built_in">NSString</span> *)pathComponent &#123;</span><br><span class="line">    <span class="built_in">NSString</span> *bundlePath = [<span class="built_in">NSBundle</span>.mainBundle.resourcePath stringByAppendingPathComponent:pathComponent];</span><br><span class="line">    <span class="built_in">NSBundle</span> *bundle = [<span class="built_in">NSBundle</span> bundleWithPath:bundlePath];</span><br><span class="line">    <span class="keyword">return</span> [<span class="built_in">UIImage</span> imageNamed:name inBundle:bundle compatibleWithTraitCollection:<span class="literal">nil</span>];</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">@end</span></span><br></pre></td></tr></table></figure></p><p>为了简化调用，提供了宏定义：每个组件调用 <code>HoloImageNamed(@&quot;&quot;)</code> 就可以访问自己 bundle 里的图片了。</p><p>这里将前文提到的四个路径做了优先级处理：</p><p>1、首先访问的是静态库 + <code>resource_bundles</code> 的路径<br>2、其次访问的是使用 <code>use_frameworks!</code> + <code>resource_bundles</code> 的动态库路径<br>3、再次访问的是使用 <code>use_frameworks!</code> +  <code>resources</code> 的动态库路径<br>4、最后是 main bundle 路径</p><p><strong>最推荐也是最合理的配置方式当然是第一种，提供静态库，使用 <code>resource_bundles</code> 存储资源文件。</strong></p><h2 id="路径-NSBundle"><a href="#路径-NSBundle" class="headerlink" title="路径 NSBundle"></a>路径 NSBundle</h2><p>既然图片可以通过这种方式封装，那么参照这种思路将 bundle 封装一个基础方法，有了统一的 bundle 调用方法，其他资源文件的访问就简单的多了。</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#if defined(HOLO_POD_NAME)</span></span><br><span class="line"><span class="meta">#define HoloCurrentBundle [NSBundle holo_bundleNamed:HOLO_POD_NAME]</span></span><br><span class="line"><span class="meta">#endif</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">@interface</span> <span class="title">NSBundle</span> (<span class="title">HoloResource</span>)</span></span><br><span class="line"></span><br><span class="line">+ (<span class="built_in">NSBundle</span> *)holo_bundleNamed:(<span class="built_in">NSString</span> *)bundleName;</span><br><span class="line"></span><br><span class="line"><span class="keyword">@end</span></span><br></pre></td></tr></table></figure><p>具体实现为：<br><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br></pre></td><td class="code"><pre><span class="line">+ (<span class="built_in">NSBundle</span> *)holo_bundleNamed:(<span class="built_in">NSString</span> *)bundleName &#123;</span><br><span class="line">    <span class="built_in">NSBundle</span> *bundle = [<span class="keyword">self</span> _holo_bundleNamed1:bundleName];</span><br><span class="line">    <span class="keyword">if</span> (!bundle) bundle = [<span class="keyword">self</span> _holo_bundleNamed2:bundleName];</span><br><span class="line">    <span class="keyword">if</span> (!bundle) bundle = [<span class="keyword">self</span> _holo_bundleNamed3:bundleName];</span><br><span class="line">    <span class="keyword">if</span> (!bundle) bundle = [<span class="keyword">self</span> _holo_bundleNamed4:bundleName];</span><br><span class="line">    <span class="keyword">return</span> bundle;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">+ (<span class="built_in">NSBundle</span> *)_holo_bundleNamed1:(<span class="built_in">NSString</span> *)bundleName &#123;</span><br><span class="line">    <span class="built_in">NSString</span> *pathComponent = [<span class="built_in">NSString</span> stringWithFormat:<span class="string">@"%@.bundle"</span>, bundleName];</span><br><span class="line">    <span class="keyword">return</span> [<span class="keyword">self</span> _holo_bundleWithPathComponent:pathComponent];</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">+ (<span class="built_in">NSBundle</span> *)_holo_bundleNamed2:(<span class="built_in">NSString</span> *)bundleName &#123;</span><br><span class="line">    <span class="built_in">NSString</span> *pathComponent = [<span class="built_in">NSString</span> stringWithFormat:<span class="string">@"Frameworks/%@.framework/%@.bundle"</span>, bundleName, bundleName];</span><br><span class="line">    <span class="keyword">return</span> [<span class="keyword">self</span> _holo_bundleWithPathComponent:pathComponent];</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">+ (<span class="built_in">NSBundle</span> *)_holo_bundleNamed3:(<span class="built_in">NSString</span> *)bundleName &#123;</span><br><span class="line">    <span class="built_in">NSString</span> *pathComponent = [<span class="built_in">NSString</span> stringWithFormat:<span class="string">@"Frameworks/%@.framework"</span>, bundleName];</span><br><span class="line">    <span class="keyword">return</span> [<span class="keyword">self</span> _holo_bundleWithPathComponent:pathComponent];</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">+ (<span class="built_in">NSBundle</span> *)_holo_bundleNamed4:(<span class="built_in">NSString</span> *)bundleName &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">NSBundle</span>.mainBundle;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">+ (<span class="built_in">NSBundle</span> *)_holo_bundleWithPathComponent:(<span class="built_in">NSString</span> *)pathComponent &#123;</span><br><span class="line">    <span class="built_in">NSString</span> *bundlePath = [<span class="built_in">NSBundle</span>.mainBundle.resourcePath stringByAppendingPathComponent:pathComponent];</span><br><span class="line">    <span class="keyword">return</span> [<span class="built_in">NSBundle</span> bundleWithPath:bundlePath];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>为了简化调用，提供了宏定义：每个组件调用 <code>HoloCurrentBundle</code> 就可以拿到自己的 bundle 了。</p><p>优先级处理和图片的处理逻辑一致。</p><p>需要注意的是，如果一个组件拥有多个bundle 的情况，这里只会返回优先级较高的 bundle。</p><h2 id="字体-UIFont"><a href="#字体-UIFont" class="headerlink" title="字体 UIFont"></a>字体 UIFont</h2><p>APP 中使用自定义字体的场景也比较常见，提供了注册字体的便捷方法。关于注册字体，字体资源文件在主工程的时候是存储在 main bundle 里的，在主工程的 info.plist 文件里注册 key：UIAppFonts，会自动去 main bundle 里注册对应的字体文件。</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">key</span>&gt;</span>UIAppFonts<span class="tag">&lt;/<span class="name">key</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">array</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">string</span>&gt;</span>GOTHIC.TTF<span class="tag">&lt;/<span class="name">string</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">string</span>&gt;</span>GOTHICB.TTF<span class="tag">&lt;/<span class="name">string</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">string</span>&gt;</span>DIN-BOLD.OTF<span class="tag">&lt;/<span class="name">string</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">string</span>&gt;</span>DIN-REGULAR.OTF<span class="tag">&lt;/<span class="name">string</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">array</span>&gt;</span></span><br></pre></td></tr></table></figure><p>将字体资源文件拿到组件之后，如果最终资源文件存储在 main bnundle 里的话，以上的 info.plist 里的注册依然可以生效，但如果不在 main bundle 的话就需要通过代码访问资源路径去注册了</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#if defined(HOLO_POD_NAME)</span></span><br><span class="line"><span class="comment">/// If the pod currently has multiple bundles, please check the target resource or use 'holo_registerFont:withExtension:inBundle:'</span></span><br><span class="line"><span class="meta">#define HoloRegisterFont(name, extension) [UIFont holo_registerFont:name withExtension:extension bundleName:HOLO_POD_NAME]</span></span><br><span class="line"><span class="meta">#endif</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">@interface</span> <span class="title">UIFont</span> (<span class="title">HoloResource</span>)</span></span><br><span class="line"></span><br><span class="line"><span class="comment">/// If the pod currently has multiple bundles, please check the target resource or use 'holo_registerFont:withExtension:inBundle:'</span></span><br><span class="line">+ (<span class="keyword">void</span>)holo_registerFont:(<span class="built_in">NSString</span> *)fontName withExtension:(<span class="built_in">NSString</span> *)extension bundleName:(<span class="built_in">NSString</span> *)bundleName;</span><br><span class="line"></span><br><span class="line">+ (<span class="keyword">void</span>)holo_registerFont:(<span class="built_in">NSString</span> *)fontName withExtension:(<span class="built_in">NSString</span> *)extension inBundle:(<span class="built_in">NSBundle</span> *)bundle;</span><br><span class="line"></span><br><span class="line"><span class="keyword">@end</span></span><br></pre></td></tr></table></figure><p>具体实现为：<br><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">@implementation</span> <span class="title">UIFont</span> (<span class="title">HoloResource</span>)</span></span><br><span class="line"></span><br><span class="line"><span class="comment">/// If the pod currently has multiple bundles, please check the target resource or use 'holo_registerFont:withExtension:inBundle:'</span></span><br><span class="line">+ (<span class="keyword">void</span>)holo_registerFont:(<span class="built_in">NSString</span> *)fontName withExtension:(<span class="built_in">NSString</span> *)extension bundleName:(<span class="built_in">NSString</span> *)bundleName &#123;</span><br><span class="line">    <span class="built_in">NSBundle</span> *bunlde = [<span class="built_in">NSBundle</span> holo_bundleNamed:bundleName];</span><br><span class="line">    [<span class="keyword">self</span> holo_registerFont:fontName withExtension:extension inBundle:bunlde];</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">+ (<span class="keyword">void</span>)holo_registerFont:(<span class="built_in">NSString</span> *)fontName withExtension:(<span class="built_in">NSString</span> *)extension inBundle:(<span class="built_in">NSBundle</span> *)bundle &#123;</span><br><span class="line">    <span class="built_in">NSURL</span> *fontURL = [bundle URLForResource:fontName withExtension:extension];</span><br><span class="line">    <span class="built_in">NSData</span> *inData = [<span class="built_in">NSData</span> dataWithContentsOfURL:fontURL];</span><br><span class="line">    <span class="built_in">CFErrorRef</span> error;</span><br><span class="line">    <span class="built_in">CGDataProviderRef</span> provider = <span class="built_in">CGDataProviderCreateWithCFData</span>((<span class="built_in">CFDataRef</span>)inData);</span><br><span class="line">    <span class="built_in">CGFontRef</span> font = <span class="built_in">CGFontCreateWithDataProvider</span>(provider);</span><br><span class="line">    <span class="keyword">if</span> (!<span class="built_in">CTFontManagerRegisterGraphicsFont</span>(font, &amp;error)) &#123;</span><br><span class="line">        <span class="built_in">CFStringRef</span> errorDescription = <span class="built_in">CFErrorCopyDescription</span>(error);</span><br><span class="line">        <span class="comment">// Failed to load font</span></span><br><span class="line">        <span class="built_in">CFRelease</span>(errorDescription);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (font != <span class="literal">NULL</span>) <span class="built_in">CFRelease</span>(font);</span><br><span class="line">    <span class="keyword">if</span> (provider != <span class="literal">NULL</span>) <span class="built_in">CFRelease</span>(provider);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">@end</span></span><br></pre></td></tr></table></figure></p><p>为了简化调用，提供了宏定义：调用 <code>HoloRegisterFont(name, extension)</code> 就可以访问自己 bundle 里的字体资源文件注册了。要注意的是，如果字体资源文件所在的组件有多个 bundle 的话，且 字体文件在优先级较低的 bundle 里，使用这个宏就会出问题了。这时可以通过调用 <code>holo_registerFont:withExtension:inBundle:</code> 方法，传入指定 bundle 即可。</p><h2 id="本地化语言-Localized"><a href="#本地化语言-Localized" class="headerlink" title="本地化语言 Localized"></a>本地化语言 Localized</h2><p>对于本地化语言的资源文件，以下是系统提供的宏定义：</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#define NSLocalizedString(key, comment) \</span></span><br><span class="line">    [<span class="built_in">NSBundle</span>.mainBundle localizedStringForKey:(key) value:<span class="string">@""</span> table:<span class="literal">nil</span>]</span><br><span class="line"><span class="meta">#define NSLocalizedStringFromTable(key, tbl, comment) \</span></span><br><span class="line">    [<span class="built_in">NSBundle</span>.mainBundle localizedStringForKey:(key) value:<span class="string">@""</span> table:(tbl)]</span><br><span class="line"><span class="meta">#define NSLocalizedStringFromTableInBundle(key, tbl, bundle, comment) \</span></span><br><span class="line">    [bundle localizedStringForKey:(key) value:<span class="string">@""</span> table:(tbl)]</span><br><span class="line"><span class="meta">#define NSLocalizedStringWithDefaultValue(key, tbl, bundle, val, comment) \</span></span><br><span class="line">    [bundle localizedStringForKey:(key) value:(val) table:(tbl)]</span><br></pre></td></tr></table></figure><p>其中 <code>NSLocalizedStringFromTable(key, tbl, comment)</code> 方法，虽然它也是去 main bundle 里取值，但可以根据 table 划分不同的模块，已经可以解决同名覆盖的问题，但是将这些资源文件拿到 pod 里之后，因为 pod 拥有自己的 bundle，那么这个宏就失效了，所以需要使用下边两个宏定义。前边章节里我们已经等装好了 bundle 统一获取方法，那么这里可以提供一个自定义的宏来简化调用，少传一个 bundle 参数：</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#define HoloLocalizedStringFromTable(key, tbl, comment) \</span></span><br><span class="line">        <span class="built_in">NSLocalizedStringFromTableInBundle</span>(key, tbl, [<span class="built_in">NSBundle</span> holo_bundleNamed:HOLO_POD_NAME], comment)</span><br><span class="line"><span class="meta">#define HoloLocalizedStringWithDefaultValue(key, tbl, val, comment) \</span></span><br><span class="line">        <span class="built_in">NSLocalizedStringWithDefaultValue</span>(key, tbl, [<span class="built_in">NSBundle</span> holo_bundleNamed:HOLO_POD_NAME], val, comment)</span><br></pre></td></tr></table></figure><p>其中， <code>localizedStringForKey:value:table:</code> 方法能够实现本地化的原理只不过是根据不同语言访问不同路径下的本地化文件而已，比如 英语 语言，上述方法访问的就是 <code>./en.lproj/HoloResourceDemoA.strings：</code>路径下的 <code>.strings</code> 文件</p><figure class="highlight stylus"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line">.</span><br><span class="line">├── Assets.car</span><br><span class="line">├── Info.plist</span><br><span class="line">├── _CodeSignature</span><br><span class="line">│   ├── CodeDirectory</span><br><span class="line">│   ├── CodeRequirements</span><br><span class="line">│   ├── CodeRequirements-<span class="number">1</span></span><br><span class="line">│   ├── CodeResources</span><br><span class="line">│   └── CodeSignature</span><br><span class="line">├── en.lproj</span><br><span class="line">│   └── HoloResourceDemoA.strings</span><br><span class="line">├── ja.lproj</span><br><span class="line">│   └── HoloResourceDemoA.strings</span><br><span class="line">└── <span class="selector-tag">th</span>.lproj</span><br><span class="line">    └── HoloResourceDemoA.strings</span><br></pre></td></tr></table></figure><p>通常工程中有自定义语言的功能，那么可以 hook bundle 的 <code>localizedStringForKey:value:table:</code> 方法，根据自定义的语言标识访问目标文件。</p><p>提供 <code>[NSBundle holo_configureLanguage:@&quot;ja&quot;]</code>方法设置当前语言，从而决定访问的目标本地化文件。实现如下：</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">static</span> <span class="built_in">NSString</span> *holoLanguage = <span class="literal">nil</span>;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">@implementation</span> <span class="title">NSBundle</span> (<span class="title">HoloLocalized</span>)</span></span><br><span class="line"></span><br><span class="line">+ (<span class="keyword">void</span>)load &#123;</span><br><span class="line">    [<span class="keyword">self</span> jr_swizzleMethod:<span class="keyword">@selector</span>(localizedStringForKey:value:table:)</span><br><span class="line">                withMethod:<span class="keyword">@selector</span>(_holo_localizedStringForKey:value:table:)</span><br><span class="line">                     error:<span class="literal">nil</span>];</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">- (<span class="built_in">NSString</span> *)_holo_localizedStringForKey:(<span class="built_in">NSString</span> *)key value:(<span class="built_in">NSString</span> *)value table:(<span class="built_in">NSString</span> *)tableName &#123;</span><br><span class="line">    <span class="built_in">NSString</span> *path = [<span class="keyword">self</span> pathForResource:holoLanguage ofType:<span class="string">@"lproj"</span>];</span><br><span class="line">    <span class="built_in">NSBundle</span> *bundle = [<span class="built_in">NSBundle</span> bundleWithPath:path];</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">if</span> (holoLanguage &amp;&amp; bundle) &#123;</span><br><span class="line">        <span class="keyword">return</span> [bundle localizedStringForKey:key value:value table:tableName];</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> [<span class="keyword">self</span> _holo_localizedStringForKey:key value:value table:tableName];</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">+ (<span class="keyword">void</span>)holo_configureLanguage:(<span class="built_in">NSString</span> *)language &#123;</span><br><span class="line">    holoLanguage = language;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">@end</span></span><br></pre></td></tr></table></figure><h2 id="xib-等资源，传递-bundle"><a href="#xib-等资源，传递-bundle" class="headerlink" title="xib 等资源，传递 bundle"></a>xib 等资源，传递 bundle</h2><p>其他的资源问题处理方式也都相同，都是根据 bundle 获取到目标文件访问，系统也都提供了根据 bundle 取值的方法，如 <code>UINib</code>：</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// If the bundle parameter is nil, the main bundle is used.</span></span><br><span class="line"><span class="comment">// Releases resources in response to memory pressure (e.g. memory warning), reloading from the bundle when necessary.</span></span><br><span class="line">+ (<span class="built_in">UINib</span> *)nibWithNibName:(<span class="built_in">NSString</span> *)name bundle:(<span class="keyword">nullable</span> <span class="built_in">NSBundle</span> *)bundleOrNil;</span><br><span class="line"></span><br><span class="line"><span class="comment">// If the bundle parameter is nil, the main bundle is used.</span></span><br><span class="line">+ (<span class="built_in">UINib</span> *)nibWithData:(<span class="built_in">NSData</span> *)data bundle:(<span class="keyword">nullable</span> <span class="built_in">NSBundle</span> *)bundleOrNil;</span><br></pre></td></tr></table></figure><p>注册 xib 文件：<br><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">NSBundle</span> *bundle = [<span class="built_in">NSBundle</span> holo_bundleNamed:HOLO_POD_NAME];</span><br><span class="line"><span class="built_in">UINib</span> *nib = [<span class="built_in">UINib</span> nibWithNibName:<span class="built_in">NSStringFromClass</span>([ExampleTableViewCell <span class="keyword">class</span>]) bundle:bundle];</span><br><span class="line">[<span class="keyword">self</span>.tableView registerNib:nib forCellReuseIdentifier:<span class="built_in">NSStringFromClass</span>([ExampleTableViewCell <span class="keyword">class</span>])];</span><br></pre></td></tr></table></figure></p><h1 id="后记"><a href="#后记" class="headerlink" title="后记"></a>后记</h1><p>1、代码已开源，具体实现及思路请参照源码：<a href="https://github.com/gonghonglou/HoloResource" target="_blank" rel="noopener">HoloResource</a></p><p>2、大家的资源文件都是怎么管理的呢？欢迎留言讨论指教。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h1 id=&quot;前言&quot;&gt;&lt;a href=&quot;#前言&quot; class=&quot;headerlink&quot; title=&quot;前言&quot;&gt;&lt;/a&gt;前言&lt;/h1&gt;&lt;p&gt;最初，所有代码都在主工程的时候，资源文件也在主工程，尤其是大量的图片资源，不可避免的就是命名冲突问题。一般而言，我们需要一份严谨的命名规范，
      
    
    </summary>
    
      <category term="iOS" scheme="http://gonghonglou.com/categories/iOS/"/>
    
    
  </entry>
  
  <entry>
    <title>组件化分发生命周期 - AOP 方案</title>
    <link href="http://gonghonglou.com/2020/03/08/pod-lifecycle-aop/"/>
    <id>http://gonghonglou.com/2020/03/08/pod-lifecycle-aop/</id>
    <published>2020-03-08T15:55:49.000Z</published>
    <updated>2020-09-06T12:20:07.784Z</updated>
    
    <content type="html"><![CDATA[<p>组件化分发生命周期 方案实现探索系列文章：<br>1、<a href="http://gonghonglou.com/2019/08/29/pod-lifecycle/">组件化分发生命周期</a><br><strong>2、<a href="http://gonghonglou.com/2020/03/08/pod-lifecycle-aop/">组件化分发生命周期 - AOP 方案</a></strong><br>3、<a href="http://gonghonglou.com/2020/09/06/pod-lifecycle-aop-libffi/">组件化分发生命周期 - AOP 方案（libffi 实现）</a></p><p>在之前的文章（ <strong><a href="http://gonghonglou.com/2019/08/29/pod-lifecycle/">组件化分发生命周期</a></strong> ）里已经介绍过了 组件化分发生命周期 的概念及理由，并总结了几种分发生命周期的方案，最后一节的方案本以为已经很完美了，但是在项目中实际应用的时候还是发现了问题。</p><h1 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h1><p>因为我们工程里用了 Firebase 打点，其中 Firebase 依赖的一个库 <code>GoogleUtilities</code> 也对 UIApplication 的 delegate 里的一些方法做了 hook 操作，对 APP 做了一些监听操作。以下是 <code>GoogleUtilities</code> 产生影响的关键代码：<br><figure class="highlight objectivec"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line">+ (GULApplication *)sharedApplication &#123;</span><br><span class="line">  <span class="keyword">if</span> ([GULAppEnvironmentUtil isAppExtension]) &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">nil</span>;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">id</span> sharedApplication = <span class="literal">nil</span>;</span><br><span class="line">  Class uiApplicationClass = <span class="built_in">NSClassFromString</span>(kGULApplicationClassName);</span><br><span class="line">  <span class="keyword">if</span> (uiApplicationClass &amp;&amp;</span><br><span class="line">      [uiApplicationClass respondsToSelector:(<span class="built_in">NSSelectorFromString</span>(<span class="string">@"sharedApplication"</span>))]) &#123;</span><br><span class="line">    sharedApplication = [uiApplicationClass sharedApplication];</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> sharedApplication;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>其中 <code>kGULApplicationClassName</code> 就是 <code>@&quot;UIApplication&quot;</code>。因为在上一种方案里我们将系统的 delegate 重设成了我们自己的类：<code>GHLApplicationDelegate</code>，并通过消息转发的方式统一处理了原 <code>AppDelegate</code> 里的所有方法。<br>这样一来，Firebase 通过以上代码得到的 <code>GULApplication</code> 就变成了 <code>GHLApplicationDelegate</code>，再去 hook <code>application:didFinishLaunchingWithOptions:</code> 等方法的时候就找不到这些方法了，即 Firebase 失去了相应的功能。</p><p>这里是 Firebase 的场景，其他第三方库有相应 hook 操作的话也会有相同的问题，而我们在实际开发过程中没法避免其他库的 hook 操作，甚至可能察觉不到问题的存在，所以之前提到的方案是有问题的。</p><h1 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h1><p>这样一想，原本的方法的确存在设计上的问题，我们只顾着自己的 hook，影响了其他第三方对 <code>AppDelegate</code> 的 hook 操作，这显然不合理。</p><p>为了解决以上的问题，不得不再次寻找方案。重新梳理下我们的需求：</p><p>在主工程的 <code>AppDelegate</code> 里执行 <code>UIApplicationDelegate</code> 的一系列生命周期的方法的时候，相应的各个 Pod 里也能有对应方法同时被执行，且不影响其他第三方对 <code>AppDelegate</code> 的 hook 操作。例如：主工程 <code>AppDelegate</code> 里的 <code>application:didFinishLaunchingWithOptions:</code> 方法被执行时，各个 Pod 里也能有对应的 <code>application:didFinishLaunchingWithOptions:</code> 方法被执行。</p><p>那我们也可以 hook <code>AppDelegate</code> 的各阶段方法，遍历执行各个 Pod 里的对应方法之后，再执行下原本的方法。这样大家都保证各自的 hook 链不会断掉就 ok 了。</p><h1 id="解决方案：AOP"><a href="#解决方案：AOP" class="headerlink" title="解决方案：AOP"></a>解决方案：AOP</h1><p>想起 <a href="https://github.com/steipete/Aspects" target="_blank" rel="noopener">Aspects</a> 这个库。或许，AOP 是一种不错的解决方案。思路如下：</p><p>1）首先还是采用上一篇文章（<a href="http://gonghonglou.com/2019/08/29/pod-lifecycle/">组件化分发生命周期</a>）的方案，在各个 Pod 里通过继承 <code>HoloLifecycle</code> 类的方式让其子类拥有执行 UIApplicationDelegate 生命周期方法的能力。</p><p>2）hook <code>UIApplication</code> 的 <code>setDelegate:</code> 方法，拿到 <code>AppDelegate</code></p><p>3）遍历 <code>AppDelegate</code> 的方法列表，判断是否是 <code>UIApplicationDelegate</code> 的协议方法</p><p>4）使用 AOP 的方法，对以上遍历加判断筛选拿到的方法一一进行 hook</p><p>5）在被 hook 的每个方法执行前，遍历 <code>HoloLifecycle</code> 的所有子类，执行同名方法</p><p>以上就是根据需求想到的解决步骤，唯一的关键就在最后一步，执行 <code>HoloLifecycle</code> 类里的方法时，因为入参不确定，<code>performSelector:withObject:</code> 方法肯定是不适用了，可以用 <code>NSInvocation</code> 的方法转发消息，参考 <code>Aspects</code>。</p><p>大体思路就是：</p><p>1）通过 <code>Aspects</code> hook <code>AppDelegate</code> 方法，让被 hook 的方法不会被正常执行，而是走消息转发流程（<code>Aspects</code> 的思路）</p><p>2）在消息转发的最后一步 <code>forwardInvocation:</code> 方法里获取当前所 hook 方法的 <code>NSInvocation</code> 对象 A，在这个对象里存储着转发的方法名，参数值等信息</p><p>3）根据 <code>HoloLifecycle</code> 类对象（target）和想要分发的方法的方法名（selector），创建一个 <code>NSInvocation</code> 对象 B</p><p>4）遍历 <code>NSInvocation</code> 对象 A 的所有参数，把它们依次赋值给 <code>NSInvocation</code> 对象 B，并发送对象 B</p><p>遍历所有的 <code>HoloLifecycle</code> 类对象，循环执行以上步骤就可以了，这样就做到了 hook <code>AppDelegate</code> 各生命周期方法，在各方法执行前遍历执行 <code>HoloLifecycle</code> 类对象的对应同名方法，也就是：组件化分发生命周期 的最终效果。</p><p>一开始以为 <code>Aspects</code> 暴露的方法里没法获取所 hook 方法的 <code>NSInvocation</code>，所以花了些力气参考 <code>Aspects</code> 的源码实现了一套方案。能够正常工作之后才发现 <code>Aspects</code> 的 hook 方法的 block 里可以拿到 <code>AspectInfo</code> 这个对象，而这个对象持有的 <code>originalInvocation</code> 属性，就是所 hook 方法走 <code>forwardInvocation:</code> 方法时拿到的 <code>NSInvocation</code>。所以，最后借助 <code>Aspects</code> 的实现简单了很多，<code>Aspects</code> 已经完成了 hook 和返回 <code>NSInvocation</code> 的功能，我们主要完成以上 3）、4）步骤即可，以下是关键代码：<br><figure class="highlight objectivec"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">@implementation</span> <span class="title">UIApplication</span> (<span class="title">HoloLifecycle</span>)</span></span><br><span class="line"></span><br><span class="line">+ (<span class="keyword">void</span>)load &#123;</span><br><span class="line">    [<span class="keyword">self</span> jr_swizzleMethod:<span class="keyword">@selector</span>(setDelegate:) withMethod:<span class="keyword">@selector</span>(_holo_setDelegate:) error:<span class="literal">nil</span>];</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">- (<span class="keyword">void</span>)_holo_setDelegate:(<span class="keyword">id</span> &lt;<span class="built_in">UIApplicationDelegate</span>&gt;)delegate &#123;</span><br><span class="line">    <span class="keyword">unsigned</span> count = <span class="number">0</span>;</span><br><span class="line">    Method *methods = class_copyMethodList([delegate <span class="keyword">class</span>], &amp;count);</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; count; i++) &#123;</span><br><span class="line">        Method method = methods[i];</span><br><span class="line">        SEL sel = method_getName(method);</span><br><span class="line">        <span class="keyword">struct</span> objc_method_description methodDesc = protocol_getMethodDescription(<span class="class"><span class="keyword">@protocol</span>(<span class="title">UIApplicationDelegate</span>), <span class="title">sel</span>, <span class="title">NO</span>, <span class="title">YES</span>);</span></span><br><span class="line">        <span class="keyword">if</span> (methodDesc.name != <span class="literal">NULL</span> &amp;&amp; methodDesc.types != <span class="literal">NULL</span>) &#123;</span><br><span class="line">            [<span class="keyword">self</span> _aspect_hookSelectorWithDelegate:delegate sel:sel];</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    [<span class="keyword">self</span> _holo_setDelegate:delegate];</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">- (<span class="keyword">void</span>)_aspect_hookSelectorWithDelegate:(<span class="keyword">id</span> &lt;<span class="built_in">UIApplicationDelegate</span>&gt;)delegate sel:(SEL)sel &#123;</span><br><span class="line">    [(<span class="built_in">NSObject</span> *)delegate aspect_hookSelector:sel withOptions:AspectPositionBefore usingBlock:^(<span class="keyword">id</span>&lt;AspectInfo&gt; info) &#123;</span><br><span class="line">        </span><br><span class="line">        <span class="keyword">for</span> (HoloLifecycle *lifecycle <span class="keyword">in</span> [HoloLifecycleManager sharedInstance].subClasses) &#123;</span><br><span class="line">            <span class="keyword">if</span> ([lifecycle respondsToSelector:sel]) &#123;</span><br><span class="line">                <span class="built_in">NSMethodSignature</span> *signature = [lifecycle methodSignatureForSelector:sel];</span><br><span class="line">                <span class="built_in">NSInvocation</span> *invocation = [<span class="built_in">NSInvocation</span> invocationWithMethodSignature:signature];</span><br><span class="line">                invocation.target = lifecycle;</span><br><span class="line">                invocation.selector = sel;</span><br><span class="line">                </span><br><span class="line">                <span class="built_in">NSUInteger</span> numberOfArguments = signature.numberOfArguments;</span><br><span class="line">                <span class="keyword">if</span> (numberOfArguments &gt; info.originalInvocation.methodSignature.numberOfArguments) &#123;</span><br><span class="line">                    <span class="built_in">NSLog</span>(<span class="string">@"lifecycle has too many arguments. Not calling %@"</span>, info);</span><br><span class="line">                    <span class="keyword">continue</span>;</span><br><span class="line">                &#125;</span><br><span class="line">                </span><br><span class="line">                <span class="keyword">void</span> *argBuf = <span class="literal">NULL</span>;</span><br><span class="line">                <span class="keyword">for</span> (<span class="built_in">NSUInteger</span> idx = <span class="number">2</span>; idx &lt; numberOfArguments; idx++) &#123;</span><br><span class="line">                    <span class="keyword">const</span> <span class="keyword">char</span> *type = [info.originalInvocation.methodSignature getArgumentTypeAtIndex:idx];</span><br><span class="line">                    <span class="built_in">NSUInteger</span> argSize;</span><br><span class="line">                    <span class="built_in">NSGetSizeAndAlignment</span>(type, &amp;argSize, <span class="literal">NULL</span>);</span><br><span class="line">                    </span><br><span class="line">                    <span class="keyword">if</span> (!(argBuf = reallocf(argBuf, argSize))) &#123;</span><br><span class="line">                        <span class="built_in">NSLog</span>(<span class="string">@"Failed to allocate memory for lifecycle invocation."</span>);</span><br><span class="line">                        <span class="keyword">break</span>;</span><br><span class="line">                    &#125;</span><br><span class="line">                    </span><br><span class="line">                    [info.originalInvocation getArgument:argBuf atIndex:idx];</span><br><span class="line">                    [invocation setArgument:argBuf atIndex:idx];</span><br><span class="line">                &#125;</span><br><span class="line">                </span><br><span class="line">                [invocation invoke];</span><br><span class="line">                </span><br><span class="line">                <span class="keyword">if</span> (argBuf != <span class="literal">NULL</span>) &#123;</span><br><span class="line">                    free(argBuf);</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125; error:<span class="literal">nil</span>];</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">@end</span></span><br></pre></td></tr></table></figure></p><h1 id="补充功能"><a href="#补充功能" class="headerlink" title="补充功能"></a>补充功能</h1><p>1、参考青木的 <a href="https://github.com/DManager/TDFModuleKit" target="_blank" rel="noopener">TDFModuleKit</a>，也给 <code>HoloLifecycle</code> 类添加了优先级方法，在子类里重写该方法返回合适的优先级即可决定执行顺序，<br>并且 <code>HoloLifecycle</code> 支持在 <code>AppDelegate</code> 方法之前、之后两阶段执行：<br><figure class="highlight lsl"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/// 调用优先级</span></span><br><span class="line">typedef NS_ENUM(NSInteger, HoloLifecyclePriority) &#123;</span><br><span class="line">    <span class="comment">// 晚于 AppDelegate 执行</span></span><br><span class="line">    HoloLifecyclePriorityAfterLow = <span class="number">50</span>,</span><br><span class="line">    HoloLifecyclePriorityAfterMedium = <span class="number">150</span>,</span><br><span class="line">    HoloLifecyclePriorityAfterHigh = <span class="number">250</span>,</span><br><span class="line">    <span class="comment">// 早于 AppDelegate 执行</span></span><br><span class="line">    HoloLifecyclePriorityBeforeLow = <span class="number">500</span>,</span><br><span class="line">    HoloLifecyclePriorityBeforeMedium = <span class="number">750</span>, <span class="comment">// default</span></span><br><span class="line">    HoloLifecyclePriorityBeforeHigh = <span class="number">1000</span>,</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment">/// 调用优先级</span></span><br><span class="line"><span class="comment">/// 子类重写该方法 return 合适的优先级</span></span><br><span class="line"><span class="comment">/// AppDelegate 的优先级为 300，若 HoloLifecycle 子类同样定义为 300，则先于 AppDelegate 执行</span></span><br><span class="line"><span class="comment">/// @return 合适的优先级，默认为 HoloLifecyclePriorityBeforeMedium</span></span><br><span class="line">+ (HoloLifecyclePriority)priority;</span><br></pre></td></tr></table></figure></p><p>2、使用 <code>HoloLifecycleManager</code> 类的以下方法可以添加 log 功能，方便监控各个方法的调用顺序和耗时：<br><figure class="highlight actionscript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/// 打印所有 HoloLifecycle 子类执行方法及耗时</span></span><br><span class="line"><span class="comment">/// 在 + load 方法里调用该方法，以保证该方法早于 UIApplicationDelegate 方法调用</span></span><br><span class="line">- (<span class="keyword">void</span>)logSelectorsAndPerformTime;</span><br></pre></td></tr></table></figure></p><p>代码已开源至：<a href="https://github.com/gonghonglou/HoloLifecycle" target="_blank" rel="noopener">HoloLifecycle</a>，欢迎使用。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;组件化分发生命周期 方案实现探索系列文章：&lt;br&gt;1、&lt;a href=&quot;http://gonghonglou.com/2019/08/29/pod-lifecycle/&quot;&gt;组件化分发生命周期&lt;/a&gt;&lt;br&gt;&lt;strong&gt;2、&lt;a href=&quot;http://gonghong
      
    
    </summary>
    
      <category term="iOS" scheme="http://gonghonglou.com/categories/iOS/"/>
    
    
  </entry>
  
  <entry>
    <title>SDWebImage 4.x &amp; 5.x 对 GIF 类型的处理问题</title>
    <link href="http://gonghonglou.com/2020/03/01/sdwebimage-gif/"/>
    <id>http://gonghonglou.com/2020/03/01/sdwebimage-gif/</id>
    <published>2020-03-01T13:54:10.000Z</published>
    <updated>2020-10-05T11:37:56.250Z</updated>
    
    <content type="html"><![CDATA[<p>之前工作中遇到的问题，使用 SDWebImage（v4.4.8）下载并存储了一份 GIF 图片，第一次是可以正常显示的，但之后再从缓存中取出的时候就变成了静态图片。debug 发现是因为从磁盘里取出该图片时已经是 PNG 格式的了。为了解决这个问题所以去仔细看了下 SD 对 GIF 的处理。</p><p>测试代码大概是这个样子的：<br><figure class="highlight objectivec"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">NSURL</span> *url = [<span class="built_in">NSURL</span> URLWithString:<span class="string">@"http://img.xxx.gif"</span>];</span><br><span class="line">[[SDWebImageDownloader sharedDownloader] downloadImageWithURL:url options:SDWebImageDownloaderLowPriority progress:^(<span class="built_in">NSInteger</span> receivedSize, <span class="built_in">NSInteger</span> expectedSize, <span class="built_in">NSURL</span> * _Nullable targetURL) &#123;</span><br><span class="line">&#125; completed:^(<span class="built_in">UIImage</span> * _Nullable image, <span class="built_in">NSData</span> * _Nullable data, <span class="built_in">NSError</span> * _Nullable error, <span class="built_in">BOOL</span> finished) &#123;</span><br><span class="line">    </span><br><span class="line">    [[SDImageCache sharedImageCache] storeImage:image forKey:url.absoluteString completion:<span class="literal">nil</span>];            </span><br><span class="line">&#125;];</span><br></pre></td></tr></table></figure></p><p>发现问题主要是出在 <code>SDImageCache</code> 这个类的 <code>storeImage:forKey:completion:</code> 方法，在 <a href="https://github.com/SDWebImage/SDWebImage/blob/aa7ff6f060d8f34d2c66a716aefff1a3bc9ef816/SDWebImage/Core/SDImageCache.m#L192-L197" target="_blank" rel="noopener">L192-L197</a> 行是这样的：<br><figure class="highlight fortran"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> (<span class="comment">!data &amp;&amp; image) &#123;</span></span><br><span class="line">    // <span class="keyword">If</span> we <span class="keyword">do</span> not have <span class="built_in">any</span> <span class="keyword">data</span> to detect image <span class="keyword">format</span>, check whether it <span class="keyword">contains</span> alpha channel to <span class="keyword">use</span> PNG or JPEG <span class="keyword">format</span></span><br><span class="line">    SDImageFormat <span class="keyword">format</span>;</span><br><span class="line">    <span class="keyword">if</span> ([SDImageCoderHelper CGImageContainsAlpha:image.CGImage]) &#123;</span><br><span class="line">        <span class="keyword">format</span> = SDImageFormatPNG;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="keyword">format</span> = SDImageFormatJPEG;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">data</span> = [[SDImageCodersManager sharedManager] encodedDataWithImage:image <span class="keyword">format</span>:<span class="keyword">format</span> options:nil];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>注释也写的明白：</p><blockquote><p>If we do not have any data to detect image format, check whether it contains alpha channel to use PNG or JPEG format</p></blockquote><p>判断图片格式主要用的这个方法：<a href="https://github.com/SDWebImage/SDWebImage/blob/aa7ff6f060d8f34d2c66a716aefff1a3bc9ef816/SDWebImage/Core/NSData%2BImageContentType.m#L23-L70" target="_blank" rel="noopener">NSData+ImageContentType.m #L23-L70</a>：<br><figure class="highlight groovy"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line">+ (SDImageFormat)<span class="string">sd_imageFormatForImageData:</span>(nullable NSData *)data &#123;</span><br><span class="line">    <span class="keyword">if</span> (!data) &#123;</span><br><span class="line">        <span class="keyword">return</span> SDImageFormatUndefined;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// File signatures table: http://www.garykessler.net/library/file_sigs.html</span></span><br><span class="line">    uint8_t c;</span><br><span class="line">    [data <span class="string">getBytes:</span>&amp;c <span class="string">length:</span><span class="number">1</span>];</span><br><span class="line">    <span class="keyword">switch</span> (c) &#123;</span><br><span class="line">        <span class="keyword">case</span> <span class="number">0xFF</span>:</span><br><span class="line">            <span class="keyword">return</span> SDImageFormatJPEG;</span><br><span class="line">        <span class="keyword">case</span> <span class="number">0x89</span>:</span><br><span class="line">            <span class="keyword">return</span> SDImageFormatPNG;</span><br><span class="line">        <span class="keyword">case</span> <span class="number">0x47</span>:</span><br><span class="line">            <span class="keyword">return</span> SDImageFormatGIF;</span><br><span class="line">        <span class="keyword">case</span> <span class="number">0x49</span>:</span><br><span class="line">        <span class="keyword">case</span> <span class="number">0x4D</span>:</span><br><span class="line">            <span class="keyword">return</span> SDImageFormatTIFF;</span><br><span class="line">        <span class="keyword">case</span> <span class="number">0x52</span>: &#123;</span><br><span class="line">            ...</span><br></pre></td></tr></table></figure></p><p>因为所用的缓存方法只传了 UIImage 进去，没有 NSData，SD 无法判断图片格式，所以就根据 alpha channel 使用了 PNG 或者 JPEG 格式。</p><p>当时为了快速解决 bug，项目上线。所以将 <code>storeImage:forKey:completion:</code> 方法替换为了 <code>storeImage:imageData:forKey:toDisk:completion:</code>，即：<br><figure class="highlight less"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="selector-attr">[[SDWebImageDownloader sharedDownloader]</span> <span class="selector-tag">downloadImageWithURL</span><span class="selector-pseudo">:url</span> <span class="selector-tag">options</span><span class="selector-pseudo">:SDWebImageDownloaderLowPriority</span> <span class="selector-tag">progress</span>:^(NSInteger receivedSize, NSInteger expectedSize, NSURL * _Nullable targetURL) &#123;</span><br><span class="line">&#125; <span class="attribute">completed</span>:^(UIImage * _Nullable image, NSData * _Nullable data, NSError * _Nullable error, BOOL finished) &#123;</span><br><span class="line">    <span class="comment">// 替换缓存方法为</span></span><br><span class="line">    [[SDImageCache sharedImageCache] <span class="attribute">storeImage</span>:image <span class="attribute">imageData</span>:data <span class="attribute">forKey</span>:url.absoluteString <span class="attribute">toDisk</span>:YES <span class="attribute">completion</span>:nil];</span><br><span class="line">&#125;];</span><br></pre></td></tr></table></figure></p><p>并在代码里加了备注，解释了原因及提醒了相关维护同学。</p><p>这个周末有空了，突然想起来这个问题，想着既然图片是由 SD 下载的，那它肯定能拿到图片的 NSData。既然这样，可以在下载的时候根据 NSData 识别图片格式，给 UIImage 打个标签，这样，在存储的时候不就可以识别到该图片的 GIF 格式了嘛。然后就去浏览了下 SD 的源码，发现，其实 SD 已经给所有它经手的图片打过了标签：<code>sd_imageFormat</code>，即 <code>UIImage+Metadata</code> 这个分类的属性：<br><figure class="highlight objectivec"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line">- (SDImageFormat)sd_imageFormat &#123;</span><br><span class="line">    SDImageFormat imageFormat = SDImageFormatUndefined;</span><br><span class="line">    <span class="built_in">NSNumber</span> *value = objc_getAssociatedObject(<span class="keyword">self</span>, <span class="keyword">@selector</span>(sd_imageFormat));</span><br><span class="line">    <span class="keyword">if</span> ([value isKindOfClass:[<span class="built_in">NSNumber</span> <span class="keyword">class</span>]]) &#123;</span><br><span class="line">        imageFormat = value.integerValue;</span><br><span class="line">        <span class="keyword">return</span> imageFormat;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// Check CGImage's UTType, may return nil for non-Image/IO based image</span></span><br><span class="line">    <span class="keyword">if</span> (@available(iOS <span class="number">9.0</span>, tvOS <span class="number">9.0</span>, macOS <span class="number">10.11</span>, watchOS <span class="number">2.0</span>, *)) &#123;</span><br><span class="line">        <span class="built_in">CFStringRef</span> uttype = <span class="built_in">CGImageGetUTType</span>(<span class="keyword">self</span>.CGImage);</span><br><span class="line">        imageFormat = [<span class="built_in">NSData</span> sd_imageFormatFromUTType:uttype];</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> imageFormat;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">- (<span class="keyword">void</span>)setSd_imageFormat:(SDImageFormat)sd_imageFormat &#123;</span><br><span class="line">    objc_setAssociatedObject(<span class="keyword">self</span>, <span class="keyword">@selector</span>(sd_imageFormat), @(sd_imageFormat), OBJC_ASSOCIATION_RETAIN_NONATOMIC);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>既然如此的话，那在 <code>SDImageCache</code> 这个类的 <code>storeImage:forKey:completion:</code> 方法 <a href="https://github.com/SDWebImage/SDWebImage/blob/aa7ff6f060d8f34d2c66a716aefff1a3bc9ef816/SDWebImage/Core/SDImageCache.m#L192-L197" target="_blank" rel="noopener">L192-L197</a> 行优先识别下 <code>sd_imageFormat</code> 不就好了，即：<br><figure class="highlight fortran"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> (<span class="comment">!data &amp;&amp; image) &#123;</span></span><br><span class="line">    // <span class="keyword">If</span> we <span class="keyword">do</span> not have <span class="built_in">any</span> <span class="keyword">data</span> to detect image <span class="keyword">format</span>, check whether it <span class="keyword">contains</span> alpha channel to <span class="keyword">use</span> PNG or JPEG <span class="keyword">format</span></span><br><span class="line">    SDImageFormat <span class="keyword">format</span> = image.sd_imageFormat;</span><br><span class="line">    <span class="keyword">if</span> (<span class="keyword">format</span> == SDImageFormatUndefined) &#123;</span><br><span class="line">        <span class="keyword">if</span> ([SDImageCoderHelper CGImageContainsAlpha:image.CGImage]) &#123;</span><br><span class="line">            <span class="keyword">format</span> = SDImageFormatPNG;</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="keyword">format</span> = SDImageFormatJPEG;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">data</span> = [[SDImageCodersManager sharedManager] encodedDataWithImage:image <span class="keyword">format</span>:<span class="keyword">format</span> options:nil];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>虽然那些未经 SD 处理过的 GIF 格式的 UIImage 使用该方法时仍然会判定为 PNG 或者 JPEG，但经过 SD 下载来的图片做缓存存取的话都会保证格式的。我提了 <a href="https://github.com/SDWebImage/SDWebImage/issues/2952" target="_blank" rel="noopener">Issue #2952</a> ，SD 的维护者已经采纳了意见，提交了 <a href="https://github.com/SDWebImage/SDWebImage/pull/2953" target="_blank" rel="noopener">PR #2953</a>，改动了代码到 Master 了，预计 SD 5.6.0 版本会生效。</p><p>看到 5.6.0 版本可能会很突兀，文章开头不是说 SDWebImage（v4.4.8）吗？</p><p>是这样的，我到 SD 官网查代码的时候发现 SD 在去年就发布了 5.x 版本。经测试，存储 GIF 图片再取出时仍然是动图，似乎已经修复了这个问题，但是通过 debug 发现，仍然是动图的原因是 SD 将图片的编解码方式改了，实际存储到磁盘的是 <code>_UIAnimatedImage</code> 类型，即直接设置 <code>_UIAnimatedImage</code> 类型给 UIImageView.image 也是动图效果，但是从取出图片的 NSData 头发现仍然是 0x89 PNG 类型。这也是为什么我仍然在 5.x 版本提交 Issus 的原因。</p><p>顺便提一下，SD v5.x 版本移除了 <code>FLAnimatedImageView+WebCache</code> 分类，新增了 <code>SDAnimatedImageView</code> 类来处理 GIF 等格式。其实，得益于 v5.x 新的编解码方法，直接使用 UIImageView 就可以正常显示 GIF。<br>你仍然可以使用 <code>FLAnimatedImageView+WebCache</code> 这个分类兼容代码，通过引入 <code>SDWebImageFLPlugin</code> 插件的方式。<br>可以去了解下 SD 的 README，提供了一堆的 Plugin 和 Coder 来支持各种类型的图片和三方库。这个周末还新增了 <code>SDWebImageLottiePlugin</code> 插件，支持 JSON 动画。<br>通过无限插件的形式来丰富 SD 的功能，感觉很牛逼。升级升级！</p><p>言归正传，现在，我们来看一下关键代码，为什么同样存储为 0x89，v4.x 版本就是静态图片，而 v5.x 就是动图呢？</p><h2 id="v4-x"><a href="#v4-x" class="headerlink" title="v4.x"></a>v4.x</h2><p>在 v4.x 的代码里，<code>SDWebImageDownloaderOperation</code> 这个类负责下载数据，在 <a href="https://github.com/SDWebImage/SDWebImage/blob/a0b269a4bc353257fac482476d75f7bd2fcb1b0a/SDWebImage/SDWebImageDownloaderOperation.m#L418" target="_blank" rel="noopener">#L418</a> 这一行将 NSData 转换为 UIImage。继续查看 <code>SDWebImageCodersManager</code> 这个类的解码（<code>decodedImageWithData:</code>）方法：<br><figure class="highlight objectivec"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">- (<span class="built_in">UIImage</span> *)decodedImageWithData:(<span class="built_in">NSData</span> *)data &#123;</span><br><span class="line">    LOCK(<span class="keyword">self</span>.codersLock);</span><br><span class="line">    <span class="built_in">NSArray</span>&lt;<span class="keyword">id</span>&lt;SDWebImageCoder&gt;&gt; *coders = <span class="keyword">self</span>.coders;</span><br><span class="line">    UNLOCK(<span class="keyword">self</span>.codersLock);</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">id</span>&lt;SDWebImageCoder&gt; coder <span class="keyword">in</span> coders.reverseObjectEnumerator) &#123;</span><br><span class="line">        <span class="keyword">if</span> ([coder canDecodeFromData:data]) &#123;</span><br><span class="line">            <span class="keyword">return</span> [coder decodedImageWithData:data];</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">nil</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>遍历 self.coders 寻找能够解码当前 NSData 的 coder，而此时 self.coders 数组里也就只有 <code>SDWebImageImageIOCoder</code> 这个类而已，继续查看这个类的解码方法：<br><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">- (UIImage *)decodedImageWithData:(NSData *)<span class="keyword">data</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (!<span class="keyword">data</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> nil;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    UIImage *image = [[UIImage alloc] initWithData:<span class="keyword">data</span>];</span><br><span class="line">    image.sd_imageFormat = [NSData sd_imageFormatForImageData:<span class="keyword">data</span>];</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> image;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>直接将 data 转成了 UIImage，并且在这个时候根据 data，拿到了图片格式，设置了 <code>sd_imageFormat</code>。</p><p>这里的 image 就是最终回调的 image 了，然后使用<br>1、<code>SDImageCache</code> 类的 <code>storeImage:forKey:completion:</code> 方法将该 image 存到磁盘的时候，还是调用的<br>2、<code>SDWebImageCodersManager</code> 这个类的编码（<code>encodedDataWithImage:format:options:</code>）方法，遍历 self.coders 数组，使用唯一的类：<br>3、<code>SDWebImageImageIOCoder</code> 的编码方法（<code>encodedDataWithImage:format:</code>）将 image 按 PNG 格式编码成了 NSData，完成这个方法图片就彻底变成了静态图片，后续存到了磁盘里。<strong>编码方法即为关键代码</strong>：<br><figure class="highlight objectivec"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br></pre></td><td class="code"><pre><span class="line">- (<span class="built_in">NSData</span> *)encodedDataWithImage:(<span class="built_in">UIImage</span> *)image format:(SDImageFormat)format &#123;</span><br><span class="line">    <span class="keyword">if</span> (!image) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">nil</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">if</span> (format == SDImageFormatUndefined) &#123;</span><br><span class="line">        <span class="built_in">BOOL</span> hasAlpha = SDCGImageRefContainsAlpha(image.CGImage);</span><br><span class="line">        <span class="keyword">if</span> (hasAlpha) &#123;</span><br><span class="line">            format = SDImageFormatPNG;</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            format = SDImageFormatJPEG;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="built_in">NSMutableData</span> *imageData = [<span class="built_in">NSMutableData</span> data];</span><br><span class="line">    <span class="built_in">CFStringRef</span> imageUTType = [<span class="built_in">NSData</span> sd_UTTypeFromSDImageFormat:format];</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// Create an image destination.</span></span><br><span class="line">    <span class="built_in">CGImageDestinationRef</span> imageDestination = <span class="built_in">CGImageDestinationCreateWithData</span>((__bridge <span class="built_in">CFMutableDataRef</span>)imageData, imageUTType, <span class="number">1</span>, <span class="literal">NULL</span>);</span><br><span class="line">    <span class="keyword">if</span> (!imageDestination) &#123;</span><br><span class="line">        <span class="comment">// Handle failure.</span></span><br><span class="line">        <span class="keyword">return</span> <span class="literal">nil</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="built_in">NSMutableDictionary</span> *properties = [<span class="built_in">NSMutableDictionary</span> dictionary];</span><br><span class="line"><span class="meta">#if SD_UIKIT || SD_WATCH</span></span><br><span class="line">    <span class="built_in">NSInteger</span> exifOrientation = [SDWebImageCoderHelper exifOrientationFromImageOrientation:image.imageOrientation];</span><br><span class="line">    [properties setValue:@(exifOrientation) forKey:(__bridge <span class="built_in">NSString</span> *)kCGImagePropertyOrientation];</span><br><span class="line"><span class="meta">#endif</span></span><br><span class="line">    </span><br><span class="line">    <span class="comment">// Add your image to the destination.</span></span><br><span class="line">    <span class="built_in">CGImageDestinationAddImage</span>(imageDestination, image.CGImage, (__bridge <span class="built_in">CFDictionaryRef</span>)properties);</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// Finalize the destination.</span></span><br><span class="line">    <span class="keyword">if</span> (<span class="built_in">CGImageDestinationFinalize</span>(imageDestination) == <span class="literal">NO</span>) &#123;</span><br><span class="line">        <span class="comment">// Handle failure.</span></span><br><span class="line">        imageData = <span class="literal">nil</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="built_in">CFRelease</span>(imageDestination);</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> [imageData <span class="keyword">copy</span>];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>所以这一步存储的时候如果可以通过 <code>sd_imageFormat</code> 拿到正确的 GIF 格式的话，在存储时按照 GIF 格式编码，那么下次取出来动图还是可以生效的。</p><h2 id="v5-x"><a href="#v5-x" class="headerlink" title="v5.x"></a>v5.x</h2><p>在 v5.x 的代码里，仍然是 <code>SDWebImageDownloaderOperation</code> 这个类负责下载数据，在 <a href="https://github.com/SDWebImage/SDWebImage/blob/aa7ff6f060d8f34d2c66a716aefff1a3bc9ef816/SDWebImage/Core/SDWebImageDownloaderOperation.m#L464" target="_blank" rel="noopener">#L464</a> 这行处理 NSData 数据。继续查看 <code>SDImageLoader</code> 这个类提供的 <code>SDImageLoaderDecodeImageData</code> 方法：<br><figure class="highlight mel"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line">...</span><br><span class="line"><span class="keyword">if</span> (!decodeFirstFrame) &#123;</span><br><span class="line">    <span class="comment">// check whether we should use `SDAnimatedImage`</span></span><br><span class="line">    Class animatedImageClass = context[SDWebImageContextAnimatedImageClass];</span><br><span class="line">    <span class="keyword">if</span> ([animatedImageClass isSubclassOfClass:[UIImage class]] &amp;&amp; [animatedImageClass conformsToProtocol:@protocol(SDAnimatedImage)]) &#123;</span><br><span class="line">        <span class="keyword">image</span> = [[animatedImageClass alloc] initWithData:imageData <span class="keyword">scale</span>:<span class="keyword">scale</span> options:coderOptions];</span><br><span class="line">        <span class="keyword">if</span> (<span class="keyword">image</span>) &#123;</span><br><span class="line">            <span class="comment">// Preload frames if supported</span></span><br><span class="line">            <span class="keyword">if</span> (options &amp; SDWebImagePreloadAllFrames &amp;&amp; [<span class="keyword">image</span> respondsToSelector:@selector(preloadAllFrames)]) &#123;</span><br><span class="line">                [((id&lt;SDAnimatedImage&gt;)<span class="keyword">image</span>) preloadAllFrames];</span><br><span class="line">            &#125;</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="comment">// Check image class matching</span></span><br><span class="line">            <span class="keyword">if</span> (options &amp; SDWebImageMatchAnimatedImageClass) &#123;</span><br><span class="line">                <span class="keyword">return</span> nil;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">if</span> (!<span class="keyword">image</span>) &#123;</span><br><span class="line">    <span class="keyword">image</span> = [[SDImageCodersManager sharedManager] decodedImageWithData:imageData options:coderOptions];</span><br><span class="line">&#125;</span><br><span class="line">...</span><br></pre></td></tr></table></figure></p><p>在这个方法里判断是否要用 <code>SDAnimatedImage</code> 这个类来解码，主要给 <code>SDAnimatedImageView</code> 或者 <code>FLAnimatedImageView</code> 展示动图时用的。</p><p>这里也是和 v4.x 设计不同的地方：v4.x 是在 <code>FLAnimatedImageView+WebCache</code> 分类里拿到回调的 image 和 data 之后再将 data 交由 <code>FLAnimatedImage</code> 处理，而 v5.x 由自己处理动图的编解码工作，在 <code>SDAnimatedImageView+WebCache</code> / <code>FLAnimatedImageView+WebCache</code> 分类里下载图片一开始打上 <code>SDWebImageContextAnimatedImageClass</code> 的标记，然后在当前方法里判断标记从而决定是否使用 <code>SDAnimatedImage</code> 解码。</p><p>我们这里是直接使用的 <code>SDWebImageDownloader</code> 下载图片，所以最终会由 <code>SDImageCodersManager</code> 这个类的 <code>decodedImageWithData:options:</code>方法来解码：<br><figure class="highlight objectivec"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line">- (<span class="built_in">UIImage</span> *)decodedImageWithData:(<span class="built_in">NSData</span> *)data options:(<span class="keyword">nullable</span> SDImageCoderOptions *)options &#123;</span><br><span class="line">    <span class="keyword">if</span> (!data) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">nil</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">UIImage</span> *image;</span><br><span class="line">    <span class="built_in">NSArray</span>&lt;<span class="keyword">id</span>&lt;SDImageCoder&gt;&gt; *coders = <span class="keyword">self</span>.coders;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">id</span>&lt;SDImageCoder&gt; coder <span class="keyword">in</span> coders.reverseObjectEnumerator) &#123;</span><br><span class="line">        <span class="keyword">if</span> ([coder canDecodeFromData:data]) &#123;</span><br><span class="line">            image = [coder decodedImageWithData:data options:options];</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> image;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>这里就和 v4.x 很相似了，遍历 self.coders 寻找能够解码当前 NSData 的 coder，而此时 self.coders 数组里有 <code>SDImageIOCoder</code>、<code>SDImageGIFCoder</code>、<code>SDImageAPNGCoder</code> 三种 coder，当然匹配到 <code>SDImageGIFCoder</code> 来解码当前 data 了，其实走的也是它的父类（<code>SDImageIOAnimatedCoder</code>）方法：<code>decodedImageWithData:options:</code>，即为关键代码：<br><figure class="highlight objectivec"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br></pre></td><td class="code"><pre><span class="line"></span><br><span class="line">- (<span class="built_in">UIImage</span> *)decodedImageWithData:(<span class="built_in">NSData</span> *)data options:(<span class="keyword">nullable</span> SDImageCoderOptions *)options &#123;</span><br><span class="line">    <span class="keyword">if</span> (!data) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">nil</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">CGFloat</span> scale = <span class="number">1</span>;</span><br><span class="line">    <span class="built_in">NSNumber</span> *scaleFactor = options[SDImageCoderDecodeScaleFactor];</span><br><span class="line">    <span class="keyword">if</span> (scaleFactor != <span class="literal">nil</span>) &#123;</span><br><span class="line">        scale = MAX([scaleFactor doubleValue], <span class="number">1</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="built_in">CGSize</span> thumbnailSize = <span class="built_in">CGSizeZero</span>;</span><br><span class="line">    <span class="built_in">NSValue</span> *thumbnailSizeValue = options[SDImageCoderDecodeThumbnailPixelSize];</span><br><span class="line">    <span class="keyword">if</span> (thumbnailSizeValue != <span class="literal">nil</span>) &#123;</span><br><span class="line"><span class="meta">#if SD_MAC</span></span><br><span class="line">        thumbnailSize = thumbnailSizeValue.sizeValue;</span><br><span class="line"><span class="meta">#else</span></span><br><span class="line">        thumbnailSize = thumbnailSizeValue.CGSizeValue;</span><br><span class="line"><span class="meta">#endif</span></span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="built_in">BOOL</span> preserveAspectRatio = <span class="literal">YES</span>;</span><br><span class="line">    <span class="built_in">NSNumber</span> *preserveAspectRatioValue = options[SDImageCoderDecodePreserveAspectRatio];</span><br><span class="line">    <span class="keyword">if</span> (preserveAspectRatioValue != <span class="literal">nil</span>) &#123;</span><br><span class="line">        preserveAspectRatio = preserveAspectRatioValue.boolValue;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line"><span class="meta">#if SD_MAC</span></span><br><span class="line">    <span class="comment">// If don't use thumbnail, prefers the built-in generation of frames (GIF/APNG)</span></span><br><span class="line">    <span class="comment">// Which decode frames in time and reduce memory usage</span></span><br><span class="line">    <span class="keyword">if</span> (thumbnailSize.width == <span class="number">0</span> || thumbnailSize.height == <span class="number">0</span>) &#123;</span><br><span class="line">        SDAnimatedImageRep *imageRep = [[SDAnimatedImageRep alloc] initWithData:data];</span><br><span class="line">        <span class="built_in">NSSize</span> size = <span class="built_in">NSMakeSize</span>(imageRep.pixelsWide / scale, imageRep.pixelsHigh / scale);</span><br><span class="line">        imageRep.size = size;</span><br><span class="line">        <span class="built_in">NSImage</span> *animatedImage = [[<span class="built_in">NSImage</span> alloc] initWithSize:size];</span><br><span class="line">        [animatedImage addRepresentation:imageRep];</span><br><span class="line">        <span class="keyword">return</span> animatedImage;</span><br><span class="line">    &#125;</span><br><span class="line"><span class="meta">#endif</span></span><br><span class="line">    </span><br><span class="line">    <span class="built_in">CGImageSourceRef</span> source = <span class="built_in">CGImageSourceCreateWithData</span>((__bridge <span class="built_in">CFDataRef</span>)data, <span class="literal">NULL</span>);</span><br><span class="line">    <span class="keyword">if</span> (!source) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">nil</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    size_t count = <span class="built_in">CGImageSourceGetCount</span>(source);</span><br><span class="line">    <span class="built_in">UIImage</span> *animatedImage;</span><br><span class="line">    </span><br><span class="line">    <span class="built_in">BOOL</span> decodeFirstFrame = [options[SDImageCoderDecodeFirstFrameOnly] boolValue];</span><br><span class="line">    <span class="keyword">if</span> (decodeFirstFrame || count &lt;= <span class="number">1</span>) &#123;</span><br><span class="line">        animatedImage = [<span class="keyword">self</span>.class createFrameAtIndex:<span class="number">0</span> source:source scale:scale preserveAspectRatio:preserveAspectRatio thumbnailSize:thumbnailSize];</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="built_in">NSMutableArray</span>&lt;SDImageFrame *&gt; *frames = [<span class="built_in">NSMutableArray</span> array];</span><br><span class="line">        </span><br><span class="line">        <span class="keyword">for</span> (size_t i = <span class="number">0</span>; i &lt; count; i++) &#123;</span><br><span class="line">            <span class="built_in">UIImage</span> *image = [<span class="keyword">self</span>.class createFrameAtIndex:i source:source scale:scale preserveAspectRatio:preserveAspectRatio thumbnailSize:thumbnailSize];</span><br><span class="line">            <span class="keyword">if</span> (!image) &#123;</span><br><span class="line">                <span class="keyword">continue</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            </span><br><span class="line">            <span class="built_in">NSTimeInterval</span> duration = [<span class="keyword">self</span>.class frameDurationAtIndex:i source:source];</span><br><span class="line">            </span><br><span class="line">            SDImageFrame *frame = [SDImageFrame frameWithImage:image duration:duration];</span><br><span class="line">            [frames addObject:frame];</span><br><span class="line">        &#125;</span><br><span class="line">        </span><br><span class="line">        <span class="built_in">NSUInteger</span> loopCount = [<span class="keyword">self</span>.class imageLoopCountWithSource:source];</span><br><span class="line">        </span><br><span class="line">        animatedImage = [SDImageCoderHelper animatedImageWithFrames:frames];</span><br><span class="line">        animatedImage.sd_imageLoopCount = loopCount;</span><br><span class="line">    &#125;</span><br><span class="line">    animatedImage.sd_imageFormat = <span class="keyword">self</span>.class.imageFormat;</span><br><span class="line">    <span class="built_in">CFRelease</span>(source);</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> animatedImage;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>在这个方法拿到 GIF 的每一帧处理成一组 <code>SDImageFrame</code> 类组成的数组，交由 <code>SDImageCoderHelper</code> 这个类处理：<br><figure class="highlight objectivec"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br></pre></td><td class="code"><pre><span class="line">+ (<span class="built_in">UIImage</span> *)animatedImageWithFrames:(<span class="built_in">NSArray</span>&lt;SDImageFrame *&gt; *)frames &#123;</span><br><span class="line">    <span class="built_in">NSUInteger</span> frameCount = frames.count;</span><br><span class="line">    <span class="keyword">if</span> (frameCount == <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">nil</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="built_in">UIImage</span> *animatedImage;</span><br><span class="line">    </span><br><span class="line"><span class="meta">#if SD_UIKIT || SD_WATCH</span></span><br><span class="line">    <span class="built_in">NSUInteger</span> durations[frameCount];</span><br><span class="line">    <span class="keyword">for</span> (size_t i = <span class="number">0</span>; i &lt; frameCount; i++) &#123;</span><br><span class="line">        durations[i] = frames[i].duration * <span class="number">1000</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">NSUInteger</span> <span class="keyword">const</span> gcd = gcdArray(frameCount, durations);</span><br><span class="line">    __block <span class="built_in">NSUInteger</span> totalDuration = <span class="number">0</span>;</span><br><span class="line">    <span class="built_in">NSMutableArray</span>&lt;<span class="built_in">UIImage</span> *&gt; *animatedImages = [<span class="built_in">NSMutableArray</span> arrayWithCapacity:frameCount];</span><br><span class="line">    [frames enumerateObjectsUsingBlock:^(SDImageFrame * _Nonnull frame, <span class="built_in">NSUInteger</span> idx, <span class="built_in">BOOL</span> * _Nonnull stop) &#123;</span><br><span class="line">        <span class="built_in">UIImage</span> *image = frame.image;</span><br><span class="line">        <span class="built_in">NSUInteger</span> duration = frame.duration * <span class="number">1000</span>;</span><br><span class="line">        totalDuration += duration;</span><br><span class="line">        <span class="built_in">NSUInteger</span> repeatCount;</span><br><span class="line">        <span class="keyword">if</span> (gcd) &#123;</span><br><span class="line">            repeatCount = duration / gcd;</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            repeatCount = <span class="number">1</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">for</span> (size_t i = <span class="number">0</span>; i &lt; repeatCount; ++i) &#123;</span><br><span class="line">            [animatedImages addObject:image];</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;];</span><br><span class="line">    </span><br><span class="line">    animatedImage = [<span class="built_in">UIImage</span> animatedImageWithImages:animatedImages duration:totalDuration / <span class="number">1000.</span>f];</span><br><span class="line">    </span><br><span class="line"><span class="meta">#else</span></span><br><span class="line">    </span><br><span class="line">    <span class="built_in">NSMutableData</span> *imageData = [<span class="built_in">NSMutableData</span> data];</span><br><span class="line">    <span class="built_in">CFStringRef</span> imageUTType = [<span class="built_in">NSData</span> sd_UTTypeFromImageFormat:SDImageFormatGIF];</span><br><span class="line">    <span class="comment">// Create an image destination. GIF does not support EXIF image orientation</span></span><br><span class="line">    <span class="built_in">CGImageDestinationRef</span> imageDestination = <span class="built_in">CGImageDestinationCreateWithData</span>((__bridge <span class="built_in">CFMutableDataRef</span>)imageData, imageUTType, frameCount, <span class="literal">NULL</span>);</span><br><span class="line">    <span class="keyword">if</span> (!imageDestination) &#123;</span><br><span class="line">        <span class="comment">// Handle failure.</span></span><br><span class="line">        <span class="keyword">return</span> <span class="literal">nil</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">for</span> (size_t i = <span class="number">0</span>; i &lt; frameCount; i++) &#123;</span><br><span class="line">        <span class="keyword">@autoreleasepool</span> &#123;</span><br><span class="line">            SDImageFrame *frame = frames[i];</span><br><span class="line">            <span class="built_in">NSTimeInterval</span> frameDuration = frame.duration;</span><br><span class="line">            <span class="built_in">CGImageRef</span> frameImageRef = frame.image.CGImage;</span><br><span class="line">            <span class="built_in">NSDictionary</span> *frameProperties = @&#123;(__bridge <span class="built_in">NSString</span> *)kCGImagePropertyGIFDictionary : @&#123;(__bridge <span class="built_in">NSString</span> *)kCGImagePropertyGIFDelayTime : @(frameDuration)&#125;&#125;;</span><br><span class="line">            <span class="built_in">CGImageDestinationAddImage</span>(imageDestination, frameImageRef, (__bridge <span class="built_in">CFDictionaryRef</span>)frameProperties);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// Finalize the destination.</span></span><br><span class="line">    <span class="keyword">if</span> (<span class="built_in">CGImageDestinationFinalize</span>(imageDestination) == <span class="literal">NO</span>) &#123;</span><br><span class="line">        <span class="comment">// Handle failure.</span></span><br><span class="line">        <span class="built_in">CFRelease</span>(imageDestination);</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">nil</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">CFRelease</span>(imageDestination);</span><br><span class="line">    <span class="built_in">CGFloat</span> scale = MAX(frames.firstObject.image.scale, <span class="number">1</span>);</span><br><span class="line">    </span><br><span class="line">    SDAnimatedImageRep *imageRep = [[SDAnimatedImageRep alloc] initWithData:imageData];</span><br><span class="line">    <span class="built_in">NSSize</span> size = <span class="built_in">NSMakeSize</span>(imageRep.pixelsWide / scale, imageRep.pixelsHigh / scale);</span><br><span class="line">    imageRep.size = size;</span><br><span class="line">    animatedImage = [[<span class="built_in">NSImage</span> alloc] initWithSize:size];</span><br><span class="line">    [animatedImage addRepresentation:imageRep];</span><br><span class="line"><span class="meta">#endif</span></span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> animatedImage;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>最终调用的<strong>关键代码</strong>就是：<figure class="highlight ebnf"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="attribute">animatedImage</span> = [UIImage animatedImageWithImages:animatedImages duration:totalDuration / 1000.f];</span><br></pre></td></tr></table></figure></p><p>option 键看一下这个方法的解释：<img src="http://image.gonghonglou.com/sdwebimage-gif/animatedImage.png" alt="animatedImageWithImages:"></p><p>控制台打印下被上述方法处理过后的 animatedImage，看看它具体变成了什么：<br><figure class="highlight dust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="xml">(lldb) po animatedImage</span></span><br><span class="line"><span class="xml"><span class="tag">&lt;<span class="name">_UIAnimatedImage:0x600002040aa0</span> <span class="attr">anonymous</span> </span></span><span class="template-variable">&#123;128, 128&#125;</span><span class="xml"><span class="tag">&gt;</span></span></span><br><span class="line"><span class="xml"></span></span><br><span class="line"><span class="xml">(lldb)</span></span><br></pre></td></tr></table></figure></p><p>这个 <code>_UIAnimatedImage</code> 类就是为什么直接将该类设置给 UIImageView.image 也是能够展示动图效果的原因了（注意这个类苹果并没有暴露给开发者使用）。</p><p>这里的 image 就是最终回调的 image 了，然后使用<br>1、<code>SDImageCache</code> 类的 <code>storeImage:forKey:completion:</code> 方法将该 image 存到磁盘的时候，还是调用的<br>2、<code>SDWebImageCodersManager</code> 这个类的编码（<code>encodedDataWithImage:format:options:</code>）方法，遍历 self.coders 数组（<code>SDImageIOCoder</code>、<code>SDImageGIFCoder</code>、<code>SDImageAPNGCoder</code>），当前版本v5.5.2使用 <code>SDImageAPNGCoder</code>，后期版本，预计v5.6.0改正之后会使用 <code>SDImageGIFCoder</code> 来编码，其实都是调用它们的父类：<br>3、<code>SDImageIOAnimatedCoder</code> 的编码方法（<code>encodedDataWithImage:format:options:</code>）将 _UIAnimatedImage 由：<br>4、<code>SDImageCoderHelper</code> 根据 image 取出一组 <code>SDImageFrame</code> 类（对 UIImage 的封装）组成的数组，并将这一组 UIImage 编码成合理的 NSData，后续存到了磁盘里。<strong>编码方法即为关键代码</strong>：<br><figure class="highlight objectivec"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br></pre></td><td class="code"><pre><span class="line">- (<span class="built_in">NSData</span> *)encodedDataWithImage:(<span class="built_in">UIImage</span> *)image format:(SDImageFormat)format options:(<span class="keyword">nullable</span> SDImageCoderOptions *)options &#123;</span><br><span class="line">    <span class="keyword">if</span> (!image) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">nil</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">if</span> (format != <span class="keyword">self</span>.class.imageFormat) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">nil</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="built_in">NSMutableData</span> *imageData = [<span class="built_in">NSMutableData</span> data];</span><br><span class="line">    <span class="built_in">CFStringRef</span> imageUTType = [<span class="built_in">NSData</span> sd_UTTypeFromImageFormat:format];</span><br><span class="line">    <span class="built_in">NSArray</span>&lt;SDImageFrame *&gt; *frames = [SDImageCoderHelper framesFromAnimatedImage:image];</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// Create an image destination. Animated Image does not support EXIF image orientation TODO</span></span><br><span class="line">    <span class="comment">// The `CGImageDestinationCreateWithData` will log a warning when count is 0, use 1 instead.</span></span><br><span class="line">    <span class="built_in">CGImageDestinationRef</span> imageDestination = <span class="built_in">CGImageDestinationCreateWithData</span>((__bridge <span class="built_in">CFMutableDataRef</span>)imageData, imageUTType, frames.count ?: <span class="number">1</span>, <span class="literal">NULL</span>);</span><br><span class="line">    <span class="keyword">if</span> (!imageDestination) &#123;</span><br><span class="line">        <span class="comment">// Handle failure.</span></span><br><span class="line">        <span class="keyword">return</span> <span class="literal">nil</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">NSMutableDictionary</span> *properties = [<span class="built_in">NSMutableDictionary</span> dictionary];</span><br><span class="line">    <span class="keyword">double</span> compressionQuality = <span class="number">1</span>;</span><br><span class="line">    <span class="keyword">if</span> (options[SDImageCoderEncodeCompressionQuality]) &#123;</span><br><span class="line">        compressionQuality = [options[SDImageCoderEncodeCompressionQuality] doubleValue];</span><br><span class="line">    &#125;</span><br><span class="line">    properties[(__bridge <span class="built_in">NSString</span> *)kCGImageDestinationLossyCompressionQuality] = @(compressionQuality);</span><br><span class="line">    </span><br><span class="line">    <span class="built_in">BOOL</span> encodeFirstFrame = [options[SDImageCoderEncodeFirstFrameOnly] boolValue];</span><br><span class="line">    <span class="keyword">if</span> (encodeFirstFrame || frames.count == <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="comment">// for static single images</span></span><br><span class="line">        <span class="built_in">CGImageDestinationAddImage</span>(imageDestination, image.CGImage, (__bridge <span class="built_in">CFDictionaryRef</span>)properties);</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="comment">// for animated images</span></span><br><span class="line">        <span class="built_in">NSUInteger</span> loopCount = image.sd_imageLoopCount;</span><br><span class="line">        <span class="built_in">NSDictionary</span> *containerProperties = @&#123;<span class="keyword">self</span>.class.loopCountProperty : @(loopCount)&#125;;</span><br><span class="line">        properties[<span class="keyword">self</span>.class.dictionaryProperty] = containerProperties;</span><br><span class="line">        <span class="built_in">CGImageDestinationSetProperties</span>(imageDestination, (__bridge <span class="built_in">CFDictionaryRef</span>)properties);</span><br><span class="line">        </span><br><span class="line">        <span class="keyword">for</span> (size_t i = <span class="number">0</span>; i &lt; frames.count; i++) &#123;</span><br><span class="line">            SDImageFrame *frame = frames[i];</span><br><span class="line">            <span class="built_in">NSTimeInterval</span> frameDuration = frame.duration;</span><br><span class="line">            <span class="built_in">CGImageRef</span> frameImageRef = frame.image.CGImage;</span><br><span class="line">            <span class="built_in">NSDictionary</span> *frameProperties = @&#123;<span class="keyword">self</span>.class.dictionaryProperty : @&#123;<span class="keyword">self</span>.class.delayTimeProperty : @(frameDuration)&#125;&#125;;</span><br><span class="line">            <span class="built_in">CGImageDestinationAddImage</span>(imageDestination, frameImageRef, (__bridge <span class="built_in">CFDictionaryRef</span>)frameProperties);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// Finalize the destination.</span></span><br><span class="line">    <span class="keyword">if</span> (<span class="built_in">CGImageDestinationFinalize</span>(imageDestination) == <span class="literal">NO</span>) &#123;</span><br><span class="line">        <span class="comment">// Handle failure.</span></span><br><span class="line">        imageData = <span class="literal">nil</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="built_in">CFRelease</span>(imageDestination);</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> [imageData <span class="keyword">copy</span>];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>以上，就是为什么同样存储为 0x89，v4.x 版本是静态图片，而 v5.x 是动图的原因。并且分析了 SD 的处理逻辑：v4.x &amp; v5.x 都是由下载模块下载资源拿到 data 交由编解码模块解码成 image 回调使用；存储过程又是将 image 交由编解码模块编码成 data 存储到磁盘。</p><p>需要注意的版本差异是：<br>使用 <code>[[SDImageCache sharedImageCache] storeImage:image forKey:key completion:nil];</code> 方法存储 GIF 时，从磁盘再次取出该图片：<br>v5.x 得益于新的编解码方法会保留动图效果，其中 v5.6.0 之前虽然有动图效果，但取出的 NSData 头信息仍然是 0x89 即 PNG 格式；v5.6.0 之后会修正为 0x47 GIF 格式（根据 SD 维护者提供的预计版本，见 <a href="https://github.com/SDWebImage/SDWebImage/issues/2952" target="_blank" rel="noopener">Issue #2952</a> 和 <a href="https://github.com/SDWebImage/SDWebImage/pull/2953" target="_blank" rel="noopener">PR #2953</a>）<br>v4.x 则会失去动图效果</p><p>再次需要注意的是：</p><p>使用以上方法所存储的 image 如果是 SD 经手过的，由 NSData 类型转换来的 UIImage，SD 会给它打上 <code>sd_imageFormat</code> 标签，即能够正确识别图片类型。如果并非 SD 经手的话，即没有 <code>sd_imageFormat</code> 标签的情况，该方法还是会将图片编码为 PNG 或者 JPEG，除非使用 <code>[[SDImageCache sharedImageCache] storeImage:image imageData:imageData forKey:key toDisk:YES completion:nil];</code> 方法，同时传入 image 和 imageData。</p><p>当然，使用该方法传入 imageData 的话，存储过程中就节省了 image 转 data 的操作，这就是 API 易用性和高性能的选择了。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;之前工作中遇到的问题，使用 SDWebImage（v4.4.8）下载并存储了一份 GIF 图片，第一次是可以正常显示的，但之后再从缓存中取出的时候就变成了静态图片。debug 发现是因为从磁盘里取出该图片时已经是 PNG 格式的了。为了解决这个问题所以去仔细看了下 SD 对
      
    
    </summary>
    
      <category term="iOS" scheme="http://gonghonglou.com/categories/iOS/"/>
    
    
  </entry>
  
  <entry>
    <title>Say Bye 2019</title>
    <link href="http://gonghonglou.com/2019/12/29/saybye2019/"/>
    <id>http://gonghonglou.com/2019/12/29/saybye2019/</id>
    <published>2019-12-29T13:59:42.000Z</published>
    <updated>2020-03-01T13:47:33.198Z</updated>
    
    <content type="html"><![CDATA[<p>遇见十二月，又到年终总结！</p><p> .<br> .<br> .<br> .<br> .<br> .<br><strong><em> 创建于 19 年底，更新于 20 年初，不是美丽的新年，失去力气 Say Bye。 </em></strong></p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;遇见十二月，又到年终总结！&lt;/p&gt;
&lt;p&gt; .&lt;br&gt; .&lt;br&gt; .&lt;br&gt; .&lt;br&gt; .&lt;br&gt; .&lt;br&gt;&lt;strong&gt;&lt;em&gt; 创建于 19 年底，更新于 20 年初，不是美丽的新年，失去力气 Say Bye。 &lt;/em&gt;&lt;/strong&gt;&lt;/p&gt;

      
    
    </summary>
    
      <category term="Life" scheme="http://gonghonglou.com/categories/Life/"/>
    
    
  </entry>
  
  <entry>
    <title>组件化分发生命周期</title>
    <link href="http://gonghonglou.com/2019/08/29/pod-lifecycle/"/>
    <id>http://gonghonglou.com/2019/08/29/pod-lifecycle/</id>
    <published>2019-08-29T13:50:33.000Z</published>
    <updated>2020-09-06T12:20:12.664Z</updated>
    
    <content type="html"><![CDATA[<p>组件化分发生命周期 方案实现探索系列文章：<br><strong>1、<a href="http://gonghonglou.com/2019/08/29/pod-lifecycle/">组件化分发生命周期</a></strong><br>2、<a href="http://gonghonglou.com/2020/03/08/pod-lifecycle-aop/">组件化分发生命周期 - AOP 方案</a><br>3、<a href="http://gonghonglou.com/2020/09/06/pod-lifecycle-aop-libffi/">组件化分发生命周期 - AOP 方案（libffi 实现）</a></p><h1 id="是什么"><a href="#是什么" class="headerlink" title="是什么"></a>是什么</h1><p>组件化分发生命周期是什么？就是将主工程的生命周期分发到各个组件里去。直观些的介绍则是：AppDelegate 遵循并实现了 UIApplicationDelegate 代理，其中包括 <code>willFinishLaunchingWithOptions:</code>、<code>didFinishLaunchingWithOptions:</code>、<code>applicationWillEnterForeground:</code>、<code>applicationDidEnterBackground:</code> 等等方法，包含了主工程的各个阶段将执行的方法，我们要做的就是在主工程的这些阶段方法被执行的时候，各个组件里相对应的阶段方法同时会被执行，这样，主工程和各个组件便共享了生命周期，</p><h1 id="为什么"><a href="#为什么" class="headerlink" title="为什么"></a>为什么</h1><p>至于为什么要将主工程的生命周期分发到各个组件中，原因有以下几点：</p><p>1、替换 load 方法<br>因为 load 方法时机较早，所有很多时候会在 load 方法里执行注册，初始化等操作，但这也会导致 load 方法的滥用，将一些本可以靠后执行的操作提前执行了，可能引发 APP 启动耗时过长的问题，需要做 load 耗时监测，治理起来困难，所以很多团队是禁用 load 方法的。<br>将这些操作方法放到生命周期方法里去做显然更好，寻找合理的时机执行相应的操作，耗时检测功能也比较好做。</p><p>2、解决 AppDelegate 臃肿问题<br>工程中难免有一系列的注册、初始化操作，比如：APP 性能检测、bug 收集、打点等一系列工具的注册；各种基础组件涉及的初始化或重置操作。<br>将这些操作放到组件自己的生命周期方法里去执行，避免了 AppDelegate 的臃肿，而且各基础组件与主工程解耦，开发维护更方便。</p><p>3、Debugger 类组件可插拔<br>某些 Debugger 类组件在工作前可能需要注册操作，将注册操作放在 Pod 自己的生命周期里。这样一来，对于 Debugger 类组件只需要在 Podfile 里控制加载形式，即可做到 Debug/Release 环境组件可插拔，如：<br><figure class="highlight ruby"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">pod <span class="string">'DebuggerKit'</span>, <span class="symbol">:configurations</span> =&gt; [<span class="string">'Debug'</span>]</span><br></pre></td></tr></table></figure></p><h1 id="怎么做"><a href="#怎么做" class="headerlink" title="怎么做"></a>怎么做</h1><p>相比于将 AppDelegate 里的所有阶段方法分发出去，先介绍两种相对轻量的做法，也能做到和分发生命周期类似的能力：</p><h2 id="1、sunnyxx-的-Notification-Once"><a href="#1、sunnyxx-的-Notification-Once" class="headerlink" title="1、sunnyxx 的 Notification Once"></a>1、sunnyxx 的 Notification Once</h2><p>巧妙的通知注册<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">+ (void)load &#123;</span><br><span class="line">    __block id observer =</span><br><span class="line">    [[NSNotificationCenter defaultCenter]</span><br><span class="line">     addObserverForName:UIApplicationDidFinishLaunchingNotification</span><br><span class="line">     object:nil</span><br><span class="line">     queue:nil</span><br><span class="line">     usingBlock:^(NSNotification *note) &#123;</span><br><span class="line">         [self setup]; // Do whatever you want</span><br><span class="line">         [[NSNotificationCenter defaultCenter] removeObserver:observer];</span><br><span class="line">     &#125;];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>很巧妙的方法，优点很明显：轻量！虽然侵入了 load 方法，不过如果没有 load 的滥用的话也可以接受，毕竟只是在 load 里执行了注册行为，具体的执行时机还是 <code>UIApplicationDidFinishLaunchingNotification</code></p><p>缺点是该方法的响应是在 <code>- application:didFinishLaunchingWithOptions:</code> 调用完成后发送，时机没法精确控制，因为有的时候因为时机问题，我们想让各种 Pod 里的注册操作在 AppDelegate 的 <code>didFinishLaunchingWithOptions:</code> 方法靠前执行，即先执行组件里的注册操作再执行 AppDelegate 里的操作。</p><p>参考原文：<a href="https://blog.sunnyxx.com/2015/03/09/notification-once/" target="_blank" rel="noopener">Notification Once</a></p><h2 id="2、美团的-Kylin-注册函数"><a href="#2、美团的-Kylin-注册函数" class="headerlink" title="2、美团的 Kylin 注册函数"></a>2、美团的 Kylin 注册函数</h2><p>在编译时把数据（如函数指针）写入到可执行文件的 __DATA 段中，在运行时的某个阶段（如 willFinishLaunch）再从 __DATA 段取出数据进行相应的操作（调用函数）。</p><blockquote><p>Kylin实现原理简述：Clang 提供了很多的编译器函数，它们可以完成不同的功能。其中一种就是 section() 函数，section()函数提供了二进制段的读写能力，它可以将一些编译期就可以确定的常量写入数据段。 在具体的实现中，主要分为编译期和运行时两个部分。在编译期，编译器会将标记了 <strong>attribute</strong>((section())) 的数据写到指定的数据段中，例如写一个 {key(key代表不同的启动阶段), *pointer} 对到数据段。到运行时，在合适的时间节点，在根据key读取出函数指针，完成函数的调用。</p></blockquote><p>这种方案呢，最好去写个专门的工具，如 Kylin，去实现 {key, *pointer} 对的注册和调用操作。对于使用方来说，添加一种函数执行时，使用 Kylin 注册，并在 AppDelegate 的合理阶段调起方法。</p><p>参考原文：<a href="https://tech.meituan.com/2018/12/06/waimai-ios-optimizing-startup.html" target="_blank" rel="noopener">美团外卖iOS App冷启动治理</a></p><p>除了以上方法之外还有一些比较“大型”的做法就是把 AppDelegate 的生命周期完整的分发出去：</p><h2 id="3、手动注册、遍历分发"><a href="#3、手动注册、遍历分发" class="headerlink" title="3、手动注册、遍历分发"></a>3、手动注册、遍历分发</h2><p>这是我之前公司的做法，提前注册 Lifecycle 类（可实现 AppDelegate 的各阶段方法），在 AppDelegate 各阶段方法执行的同时遍历 lifecycle 类执行相应方法。具体的做法是，</p><p>1）项目中存在一份配置文件，文件里配置着各个 pod 的 Lifecycle 类名，该类里实现了 AppDelegate 的某几个阶段方法。</p><p>2）项目启动的时候加载这份配置文件，根据类名反射成 Lifecycle 类，将所有的类添加到一个数组中（LifecycleArray）。</p><p>3）在 AppDelegate 和 UIResponder 的继承中间加一个 MyAppDelegate 类（GHLAppDelegate : MyAppDelegate : UIResponder），该类拥有 AppDelegate 的所有方法，在每个阶段的方法里遍历 LifecycleArray 数组，调用各个 Lifecycle 类的本阶段方法。</p><p>4）在 AppDelegate 的各阶段方法里首先调用一下 super 方法。</p><p>这样，在 AppDelegate 各阶段执行的时候就会执行父类方法，遍历所有 pod 里的 Lifecycle 类，执行相应方法，从而实现生命周期的分发。<br><img src="http://image.gonghonglou.com/pod-lifecycle/registe-for.png" alt=""></p><p>这种做法的优点是没什么骚操作（姑且算优点吧），都是基本方法遍历调用，就一个反射操作也算常用吧。弊端就显而易见了：</p><p>1）需要注册行为。每添加一个 pod，想要为该 Pod 配置生命周期管理类的话都要去配置文件里注册一次。虽然项目稳定下来后 pod 基本不会变动，但使用起来总归不够理想，而且因为配置文件的存在，这种中心化的写法会导致代码臃肿，阅读维护困难。<br>2）侵入 AppDelegate 类。需要更改 AppDelegate 的父类，并且在 AppDelegate 的各阶段里调用 super。</p><h2 id="4、Category-覆盖、追加方法⚠️"><a href="#4、Category-覆盖、追加方法⚠️" class="headerlink" title="4、Category 覆盖、追加方法⚠️"></a>4、Category 覆盖、追加方法⚠️</h2><p>因为上一种方案中存在的问题，所以我在想怎么做既可以不用注册，又不用侵入 AppDelegate 呢？Category！我想到这种方案：</p><p>1）新建 Lifecycle 类，用于向相应的 pod（第三步提到的建过分类的 Pod） 分发生命周期。该类拥有 AppDelegate 的所有生命周期方法。</p><p>2）为了不侵入 AppDelegate，给 AppDelegate 添加分类（AppDelegate+Lifecycle），用于在相应阶段调用 Lifecycle 相应方法。在该分类里重写 AppDelegate 的各阶段方法，在各个方法里分别调用 Lifecycle 类的对应方法（这里其实也可以在分类里 hook 本类的方法来实现，但是为了写法方便 Demo 里的做法是使用了第四步提供的方法：遍历方法列表找到最后一个方法执行）。</p><p>3）对使用方来说，只需要在自己的 pod 里新建 Lifecycle 类的分类（如：Lifecycle + Home、Lifecycle + Deatil 等），复写本类的方法，即 AppDelegate 的生命周期方法，这些 pod 里的这些分类的这些方法会被 Lifecycle 类全部执行。</p><p>4）怎么全部执行呢？多个分类会根据加载顺序互相覆盖方法，正常情况下只执行最后加载的那个分类的方法，因为最后加载的分类的方法被最后加到方法列表里，消息发送过程中最先被找到。解决思路是：找到那些被覆盖的方法去执行对应的 IMP：</p><p>4.1）在 Lifecycle 本类里去遍历本类的方法列表，为了避免无限循环，除了本类的方法（即方法列表的最后一个），对其他的同名 SEL 都执行对应 IMP：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line">+ (void)execCategorySelector:(SEL)selector forClass:(Class)class withParam1:(id)param1 param2:(id)param2 &#123;</span><br><span class="line">    BOOL isFirst = NO;</span><br><span class="line">    unsigned int methodCount = 0;</span><br><span class="line">    Method *methods = class_copyMethodList(class, &amp;methodCount);</span><br><span class="line">    for (int i = methodCount - 1; i &gt;= 0; i--) &#123;</span><br><span class="line">        Method method = methods[i];</span><br><span class="line">        SEL sel = method_getName(method);</span><br><span class="line">        if ([NSStringFromSelector(sel) isEqualToString:NSStringFromSelector(selector)]) &#123;</span><br><span class="line">            if (!isFirst) &#123;</span><br><span class="line">                isFirst = YES;</span><br><span class="line">            &#125; else &#123;</span><br><span class="line">                IMP imp = method_getImplementation(method);</span><br><span class="line">                void (*func)(id, SEL,id,id) = (void *)imp;</span><br><span class="line">                func(self, sel, param1, param2);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    free(methods);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>4.2）为了写法方便和统一，这里给第二步也提供了执行 AppDelegate 本类方法的实现。在 AppDelegate 的方法列表里寻找同名的的最后一个 SEL，执行对应的 IMP。<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line">+ (void)execClassSelector:(SEL)selector forClass:(Class)class withParam1:(id)param1 param2:(id)param2 &#123;</span><br><span class="line">    unsigned int methodCount = 0;</span><br><span class="line">    Method *methods = class_copyMethodList(class, &amp;methodCount);</span><br><span class="line">    for (int i = methodCount - 1; i &gt;= 0; i--) &#123;</span><br><span class="line">        Method method = methods[i];</span><br><span class="line">        SEL sel = method_getName(method);</span><br><span class="line">        if ([NSStringFromSelector(sel) isEqualToString:NSStringFromSelector(selector)]) &#123;</span><br><span class="line">            IMP imp = method_getImplementation(method);</span><br><span class="line">            void (*func)(id, SEL,id,id) = (void *)imp;</span><br><span class="line">            func(self, sel, param1, param2);</span><br><span class="line">            break;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    free(methods);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>这样就能调用到所有 Category 的生命周期方法，起到分发的效果。<br><img src="http://image.gonghonglou.com/pod-lifecycle/category-for.png" alt=""></p><p>优点即是：<br>1）对使用方来说不必注册，只需创建 Lifecycle 的分类<br>2）不侵入 AppDelegate 代码</p><p>缺点是：<br>1）要手动在主工程创建 AppDelegate 分类。<br>2）所添加的分类里的同名方法不会被覆盖，有反常识。<br>3）还有个缺点，也是为什么在标题上加一个 ⚠️ 的原因，是因为给一个类添加多个 Category，并分别覆盖本类方法时 Xcode 会提示 warning</p><blockquote><p>Category is implementing a method which will also be implemented by its primary class</p></blockquote><h2 id="5、消息转发"><a href="#5、消息转发" class="headerlink" title="5、消息转发"></a>5、消息转发</h2><p>因为上一种方案中存在的问题，我在想还有什么更好的方案，只提供一个 Pod 组件，就可以完成所有操作的方案。然后找到了青木同学的 <a href="https://triplecc.github.io/2017/10/25/2017-10-25-zu-jian-sheng-ming-zhou-qi/" target="_blank" rel="noopener">组件化之组件生命周期管理</a> 这篇文章，实现方案在文章里已经讲的很详细了，思路就是：</p><p>1）新建 Module 类，提供注册功能，并且可以设置优先级。使用方在自己的 Pod 继承该类创建自己的生命周期管理类，并且在 load 方法调用 Module 类的注册方法。</p><p>2）新建 UIApplication 的分类：<code>UIApplication (Module)</code>，hook 掉 setDelegate: 方法，将代理设置给自己创建的类：ApplicationDelegateProxy。同时对包含所有注册类的数组根据优先级进行排序。</p><p>3）ApplicationDelegateProxy 类里不会实现 AppDelegate 里的那些方法，所以当系统来调用这些方法的时候，因为找不到 SEL 会进入消息转发过程：</p><p>3.1） <code>-respondsToSelector:</code>：系统内部会调用这个方法。<br>判断是否实现了对应的 UIApplicationDelegate 代理方法。重写该方法结合 AppDelegate 以及所有注册的 Module 判断是否有相应实现。</p><p>3.2）<code>-forwardingTargetForSelector:</code>： <code>-respondsToSelector:</code> 返回 YES ，便进入消息转发阶段，消息转发的第二步就是该方法。<br>判断要转发的方法是否为 UIApplicationDelegate 的代理方法，如果不是，并且 AppDelegate 能响应，把消息转发给 AppDelegate 去处理。</p><p>3.3）<code>-methodSignatureForSelector:</code> 和 <code>-forwardInvocation:</code> ：如果消息没有发给 AppDelegate，由自己来处理，将会这执行这些方法。<br>在这一步首先根据协议直接返回代理方法的签名，然后在 <code>-forwardInvocation:</code> 方法中，按照优先级，依次把消息转发给注册的模块。</p><p>3.4）消息转发中处理返回值为 BOOL 类型的情况。</p><p>这样就通过消息转发完成了生命周期的分发。已经是很不错的实现了，对外部文件没有侵入，唯一的缺点就是需要一个注册操作，而且还是在 load 方法里。</p><h2 id="6、最后的优化"><a href="#6、最后的优化" class="headerlink" title="6、最后的优化"></a>6、最后的优化</h2><p>我在想有什么方法可以去掉这个注册操作？如果我们让所有组件里控制生命周期的类都继承自 Lifecycle 类，那么我们通过获取 Lifecycle 的所有子类就能够完成注册操作了。思路很简单：通过 runtime 获取所有注册的类，遍历这些类判断其父类是否是 Lifecycle 类。<br><img src="http://image.gonghonglou.com/pod-lifecycle/forwarding-msg.png" alt=""></p><p>照着这样的思路又实现了一份优化过的代码，收集注册子类：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br></pre></td><td class="code"><pre><span class="line">- (instancetype)init &#123;</span><br><span class="line">    self = [super init];</span><br><span class="line">    if (self) &#123;</span><br><span class="line">        NSArray *stringArray = [self _findAllSubClass:[GHLLifecycle class]];</span><br><span class="line">        self.subClasses = [self _classArrayWithStringArray:stringArray];</span><br><span class="line">    &#125;</span><br><span class="line">    return self;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">- (NSArray *)_classArrayWithStringArray:(NSArray *)stringArray &#123;</span><br><span class="line">    NSMutableArray *classArray = [NSMutableArray new];</span><br><span class="line">    [stringArray enumerateObjectsUsingBlock:^(id  _Nonnull obj, NSUInteger idx, BOOL * _Nonnull stop) &#123;</span><br><span class="line">        Class cls = NSClassFromString(obj);</span><br><span class="line">        if (cls) [classArray addObject:[cls new]];</span><br><span class="line">    &#125;];</span><br><span class="line">    return [classArray copy];</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">- (NSArray *)_findAllSubClass:(Class)class &#123;</span><br><span class="line">    // 注册类的总数</span><br><span class="line">    int count = objc_getClassList(NULL, 0);</span><br><span class="line">    NSMutableArray *array = [NSMutableArray new];</span><br><span class="line">    // 获取所有已注册的类</span><br><span class="line">    Class *classes = (Class *)malloc(sizeof(Class) * count);</span><br><span class="line">    objc_getClassList(classes, count);</span><br><span class="line">    </span><br><span class="line">    for (int i = 0; i &lt; count; i++) &#123;</span><br><span class="line">        if (class == class_getSuperclass(classes[i])) &#123;</span><br><span class="line">            [array addObject:[NSString stringWithFormat:@&quot;%@&quot;, classes[i]]];</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    free(classes);</span><br><span class="line">    return array;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>消息转发过程：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br></pre></td><td class="code"><pre><span class="line">- (BOOL)_containsProtocolMethod:(SEL)selector &#123;</span><br><span class="line">    </span><br><span class="line">    unsigned int outCount = 0;</span><br><span class="line">    struct objc_method_description *methodDesc = protocol_copyMethodDescriptionList(@protocol(UIApplicationDelegate), NO, YES, &amp;outCount);</span><br><span class="line">    for (int idx = 0; idx &lt; outCount; idx++) &#123;</span><br><span class="line">        if (selector == methodDesc[idx].name) &#123;</span><br><span class="line">            free(methodDesc);</span><br><span class="line">            return YES;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    free(methodDesc);</span><br><span class="line">    return NO;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">- (BOOL)respondsToSelector:(SEL)aSelector &#123;</span><br><span class="line">    if ([self.realDelegate respondsToSelector:aSelector]) &#123;</span><br><span class="line">        return YES;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    for (GHLLifecycle *module in self.subClasses) &#123;</span><br><span class="line">        if ([self _containsProtocolMethod:aSelector] &amp;&amp; [module respondsToSelector:aSelector]) &#123;</span><br><span class="line">            return YES;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    return [super respondsToSelector:aSelector];</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">- (id)forwardingTargetForSelector:(SEL)aSelector &#123;</span><br><span class="line">    if (![self _containsProtocolMethod:aSelector] &amp;&amp; [self.realDelegate respondsToSelector:aSelector]) &#123;</span><br><span class="line">        return self.realDelegate;</span><br><span class="line">    &#125;</span><br><span class="line">    return self;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">- (NSMethodSignature *)methodSignatureForSelector:(SEL)aSelector &#123;</span><br><span class="line">    struct objc_method_description methodDesc = protocol_getMethodDescription(@protocol(UIApplicationDelegate), aSelector, NO, YES);</span><br><span class="line">    </span><br><span class="line">    if (methodDesc.name == NULL &amp;&amp; methodDesc.types == NULL) &#123;</span><br><span class="line">        return [[self class] instanceMethodSignatureForSelector:@selector(doNothing)];</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    return [NSMethodSignature signatureWithObjCTypes:methodDesc.types];</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">- (void)forwardInvocation:(NSInvocation *)anInvocation &#123;</span><br><span class="line">    NSMutableArray *allModules = [NSMutableArray arrayWithObjects:self.realDelegate, nil];</span><br><span class="line">    [allModules addObjectsFromArray:self.subClasses];</span><br><span class="line">    </span><br><span class="line">    // BOOL 型返回值特殊处理</span><br><span class="line">    if (anInvocation.methodSignature.methodReturnType[0] == &apos;B&apos;) &#123;</span><br><span class="line">        BOOL realReturnValue = NO;</span><br><span class="line">        </span><br><span class="line">        for (GHLLifecycle *module in allModules) &#123;</span><br><span class="line">            if ([module respondsToSelector:anInvocation.selector]) &#123;</span><br><span class="line">                [anInvocation invokeWithTarget:module];</span><br><span class="line">                </span><br><span class="line">                BOOL returnValue = NO;</span><br><span class="line">                [anInvocation getReturnValue:&amp;returnValue];</span><br><span class="line">                </span><br><span class="line">                realReturnValue = returnValue || realReturnValue;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        </span><br><span class="line">        [anInvocation setReturnValue:&amp;realReturnValue];</span><br><span class="line">    &#125; else &#123;</span><br><span class="line">        for (GHLLifecycle *module in allModules) &#123;</span><br><span class="line">            if ([module respondsToSelector:anInvocation.selector]) &#123;</span><br><span class="line">                [anInvocation invokeWithTarget:module];</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">- (void)doNothing &#123;</span><br><span class="line">    </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>无侵入、无注册，个人感觉还是比较完美的。虽然最后只是基于青木的方案做了免注册的优化，但是思考过程中的其他方案也是值得分享的！</p><p>以上，就是总结的所有组件化分发生命周期的方案了。如果你还有其他更好方案，欢迎讨论！<br>所有的实践都在 Demo 里了： <a href="https://github.com/gonghonglou/DemoRepo/tree/master/GHLShareLifecycle/GHLShareLifecycle" target="_blank" rel="noopener">GHLShareLifecycle</a></p><h1 id="2020-03-08-更新"><a href="#2020-03-08-更新" class="headerlink" title="2020.03.08 更新:"></a>2020.03.08 更新:</h1><p><strong>本以为上一种方案已经很完美了，但是在项目中应用的时候还是发现了问题。最新解决方案见：<a href="http://gonghonglou.com/2020/03/08/pod-lifecycle-aop/">组件化分发生命周期 - AOP 方案</a></strong></p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;组件化分发生命周期 方案实现探索系列文章：&lt;br&gt;&lt;strong&gt;1、&lt;a href=&quot;http://gonghonglou.com/2019/08/29/pod-lifecycle/&quot;&gt;组件化分发生命周期&lt;/a&gt;&lt;/strong&gt;&lt;br&gt;2、&lt;a href=&quot;http:/
      
    
    </summary>
    
      <category term="iOS" scheme="http://gonghonglou.com/categories/iOS/"/>
    
    
  </entry>
  
  <entry>
    <title>Crash 防护方案（六）：NSNotificationCenter</title>
    <link href="http://gonghonglou.com/2019/07/08/crash-guard-nsnotificationcenter/"/>
    <id>http://gonghonglou.com/2019/07/08/crash-guard-nsnotificationcenter/</id>
    <published>2019-07-08T12:00:00.000Z</published>
    <updated>2020-03-14T12:56:24.164Z</updated>
    
    <content type="html"><![CDATA[<blockquote><p>If your app targets iOS 9.0 and later or macOS 10.11 and later, you don’t need to unregister an observer in its dealloc method. Otherwise, you should call this method or removeObserver:name:object: before observer or any object specified in addObserverForName:object:queue:usingBlock: or addObserver:selector:name:object: is deallocated.<br>You shouldn’t use this method to remove all observers from a long-lived object, because your code may not be the only code adding observers that involve the object.<br>The following example illustrates how to unregister someObserver for all notifications for which it had previously registered. This is safe to do in the dealloc method, but should not otherwise be used (use removeObserver:name:object: instead).</p></blockquote><p>这是苹果官方文档的说法，也就是说 iOS9 之前，当一个对象添加了 notification 之后，如果 dealloc 的时候，仍然持有 notification，就会出现 NSNotification 类型的 Crash。</p><p>防护方案很简单就是 Hook NSObject 的 dealloc 方法，在对象真正 dealloc 之前先调用一下移除操作：<br><figure class="highlight scheme"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">[[<span class="name">NSNotificationCenter</span> defaultCenter] removeObserver:self]<span class="comment">;</span></span><br></pre></td></tr></table></figure></p><p>当然，为了不必要的操作，我们应该只在添加了通知的对象里去执行移除。同样的，Hook NSNotificationCenter 的 <code>- (void)addObserver:(id)observer selector:(SEL)aSelector name:(nullable NSNotificationName)aName object:(nullable id)anObject;</code> 方法，添加一个标记，在 dealloc 方法里根据这个标记来执行移除操作。</p><p>代码实现，NSNotificationCenter+GHLCrashGuard 分类里添加标记：<br><figure class="highlight groovy"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@implementation</span> NSNotificationCenter (GHLCrashGuard)</span><br><span class="line"></span><br><span class="line">+ (<span class="keyword">void</span>)load &#123;</span><br><span class="line">    <span class="keyword">if</span> ([UIDevice currentDevice].systemVersion.doubleValue &lt; <span class="number">9.0</span>) &#123;</span><br><span class="line">        [self <span class="string">jr_swizzleMethod:</span><span class="meta">@selector</span>(<span class="string">addObserver:</span><span class="string">selector:</span><span class="string">name:</span><span class="string">object:</span>) <span class="string">withMethod:</span><span class="meta">@selector</span>(<span class="string">ghl_addObserver:</span><span class="string">selector:</span><span class="string">name:</span><span class="string">object:</span>) <span class="string">error:</span>nil];</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">- (<span class="keyword">void</span>)<span class="string">ghl_addObserver:</span>(id)observer <span class="string">selector:</span>(SEL)aSelector <span class="string">name:</span>(NSNotificationName)aName <span class="string">object:</span>(id)anObject &#123;</span><br><span class="line">    [self <span class="string">ghl_addObserver:</span>observer <span class="string">selector:</span>aSelector <span class="string">name:</span>aName <span class="string">object:</span>anObject];</span><br><span class="line">    </span><br><span class="line">    objc_setAssociatedObject(observer, <span class="string">"addObserverFlag"</span>, <span class="meta">@YES</span>, OBJC_ASSOCIATION_RETAIN_NONATOMIC);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">@end</span></span><br></pre></td></tr></table></figure></p><p>NSObject+GHLCrashGuard 分类里 Hook dealloc 方法：<br><figure class="highlight objectivec"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">+ (<span class="keyword">void</span>)load &#123;</span><br><span class="line">    [<span class="keyword">self</span> jr_swizzleMethod:<span class="built_in">NSSelectorFromString</span>(<span class="string">@"dealloc"</span>) withMethod:<span class="keyword">@selector</span>(ghl_dealloc) error:<span class="literal">nil</span>];</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">- (<span class="keyword">void</span>)ghl_dealloc &#123;</span><br><span class="line">        </span><br><span class="line">    <span class="keyword">if</span> ([<span class="built_in">UIDevice</span> currentDevice].systemVersion.doubleValue &lt; <span class="number">9.0</span>) &#123;</span><br><span class="line">        [[GHLNotificationCenterManager sharedInstance] handleObjectRemoveObserver:<span class="keyword">self</span>];</span><br><span class="line">    &#125;</span><br><span class="line">    [<span class="keyword">self</span> ghl_dealloc];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>GHLNotificationCenterManager 的 handleObjectRemoveObserver: 方法实现：<br><figure class="highlight objectivec"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">- (<span class="keyword">void</span>)handleObjectRemoveObserver:(__<span class="keyword">unsafe_unretained</span> <span class="keyword">id</span>)object &#123;</span><br><span class="line">    <span class="built_in">NSString</span> *addObserver = objc_getAssociatedObject(object, <span class="string">"addObserverFlag"</span>);</span><br><span class="line">    <span class="keyword">if</span> ([addObserver boolValue]) &#123;</span><br><span class="line">        [[<span class="built_in">NSNotificationCenter</span> defaultCenter] removeObserver:<span class="keyword">self</span>];</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>Demo 地址：<a href="https://github.com/gonghonglou/GHLCrashGuard/tree/master/GHLCrashGuard/Classes/NSNotificationCenter" target="_blank" rel="noopener">GHLCrashGuard：GHLCrashGuard/Classes/NSNotificationCenter</a></p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;blockquote&gt;
&lt;p&gt;If your app targets iOS 9.0 and later or macOS 10.11 and later, you don’t need to unregister an observer in its dealloc meth
      
    
    </summary>
    
      <category term="iOS" scheme="http://gonghonglou.com/categories/iOS/"/>
    
    
  </entry>
  
  <entry>
    <title>Crash 防护方案（五）：KVO</title>
    <link href="http://gonghonglou.com/2019/07/07/crash-guard-kvo/"/>
    <id>http://gonghonglou.com/2019/07/07/crash-guard-kvo/</id>
    <published>2019-07-07T14:00:00.000Z</published>
    <updated>2020-03-14T13:00:31.330Z</updated>
    
    <content type="html"><![CDATA[<p>Apple 使用了 isa 混写（isa-swizzling）来实现 KVO 。当观察对象 A 时，KVO 机制动态创建一个新的名为：NSKVONotifying_A 的新类，该类继承自对象 A 的本类，Apple 还重写了该类的 -class 方法，返回父类，即对象 A 的本类。且 KVO 为 NSKVONotifying_A 重写观察属性的 setter 方法，setter 方法会负责在调用原 setter 方法之前和之后，通知所有观察对象属性值的更改情况。<br>isa 指针的作用：每个对象都有 isa 指针，指向该对象的类，它告诉 Runtime 系统这个对象的类是什么。所以对象注册为观察者时，isa 指针指向新子类，那么这个被观察的对象就变成新子类的对象（或实例）了。 因而在该对象上对 setter 的调用就会调用已重写的 setter，从而激活键值通知机制。</p><p>我们可以通过断点看到，被观察者对象的 isa 指针已经变成了 NSKVONotifying_ 开头的类：<br><img src="http://image.gonghonglou.com/crash-guard/kvo-isa.png" alt="KVO isa-swizzling"></p><p>对于 KVO 使用不当的话很容易出现 Crash，比如添加和移除观察不对应，重复 removeObserver: 或者移除一个不存在的观察者就会造成 Crash，尤其是在多线程操作时防不胜防：</p><blockquote><p>2019-07-13 17:50:14.805177+0800 GHLCrashGuard_Example[77448:5047850] *** Terminating app due to uncaught exception ‘NSRangeException’, reason: ‘Cannot remove an observer <ghlkvoviewcontroller 0x7fd072c17720=""> for the key path “name” from <ghltestobject 0x60000106c160=""> because it is not registered as an observer.’</ghltestobject></ghlkvoviewcontroller></p></blockquote><p>为了避免这种重复添加或者重复移除观察造成的崩溃，可以对 KVO 包装一层。创建一个额外的观察者对象，所有的添加观察和移除观察都通过这个额外的对象，这样在添加和移除的时候就可以做安全判断了。<br>FaceBook 出品的 <a href="https://github.com/facebook/KVOController" target="_blank" rel="noopener">KVOController</a> 就是做的这样的事情。<br><figure class="highlight objectivec"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">self</span>.kvo = [FBKVOController controllerWithObserver:<span class="keyword">self</span>];</span><br><span class="line">[<span class="keyword">self</span>.kvo observe:<span class="keyword">self</span>.obj keyPath:<span class="string">@"name"</span> options:<span class="built_in">NSKeyValueObservingOptionNew</span> | <span class="built_in">NSKeyValueObservingOptionOld</span> block:^(<span class="keyword">id</span>  _Nullable observer, <span class="keyword">id</span>  _Nonnull object, <span class="built_in">NSDictionary</span>&lt;<span class="built_in">NSString</span> *,<span class="keyword">id</span>&gt; * _Nonnull change) &#123;</span><br><span class="line"></span><br><span class="line">   <span class="built_in">NSLog</span>(<span class="string">@"oldName:%@"</span>, [change objectForKey:<span class="built_in">NSKeyValueChangeOldKey</span>]);</span><br><span class="line">   <span class="built_in">NSLog</span>(<span class="string">@"newName:%@"</span>, [change objectForKey:<span class="built_in">NSKeyValueChangeNewKey</span>]);</span><br><span class="line">&#125;];</span><br></pre></td></tr></table></figure></p><p>FBKVOController 的关键主要就在以下三个方法上：</p><h2 id="实例化"><a href="#实例化" class="headerlink" title="实例化"></a>实例化</h2><p><code>+ (instancetype)controllerWithObserver:(nullable id)observer;</code><br>创建 FBKVOController 对象，主要做了两件事：1、存储了观察者 _observer，2、创建了 _objectInfosMap，用于存储被观察对象的信息。</p><h2 id="添加观察"><a href="#添加观察" class="headerlink" title="添加观察"></a>添加观察</h2><p><code>- (void)observe:(nullable id)object keyPath:(NSString *)keyPath options:(NSKeyValueObservingOptions)options block:(FBKVONotificationBlock)block;</code></p><blockquote><p>1、</p></blockquote><figure class="highlight groovy"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// create info</span></span><br><span class="line">_FBKVOInfo *info = [[_FBKVOInfo alloc] <span class="string">initWithController:</span>self <span class="string">keyPath:</span>keyPath <span class="string">options:</span>options <span class="string">block:</span>block];</span><br></pre></td></tr></table></figure><p>主要在创建 _FBKVOInfo 对象，存储<br>FBKVOController（存储着观察者 _observer）<br>keyPath（观察属性）<br>options（观察时机）<br>block（回调）</p><blockquote><p>2、</p></blockquote><figure class="highlight sqf"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br></pre></td><td class="code"><pre><span class="line">- (void)<span class="variable">_observe</span>:(id)object info:(<span class="variable">_FBKVOInfo</span> *)info</span><br><span class="line">&#123;</span><br><span class="line">  <span class="comment">// lock</span></span><br><span class="line">  pthread_mutex_lock(&amp;<span class="variable">_lock</span>);</span><br><span class="line"></span><br><span class="line">  NSMutableSet *infos = [<span class="variable">_objectInfosMap</span> objectForKey:object];</span><br><span class="line"></span><br><span class="line">  <span class="comment">// check for info existence</span></span><br><span class="line">  <span class="variable">_FBKVOInfo</span> *existingInfo = [infos member:info];</span><br><span class="line">  <span class="keyword">if</span> (<span class="literal">nil</span> != existingInfo) &#123;</span><br><span class="line">    <span class="comment">// observation info already exists; do not observe it again</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// unlock and return</span></span><br><span class="line">    pthread_mutex_unlock(&amp;<span class="variable">_lock</span>);</span><br><span class="line">    return;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// lazilly create set of infos</span></span><br><span class="line">  <span class="keyword">if</span> (<span class="literal">nil</span> == infos) &#123;</span><br><span class="line">    infos = [NSMutableSet <span class="built_in">set</span>];</span><br><span class="line">    [<span class="variable">_objectInfosMap</span> setObject:infos forKey:object];</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// add info and oberve</span></span><br><span class="line">  [infos addObject:info];</span><br><span class="line"></span><br><span class="line">  <span class="comment">// unlock prior to callout</span></span><br><span class="line">  pthread_mutex_unlock(&amp;<span class="variable">_lock</span>);</span><br><span class="line"></span><br><span class="line">  [[<span class="variable">_FBKVOSharedController</span> sharedController] observe:object info:info];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>添加观察者之前做的判断，避免重复添加观察。并且添加了 pthread_mutex_lock 互斥锁保证线程安全。</p><blockquote><p>3、</p></blockquote><figure class="highlight groovy"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br></pre></td><td class="code"><pre><span class="line">- (<span class="keyword">void</span>)<span class="string">observe:</span>(id)object <span class="string">info:</span>(nullable _FBKVOInfo *)info</span><br><span class="line">&#123;</span><br><span class="line">  <span class="keyword">if</span> (nil == info) &#123;</span><br><span class="line">    <span class="keyword">return</span>;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// register info</span></span><br><span class="line">  pthread_mutex_lock(&amp;_mutex);</span><br><span class="line">  [_infos <span class="string">addObject:</span>info];</span><br><span class="line">  pthread_mutex_unlock(&amp;_mutex);</span><br><span class="line"></span><br><span class="line">  <span class="comment">// add observer</span></span><br><span class="line">  [object <span class="string">addObserver:</span>self <span class="string">forKeyPath:</span>info-&gt;_keyPath <span class="string">options:</span>info-&gt;_options <span class="string">context:</span>(<span class="keyword">void</span> *)info];</span><br><span class="line"></span><br><span class="line">  <span class="keyword">if</span> (info-&gt;_state == _FBKVOInfoStateInitial) &#123;</span><br><span class="line">    info-&gt;_state = _FBKVOInfoStateObserving;</span><br><span class="line">  &#125; <span class="keyword">else</span> <span class="keyword">if</span> (info-&gt;_state == _FBKVOInfoStateNotObserving) &#123;</span><br><span class="line">    <span class="comment">// this could happen when `NSKeyValueObservingOptionInitial` is one of the NSKeyValueObservingOptions,</span></span><br><span class="line">    <span class="comment">// and the observer is unregistered within the callback block.</span></span><br><span class="line">    <span class="comment">// at this time the object has been registered as an observer (in Foundation KVO),</span></span><br><span class="line">    <span class="comment">// so we can safely unobserve it.</span></span><br><span class="line">    [object <span class="string">removeObserver:</span>self <span class="string">forKeyPath:</span>info-&gt;_keyPath <span class="string">context:</span>(<span class="keyword">void</span> *)info];</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>调用系统方法 addObserver: 添加观察者，并且在这后判断了 info-&gt;_state 如果是非观察状态则执行 removeObserver:</p><h2 id="移除观察"><a href="#移除观察" class="headerlink" title="移除观察"></a>移除观察</h2><blockquote><p>1、</p></blockquote><figure class="highlight sqf"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><span class="line">- (void)<span class="variable">_unobserve</span>:(id)object info:(<span class="variable">_FBKVOInfo</span> *)info</span><br><span class="line">&#123;</span><br><span class="line">  <span class="comment">// lock</span></span><br><span class="line">  pthread_mutex_lock(&amp;<span class="variable">_lock</span>);</span><br><span class="line"></span><br><span class="line">  <span class="comment">// get observation infos</span></span><br><span class="line">  NSMutableSet *infos = [<span class="variable">_objectInfosMap</span> objectForKey:object];</span><br><span class="line"></span><br><span class="line">  <span class="comment">// lookup registered info instance</span></span><br><span class="line">  <span class="variable">_FBKVOInfo</span> *registeredInfo = [infos member:info];</span><br><span class="line"></span><br><span class="line">  <span class="keyword">if</span> (<span class="literal">nil</span> != registeredInfo) &#123;</span><br><span class="line">    [infos removeObject:registeredInfo];</span><br><span class="line"></span><br><span class="line">    <span class="comment">// remove no longer used infos</span></span><br><span class="line">    <span class="keyword">if</span> (<span class="number">0</span> == infos.<span class="built_in">count</span>) &#123;</span><br><span class="line">      [<span class="variable">_objectInfosMap</span> removeObjectForKey:object];</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// unlock</span></span><br><span class="line">  pthread_mutex_unlock(&amp;<span class="variable">_lock</span>);</span><br><span class="line"></span><br><span class="line">  <span class="comment">// unobserve</span></span><br><span class="line">  [[<span class="variable">_FBKVOSharedController</span> sharedController] unobserve:object info:registeredInfo];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>同样是做了安全判断，并通过 pthread_mutex_lock 锁保证线程安全</p><blockquote><p>2、</p></blockquote><figure class="highlight sqf"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line">- (void)unobserve:(id)object info:(nullable <span class="variable">_FBKVOInfo</span> *)info</span><br><span class="line">&#123;</span><br><span class="line">  <span class="keyword">if</span> (<span class="literal">nil</span> == info) &#123;</span><br><span class="line">    return;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// unregister info</span></span><br><span class="line">  pthread_mutex_lock(&amp;<span class="variable">_mutex</span>);</span><br><span class="line">  [<span class="variable">_infos</span> removeObject:info];</span><br><span class="line">  pthread_mutex_unlock(&amp;<span class="variable">_mutex</span>);</span><br><span class="line"></span><br><span class="line">  <span class="comment">// remove observer</span></span><br><span class="line">  <span class="keyword">if</span> (info-&gt;<span class="variable">_state</span> == <span class="variable">_FBKVOInfoStateObserving</span>) &#123;</span><br><span class="line">    [object removeObserver:self forKeyPath:info-&gt;<span class="variable">_keyPath</span> context:(void *)info];</span><br><span class="line">  &#125;</span><br><span class="line">  info-&gt;<span class="variable">_state</span> = <span class="variable">_FBKVOInfoStateNotObserving</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>最后一步调用系统方法 removeObserver: 移除观察者，info-&gt;_state 设置为非观察状态</p><p>Demo 地址：<a href="https://github.com/gonghonglou/DemoRepo/tree/master/GHLCrashGuard" target="_blank" rel="noopener">GHLCrashGuard</a></p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;Apple 使用了 isa 混写（isa-swizzling）来实现 KVO 。当观察对象 A 时，KVO 机制动态创建一个新的名为：NSKVONotifying_A 的新类，该类继承自对象 A 的本类，Apple 还重写了该类的 -class 方法，返回父类，即对象 A 
      
    
    </summary>
    
      <category term="iOS" scheme="http://gonghonglou.com/categories/iOS/"/>
    
    
  </entry>
  
  <entry>
    <title>Crash 防护方案（四）：NSTimer</title>
    <link href="http://gonghonglou.com/2019/07/07/crash-guard-nstimer/"/>
    <id>http://gonghonglou.com/2019/07/07/crash-guard-nstimer/</id>
    <published>2019-07-07T13:00:00.000Z</published>
    <updated>2020-03-14T13:00:25.655Z</updated>
    
    <content type="html"><![CDATA[<figure class="highlight objectivec"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">+ (<span class="built_in">NSTimer</span> *)scheduledTimerWithTimeInterval:(<span class="built_in">NSTimeInterval</span>)ti</span><br><span class="line">                                     target:(<span class="keyword">id</span>)aTarget</span><br><span class="line">                                   selector:(SEL)aSelector</span><br><span class="line">                                   userInfo:(<span class="keyword">nullable</span> <span class="keyword">id</span>)userInfo</span><br><span class="line">                                    repeats:(<span class="built_in">BOOL</span>)yesOrNo;</span><br></pre></td></tr></table></figure><blockquote><p>用此方法创建出来的计时器，会在指定的时间间隔之后执行任务。也可以令其反复执行任务，直到开发者稍后将其手动关闭为止。target 与 selector 参数表示计时器将在哪个对象上调用哪个方法。计时器会保留其目标对象，等到自身“失效”时再释放此对象。调用 invalidate 方法可令计时器失效；执行完相关任务之后，一次性的计时器也会失效。开发者若将计时器设置成重复执行模式，那么必须自己调用 invalidate 方法，才能令其停止。</p><p>由于计时器会保留其目标对象，所以反复执行任务通常会导致应用程序出问题。也就是说，设置成重复执行模式的那种计时器，很容易引入“保留环”。</p></blockquote><p>这是《Effective Objective-C 2.0》书中”第 52 条：别忘了 NSTimer 会保留其目标对象“ 一章中的说法。苹果在其文档中的说明：</p><blockquote><p>repeats<br>If YES, the timer will repeatedly reschedule itself until invalidated. If NO, the timer will be invalidated after it fires.</p></blockquote><p>并且我们在 Demo 中实验也确实如此，调用 <code>+ (NSTimer *)scheduledTimerWithTimeInterval:</code> 方法时如果 repeats = NO 的话是没什么问题的，执行一次后 NSTimer 会自动 invalidate，但 repeats = YES 的话并不会，而且因为 NSTimer 的写法是这样的：</p><figure class="highlight objectivec"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line">- (<span class="keyword">void</span>)dealloc &#123;</span><br><span class="line">    [_timer invalidate];</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">- (<span class="keyword">void</span>)viewDidLoad &#123;</span><br><span class="line">    [<span class="keyword">super</span> viewDidLoad];</span><br><span class="line">    <span class="comment">// Do any additional setup after loading the view.</span></span><br><span class="line">    </span><br><span class="line">    <span class="keyword">self</span>.timer = [<span class="built_in">NSTimer</span> scheduledTimerWithTimeInterval:<span class="number">1</span> target:<span class="keyword">self</span> selector:<span class="keyword">@selector</span>(repeatLog) userInfo:<span class="literal">nil</span> repeats:<span class="literal">YES</span>];</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">- (<span class="keyword">void</span>)repeatLog &#123;</span><br><span class="line">    <span class="built_in">NSLog</span>(<span class="string">@"timer"</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>self 持有 timer，timer 设置了 target 又会持有 self，造成循环引用，所以 dealloc 永远不会执行。反复执行任务则有可能出现崩溃。当然苹果在 iOS10 之后出了新的方法使用 block 的方式可以避免循环引用：<br><figure class="highlight css"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">+ (<span class="selector-tag">NSTimer</span> *)<span class="selector-tag">scheduledTimerWithTimeInterval</span><span class="selector-pseudo">:(NSTimeInterval)interval</span></span><br><span class="line">                                    <span class="selector-tag">repeats</span><span class="selector-pseudo">:(BOOL)repeats</span></span><br><span class="line">                                      <span class="selector-tag">block</span><span class="selector-pseudo">:(void</span> (^)(<span class="selector-tag">NSTimer</span> *<span class="selector-tag">timer</span>))<span class="selector-tag">block</span></span><br><span class="line"><span class="selector-tag">API_AVAILABLE</span>(<span class="selector-tag">macosx</span>(10<span class="selector-class">.12</span>), <span class="selector-tag">ios</span>(10<span class="selector-class">.0</span>), <span class="selector-tag">watchos</span>(3<span class="selector-class">.0</span>), <span class="selector-tag">tvos</span>(10<span class="selector-class">.0</span>));</span><br></pre></td></tr></table></figure></p><p>但我们总要兼容老版本，很少有 APP 会直接舍弃 iOS 10 之前的用户。所以《Effective Objective-C 2.0》书中也给出的解决方案是给 NSTimer 添加一个 Category，在 Category 里添加对 <code>+scheduledTimerWithTimeInterval:</code> 方法的封装方法，也就是想达到这样的效果：在 VC 中使用的时候，self 持有 timer，timer 持有 category（NSTimer 类对象），self 调用 timer 的时候传入 block 给 category 执行。这样就能避免循环引用了。</p><h1 id="BlocksKit-实现"><a href="#BlocksKit-实现" class="headerlink" title="BlocksKit 实现"></a>BlocksKit 实现</h1><p><a href="https://github.com/BlocksKit/BlocksKit" target="_blank" rel="noopener">BlocksKit</a> 也提供了一个 <a href="https://github.com/BlocksKit/BlocksKit/blob/master/BlocksKit/Core/NSTimer%2BBlocksKit.m" target="_blank" rel="noopener">NSTimer+BlocksKit.m</a> 分类实现了相同的功能</p><p>BlocksKit 最新的 tag（2.2.5）及之前的版本的实现是：<br><figure class="highlight objectivec"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">@implementation</span> <span class="title">NSTimer</span> (<span class="title">BlocksKit</span>)</span></span><br><span class="line"></span><br><span class="line">+ (<span class="keyword">id</span>)bk_scheduledTimerWithTimeInterval:(<span class="built_in">NSTimeInterval</span>)inTimeInterval block:(<span class="keyword">void</span> (^)(<span class="built_in">NSTimer</span> *timer))block repeats:(<span class="built_in">BOOL</span>)inRepeats</span><br><span class="line">&#123;</span><br><span class="line"><span class="built_in">NSParameterAssert</span>(block != <span class="literal">nil</span>);</span><br><span class="line"><span class="keyword">return</span> [<span class="keyword">self</span> scheduledTimerWithTimeInterval:inTimeInterval target:<span class="keyword">self</span> selector:<span class="keyword">@selector</span>(bk_executeBlockFromTimer:) userInfo:[block <span class="keyword">copy</span>] repeats:inRepeats];</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">+ (<span class="keyword">id</span>)bk_timerWithTimeInterval:(<span class="built_in">NSTimeInterval</span>)inTimeInterval block:(<span class="keyword">void</span> (^)(<span class="built_in">NSTimer</span> *timer))block repeats:(<span class="built_in">BOOL</span>)inRepeats</span><br><span class="line">&#123;</span><br><span class="line"><span class="built_in">NSParameterAssert</span>(block != <span class="literal">nil</span>);</span><br><span class="line"><span class="keyword">return</span> [<span class="keyword">self</span> timerWithTimeInterval:inTimeInterval target:<span class="keyword">self</span> selector:<span class="keyword">@selector</span>(bk_executeBlockFromTimer:) userInfo:[block <span class="keyword">copy</span>] repeats:inRepeats];</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">+ (<span class="keyword">void</span>)bk_executeBlockFromTimer:(<span class="built_in">NSTimer</span> *)aTimer &#123;</span><br><span class="line"><span class="keyword">void</span> (^block)(<span class="built_in">NSTimer</span> *) = [aTimer userInfo];</span><br><span class="line"><span class="keyword">if</span> (block) block(aTimer);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">@end</span></span><br></pre></td></tr></table></figure></p><p>代码很简单，正如《Effective Objective-C 2.0》书中所讲的思路：</p><blockquote><p>这段代码将计时器所应执行的任务封装成“块”，在调用的计时器函数时，把它作为 userInfo 参数传进去。该参数可用来存放“万能值”，只要计时器还有效，就会一直保留着它。传入参数时要通过 copy 方法将 block 拷贝到“堆”上，否则等到稍后要执行他的时候，该块可能已经无效了。计时器现在的 target 是 NSTimer 类对象，这是个单例，因为计时器是否会保留它，其实都无所谓。此处依然有保留环，然而因为类对象（class object）无须回收，所以不用担心。</p></blockquote><p>只需要在使用的时候注意避免 block 产生循环引用即可，用 <code>__weak typeof(self) weakSelf = self;</code>，<code>__strong typeof(weakSelf) strongSelf = weakSelf;</code> 即可避免。</p><h1 id="BlocksKit-新实现（未打-tag）"><a href="#BlocksKit-新实现（未打-tag）" class="headerlink" title="BlocksKit 新实现（未打 tag）"></a>BlocksKit 新实现（未打 tag）</h1><p>值得提一下的是 <a href="https://github.com/BlocksKit/BlocksKit" target="_blank" rel="noopener">BlocksKit</a> 当前的最新代码里的 NSTimer+BlocksKit.m 又有了不同的实现，直接抛弃了 NSTimer，而是用了 CFRunLoopTimerCreateWithHandler 来实现：<br><figure class="highlight objectivec"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">@implementation</span> <span class="title">NSTimer</span> (<span class="title">BlocksKit</span>)</span></span><br><span class="line"></span><br><span class="line">+ (<span class="keyword">instancetype</span>)bk_scheduleTimerWithTimeInterval:(<span class="built_in">NSTimeInterval</span>)seconds repeats:(<span class="built_in">BOOL</span>)repeats usingBlock:(<span class="keyword">void</span> (^)(<span class="built_in">NSTimer</span> *timer))block</span><br><span class="line">&#123;</span><br><span class="line">    <span class="built_in">NSTimer</span> *timer = [<span class="keyword">self</span> bk_timerWithTimeInterval:seconds repeats:repeats usingBlock:block];</span><br><span class="line">    [<span class="built_in">NSRunLoop</span>.currentRunLoop addTimer:timer forMode:<span class="built_in">NSDefaultRunLoopMode</span>];</span><br><span class="line">    <span class="keyword">return</span> timer;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">+ (<span class="keyword">instancetype</span>)bk_timerWithTimeInterval:(<span class="built_in">NSTimeInterval</span>)inSeconds repeats:(<span class="built_in">BOOL</span>)repeats usingBlock:(<span class="keyword">void</span> (^)(<span class="built_in">NSTimer</span> *timer))block</span><br><span class="line">&#123;</span><br><span class="line">    <span class="built_in">NSParameterAssert</span>(block != <span class="literal">nil</span>);</span><br><span class="line">    <span class="built_in">CFAbsoluteTime</span> seconds = fmax(inSeconds, <span class="number">0.0001</span>);</span><br><span class="line">    <span class="built_in">CFAbsoluteTime</span> interval = repeats ? seconds : <span class="number">0</span>;</span><br><span class="line">    <span class="built_in">CFAbsoluteTime</span> fireDate = <span class="built_in">CFAbsoluteTimeGetCurrent</span>() + seconds;</span><br><span class="line">    <span class="keyword">return</span> (__bridge_transfer <span class="built_in">NSTimer</span> *)<span class="built_in">CFRunLoopTimerCreateWithHandler</span>(<span class="literal">NULL</span>, fireDate, interval, <span class="number">0</span>, <span class="number">0</span>, (<span class="keyword">void</span>(^)(<span class="built_in">CFRunLoopTimerRef</span>))block);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">@end</span></span><br></pre></td></tr></table></figure></p><p>Demo 地址：<a href="https://github.com/gonghonglou/DemoRepo/tree/master/GHLCrashGuard" target="_blank" rel="noopener">GHLCrashGuard</a></p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;figure class=&quot;highlight objectivec&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;gutter&quot;&gt;&lt;pre&gt;&lt;span class=&quot;line&quot;&gt;1&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;2&lt;/span&gt;&lt;br&gt;&lt;span 
      
    
    </summary>
    
      <category term="iOS" scheme="http://gonghonglou.com/categories/iOS/"/>
    
    
  </entry>
  
  <entry>
    <title>Crash 防护方案（三）：Container （NSArray、NSDictionary、NSNumber etc.）</title>
    <link href="http://gonghonglou.com/2019/07/07/crash-guard-container/"/>
    <id>http://gonghonglou.com/2019/07/07/crash-guard-container/</id>
    <published>2019-07-07T12:00:00.000Z</published>
    <updated>2020-03-14T13:00:37.335Z</updated>
    
    <content type="html"><![CDATA[<p>数组越界这类的 Crash 是最简单的也是最容易出现，业务开发过程中很可能操作某个 NSArray 类型的对象时忘记判空或者忘记长度判断而造成数组越界崩溃。所以最好是在线上环境接入这类的 Crash 防护。当然，在开发环境下最好不要接入，避免纵容开发者出现这类遗忘判断的错误。</p><p>这类崩溃的防护方案无非就是 Hook 可能产生 Crash 的类的相关方法。之前有过一篇文章是讲这类防护的：<a href="http://gonghonglou.com/2017/09/07/analyse-safekit/">从 SafeKit 看异常保护及 Method Swizzling 使用分析</a><br>但 <a href="https://github.com/JJMM/SafeKit" target="_blank" rel="noopener">SafeKit</a> 并未 Hook 全可能出现 Crash 的类及其方法，尤其是 NSArray 类簇。</p><p>关于类簇这里是苹果官网文档：<a href="https://developer.apple.com/library/archive/documentation/General/Conceptual/CocoaEncyclopedia/ClassClusters/ClassClusters.html" target="_blank" rel="noopener">Class Clusters</a><br>以及 sunnyxx 在 <a href="https://blog.sunnyxx.com/2014/12/18/class-cluster/" target="_blank" rel="noopener">从NSArray看类簇</a> 文章里的说法：</p><blockquote><p>Class Clusters（类簇）是抽象工厂模式在 iOS 下的一种实现，众多常用类，如 NSString，NSArray，NSDictionary，NSNumber 都运作在这一模式下，它是接口简单性和扩展性的权衡体现，在我们完全不知情的情况下，偷偷隐藏了很多具体的实现类，只暴露出简单的接口。</p></blockquote><p>我们来仔细打印下看看：<br><figure class="highlight less"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// NSArray</span></span><br><span class="line"><span class="selector-tag">NSLog</span>(@<span class="string">"arr alloc:%@"</span>, [NSArray alloc].class); <span class="comment">// __NSPlaceholderArray</span></span><br><span class="line"><span class="selector-tag">NSLog</span>(@<span class="string">"arr init:%@"</span>, [[NSArray alloc] init].class); <span class="comment">// __NSArray0</span></span><br><span class="line"></span><br><span class="line"><span class="selector-tag">NSLog</span>(@<span class="string">"arr:%@"</span>, [@[] class]); <span class="comment">// __NSArray0</span></span><br><span class="line"><span class="selector-tag">NSLog</span>(@<span class="string">"arr:%@"</span>, [@[<span class="variable">@1</span>] class]); <span class="comment">// __NSSingleObjectArrayI</span></span><br><span class="line"><span class="selector-tag">NSLog</span>(@<span class="string">"arr:%@"</span>, [@[<span class="variable">@1</span>, <span class="variable">@2</span>] class]); <span class="comment">// __NSArrayI</span></span><br><span class="line">    </span><br><span class="line"><span class="comment">// NSMutableArray</span></span><br><span class="line"><span class="selector-tag">NSLog</span>(@<span class="string">"mutA alloc:%@"</span>, [NSMutableArray alloc].class); <span class="comment">// __NSPlaceholderArray</span></span><br><span class="line"><span class="selector-tag">NSLog</span>(@<span class="string">"mutA init:%@"</span>, [[NSMutableArray alloc] init].class); <span class="comment">// __NSArrayM</span></span><br><span class="line"></span><br><span class="line"><span class="selector-tag">NSLog</span>(@<span class="string">"mutA:%@"</span>, [@[].mutableCopy class]); <span class="comment">// __NSArrayM</span></span><br><span class="line"><span class="selector-tag">NSLog</span>(@<span class="string">"mutA:%@"</span>, [@[<span class="variable">@1</span>].mutableCopy class]); <span class="comment">// __NSArrayM</span></span><br><span class="line"><span class="selector-tag">NSLog</span>(@<span class="string">"mutA:%@"</span>, [@[<span class="variable">@1</span>, <span class="variable">@2</span>].mutableCopy class]); <span class="comment">// __NSArrayM</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// NSDictionary</span></span><br><span class="line"><span class="selector-tag">NSLog</span>(@<span class="string">"dict alloc:%@"</span>, [NSDictionary alloc].class); <span class="comment">// __NSPlaceholderDictionary</span></span><br><span class="line"><span class="selector-tag">NSLog</span>(@<span class="string">"dict init:%@"</span>, [[NSDictionary alloc] init].class); <span class="comment">// __NSDictionary0</span></span><br><span class="line"></span><br><span class="line"><span class="selector-tag">NSLog</span>(@<span class="string">"dict:%@"</span>, [@&#123;&#125; class]); <span class="comment">// __NSDictionary0</span></span><br><span class="line"><span class="selector-tag">NSLog</span>(@<span class="string">"dict:%@"</span>, [@&#123;<span class="variable">@1:</span><span class="variable">@1</span>&#125; class]); <span class="comment">// __NSSingleEntryDictionaryI</span></span><br><span class="line"><span class="selector-tag">NSLog</span>(@<span class="string">"dict:%@"</span>, [@&#123;<span class="variable">@1:</span><span class="variable">@1</span>, <span class="variable">@2</span>:<span class="variable">@2</span>&#125; class]); <span class="comment">// __NSDictionaryI</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// NSMutableDictionary</span></span><br><span class="line"><span class="selector-tag">NSLog</span>(@<span class="string">"mutD alloc:%@"</span>, [NSMutableDictionary alloc].class); <span class="comment">// __NSPlaceholderDictionary</span></span><br><span class="line"><span class="selector-tag">NSLog</span>(@<span class="string">"mutD init:%@"</span>, [[NSMutableDictionary alloc] init].class); <span class="comment">// __NSDictionaryM</span></span><br><span class="line"></span><br><span class="line"><span class="selector-tag">NSLog</span>(@<span class="string">"mutD:%@"</span>, [@&#123;&#125;.mutableCopy class]); <span class="comment">// __NSDictionaryM</span></span><br><span class="line"><span class="selector-tag">NSLog</span>(@<span class="string">"mutD:%@"</span>, [@&#123;<span class="variable">@1:</span><span class="variable">@1</span>&#125;.mutableCopy class]); <span class="comment">// __NSDictionaryM</span></span><br><span class="line"><span class="selector-tag">NSLog</span>(@<span class="string">"mutD:%@"</span>, [@&#123;<span class="variable">@1:</span><span class="variable">@1</span>, <span class="variable">@2</span>:<span class="variable">@2</span>&#125;.mutableCopy class]); <span class="comment">// __NSDictionaryM</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// NSString</span></span><br><span class="line"><span class="selector-tag">NSLog</span>(@<span class="string">"str:%@"</span>, [@<span class="string">""</span> class]); <span class="comment">// __NSCFConstantString</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// NSNumber</span></span><br><span class="line"><span class="selector-tag">NSLog</span>(@<span class="string">"num:%@"</span>, [<span class="variable">@1</span> class]); <span class="comment">// __NSCFNumber</span></span><br></pre></td></tr></table></figure></p><p>以 NSArray 为例，他在 alloc 阶段生成的是 <code>__NSPlaceholderArray</code> 的中间对象，然后在 init 阶段给这个中间对象发消息，由它做工厂，生成真正的对象。其中 NSMutableArray 生成的都是 <code>__NSArrayM</code> 类型，M 代表的就是 Mutable。NSArray 则区分了数组里：包含 0 个对象时生成的是 <code>__NSArray0</code> 类型，包含 1 个对象生成的是 <code>__NSSingleObjectArrayI</code> 类型，包含多个对象时生成的是 <code>__NSArrayI</code> 类型。</p><p>NSDictionary 同样是类似的。那我们的防护方案里则是 Hook 全这些类型，比如 NSArray 的 Category：<br><figure class="highlight groovy"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br></pre></td><td class="code"><pre><span class="line">+ (<span class="keyword">void</span>)load &#123;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// [NSArray alloc]</span></span><br><span class="line">    [NSClassFromString(@<span class="string">"__NSPlaceholderArray"</span>) <span class="string">jr_swizzleMethod:</span><span class="meta">@selector</span>(<span class="string">initWithObjects:</span><span class="string">count:</span>) <span class="string">withMethod:</span><span class="meta">@selector</span>(<span class="string">initWithObjects_guard:</span><span class="string">count:</span>) <span class="string">error:</span>nil];</span><br><span class="line">    <span class="comment">// @[]</span></span><br><span class="line">    [NSClassFromString(@<span class="string">"__NSArray0"</span>) <span class="string">jr_swizzleMethod:</span><span class="meta">@selector</span>(<span class="string">objectAtIndex:</span>) <span class="string">withMethod:</span><span class="meta">@selector</span>(<span class="string">guard_objectAtIndex:</span>) <span class="string">error:</span>nil];</span><br><span class="line">    [NSClassFromString(@<span class="string">"__NSArray0"</span>) <span class="string">jr_swizzleMethod:</span><span class="meta">@selector</span>(<span class="string">arrayByAddingObject:</span>) <span class="string">withMethod:</span><span class="meta">@selector</span>(<span class="string">guard_arrayByAddingObject:</span>) <span class="string">error:</span>nil];</span><br><span class="line">    <span class="comment">// @[@1]</span></span><br><span class="line">    [NSClassFromString(@<span class="string">"__NSSingleObjectArrayI"</span>) <span class="string">jr_swizzleMethod:</span><span class="meta">@selector</span>(<span class="string">objectAtIndex:</span>) <span class="string">withMethod:</span><span class="meta">@selector</span>(<span class="string">guard_objectAtIndex:</span>) <span class="string">error:</span>nil];</span><br><span class="line">    [NSClassFromString(@<span class="string">"__NSSingleObjectArrayI"</span>) <span class="string">jr_swizzleMethod:</span><span class="meta">@selector</span>(<span class="string">arrayByAddingObject:</span>) <span class="string">withMethod:</span><span class="meta">@selector</span>(<span class="string">guard_arrayByAddingObject:</span>) <span class="string">error:</span>nil];</span><br><span class="line">    <span class="comment">// @[@1, @2]</span></span><br><span class="line">    [NSClassFromString(@<span class="string">"__NSArrayI"</span>) <span class="string">jr_swizzleMethod:</span><span class="meta">@selector</span>(<span class="string">objectAtIndex:</span>) <span class="string">withMethod:</span><span class="meta">@selector</span>(<span class="string">guard_objectAtIndex:</span>) <span class="string">error:</span>nil];</span><br><span class="line">    [NSClassFromString(@<span class="string">"__NSArrayI"</span>) <span class="string">jr_swizzleMethod:</span><span class="meta">@selector</span>(<span class="string">arrayByAddingObject:</span>) <span class="string">withMethod:</span><span class="meta">@selector</span>(<span class="string">guard_arrayByAddingObject:</span>) <span class="string">error:</span>nil];</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">- (instancetype)<span class="string">initWithObjects_guard:</span>(id *)objects <span class="string">count:</span>(NSUInteger)cnt &#123;</span><br><span class="line">    NSUInteger newCnt = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span> (NSUInteger i = <span class="number">0</span>; i &lt; cnt; i++) &#123;</span><br><span class="line">        <span class="keyword">if</span> (!objects[i]) &#123;</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        newCnt++;</span><br><span class="line">    &#125;</span><br><span class="line">    self = [self <span class="string">initWithObjects_guard:</span>objects <span class="string">count:</span>newCnt];</span><br><span class="line">    <span class="keyword">return</span> self;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">- (id)<span class="string">guard_objectAtIndex:</span>(NSUInteger)index &#123;</span><br><span class="line">    <span class="keyword">if</span> (index &gt;= [self count]) &#123;</span><br><span class="line">        <span class="comment">// 收集堆栈，上报 Crash</span></span><br><span class="line">        <span class="keyword">return</span> nil;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> [self <span class="string">guard_objectAtIndex:</span>index];</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">- (NSArray *)<span class="string">guard_arrayByAddingObject:</span>(id)anObject &#123;</span><br><span class="line">    <span class="keyword">if</span> (!anObject) &#123;</span><br><span class="line">        <span class="comment">// 收集堆栈，上报 Crash</span></span><br><span class="line">        <span class="keyword">return</span> self;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> [self <span class="string">guard_arrayByAddingObject:</span>anObject];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>当然 NSArray、NSMutableArray、NSDictionary、NSMutableDictionary、NSString、NSMutableString、NSNumber 这些类都提供了跟多的方法，只要细心仔细的将他们全 Hook 掉就好了。当然实际开发中可能常用的就那么几个方法，Hook 那些就已经足够了。</p><p>线上接入了这类的防护之后要比前边的文章讲的 Unrecognized Selector Crash 和 EXC_BAD_ACCESS Crash 更容易造成业务逻辑的错乱，毕竟业务逻辑中不可避免的要用到大量的 NSArray、NSDictionary 类，可能在接入这类防护后会操成点击无响应或者页面卡死，有时候这种情况甚至比程序崩溃还让用户崩溃，所以也要看实际开发需要的取舍。在接入防护后尤其要做好堆栈收集，上报 Crash 的工作，及时解决掉问题。</p><p>Demo 地址：<a href="https://github.com/gonghonglou/DemoRepo/tree/master/GHLCrashGuard/GHLCrashGuard/Classes/Container" target="_blank" rel="noopener">GHLCrashGuard：GHLCrashGuard/Classes/Container</a></p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;数组越界这类的 Crash 是最简单的也是最容易出现，业务开发过程中很可能操作某个 NSArray 类型的对象时忘记判空或者忘记长度判断而造成数组越界崩溃。所以最好是在线上环境接入这类的 Crash 防护。当然，在开发环境下最好不要接入，避免纵容开发者出现这类遗忘判断的错误
      
    
    </summary>
    
      <category term="iOS" scheme="http://gonghonglou.com/categories/iOS/"/>
    
    
  </entry>
  
  <entry>
    <title>Crash 防护方案（二）：EXC_BAD_ACCESS</title>
    <link href="http://gonghonglou.com/2019/07/06/crash-guard-bad-access/"/>
    <id>http://gonghonglou.com/2019/07/06/crash-guard-bad-access/</id>
    <published>2019-07-06T12:00:00.000Z</published>
    <updated>2020-03-14T12:56:30.840Z</updated>
    
    <content type="html"><![CDATA[<blockquote><p>大家都知道，向业已回收的对象发送消息是不安全的。这么做有时可以，有时不行。具体可行与否，完全取决于对象所占内存有没有为其他内容所覆写。而这块内存有没有移作他用，又无法确定，因此，应用程序只是偶尔崩溃。在没有崩溃的情况下，那块内存可能只复用了其中一部分，所以对象中的某些二进制数据依然有效。还有一种可能，就是那块内存恰好为另外一个有效且存活的对象所占据。在这种情况下，运行期系统会把消息发到新对象那里，而此对象也许能应答，也许不能。如果能，那程序就不崩溃</p></blockquote><p>这是《Effective Objective-C 2.0》书中”第 35 条：用“僵尸对象”调试内存管理问题“一章中对野指针的介绍，这便是野指针出现的原因。</p><p>本篇是 Crash 防护方案系列的第二篇文章，同样是非常常见的 Crash 类型：EXC_BAD_ACCESS，文章会涉及以下几点：</p><ul><li>重现 EXC_BAD_ACCESS Crash</li><li>分析 Xcode 中 Zoombie Objects 僵尸对象调试原理</li><li>自己实现僵尸对象调试</li><li>EXC_BAD_ACCESS 防护</li></ul><h1 id="重现-EXC-BAD-ACCESS-Crash"><a href="#重现-EXC-BAD-ACCESS-Crash" class="headerlink" title="重现 EXC_BAD_ACCESS Crash"></a>重现 EXC_BAD_ACCESS Crash</h1><p>我们先模拟一下看看野指针崩溃的样子：<br><img src="http://image.gonghonglou.com/crash-guard/EXC_BAD_ACCESS.png" alt="EXC_BAD_ACCESS"></p><p>崩溃的原因是 obj 对象是用 assign 修饰的，self 并未强引用该对象，GHLTestObject 对象创建之后因为没人引用他所以就被回收了，之后再次调用 GHLTestObject 的 log 方法则出现了 EXC_BAD_ACCESS 崩溃，这便是向已回收的对象发送消息产生的崩溃。</p><p>多说一句，这里如果把 obj 对象的 assign 修饰改成 strong，则 GHLTestObject 的 log 方法可以正常执行，因为 obj 对象被 self 强引用了。如果把 obj 对象的 assign 修饰改成 weak，虽然 GHLTestObject 的 log 方法不会执行，但程序也不会崩溃，因为被 weak 修饰的指针会在对象销毁后自动置空，在 OC 中向一个空对象发消息是不会崩溃的。</p><h1 id="Xcode-中-Zoombie-Objects-僵尸对象调试原理"><a href="#Xcode-中-Zoombie-Objects-僵尸对象调试原理" class="headerlink" title="Xcode 中 Zoombie Objects 僵尸对象调试原理"></a>Xcode 中 Zoombie Objects 僵尸对象调试原理</h1><p>我们开启 Xcode 的 Zoombie Objects 选项看一下效果（Edit Scheme -&gt; Diagnostics -&gt; Zoombie Objects）：<br><img src="http://image.gonghonglou.com/crash-guard/ZoombieObjects.png" alt="Zoombie Objects"></p><p>可以看到控制台打印了明确的报错信息：</p><blockquote><p>2019-07-09 18:59:43.894822+0800 GHLCrashGuard_Example[51380:3729261] *** -[GHLTestObject retain]: message sent to deallocated instance 0x6000001356f0</p></blockquote><p>并且能看到 self 的 obj 属性从 GHLTestObject 类变成了 _NSZombie_GHLTestObject 类。 其实，在启用僵尸对象后，在运行期发现 GHLTestObject 变成了僵尸对象，那么便动态的创建一个 _NSZombie_GHLTestObject 类，将 GHLTestObject 对象的 isa 指针指向这个新的类，再次向 GHLTestObject 对象发消息的话就会去 _NSZombie_GHLTestObject 这个类里去找相应的方法，然而 _NSZombie_GHLTestObject 这个类没有实现任何方法，那么发给他的全部消息都要经过“完整的消息转发机制”。<br>在发生崩溃的栈回溯消息能能看到 <code>___forwarding___</code> 函数，该函数首先要做的事情就是检查接受对象所属的类名，如果类名前缀为 <code>_NSZombie_</code>，则表明消息接收者是僵尸对象，那么会在控制台打印一条消息。将消息接受对象所属的类名去掉 <code>_NSZombie_</code> 前缀就能得到原始类名了。</p><h1 id="自己实现僵尸对象调试"><a href="#自己实现僵尸对象调试" class="headerlink" title="自己实现僵尸对象调试"></a>自己实现僵尸对象调试</h1><p>有时我们可能实现脱离 Xcode 的僵尸对象调试，方便开发和测试的调试工走，那么可以参照 Xcode 的思路自己来实现，即：</p><p>1、Hook NSObject 的 dealloc 方法<br>2、运行时动态生成新类，用 <code>_GHLZoombie_</code> 做前缀拼接原始类名<br>3、将僵尸对象的 isa 指针指向 <code>_GHLZoombie_</code> 新类<br>4、给 <code>_GHLZoombie_</code> 新类添加 forwardingTargetForSelector 方法<br>5、在 forwardingTargetForSelector 方法里去掉 <code>_GHLZoombie_</code> 前缀获取原始类名，和调用方法名打印出来<br>6、终止程序</p><p>代码实现：<br><figure class="highlight groovy"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">+ (<span class="keyword">void</span>)load &#123;</span><br><span class="line">    <span class="comment">// Bad Access</span></span><br><span class="line">    [self <span class="string">jr_swizzleMethod:</span>NSSelectorFromString(@<span class="string">"dealloc"</span>) <span class="string">withMethod:</span><span class="meta">@selector</span>(zoombie_dealloc) <span class="string">error:</span>nil];</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">- (<span class="keyword">void</span>)zoombie_dealloc &#123;</span><br><span class="line">    </span><br><span class="line">    [[GHLBadAccessManager sharedInstance] <span class="string">handleDeallocObject:</span>self];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>GHLBadAccessManager 类里的处理：<br><figure class="highlight objectivec"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">NSString</span> *GHLZoombieClassPrefix = <span class="string">@"_GHLZoombie_"</span>;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">- (<span class="keyword">void</span>)handleDeallocObject:(__<span class="keyword">unsafe_unretained</span> <span class="keyword">id</span>)object &#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 指向动态生成的类，用 _GHLZoombie_ 拼接原有类名</span></span><br><span class="line">    <span class="built_in">NSString</span> *className = <span class="built_in">NSStringFromClass</span>([object <span class="keyword">class</span>]);</span><br><span class="line">    <span class="built_in">NSString</span> *zombieClassName = [GHLZoombieClassPrefix stringByAppendingString: className];</span><br><span class="line">    Class zombieClass = <span class="built_in">NSClassFromString</span>(zombieClassName);</span><br><span class="line">    <span class="keyword">if</span>(zombieClass) <span class="keyword">return</span>;</span><br><span class="line"></span><br><span class="line">    zombieClass = objc_allocateClassPair([<span class="built_in">NSObject</span> <span class="keyword">class</span>], [zombieClassName UTF8String], <span class="number">0</span>);</span><br><span class="line">    objc_registerClassPair(zombieClass);</span><br><span class="line">    class_addMethod([zombieClass <span class="keyword">class</span>], <span class="keyword">@selector</span>(forwardingTargetForSelector:), (IMP)forwardingTargetForSelector, <span class="string">"@@:@"</span>);</span><br><span class="line"></span><br><span class="line">    object_setClass(object, zombieClass);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">id</span> forwardingTargetForSelector(<span class="keyword">id</span> object, SEL _cmd, SEL aSelector) &#123;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">NSString</span> *className = <span class="built_in">NSStringFromClass</span>([object <span class="keyword">class</span>]);</span><br><span class="line">    <span class="built_in">NSString</span> *realClass = [className stringByReplacingOccurrencesOfString:GHLZoombieClassPrefix withString:<span class="string">@""</span>];</span><br><span class="line"></span><br><span class="line">    <span class="built_in">NSLog</span>(<span class="string">@"[%@ %@] message sent to deallocated instance %@"</span>, realClass, <span class="built_in">NSStringFromSelector</span>(aSelector), object);</span><br><span class="line">    abort();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><blockquote><p>2019-07-09 19:37:01.766612+0800 GHLCrashGuard_Example[51942:3759054] [GHLTestObject log] message sent to deallocated instance &lt;_GHLZoombie_GHLTestObject: 0x600002b05e90&gt;</p></blockquote><p>运行程序发现能够实现和 Xcode 开启僵尸对象同样的效果</p><h1 id="EXC-BAD-ACCESS-防护"><a href="#EXC-BAD-ACCESS-防护" class="headerlink" title="EXC_BAD_ACCESS 防护"></a>EXC_BAD_ACCESS 防护</h1><p>既然我们实现了和 Xcode 开启僵尸对象同样的效果，那我们可以在最后一步不选择终止程序，而是让程序进入消息转发机制。</p><p>不过我们的防护方案里也可以更简单的将原始僵尸对象的 isa 指针指向一个固定的类：GHLZoombie，不必在运行时动态的创建，至于获取原始类名的问题，可以通过 objc_setAssociatedObject 的方式将原始类名保存进 GHLZoombie 对象里，在 GHLZoombie 对象里重载 - (id)forwardingTargetForSelector: 方法，通过 objc_getAssociatedObject 取出原始类名，在控制台打印，并将消息转发给 GHLCrashGuardProxy 对像，在上一篇 <a href="http://gonghonglou.com/2019/07/06/crash-guard-unrecognized-selector/">Crash 防护方案（一）：Unrecognized Selector</a> 里讲过，GHLCrashGuardProxy 对像里重载了 + (BOOL)resolveInstanceMethod: 方法避免崩溃，并收集堆栈，上报 Crash。</p><p>代码实现<br>GHLBadAccessManager 类里的处理：<br><figure class="highlight ceylon"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">- (<span class="keyword">void</span>)handleDeallocObject:(<span class="number">__u</span>nsafe<span class="number">_u</span>nretained id)<span class="keyword">object</span> &#123;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 指向固定的类，原有类名存储在关联对象中</span></span><br><span class="line">    NSString *originClassName = NSStringFromClass([<span class="keyword">object</span> <span class="keyword">class</span>]);</span><br><span class="line">    objc<span class="number">_</span>setAssociatedObject(<span class="keyword">object</span>, <span class="string">"originClassName"</span>, originClassName, OBJC<span class="number">_</span>ASSOCIATION<span class="number">_</span>COPY<span class="number">_</span>NONATOMIC);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">object</span><span class="number">_</span>setClass(<span class="keyword">object</span>, [GHLZoombie <span class="keyword">class</span>]);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>GHLZoombie 类里的实现：<br><figure class="highlight objectivec"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">- (<span class="keyword">id</span>)forwardingTargetForSelector:(SEL)aSelector &#123;</span><br><span class="line">    </span><br><span class="line">    <span class="built_in">NSLog</span>(<span class="string">@"[%@ %@] message sent to deallocated instance %@"</span>, objc_getAssociatedObject(<span class="keyword">self</span>, <span class="string">"originClassName"</span>), <span class="built_in">NSStringFromSelector</span>(aSelector), <span class="keyword">self</span>);</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> [GHLCrashGuardProxy new];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>剩下的就是上一篇文章的内容了，这样就能做到 EXC_BAD_ACCESS Crash 的防护。</p><p><strong><em>但仍然存在问题是延迟释放内存会造成性能浪费，所以可以设置一个默认的缓存僵尸对象的实例数量（50）或者给定一个固定内存大小（2M），超出这个限制就会释放，当然在释放之后如果再此触发了刚好释放掉的野指针，还是会造成 Crash 的。</em></strong></p><p>Demo 地址：<a href="https://github.com/gonghonglou/GHLCrashGuard/tree/master/GHLCrashGuard/Classes/EXC_BAD_ACCESS" target="_blank" rel="noopener">GHLCrashGuard：GHLCrashGuard/Classes/EXC_BAD_ACCESS</a></p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;blockquote&gt;
&lt;p&gt;大家都知道，向业已回收的对象发送消息是不安全的。这么做有时可以，有时不行。具体可行与否，完全取决于对象所占内存有没有为其他内容所覆写。而这块内存有没有移作他用，又无法确定，因此，应用程序只是偶尔崩溃。在没有崩溃的情况下，那块内存可能只复用了其中一部
      
    
    </summary>
    
      <category term="iOS" scheme="http://gonghonglou.com/categories/iOS/"/>
    
    
  </entry>
  
  <entry>
    <title>Crash 防护方案（一）：Unrecognized Selector</title>
    <link href="http://gonghonglou.com/2019/07/06/crash-guard-unrecognized-selector/"/>
    <id>http://gonghonglou.com/2019/07/06/crash-guard-unrecognized-selector/</id>
    <published>2019-07-06T08:55:06.000Z</published>
    <updated>2020-03-14T13:00:19.540Z</updated>
    
    <content type="html"><![CDATA[<p>线上 APP Crash 是比较严重的问题，既影响用户体验又不利于程序猿们的 KPI，我们应当尽量避免线上 Crash 的出现，所以希望在 APP 发生 Crash 的时候能够实现自动防护，虽然我们的手段可能会导致业务逻辑的出错，但我们可以通过记录 Crash，上报堆栈来及时解决问题，也比用户 APP 崩溃掉要好得多。</p><p>文章参考<a href="https://mp.weixin.qq.com/s?__biz=MzUxMzcxMzE5Ng==&amp;mid=2247488311&amp;idx=1&amp;sn=0db090c8d4a5efafa47f00af4b3f174f" target="_blank" rel="noopener">网易iOS App运行时Crash自动防护实践</a> 但给出了具体的实践，有一些不同的方案，分析常用开源库的做法或给出 Demo 来实现解决方案。</p><p>本系列文章防护方案应对的的 Crash 有以下几种：</p><ul><li>Unrecognized Selector Crash</li><li>EXC_BAD_ACCESS crash</li><li>Container Crash</li><li>NSTimer Crash</li><li>KVO Crash</li><li>NSNotificationCenter Crash</li></ul><h1 id="Unrecognized-Selector-Crash"><a href="#Unrecognized-Selector-Crash" class="headerlink" title="Unrecognized Selector Crash"></a>Unrecognized Selector Crash</h1><figure class="highlight cos"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">UIView *<span class="keyword">view</span> = [UIView <span class="keyword">new</span>]<span class="comment">;</span></span><br><span class="line">[<span class="keyword">view</span> performSelector:@selector(log)]<span class="comment">;</span></span><br></pre></td></tr></table></figure><blockquote><p>2019-07-08 14:07:35.895216+0800 GHLCrashGuard_Example[42376:3276094] *** Terminating app due to uncaught exception ‘NSInvalidArgumentException’, reason: ‘-[UIView log]: unrecognized selector sent to instance 0x7f8e78c13950’</p></blockquote><p>这算是 OC 里很经典常见的 Crash 了，出现的原因是向一个对象发送了该对象无法响应的消息，或者可以理解为调用一个对象不存在的方法发生的 Crash。简单陈述一下 OC 消息发送转发流程，从 objc_msgSend 方法开始，OC 消息发送机制看起来像是 objc_msgSend 返回了数据，其实 objc_msgSend 从不返回数据而是你的方法被调用后返回了数据。步骤：</p><p>1、检测这个 selector 是不是要忽略的。比如 Mac OS X 开发，有了垃圾回收就不理会 retain, release 这些函数了。<br>2、检测这个 target 是不是 nil 对象。ObjC 的特性是允许对一个 nil 对象执行任何一个方法不会 Crash，因为会被忽略掉。<br>3、如果上面两个都过了，那就开始查找这个类的 IMP，先从 cache 里面找，完了找得到就跳到对应的函数去执行。<br>4、如果 cache 找不到就找一下方法分发表。<br>5、如果分发表找不到就到超类的分发表去找，一直找，直到找到 NSObject 类为止。</p><p>如果还找不到就要开始进入动态方法解析了。当向一个对象发送消息，发现对象无法响应，对象会依次执行以下方法，这也是 ObjC 的运行时给出的三次拯救程序崩溃的机会：</p><p>6、ObjC 运行时会调用 +resolveInstanceMethod: 或者 +resolveClassMethod:，让你有机会提供一个函数实现。如果你添加了函数并返回 YES，那运行时系统就会重新启动一次消息发送的过程，如果 resolve 方法返回 NO ，运行时就会移到下一步，消息转发（Message Forwarding）。<br>7、如果目标对象实现了-forwardingTargetForSelector: 方法，Runtime 这时就会调用这个方法，给你把这个消息转发给其他对象的机会。只要这个方法返回的不是 nil 和 self，整个消息发送的过程就会被重启，当然发送的对象会变成你返回的那个对象。否则，就会继续<br>8、这一步是 Runtime 最后一次给你挽救的机会。首先它会发送 -methodSignatureForSelector: 消息获得函数的参数和返回值类型。<br>如果 -methodSignatureForSelector: 返回 nil，Runtime 则会发出 -doesNotRecognizeSelector: 消息，程序这时也就挂掉了。<br>如果返回了一个函数签名，Runtime 就会创建一个 NSInvocation 对象并发送 -forwardInvocation: 消息给目标对象。</p><h1 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h1><p>Unrecognized Selector Crash 正是发生在 -doesNotRecognizeSelector: 消息里。防护方案是去 hook NSObject 的 -forwardingTargetForSelector: 方法，具体思路是：</p><p>1、如果对象（或者父类）没有重写 forwardInvocation: 方法，那么就认为是调用出错了<br>2、为了防止 Crash 这时新建一个干净的 GHLCrashGuardProxy 对象，把方法转发给 GHLCrashGuardProxy<br>3、GHLCrashGuardProxy 在 resolveInstanceMethod: 中动态的创建一个返回空的方法，然后执行该方法防止 Crash</p><p>这里我们选择 hook forwardingTargetForSelector 方法的原因是：</p><p>1、resolveInstanceMethod 需要在类的本身上动态添加它本身不存在的方法，这些方法对于该类本身来说冗余的<br>2、forwardInvocation 可以通过 NSInvocation 的形式将消息转发给多个对象，但是其开销较大，需要创建新的 NSInvocation 对象，并且 forwardInvocation 的函数经常被使用者调用，来做多层消息转发选择机制，不适合多次重写<br>3、forwardingTargetForSelector 可以将消息转发给一个对象，开销较小，并且被重写的概率较低，适合重写</p><p><img src="http://image.gonghonglou.com/cheaptalk.png" alt=""></p><h1 id="代码实现"><a href="#代码实现" class="headerlink" title="代码实现"></a>代码实现</h1><p>Hook 示例：<br><figure class="highlight objectivec"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#import <span class="meta-string">"NSObject+GHLCrashGuard.h"</span></span></span><br><span class="line"><span class="meta">#import <span class="meta-string">&lt;JRSwizzle/JRSwizzle.h&gt;</span></span></span><br><span class="line"><span class="meta">#import <span class="meta-string">"GHLUnrecognizedSelectorManager.h"</span></span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">@implementation</span> <span class="title">NSObject</span> (<span class="title">GHLCrashGuard</span>)</span></span><br><span class="line"></span><br><span class="line">+ (<span class="keyword">void</span>)load &#123;</span><br><span class="line">    <span class="comment">// Unrecognized Selector</span></span><br><span class="line">    [<span class="keyword">self</span> jr_swizzleMethod:<span class="keyword">@selector</span>(forwardingTargetForSelector:) withMethod:<span class="keyword">@selector</span>(ghl_forwardingTargetForSelector:) error:<span class="literal">nil</span>];</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">- (<span class="keyword">id</span>)ghl_forwardingTargetForSelector:(SEL)aSelector &#123;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> [[GHLUnrecognizedSelectorManager sharedInstance] handleObject:<span class="keyword">self</span> forwardingTargetForSelector:aSelector];</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">@end</span></span><br></pre></td></tr></table></figure></p><p>GHLUnrecognizedSelectorManager 查询是否重写 forwardInvocation: 方法并做转发操作：<br><figure class="highlight objectivec"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br></pre></td><td class="code"><pre><span class="line">- (<span class="keyword">id</span>)handleObject:(__<span class="keyword">unsafe_unretained</span> <span class="keyword">id</span>)object forwardingTargetForSelector:(SEL)aSelector &#123;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">if</span> (![<span class="keyword">self</span> needGuard:[object <span class="keyword">class</span>]]) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">nil</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="built_in">NSLog</span>(<span class="string">@"[%@ %@]: unrecognized selector sent to instance %@"</span>, [object <span class="keyword">class</span>], <span class="built_in">NSStringFromSelector</span>(aSelector), object);</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> [GHLCrashGuardProxy new];</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">- (<span class="built_in">BOOL</span>)needGuard:(Class)cls &#123;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 如果重写了 forwardInvocation，说明自己要处理，这里直接返回</span></span><br><span class="line">    <span class="keyword">if</span> ([<span class="keyword">self</span> methodHasOverwrited:<span class="keyword">@selector</span>(forwardInvocation:) cls:cls]) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">NO</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">YES</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 判断 cls 是否重写了 sel 方法，递归调用判断但不包括 NSObject</span></span><br><span class="line">- (<span class="built_in">BOOL</span>)methodHasOverwrited:(SEL)sel cls:(Class)cls &#123;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">unsigned</span> <span class="keyword">int</span> methodCount = <span class="number">0</span>;</span><br><span class="line">    Method *methods = class_copyMethodList(cls, &amp;methodCount);</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; methodCount; i++) &#123;</span><br><span class="line">        Method method = methods[i];</span><br><span class="line">        <span class="keyword">if</span> (method_getName(method) == sel) &#123;</span><br><span class="line">            free(methods);</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">YES</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    free(methods);</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 可能父类实现了这个 sel，一直遍历到基类 NSObject 为止</span></span><br><span class="line">    <span class="keyword">if</span> ([cls superclass] != [<span class="built_in">NSObject</span> <span class="keyword">class</span>]) &#123;</span><br><span class="line">        <span class="keyword">return</span> [<span class="keyword">self</span> methodHasOverwrited:sel cls:[cls superclass]];</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">NO</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>GHLCrashGuardProxy 的 resolveInstanceMethod: 实现：<br><figure class="highlight objectivec"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">+ (<span class="built_in">BOOL</span>)resolveInstanceMethod:(SEL)sel &#123;</span><br><span class="line">    class_addMethod([<span class="keyword">self</span> <span class="keyword">class</span>], sel, imp_implementationWithBlock(^&#123;</span><br><span class="line">        <span class="comment">// 收集堆栈，上报 Crash</span></span><br><span class="line">        <span class="built_in">NSLog</span>(<span class="string">@"%@"</span>, [<span class="built_in">NSThread</span> callStackSymbols]);        </span><br><span class="line">        </span><br><span class="line">        <span class="keyword">return</span> <span class="literal">nil</span>;</span><br><span class="line">    &#125;), <span class="string">"@@:"</span>);</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">YES</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>这里为了方便展示用了 NSThread 的 callStackSymbols 来收集堆栈，但这个方法只能收集当前线程的对战，实际工作时可以选择 backtrace_symbols 方法或者更好的堆栈收集工具。</p><blockquote><p>The return value describes the call stack backtrace of the current thread at the moment this method was called.</p></blockquote><p>Demo 地址：<a href="https://github.com/gonghonglou/GHLCrashGuard/tree/master/GHLCrashGuard/Classes/Unrecognized%20Selector" target="_blank" rel="noopener">GHLCrashGuard：GHLCrashGuard/Classes/Unrecognized Selector</a></p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;线上 APP Crash 是比较严重的问题，既影响用户体验又不利于程序猿们的 KPI，我们应当尽量避免线上 Crash 的出现，所以希望在 APP 发生 Crash 的时候能够实现自动防护，虽然我们的手段可能会导致业务逻辑的出错，但我们可以通过记录 Crash，上报堆栈来及
      
    
    </summary>
    
      <category term="iOS" scheme="http://gonghonglou.com/categories/iOS/"/>
    
    
  </entry>
  
  <entry>
    <title>组件化解耦方案</title>
    <link href="http://gonghonglou.com/2019/06/13/pod-decoupling/"/>
    <id>http://gonghonglou.com/2019/06/13/pod-decoupling/</id>
    <published>2019-06-13T07:25:47.000Z</published>
    <updated>2020-03-14T12:58:40.005Z</updated>
    
    <content type="html"><![CDATA[<p>现在有些规模的工程大概都是实行组件化开发吧，将基础库，业务库划分成单独模块，以 Pod 的形式集成到 APP 中。其中组件化开发一个不可避免的问题就是解耦，本篇博客大概会总结一些现在常用的解耦方案。</p><p>首先，整个工程应该分为两个部分，基础库和业务库，而组件化解耦应该主要针对的是业务模块。将相似度比较高的业务或者功能明确的业务划分成模块，由一个或多个 Pod 组成，比如：首页模块、详情模块、支付模块、个人中心模块等等。每个业务模块可以依赖所有的基础库，但业务模块之间没有耦合，可以独立集成进工程中。</p><p>接下来是针对业务库的几种解耦方案</p><h1 id="Common-Pod"><a href="#Common-Pod" class="headerlink" title="Common Pod"></a>Common Pod</h1><p>最简单粗暴的方式，各个业务模块中肯定会有复用的 View，Model，业务相关的工具库等，将这部分内容归入到一个 common pod 中，所有的业务模块可以依赖该 pod。当然，随着复用的文件越来越多，common pod 肯定会越来越臃肿，那个时候还应该对 common pod 再做拆分，相当于随着业务的发展逐步抽离公用类，逐渐下沉到基础库中或者是业务基础库。</p><h1 id="去-Model-化"><a href="#去-Model-化" class="headerlink" title="去 Model 化"></a>去 Model 化</h1><p>组件化解耦中很好用的一种方案，去 Model 化，通过 NSDictionary 代替 Model，优点很明显，解耦效果显著，且节省了 JSON 转 Model 的解析时间。当然缺点也很明显，会有很多硬编码，不可避免地要做很多保护判断，做好防崩溃措施。</p><h1 id="Router"><a href="#Router" class="headerlink" title="Router"></a>Router</h1><p>组件化解耦绕不开的一个话题就是页面路由了，关于页面路由网上有很多文章在讲，这里大致介绍下其中两种常用方案：<br>1、无需注册 Target，由业务方维护自己的目标 Target 类和调起方法<br>2、提前注册 Target，无需维护调起方法</p><h2 id="Target-Action"><a href="#Target-Action" class="headerlink" title="Target-Action"></a>Target-Action</h2><p>提供一个 <code>Mediator</code> 类，执行调起方法<br><figure class="highlight objectivec"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line">- (<span class="keyword">nullable</span> <span class="keyword">id</span>)performTarget:(<span class="built_in">NSString</span> *)targetName action:(<span class="built_in">NSString</span> *)actionName params:(<span class="built_in">NSDictionary</span> *)params &#123;</span><br><span class="line">    </span><br><span class="line">    <span class="built_in">NSString</span> *targetClassString = [<span class="built_in">NSString</span> stringWithFormat:<span class="string">@"target_%@"</span>, targetName];</span><br><span class="line">    <span class="built_in">NSString</span> *actionString = [<span class="built_in">NSString</span> stringWithFormat:<span class="string">@"action_%@:"</span>, actionName];</span><br><span class="line">    </span><br><span class="line">    Class targetClass = <span class="built_in">NSClassFromString</span>(targetClassString);</span><br><span class="line">    <span class="keyword">id</span> target = [targetClass new];</span><br><span class="line">    SEL action = <span class="built_in">NSSelectorFromString</span>(actionString);</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">if</span> (target == <span class="literal">nil</span>) <span class="keyword">return</span> <span class="literal">nil</span>;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">if</span> ([target respondsToSelector:action]) &#123;</span><br><span class="line"><span class="meta">#pragma clang diagnostic push</span></span><br><span class="line"><span class="meta">#pragma clang diagnostic ignored <span class="meta-string">"-Warc-performSelector-leaks"</span></span></span><br><span class="line">        <span class="keyword">return</span> [target performSelector:action withObject:params];</span><br><span class="line"><span class="meta">#pragma clang diagnostic pop</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">nil</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>提供一个 <code>Router</code> 类，切割 url：</p><figure class="highlight objectivec"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><span class="line">- (<span class="keyword">id</span>)routerWithUrlString:(<span class="built_in">NSString</span> *)urlString &#123;</span><br><span class="line">    </span><br><span class="line">    <span class="built_in">NSURL</span> *url = [<span class="built_in">NSURL</span> URLWithString:urlString];</span><br><span class="line">    <span class="built_in">NSMutableDictionary</span> *params = [<span class="built_in">NSMutableDictionary</span> new];</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">for</span> (<span class="built_in">NSString</span> *param <span class="keyword">in</span> [url.query componentsSeparatedByString:<span class="string">@"&amp;"</span>]) &#123;</span><br><span class="line">        <span class="built_in">NSArray</span> *elts = [param componentsSeparatedByString:<span class="string">@"="</span>];</span><br><span class="line">        <span class="keyword">if</span> ([elts count] &lt; <span class="number">2</span>) <span class="keyword">continue</span>;</span><br><span class="line">        <span class="built_in">NSString</span> *key = [elts.firstObject stringByReplacingPercentEscapesUsingEncoding:<span class="built_in">NSUTF8StringEncoding</span>];</span><br><span class="line">        <span class="built_in">NSString</span> *value = [elts.lastObject stringByReplacingPercentEscapesUsingEncoding:<span class="built_in">NSUTF8StringEncoding</span>];</span><br><span class="line">        </span><br><span class="line">        params[key] = value;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> [<span class="keyword">self</span> routerWithUrlString:url.path params:params];</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">- (<span class="keyword">id</span>)routerWithUrlString:(<span class="built_in">NSString</span> *)urlString params:(<span class="built_in">NSDictionary</span> *)params &#123;</span><br><span class="line">    </span><br><span class="line">    <span class="built_in">NSURL</span> *url = [<span class="built_in">NSURL</span> URLWithString:urlString];</span><br><span class="line">    <span class="built_in">NSString</span> *target = url.pathComponents[<span class="number">0</span>];</span><br><span class="line">    <span class="built_in">NSString</span> *actionName = url.pathComponents[<span class="number">1</span>];</span><br><span class="line">    <span class="keyword">id</span> result = [[GHLMediator sharedInstance] performTarget:target action:actionName params:params];</span><br><span class="line">    <span class="keyword">return</span> result;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这样，假设想 push 一个 <code>HomeViewController</code>，提供一个 urlString：<code>home/homePage?page=1</code>，在 Home 的 POD 里提供一个 <code>target_home</code> 类，在 <code>target_home</code> 类里提供一个 <code>action_homePage</code> 方法：</p><figure class="highlight cs"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">- (UIViewController *)action_homePage:(NSDictionary *)<span class="keyword">params</span> &#123;</span><br><span class="line">    GHLHomeViewController *vc = [GHLHomeViewController <span class="keyword">new</span>];</span><br><span class="line">    vc.page = <span class="keyword">params</span>[<span class="string">@"page"</span>];</span><br><span class="line">    <span class="keyword">return</span> vc;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这样就可以通过：<br><figure class="highlight mipsasm"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">UIViewController *vc = [[GHLRouter <span class="keyword">sharedInstance] </span>routerWithUrlString:@<span class="string">"home/homePage?page=1"</span>]<span class="comment">;</span></span><br></pre></td></tr></table></figure></p><p>获取到 VC</p><ul><li>优点：无需提前注册 Target</li><li>缺点：需要业务方维护自己的 <code>target</code> 类</li></ul><p>如果想为 <code>HomeViewController</code> 对外暴露一个指定入参的方法，可以给 <code>Mediator</code> 类添加一个 Home 的 Category，并添加方法：</p><figure class="highlight groovy"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">- (UIViewController *)<span class="string">ghlHomeViewControllerWithPage:</span>(NSString *)page &#123;</span><br><span class="line">    <span class="keyword">return</span> [self <span class="string">performTarget:</span>@<span class="string">"home"</span> <span class="string">action:</span>@<span class="string">"homePage"</span> <span class="string">params:</span>@&#123;@<span class="string">"page"</span> : page&#125;];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这样就可以通过：<br><figure class="highlight mipsasm"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">UIViewController *vc = [[GHLMediator <span class="keyword">sharedInstance] </span>ghlHomeViewControllerWithPage:@<span class="string">"1"</span>]<span class="comment">;</span></span><br></pre></td></tr></table></figure></p><p>获取到 VC</p><ul><li>优点：可对外暴露指定参数的方法，方便调用者</li><li>缺点：需要业务方维护自己的 <code>Mediator</code> 的 Category</li></ul><h2 id="Register-URL"><a href="#Register-URL" class="headerlink" title="Register URL"></a>Register URL</h2><p>给每个 Pod 添加一个 <code>target</code> 资源文件，文件内记录了路由 url 和 ViewController 的对应关系，这样在调起 url 时取出 VC 之后 push 就好了。</p><figure class="highlight objectivec"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br></pre></td><td class="code"><pre><span class="line">- (<span class="keyword">id</span>)openURLString:(<span class="built_in">NSString</span> *)urlString &#123;</span><br><span class="line">    <span class="built_in">NSURL</span> *url = [<span class="built_in">NSURL</span> URLWithString:urlString];</span><br><span class="line">    <span class="built_in">NSMutableDictionary</span> *params = [<span class="built_in">NSMutableDictionary</span> new];</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">for</span> (<span class="built_in">NSString</span> *param <span class="keyword">in</span> [url.query componentsSeparatedByString:<span class="string">@"&amp;"</span>]) &#123;</span><br><span class="line">        <span class="built_in">NSArray</span> *elts = [param componentsSeparatedByString:<span class="string">@"="</span>];</span><br><span class="line">        <span class="keyword">if</span> ([elts count] &lt; <span class="number">2</span>) <span class="keyword">continue</span>;</span><br><span class="line">        <span class="built_in">NSString</span> *key = [elts.firstObject stringByReplacingPercentEscapesUsingEncoding:<span class="built_in">NSUTF8StringEncoding</span>];</span><br><span class="line">        <span class="built_in">NSString</span> *value = [elts.lastObject stringByReplacingPercentEscapesUsingEncoding:<span class="built_in">NSUTF8StringEncoding</span>];</span><br><span class="line">        </span><br><span class="line">        params[key] = value;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> [<span class="keyword">self</span> openURLString:url.path params:params];</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">- (<span class="keyword">nullable</span> <span class="keyword">id</span>)openURLString:(<span class="built_in">NSString</span> *)urlString params:(<span class="built_in">NSDictionary</span> *)params &#123;</span><br><span class="line">    <span class="built_in">NSString</span> *classString = <span class="keyword">self</span>.targetDict[urlString];</span><br><span class="line">    <span class="keyword">if</span> (classString.length) &#123;</span><br><span class="line">        Class controllerClass = <span class="built_in">NSClassFromString</span>(classString);</span><br><span class="line">        <span class="built_in">UIViewController</span> *viewController = [controllerClass new];</span><br><span class="line">        </span><br><span class="line">        objc_setAssociatedObject(viewController, <span class="keyword">@selector</span>(params), [params <span class="keyword">copy</span>], OBJC_ASSOCIATION_RETAIN_NONATOMIC);</span><br><span class="line">        <span class="keyword">return</span> viewController;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">nil</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">#pragma mark - getter</span></span><br><span class="line">- (<span class="built_in">NSDictionary</span> *)targetDict &#123;</span><br><span class="line">    <span class="keyword">if</span> (!_targetDict) &#123;</span><br><span class="line">        <span class="built_in">NSURL</span> *url = [[<span class="built_in">NSBundle</span> mainBundle] URLForResource:<span class="string">@"GHLRouter"</span> withExtension:<span class="string">@"bundle"</span>];</span><br><span class="line">        <span class="built_in">NSBundle</span> *bundle = [<span class="built_in">NSBundle</span> bundleWithURL:url];</span><br><span class="line">        <span class="built_in">NSString</span> *path = [bundle pathForResource:<span class="string">@"target"</span> ofType:<span class="string">@"plist"</span>];</span><br><span class="line">        _targetDict = [<span class="built_in">NSDictionary</span> dictionaryWithContentsOfFile:path];</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> _targetDict;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这里为了方便演示，我给 <code>GHLRouter</code> 添加一个 plist 文件，在这个文件里注册路由 url 和 VC 的对应关系，在取注册关系时写死了 bundle 名，这么做的结果就是也就是新增路由 url 时都需要更新 <code>GHLRouter</code> pod 里的 plist 文件，但正确的做法应当是业务方在自己的 pod 里维护自己的 target 文件作为配置文件，YAML 是一个不错的配置文件的选择，参考：<a href="http://www.ruanyifeng.com/blog/2016/07/yaml.html" target="_blank" rel="noopener">http://www.ruanyifeng.com/blog/2016/07/yaml.html</a><br>例如：</p><figure class="highlight arduino"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta"># yaml 参考格式</span></span><br><span class="line"></span><br><span class="line">GHLHomeViewController:</span><br><span class="line">    targets:</span><br><span class="line">        - <span class="built_in">home</span>/home_page</span><br><span class="line">        - <span class="built_in">home</span>/homePage</span><br></pre></td></tr></table></figure><p>只不过加载各个 pod 里的资源文件会有点麻烦，可以写个脚本把所有 pod 里的 target 配置文件拼接起来写入沙盒中，在编译期执行这个脚本，程序运行起来之后去读取沙盒里的拼接文件，完成注册操作。这样业务方各自维护自己的 target 文件，所配置的路由 url 也一目了然。</p><p>要注意的是：各个 Pod 里维护一个同名的 <code>target.yaml</code> 文件的话，那 podspec 文件里添加资源引用应该使用 <code>resource_bundles</code> 的方式避免重名的问题，例如：<br><figure class="highlight sml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">s.resource_bundles = &#123;</span><br><span class="line">   <span class="symbol">'GHLHome'</span> =&gt; [<span class="symbol">'GHLHome</span>/<span class="type">Assets</span>/*.png',<span class="symbol">'GHLHome</span>/<span class="type">Config</span>/*.yaml']</span><br><span class="line"> &#125;</span><br></pre></td></tr></table></figure></p><p>不同于上一种方法，传参的时候给目标 VC 一一设置入参，这种做法是直接传入一个 NSDictionary，通过 runtime 的方式 setAssociated 一个 <code>params</code> 方法给 目标 VC，写一个 UIViewController 的 Category 实现 <code>params</code> 方法：</p><figure class="highlight less"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="variable">@implementation</span> UIViewController (GHLRouter)</span><br><span class="line"></span><br><span class="line">- (NSDictionary *)params &#123;</span><br><span class="line">    <span class="selector-tag">return</span> <span class="selector-tag">objc_getAssociatedObject</span>(self, <span class="variable">@selector</span>(params));</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">@<span class="selector-tag">end</span></span><br></pre></td></tr></table></figure><p>这样就实现了参数传递。</p><ul><li>优点：不需要维护 target 类调起方法和 <code>Mediator</code> 的 Category</li><li>缺点：传递 NSDictionary 包裹参数，入参要求对外不明确</li></ul><h1 id="Selector-Service"><a href="#Selector-Service" class="headerlink" title="Selector Service"></a>Selector Service</h1><p>这其实是上边讲的第一种 <code>Mediator</code> 方法的另一种应用场景，它不仅可以用来控制页面路由，它可以执行任何方法，组件之间的方法调用为了解耦，可以通过 <code>Mediator</code> 的这种方式对外暴露调起方法，通过字符串反射来执行。</p><p>比如，PodA 有一个 selector1 方法，现在 PodB 想调用这个方法。解决方法有两种：<br>一是如果 selector1 方法比较基础、通用，可以将它挪到 common pod 里，前边也说了，任一业务组件都可以依赖 common pod，只需后期做好维护；<br>二是如果 selector1 方法不够通用，就放在 PodA 里，那可以在 PodA 里提供一个 SelectorService 类，在这个类提供一个对外暴露的方法，方法里调用 selector1，通过 <code>Mediator</code> 的方式来调用 PodA 对外暴露的方法。</p><p>当然，为了区分与页面路由，最好新起一种规范，实现思路和 <code>Mediator</code> 一致，制定一些规范，比如通过<code>/</code>来分割类名、方法名，或者大驼峰命名法来分割，实现过程中做好校验等等细节。</p><h1 id="Response-Event"><a href="#Response-Event" class="headerlink" title="Response Event"></a>Response Event</h1><p>这是一个我很喜欢的一种解耦方案，其实并非面向组件间的街耦，而是 View 间的解耦。<br>比如我们有一个 ViewController，<br>ViewController 上有一个 TableView，<br>TableView 上一个 TableViewCell，<br>TableViewCell 上有一个 Button，<br>点击 Button 更新 TableViewCell，或者更新 TableView，护着更新 ViewController 上的 View。</p><p>正常我们会怎么做呢，发一个 Notification，设置代理，传入 Block，RAC 监听……</p><p>以前我最常用的做法是从外层传入的 Model 里跟一个 Block，点击 Button 的时候触发 Block。但是在组件化解耦中有一种做法是去 Model 化，这时候就不方便传入 Block 了。</p><p>仔细看一下上边的场景会发现页面层级和响应者链的关系，我们可以将响应和数据顺着响应者链向上传递，类似通知的形式，但不会有通知满天飞的尴尬，只会顺着响应者链传递。</p><p>实现方法很简单：</p><figure class="highlight less"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="variable">@implementation</span> UIResponder (GHLEvent)</span><br><span class="line"></span><br><span class="line">- (void)<span class="attribute">ghl_event</span>:(NSString *)event <span class="attribute">params</span>:(NSDictionary *)params &#123;</span><br><span class="line">    </span><br><span class="line">    <span class="selector-attr">[self.nextResponder ghl_event:event params:params]</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="variable">@end</span></span><br></pre></td></tr></table></figure><p>在 TableViewCell 里发送通知，带着事件标识参数</p><figure class="highlight clojure"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">[self ghl_event:@<span class="string">"kHomeTableViewCellButtonClickEvent"</span> params:@&#123;@<span class="string">"page"</span>:@<span class="string">"1"</span>&#125;]<span class="comment">;</span></span><br></pre></td></tr></table></figure><p>然后在你需要响应事件的地方实现 ghl_event 方法就好了</p><figure class="highlight cs"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">- (<span class="keyword">void</span>)ghl_event:(NSString *)<span class="keyword">event</span> <span class="keyword">params</span>:(NSDictionary *)<span class="keyword">params</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> ([<span class="keyword">event</span> isEqualToString:<span class="string">@"kHomeTableViewCellButtonClickEvent"</span>]) &#123;</span><br><span class="line">        NSLog(<span class="string">@"params:%@"</span>, <span class="keyword">params</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>一种很巧妙的页面层级间的解耦方案。</p><p>最后附上 Demo 地址：<a href="https://github.com/gonghonglou/DemoRepo/tree/master/GHLDecouplingDemo" target="_blank" rel="noopener">https://github.com/gonghonglou/DemoRepo/tree/master/GHLDecouplingDemo</a></p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;现在有些规模的工程大概都是实行组件化开发吧，将基础库，业务库划分成单独模块，以 Pod 的形式集成到 APP 中。其中组件化开发一个不可避免的问题就是解耦，本篇博客大概会总结一些现在常用的解耦方案。&lt;/p&gt;
&lt;p&gt;首先，整个工程应该分为两个部分，基础库和业务库，而组件化解耦
      
    
    </summary>
    
      <category term="iOS" scheme="http://gonghonglou.com/categories/iOS/"/>
    
    
  </entry>
  
  <entry>
    <title>一探 mas_updateConstraints 究竟</title>
    <link href="http://gonghonglou.com/2019/01/18/mas-constraints/"/>
    <id>http://gonghonglou.com/2019/01/18/mas-constraints/</id>
    <published>2019-01-18T03:30:22.000Z</published>
    <updated>2020-03-14T12:55:51.602Z</updated>
    
    <content type="html"><![CDATA[<p>Masonry 的链式编程对 iOS UI 添加约束简直好用的不得了，想必在使用上大家也都早已烂熟于心。只是对我来讲很早之前就有个更新约束的问题要好好搞搞清楚，就是题目里的 <code>mas_updateConstraints:</code> 方法。在 View 初始化时会添加一系列约束控制布局，而随时更改约束来移动位置也是日常需求，但其中关于这个方法的某些设计并不是直观想象的那样，现在，终于有时间写篇博客一探究竟。</p><p>当前环境：Xcode 10.1、Simulator iPhone XS 12.1、Masonry 1.1.0</p><h1 id="探索"><a href="#探索" class="headerlink" title="探索"></a>探索</h1><p>首先来看一段布局代码：topView 和 bottomView 上下排列、左右对齐，topView 和 rightView 左右排列、上下对齐。<br><figure class="highlight scheme"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line">[<span class="name">self.topView</span> mas_makeConstraints:^(<span class="name">MASConstraintMaker</span> *make) &#123;</span><br><span class="line">   make.centerY.equalTo(<span class="name">self.view</span>)<span class="comment">;</span></span><br><span class="line">   make.left.equalTo(<span class="name">self.view</span>).offset(<span class="name">50</span>)<span class="comment">;</span></span><br><span class="line">   make.size.mas_equalTo(<span class="name">CGSizeMake</span>(<span class="name">100</span>, <span class="number">30</span>))<span class="comment">;</span></span><br><span class="line">&#125;]<span class="comment">;</span></span><br><span class="line">[<span class="name">self.bottomView</span> mas_makeConstraints:^(<span class="name">MASConstraintMaker</span> *make) &#123;</span><br><span class="line">   make.top.equalTo(<span class="name">self.topView.mas_bottom</span>).offset(<span class="name">50</span>)<span class="comment">;</span></span><br><span class="line">   make.left.equalTo(<span class="name">self.topView</span>)<span class="comment">;</span></span><br><span class="line">   make.size.mas_equalTo(<span class="name">self.topView</span>)<span class="comment">;</span></span><br><span class="line">&#125;]<span class="comment">;</span></span><br><span class="line">[<span class="name">self.rightView</span> mas_makeConstraints:^(<span class="name">MASConstraintMaker</span> *make) &#123;</span><br><span class="line">   make.top.equalTo(<span class="name">self.topView</span>)<span class="comment">;</span></span><br><span class="line">   make.right.equalTo(<span class="name">self.view</span>).offset(<span class="name">-50</span>)<span class="comment">;</span></span><br><span class="line">   make.size.mas_equalTo(<span class="name">self.topView</span>)<span class="comment">;</span></span><br><span class="line">&#125;]<span class="comment">;</span></span><br></pre></td></tr></table></figure></p><p>从12行可以看出 rightView 的 top 依赖于 topView，现在有个需求是将 rightView 与 bottomView 顶部对齐。如图：<br><img src="http://image.gonghonglou.com/mas-constraints/updateConstraints.png" alt=""><br>记得我第一次遇到这种问题的时候想当然的将 rightView 的 top 依赖于 bottomView 了：<br><figure class="highlight clojure"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">[self.rightView mas_updateConstraints:^(<span class="name">MASConstraintMaker</span> *make) &#123;</span><br><span class="line">    make.top.equalTo(<span class="name">self.bottomView</span>)<span class="comment">;</span></span><br><span class="line">&#125;]<span class="comment">;</span></span><br></pre></td></tr></table></figure></p><p>结果是报错、崩溃还是页面错落我倒是忘记了，总之是不能满足需求。当前在 Xcode 10.1、Simulator iPhone XS 12.1 环境下我试了下是页面错落。所以我将代码改成了这样：<br><figure class="highlight scheme"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">[<span class="name">self.rightView</span> mas_updateConstraints:^(<span class="name">MASConstraintMaker</span> *make) &#123;</span><br><span class="line">   make.top.equalTo(<span class="name">self.topView</span>).offset(<span class="name">80</span>)<span class="comment">;</span></span><br><span class="line">&#125;]<span class="comment">;</span></span><br></pre></td></tr></table></figure></p><p>这里当然可以使用 <code>mas_remakeConstraints:</code> 方法，只不过该方法的意思是清空所有约束重新添加（下文会有源码体现），相比 <code>mas_updateConstraints:</code> 性能低了不少，况且项目开发中往往全部约束散落在各处，难免会有遗漏或者约束错乱的情况。所以我的做法是在 <code>mas_updateConstraints:</code> 方法里不更改约束依赖的对象，而通过计算出一个合适的偏移量来更改 offset 值。所以得出了一个结论：</p><blockquote><p>Masonry 的 <code>mas_updateConstraints:</code> 方法不能更改约束依赖的对象，可以通过计算偏移量来更新布局。</p></blockquote><p>但这究竟是为什么呢？让我们来看一下 Masonry 的 <code>mas_updateConstraints:</code> 方法都做了些什么工作：<br><figure class="highlight objectivec"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">- (<span class="built_in">NSArray</span> *)mas_updateConstraints:(<span class="keyword">void</span>(^)(MASConstraintMaker *))block &#123;</span><br><span class="line">    <span class="keyword">self</span>.translatesAutoresizingMaskIntoConstraints = <span class="literal">NO</span>;</span><br><span class="line">    MASConstraintMaker *constraintMaker = [[MASConstraintMaker alloc] initWithView:<span class="keyword">self</span>];</span><br><span class="line">    constraintMaker.updateExisting = <span class="literal">YES</span>;</span><br><span class="line">    block(constraintMaker);</span><br><span class="line">    <span class="keyword">return</span> [constraintMaker install];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>constraintMaker 的 updateExisting 属性设置为 YES 之后执行了 install 方法：<br><figure class="highlight objectivec"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line">- (<span class="built_in">NSArray</span> *)install &#123;</span><br><span class="line">    <span class="keyword">if</span> (<span class="keyword">self</span>.removeExisting) &#123;</span><br><span class="line">        <span class="built_in">NSArray</span> *installedConstraints = [MASViewConstraint installedConstraintsForView:<span class="keyword">self</span>.view];</span><br><span class="line">        <span class="keyword">for</span> (MASConstraint *constraint <span class="keyword">in</span> installedConstraints) &#123;</span><br><span class="line">            [constraint uninstall];</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">NSArray</span> *constraints = <span class="keyword">self</span>.constraints.copy;</span><br><span class="line">    <span class="keyword">for</span> (MASConstraint *constraint <span class="keyword">in</span> constraints) &#123;</span><br><span class="line">        constraint.updateExisting = <span class="keyword">self</span>.updateExisting;</span><br><span class="line">        [constraint install];</span><br><span class="line">    &#125;</span><br><span class="line">    [<span class="keyword">self</span>.constraints removeAllObjects];</span><br><span class="line">    <span class="keyword">return</span> constraints;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>这段代码 <code>mas_remakeConstraints:</code> 方法也会调用，只不过是将 removeExisting 属性设为 YES，第2行的 if 判断正是上文提到的清空所有约束。后半段代码则是调用 <code>install</code> 方法更新约束，该方法则是调用自 MASConstraint 的子类：MASViewConstraint。<strong><em>下边是关键实现代码</em></strong>：</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br></pre></td><td class="code"><pre><span class="line">    MASLayoutConstraint *existingConstraint = nil;</span><br><span class="line">    <span class="keyword">if</span> (self.updateExisting) &#123;</span><br><span class="line">        existingConstraint = [self layoutConstraintSimilarTo:layoutConstraint];</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (existingConstraint) &#123;</span><br><span class="line">        <span class="comment">// just update the constant</span></span><br><span class="line">        existingConstraint.constant = layoutConstraint.constant;</span><br><span class="line">        self.layoutConstraint = existingConstraint;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        [self.installedView addConstraint:layoutConstraint];</span><br><span class="line">        self.layoutConstraint = layoutConstraint;</span><br><span class="line">        [firstLayoutItem.mas_installedConstraints addObject:self];</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">- (MASLayoutConstraint *)layoutConstraintSimilarTo:(MASLayoutConstraint *)layoutConstraint &#123;</span><br><span class="line">    <span class="comment">// check if any constraints are the same apart from the only mutable property constant</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// go through constraints in reverse as we do not want to match auto-resizing or interface builder constraints</span></span><br><span class="line">    <span class="comment">// and they are likely to be added first.</span></span><br><span class="line">    <span class="keyword">for</span> (NSLayoutConstraint *existingConstraint <span class="keyword">in</span> self.installedView.constraints.reverseObjectEnumerator) &#123;</span><br><span class="line">        <span class="keyword">if</span> (![existingConstraint isKindOfClass:MASLayoutConstraint.<span class="keyword">class</span>]) <span class="keyword">continue</span>;</span><br><span class="line">        <span class="keyword">if</span> (existingConstraint.firstItem != layoutConstraint.firstItem) <span class="keyword">continue</span>;</span><br><span class="line">        <span class="keyword">if</span> (existingConstraint.secondItem != layoutConstraint.secondItem) <span class="keyword">continue</span>;</span><br><span class="line">        <span class="keyword">if</span> (existingConstraint.firstAttribute != layoutConstraint.firstAttribute) <span class="keyword">continue</span>;</span><br><span class="line">        <span class="keyword">if</span> (existingConstraint.secondAttribute != layoutConstraint.secondAttribute) <span class="keyword">continue</span>;</span><br><span class="line">        <span class="keyword">if</span> (existingConstraint.relation != layoutConstraint.relation) <span class="keyword">continue</span>;</span><br><span class="line">        <span class="keyword">if</span> (existingConstraint.multiplier != layoutConstraint.multiplier) <span class="keyword">continue</span>;</span><br><span class="line">        <span class="keyword">if</span> (existingConstraint.priority != layoutConstraint.priority) <span class="keyword">continue</span>;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span> (id)existingConstraint;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> nil;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>逻辑很简单，根据 updateExisting，也就是之前的标识更新布局的赋值来寻找已存在的约束，如果约束存在则执行更新操作，如果约束不存在则当成一条新的约束添加给 View。</p><p><strong><em>关键就在于 <code>layoutConstraintSimilarTo:</code> 查找约束时的判断方法，22～29行</em></strong>，竟然是根据这些条件来判断，这就是为什么在 <code>mas_updateConstraints:</code> 方法里更改约束对象后造成页面错乱的原因，因为它找不到这条约束就把它当成一条新的约束添加到 View 上，导致约束冲突。</p><p>从这一点出发的话那我们首先想到的解决这个问题的方案就不再是更改偏移量了，而是通过设置约束的优先级来解决约束冲突的问题。</p><p>而且，如果遇到 View 出现重复约束时，比如：<br><figure class="highlight css"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="selector-tag">make</span><span class="selector-class">.top</span><span class="selector-class">.equalTo</span>(<span class="selector-tag">self</span><span class="selector-class">.topView</span>)<span class="selector-class">.offset</span>(10);</span><br><span class="line"><span class="selector-tag">make</span><span class="selector-class">.top</span><span class="selector-class">.equalTo</span>(<span class="selector-tag">self</span><span class="selector-class">.topView</span>)<span class="selector-class">.offset</span>(20);</span><br></pre></td></tr></table></figure></p><p>仅仅通过在 <code>mas_updateConstraints:</code> 方法里更改某条约束的偏移量并不能起到精确控制的作用。所以这种情况下设置优先级也许是个更好的解决方案。关于设置优先级值得注意的有：</p><ul><li>不特殊设置时约束的优先级默认是 UILayoutPriorityRequired，也就是最高的</li><li>优先级的设值范围在 0～1000 之间，超出这个范围则会崩溃：<blockquote><p>2019-01-18 13:00:09.449167+0800 MasonryTest[54681:15704474] *** Terminating app due to uncaught exception ‘NSInternalInconsistencyException’, reason: ‘It’s illegal to set priority:1200.  Priorities must be greater than 0 and less or equal to NSLayoutPriorityRequired, which is 1000.000000.’</p></blockquote></li></ul><p>所以我们应该将想要更新的约束提前设置一个较低的优先级，再在 <code>mas_updateConstraints:</code> 方法里更新约束并对新的约束设置一个高于原来约束的优先级，且低于 1000。例如：<br><figure class="highlight scheme"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">[<span class="name">self.rightView</span> mas_makeConstraints:^(<span class="name">MASConstraintMaker</span> *make) &#123;</span><br><span class="line">   make.top.equalTo(<span class="name">self.topView</span>).priorityLow()<span class="comment">;</span></span><br><span class="line">&#125;]<span class="comment">;</span></span><br><span class="line">    </span><br><span class="line">[<span class="name">self.rightView</span> mas_updateConstraints:^(<span class="name">MASConstraintMaker</span> *make) &#123;</span><br><span class="line">   make.top.equalTo(<span class="name">self.bottomView</span>).priorityHigh()<span class="comment">;</span></span><br><span class="line">   // or</span><br><span class="line">   // make.top.equalTo(<span class="name">self.bottomView</span>).priority(<span class="name">800</span>)<span class="comment">;</span></span><br><span class="line">&#125;]<span class="comment">;</span></span><br></pre></td></tr></table></figure></p><p>最后，认识几种优先级类型：<br><figure class="highlight objectivec"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 1000</span></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">const</span> MASLayoutPriority MASLayoutPriorityRequired = <span class="built_in">UILayoutPriorityRequired</span>;</span><br><span class="line"><span class="comment">// 750</span></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">const</span> MASLayoutPriority MASLayoutPriorityDefaultHigh = <span class="built_in">UILayoutPriorityDefaultHigh</span>;</span><br><span class="line"><span class="comment">// 500</span></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">const</span> MASLayoutPriority MASLayoutPriorityDefaultMedium = <span class="number">500</span>;</span><br><span class="line"><span class="comment">// 250</span></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">const</span> MASLayoutPriority MASLayoutPriorityDefaultLow = <span class="built_in">UILayoutPriorityDefaultLow</span>;</span><br><span class="line"><span class="comment">// 50</span></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">const</span> MASLayoutPriority MASLayoutPriorityFittingSizeLevel = <span class="built_in">UILayoutPriorityFittingSizeLevel</span>;</span><br></pre></td></tr></table></figure></p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;Masonry 的链式编程对 iOS UI 添加约束简直好用的不得了，想必在使用上大家也都早已烂熟于心。只是对我来讲很早之前就有个更新约束的问题要好好搞搞清楚，就是题目里的 &lt;code&gt;mas_updateConstraints:&lt;/code&gt; 方法。在 View 初始化时
      
    
    </summary>
    
      <category term="iOS" scheme="http://gonghonglou.com/categories/iOS/"/>
    
    
  </entry>
  
  <entry>
    <title>Say Bye 2018</title>
    <link href="http://gonghonglou.com/2018/12/17/saybye2018/"/>
    <id>http://gonghonglou.com/2018/12/17/saybye2018/</id>
    <published>2018-12-17T12:09:32.000Z</published>
    <updated>2018-12-17T13:46:34.802Z</updated>
    
    <content type="html"><![CDATA[<p>遇见十二月，又到了年终总结的时候，<br>以去年为鉴，怕挨到月底还是没有时间写这份总结，所以开始提笔</p><p>回顾今年贫瘠的博客，对这样的技术输出真是羞愧难当，其实感觉有学到不少东西，尤其来到新公司，更是见识了很多东西，包括技术上的、团队上的、流程上的、开发效率上的等等等等，但好像还没有一个好的思路来记录这一切，大概是时间不够加上工作紧张，所以还没有沉淀下来吧。</p><p>说到新公司，今年比较大的变化应该就是换工作了</p><h1 id="工作"><a href="#工作" class="headerlink" title="工作"></a>工作</h1><p>大概四月份吧开始找工作，四月底从老东家离职，五月七号入职新公司。<br>其实关于换工作已经打算了很久了，主要原因有两个吧，一是因为之前在卓骥的两年多时间里，前一年多是我的技术增长期，学习进步很快，大部分成长都是在那段时间，在过了那段时间之后就很少学到东西了，公司产品的项目量级有限，我已经将两款业务产品 iOS 端都重构过了，而且是那种遵循原有业务逻辑实现从架构、界面到实现细节的大重构。当然，只要想学肯定是能学到东西的，比如研究三方库和苹果框架的源码，或者在这个大前端时代学习前端相关的技术等，但终究因为团队环境等硬性原因收效甚微。离职的第二的原因则是公司业务始终不见起色，继而 iOS 产品也愈加没有地位，而且对于公司业务公司里的小伙伴也大都早早失去了信心，处于长远考虑吧，所以也就选择了离开。但我仍然要感谢待在卓骥的两年多的时间，对于从校园起步的我来说，学到了很多东西。</p><p>来到新公司，iOS 的开发团队大概有三五十人，我们的 APP 也是将各个业务、组件和基础模块拆分成 Pod，通过 CocoaPods 的方式集成在一起，每个人负责各自的 Pod。整个 APP 有很多业务线，互相之间除了组件和基础模块共用外，业务模块几乎是不相关的。我所在的业务线有5位 iOS 在负责，我们业务线也有属于自己的一套开发框架，主要负责复杂的表单页面，通过 json/js 的方式配置，iOS 和 Android 各自提供原生支持，目前基本已经能够覆盖需求，涉及到表单页面的时候通过一套 json/js 配置实现 iOS 和 Android 的两套页面。</p><p>整个 APP 通过 CocoaPods 实现组件化，CI持续化集成，git-flow工作流，项目立项、评审、开发、提测、上线流程，业务线内的 json/js 配置方案，等等这一切都是让我兴奋的急于消化吸收的知识、见识。</p><h1 id="生活"><a href="#生活" class="headerlink" title="生活"></a>生活</h1><p>因为换工作我来了杭州，没毕业的时候就已经打算好了以后定居杭州的，只是比原计划提前了。终于我也开始了自己租房子自己生活了，其实来了新公司之后一直比较忙，早晨起来就去上班，下班回家通常都是十点十一点了，洗漱洗漱也就该睡觉了，也是因为回来也没什么事所以也就在公司呆的晚一点吧。</p><p>五月份那会换工作要租房子，交房租押金，五一出国去了趟普吉岛，入职后买了台 MacBook Pro，这一连串的操作对于一直以来月光的我来说真的是压力山大，那两三个月真的是完全靠信用卡蚂蚁花呗借呗度日。。。过了好长一段拆东墙补西墙的日子，还好那个时期已经过去了，所以还是要存些积蓄才行啊。</p><h1 id="读书"><a href="#读书" class="headerlink" title="读书"></a>读书</h1><p>生活工作变动太大，感觉都没有多少时间安静读书了，今年的书单就很单薄了</p><p>《在细雨中呐喊》<br>《兄弟》<br>《许三观卖血记》<br>《边城》（未读完）<br>《飘》（未读完）</p><p>主要就读了余华的三部曲，《在细雨中呐喊》和《兄弟》都是悲惨的故事，还好《许三观卖血记》虽然历经曲折，但结局还算好的，而且还有些小温暖。</p><h1 id="旅行"><a href="#旅行" class="headerlink" title="旅行"></a>旅行</h1><p>本来的五月份计划改成了去普吉岛，景色很美，海很蓝。泰国，一个神奇的国度，刷三观。</p><p>十月份如期去了海南岛骑行，认识了 nice 的骑友，经历了很棒的旅程。</p><h1 id="计划"><a href="#计划" class="headerlink" title="计划"></a>计划</h1><p>还是希望新的一年有比较多的技术沉淀吧</p><p>1、维护RDER</p><p>2、新写一款 APP</p><p>3、拥有自己的服务器</p><p>4、更新博客</p><p>5、开源贡献，GitHub 开几个项目</p><p>6、公司团队内部多几次技术分享</p><p>7、多读读书</p><p>8、十一去环台湾岛骑行</p><p>对于你，全是未来！</p><p><img src="http://image.gonghonglou.com/hainan-cyclingIMG_0984.JPG" alt=""></p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;遇见十二月，又到了年终总结的时候，&lt;br&gt;以去年为鉴，怕挨到月底还是没有时间写这份总结，所以开始提笔&lt;/p&gt;
&lt;p&gt;回顾今年贫瘠的博客，对这样的技术输出真是羞愧难当，其实感觉有学到不少东西，尤其来到新公司，更是见识了很多东西，包括技术上的、团队上的、流程上的、开发效率上的等
      
    
    </summary>
    
      <category term="Life" scheme="http://gonghonglou.com/categories/Life/"/>
    
    
  </entry>
  
</feed>
